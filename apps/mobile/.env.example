# .env로 복사한 뒤 값을 채운다 — .env는 gitignore (키 실값 커밋 금지)
# 네이버 지도 모바일 클라이언트 키: NCP 콘솔 > Mobile Dynamic Map 신규 키(ncpKeyId)
# Android 패키지명 com.anonymous.fillmap 등록 필요 (기존 웹 키는 localhost:5173 전용 — 재사용 불가)
# 키를 넣거나 바꾼 뒤에는 `npx expo prebuild -p android` 재실행 + 네이티브 재빌드 필요
EXPO_PUBLIC_NAVER_MAP_CLIENT_ID=
