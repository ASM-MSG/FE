/**
 * 생성 TanStack Query 옵션 재수출 barrel (AC 2) — 웹 생성물 공유.
 * 화면은 `getMeOptions()`처럼 여기서 가져온 옵션을 `useQuery`에 넘기고,
 * 봉투 언랩은 `select: unwrapEnvelope`로 조합한다 (웹 관례).
 */
export * from "../../../../web/src/shared/api/generated/@tanstack/react-query.gen";
