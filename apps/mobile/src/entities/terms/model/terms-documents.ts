/**
 * 약관 문서 카탈로그 (MSG-448) — 정본은 `@fillmap/terms`로 옮겼다 (MSG-606). 웹 공개 페이지가
 * 같은 본문을 렌더해야 앱스토어 심사용 개인정보 처리방침 URL이 성립한다. 앱 소비처의 import 경로는
 * 그대로 두려고 여기서 재수출한다 — 문구·문서 추가는 패키지 쪽에서 한다.
 */
export * from "@fillmap/terms";
