import { AnalyzingScreen } from "../../features/upload/ui/analyzing-screen";

/** AI 분석 중 라우트 (MSG-424) — 선택 화면이 router.replace로 진입, 응답 후 자동 전환 */
export default function UploadAnalyzing() {
  return <AnalyzingScreen />;
}
