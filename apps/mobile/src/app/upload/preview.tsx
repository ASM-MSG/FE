import { PreviewScreen } from "../../features/upload/ui/preview-screen";

/** 업로드 미리보기 라우트 (MSG-305 → MSG-424) — 확정 + 완료 오버레이(D6) */
export default function UploadPreview() {
  return <PreviewScreen />;
}
