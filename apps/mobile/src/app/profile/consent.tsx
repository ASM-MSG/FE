import { ConsentSettingsScreen } from "../../features/profile/ui/consent-settings-screen";

/** 위치정보 동의 관리 라우트 (MSG-448 기준 8) — 프로필·설정에서 push */
export default function ProfileConsent() {
  return <ConsentSettingsScreen />;
}
