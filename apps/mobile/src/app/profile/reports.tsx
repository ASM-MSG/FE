import { ReportHistoryScreen } from "../../features/report-history/ui/report-history-screen";

/** 신고 관리 라우트 (MSG-448 기준 18) — 프로필·설정에서 push */
export default function ProfileReports() {
  return <ReportHistoryScreen />;
}
