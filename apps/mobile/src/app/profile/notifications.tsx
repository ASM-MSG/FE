import { NotificationInboxScreen } from "../../features/notifications/ui/notification-inbox-screen";

/** 알림함 라우트 (MSG-602) — 프로필·설정 "알림함" 행에서 push */
export default function ProfileNotifications() {
  return <NotificationInboxScreen />;
}
