import { UploadHistoryScreen } from "../../features/dex/ui/upload-history-screen";

/** 업로드 기록 전체 보기 라우트 (MSG-430 S10) — `/dex`와 다른 경로라 공존한다 */
export default function DexHistory() {
  return <UploadHistoryScreen />;
}
