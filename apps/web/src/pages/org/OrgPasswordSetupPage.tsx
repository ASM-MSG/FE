import { ConsoleStub } from "@/widgets/console-shell/ConsoleStub";

/**
 * 첫 로그인 비밀번호 강제 설정 (MSG-542) — mustChange 게이트의 착지 화면(ORG·ADMIN 공용).
 * 이 티켓(MSG-541)은 라우트 골격만 세운다 — 자리표시 제목 하나다.
 */
export const OrgPasswordSetupPage = () => <ConsoleStub title="비밀번호 설정" />;
