import { ConsoleStub } from "@/widgets/console-shell/ConsoleStub";

/**
 * 콘솔 로그인 (MSG-542) — 공개 라우트. LOCAL 로그인 폼·role별 착지 분기는 MSG-542 몫이다.
 * 이 티켓(MSG-541)은 라우트 골격만 세운다 — 자리표시 제목 하나다.
 */
export const OrgLoginPage = () => <ConsoleStub title="콘솔 로그인" />;
