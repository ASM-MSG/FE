import { ConsoleStub } from "@/widgets/console-shell/ConsoleStub";

/**
 * 비밀번호 재설정 요청·발송 완료 (MSG-542) — 공개 라우트, 화면 내 상태 전환.
 * 이 티켓(MSG-541)은 라우트 골격만 세운다 — 자리표시 제목 하나다.
 */
export const OrgPasswordResetPage = () => (
  <ConsoleStub title="비밀번호 재설정" />
);
