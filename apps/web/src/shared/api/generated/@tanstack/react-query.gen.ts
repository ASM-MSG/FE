// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, type InfiniteData, infiniteQueryOptions, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { accept, approve, delete_, deleteFriend, deleteMe, findMyBadges, getActiveMissions, getCell, getCollectionGrids, getExploreRegions, getFriendGrids, getFriendGridVideos, getFriendProfile, getFriends, getGridCover, getGridGlobalVideos, getGridVideos, getHotZones, getMe, getMyFriendCode, getOccupiedInViewport, getPlayback, getPreferences, getReceivedRequests, getRegionGrids, getRegionVideos, getReports, getStatByGrid, getStatByPoint, getStats, getSummary, getTrendingKeywords, getVideoForReview, getZones, issuePresignedUrl, login, logout, oauthCodeLogin, oauthLogin, type Options, preview, redirectToKakaoAuthorize, register, reissue, reject, reject1, replace, replaceFeatured, report, request, reverseGeocode, searchPlaces, setVisibility, signup, socialLogin, unblindVideo, unregister, update, updateNickname, upload } from '../sdk.gen';
import type { AcceptData, ApproveData, ApproveResponse, DeleteData, DeleteFriendData, DeleteMeData, FindMyBadgesData, FindMyBadgesResponse, GetActiveMissionsData, GetActiveMissionsResponse, GetCellData, GetCellResponse, GetCollectionGridsData, GetCollectionGridsResponse, GetExploreRegionsData, GetExploreRegionsResponse, GetFriendGridsData, GetFriendGridsResponse, GetFriendGridVideosData, GetFriendGridVideosResponse, GetFriendProfileData, GetFriendProfileResponse, GetFriendsData, GetFriendsResponse, GetGridCoverData, GetGridCoverResponse, GetGridGlobalVideosData, GetGridGlobalVideosResponse, GetGridVideosData, GetGridVideosResponse, GetHotZonesData, GetHotZonesResponse, GetMeData, GetMeResponse, GetMyFriendCodeData, GetMyFriendCodeResponse, GetOccupiedInViewportData, GetOccupiedInViewportResponse, GetPlaybackData, GetPlaybackResponse, GetPreferencesData, GetPreferencesResponse, GetReceivedRequestsData, GetReceivedRequestsResponse, GetRegionGridsData, GetRegionGridsResponse, GetRegionVideosData, GetRegionVideosResponse, GetReportsData, GetReportsResponse, GetStatByGridData, GetStatByGridResponse, GetStatByPointData, GetStatByPointResponse, GetStatsData, GetStatsResponse, GetSummaryData, GetSummaryResponse, GetTrendingKeywordsData, GetTrendingKeywordsResponse, GetVideoForReviewData, GetVideoForReviewResponse, GetZonesData, GetZonesResponse, IssuePresignedUrlData, IssuePresignedUrlResponse, LoginData, LoginResponse, LogoutData, OauthCodeLoginData, OauthCodeLoginResponse, OauthLoginData, OauthLoginResponse, PreviewData, PreviewResponse, RedirectToKakaoAuthorizeData, RegisterData, ReissueData, ReissueResponse, Reject1Data, Reject1Response, RejectData, ReplaceData, ReplaceFeaturedData, ReplaceFeaturedResponse, ReplaceResponse, ReportData, ReportResponse, RequestData, RequestResponse, ReverseGeocodeData, ReverseGeocodeResponse, SearchPlacesData, SearchPlacesResponse, SetVisibilityData, SetVisibilityResponse, SignupData, SignupResponse, SocialLoginData, SocialLoginResponse, UnblindVideoData, UnblindVideoResponse, UnregisterData, UpdateData, UpdateNicknameData, UpdateNicknameResponse, UpdateResponse, UploadData, UploadResponse } from '../types.gen';

/**
 * 영상 삭제
 *
 * 영상을 삭제한다. 해당 격자의 내 영상이 모두 사라지면 점령이 롤백(색칠 해제)된다.
 */
export const deleteMutation = (options?: Partial<Options<DeleteData>>): UseMutationOptions<unknown, DefaultError, Options<DeleteData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<DeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await delete_({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const getPlaybackQueryKey = (options: Options<GetPlaybackData>) => createQueryKey('getPlayback', options);

/**
 * 단건 영상 재생 조회
 *
 * 영상 하나의 표시용 메타와 재생본 presigned GET URL을 발급한다. 소유자·타인 모두 조회할 수 있으나 삭제·블라인드(타인)는 404, 비공개(타인)·친구만 공개(비친구)는 403이다. READY가 아니면 playbackUrl은 null이다.
 */
export const getPlaybackOptions = (options: Options<GetPlaybackData>) => queryOptions<GetPlaybackResponse, DefaultError, GetPlaybackResponse, ReturnType<typeof getPlaybackQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPlayback({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPlaybackQueryKey(options)
});

/**
 * 영상 교체
 *
 * 기존 영상을 새 파일로 교체한다. 좌표를 생략하면 격자를 유지하고 파일만 교체하며, 좌표를 보내면 기존과 같은 격자여야 한다(다르면 거부). 교체 직후 상태는 UPLOADED다.
 */
export const replaceMutation = (options?: Partial<Options<ReplaceData>>): UseMutationOptions<ReplaceResponse, DefaultError, Options<ReplaceData>> => {
    const mutationOptions: UseMutationOptions<ReplaceResponse, DefaultError, Options<ReplaceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await replace({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 닉네임 수정
 *
 * 닉네임(2~20자)을 교체하고 변경 후 프로필을 반환한다. 중복 닉네임은 허용된다.
 */
export const updateNicknameMutation = (options?: Partial<Options<UpdateNicknameData>>): UseMutationOptions<UpdateNicknameResponse, DefaultError, Options<UpdateNicknameData>> => {
    const mutationOptions: UseMutationOptions<UpdateNicknameResponse, DefaultError, Options<UpdateNicknameData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updateNickname({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 대표 뱃지 집합 교체
 *
 * 획득한 뱃지 중 최대 2개를 대표로 교체 지정한다(멱등). 배열 순서 = 표시 순서(rank 1·2), 빈 배열은 전부 해제. 미획득·미존재 뱃지는 7403, 중복 id 는 7400, 3개 이상은 400 이다.
 */
export const replaceFeaturedMutation = (options?: Partial<Options<ReplaceFeaturedData>>): UseMutationOptions<ReplaceFeaturedResponse, DefaultError, Options<ReplaceFeaturedData>> => {
    const mutationOptions: UseMutationOptions<ReplaceFeaturedResponse, DefaultError, Options<ReplaceFeaturedData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await replaceFeatured({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 영상 메타데이터 저장 (업로드 확정)
 *
 * S3 업로드 완료 후 영상 메타데이터를 저장하고 좌표로 격자를 매핑한다. 해당 격자에 내 첫 영상이면 점령(occupied=true)된다.
 */
export const uploadMutation = (options?: Partial<Options<UploadData>>): UseMutationOptions<UploadResponse, DefaultError, Options<UploadData>> => {
    const mutationOptions: UseMutationOptions<UploadResponse, DefaultError, Options<UploadData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await upload({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 영상 신고 접수
 *
 * 다른 사람의 영상을 사유 5종(INAPPROPRIATE, PRIVACY, SPAM, COPYRIGHT, OTHER) 중 하나와 함께 신고한다. 접수된 신고는 PENDING 으로 쌓여 관리자 처리의 입력이 되며, 접수 자체는 영상 상태를 바꾸지 않는다. 같은 영상 재신고는 409, 자기 영상 신고는 400, 없는 영상·삭제·블라인드 영상은 재생 조회와 같은 404 다.
 */
export const reportMutation = (options?: Partial<Options<ReportData>>): UseMutationOptions<ReportResponse, DefaultError, Options<ReportData>> => {
    const mutationOptions: UseMutationOptions<ReportResponse, DefaultError, Options<ReportData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await report({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 업로드용 presigned URL 발급
 *
 * 영상 파일을 S3에 직접 올릴 presigned URL을 발급한다. 이 URL로 PUT 업로드한 뒤 메타데이터 저장을 호출한다.
 */
export const issuePresignedUrlMutation = (options?: Partial<Options<IssuePresignedUrlData>>): UseMutationOptions<IssuePresignedUrlResponse, DefaultError, Options<IssuePresignedUrlData>> => {
    const mutationOptions: UseMutationOptions<IssuePresignedUrlResponse, DefaultError, Options<IssuePresignedUrlData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await issuePresignedUrl({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * FCM 토큰 해제
 *
 * 본인 소유(user_id 일치) 토큰 행을 삭제한다 — 멱등, 없는 토큰·소유 불일치 해제도 200. 로그아웃은 /api/auth/logout body 의 fcmToken 으로 한 번에 처리하고, 이 API 는 토큰 로테이션 등 로그아웃 외 정리 용도다.
 */
export const unregisterMutation = (options?: Partial<Options<UnregisterData>>): UseMutationOptions<unknown, DefaultError, Options<UnregisterData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<UnregisterData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await unregister({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * FCM 토큰 등록/갱신
 *
 * 디바이스의 FCM 토큰을 현재 계정으로 등록한다(UPSERT). 같은 토큰 재등록은 충돌 없이 user_id·platform·appVersion·last_used_at 이 갱신된다 — 재로그인·계정 전환 포함. platform 이 IOS/ANDROID/WEB(대소문자 무시) 외면 10400 이다.
 */
export const registerMutation = (options?: Partial<Options<RegisterData>>): UseMutationOptions<unknown, DefaultError, Options<RegisterData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<RegisterData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await register({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 친구 요청
 *
 * 상대의 친구 코드로 요청을 보낸다. 응답 status 가 PENDING 이면 상대 수락 대기, ACCEPTED 면 상대가 먼저 보낸 요청이 있어 즉시 친구 성립(자동 수락)이다.
 */
export const requestMutation = (options?: Partial<Options<RequestData>>): UseMutationOptions<RequestResponse, DefaultError, Options<RequestData>> => {
    const mutationOptions: UseMutationOptions<RequestResponse, DefaultError, Options<RequestData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await request({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 친구 요청 거절
 *
 * 받은 요청을 거절한다. 보낸 쪽에 통지는 없고, 상대는 다시 요청할 수 있다.
 */
export const rejectMutation = (options?: Partial<Options<RejectData>>): UseMutationOptions<unknown, DefaultError, Options<RejectData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<RejectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await reject({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 친구 요청 수락
 *
 * 받은 요청을 수락해 친구 관계를 성립시킨다. 요청의 수신자 본인만 가능하다.
 */
export const acceptMutation = (options?: Partial<Options<AcceptData>>): UseMutationOptions<unknown, DefaultError, Options<AcceptData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<AcceptData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await accept({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 이메일 회원가입
 *
 * 이메일/비밀번호/닉네임으로 신규 회원을 생성한다.
 */
export const signupMutation = (options?: Partial<Options<SignupData>>): UseMutationOptions<SignupResponse, DefaultError, Options<SignupData>> => {
    const mutationOptions: UseMutationOptions<SignupResponse, DefaultError, Options<SignupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await signup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 토큰 재발급
 *
 * 리프레시 토큰(웹=쿠키, 앱=body)으로 새 액세스 토큰과 회전된 새 리프레시 토큰을 발급받는다. 직전 리프레시 토큰은 즉시 무효화되며, 회전된 옛 토큰 재사용 시 세션 체인이 폐기된다.
 */
export const reissueMutation = (options?: Partial<Options<ReissueData>>): UseMutationOptions<ReissueResponse, DefaultError, Options<ReissueData>> => {
    const mutationOptions: UseMutationOptions<ReissueResponse, DefaultError, Options<ReissueData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await reissue({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 소셜 로그인 (OIDC)
 *
 * 소셜 제공자의 ID Token으로 로그인/가입하고 JWT 액세스 토큰과 리프레시 토큰을 발급받는다. 웹(X-Client-Type: web, 기본)은 리프레시가 HttpOnly 쿠키(Set-Cookie)로 내려가 body 의 refreshToken 이 null 이고, 앱(app)은 body 로 내려간다.
 */
export const oauthLoginMutation = (options?: Partial<Options<OauthLoginData>>): UseMutationOptions<OauthLoginResponse, DefaultError, Options<OauthLoginData>> => {
    const mutationOptions: UseMutationOptions<OauthLoginResponse, DefaultError, Options<OauthLoginData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await oauthLogin({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 소셜 로그인 (카카오 인가 코드)
 *
 * 웹에서 카카오 콜백으로 받은 인가 코드로 로그인/가입한다. 서버가 REST API 키로 카카오 토큰 엔드포인트를 호출해 ID Token 을 받은 뒤, 소셜 로그인(OIDC)과 완전히 같은 검증·발급 경로를 태운다. 인가 진입점이 심은 OAUTH_NONCE 쿠키가 함께 와야 한다(없으면 401). 응답 형태는 기존 소셜 로그인과 동일하다 — 웹(X-Client-Type: web, 기본)은 리프레시가 HttpOnly 쿠키(Set-Cookie)로 내려가 body 의 refreshToken 이 null 이고, 앱(app)은 body 로 내려간다. 네이티브 SDK 가 교환까지 해주는 앱은 이 API 가 아니라 POST /api/auth/oauth/{provider} 를 쓴다.
 */
export const oauthCodeLoginMutation = (options?: Partial<Options<OauthCodeLoginData>>): UseMutationOptions<OauthCodeLoginResponse, DefaultError, Options<OauthCodeLoginData>> => {
    const mutationOptions: UseMutationOptions<OauthCodeLoginResponse, DefaultError, Options<OauthCodeLoginData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await oauthCodeLogin({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 로그아웃
 *
 * Authorization 헤더의 액세스 토큰을 무효화하고 해당 디바이스(X-Device-Id)의 리프레시 세션을 삭제한다. X-Device-Id 가 없으면 해당 유저의 모든 디바이스 세션을 삭제한다. 선택 body 의 fcmToken 이 있으면 해당 FCM 푸시 토큰도 함께 정리된다 (MSG-178 logout 통합).
 */
export const logoutMutation = (options?: Partial<Options<LogoutData>>): UseMutationOptions<unknown, DefaultError, Options<LogoutData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<LogoutData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await logout({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 이메일 로그인
 *
 * 이메일/비밀번호로 로그인하고 JWT 액세스 토큰과 리프레시 토큰을 발급받는다. 웹(X-Client-Type: web, 기본)은 리프레시가 HttpOnly 쿠키로, 앱(app)은 body 로 내려간다.
 */
export const loginMutation = (options?: Partial<Options<LoginData>>): UseMutationOptions<LoginResponse, DefaultError, Options<LoginData>> => {
    const mutationOptions: UseMutationOptions<LoginResponse, DefaultError, Options<LoginData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await login({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * [개발용] 소셜 로그인 모의
 *
 * 실제 OIDC ID Token 검증 없이 (provider, oid)로 사용자를 find-or-create 하고 액세스+리프레시 토큰을 발급한다. 리프레시는 body 로 내려간다(앱 모드). 로컬/dev 프로파일에서만 노출.
 */
export const socialLoginMutation = (options?: Partial<Options<SocialLoginData>>): UseMutationOptions<SocialLoginResponse, DefaultError, Options<SocialLoginData>> => {
    const mutationOptions: UseMutationOptions<SocialLoginResponse, DefaultError, Options<SocialLoginData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await socialLogin({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 블라인드 해제
 *
 * BLINDED 영상을 ACTIVE 로 복구한다. 오판 복구용이며 그 신고의 RESOLVED 는 되돌리지 않는다. 없는 영상과 삭제된 영상은 404(3404), 이미 ACTIVE 면 409(3409) 다.
 */
export const unblindVideoMutation = (options?: Partial<Options<UnblindVideoData>>): UseMutationOptions<UnblindVideoResponse, DefaultError, Options<UnblindVideoData>> => {
    const mutationOptions: UseMutationOptions<UnblindVideoResponse, DefaultError, Options<UnblindVideoData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await unblindVideo({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 신고 기각
 *
 * 신고를 REJECTED 로 종결한다. 영상에는 아무 영향이 없고 응답의 videoStatus 는 현재 상태 그대로다. 없는 신고는 404(11404), 이미 처리된 신고는 409(11410) 다.
 */
export const reject1Mutation = (options?: Partial<Options<Reject1Data>>): UseMutationOptions<Reject1Response, DefaultError, Options<Reject1Data>> => {
    const mutationOptions: UseMutationOptions<Reject1Response, DefaultError, Options<Reject1Data>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await reject1({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 신고 승인
 *
 * 신고를 RESOLVED 로 종결하고 대상 영상을 블라인드한다 — 한 트랜잭션이다. 영상이 이미 BLINDED 거나 DELETED 면 영상 전이 없이 신고만 종결하며, 응답의 videoStatus 로 구분할 수 있다. 없는 신고는 404(11404), 이미 처리된 신고와 동시 처리의 늦은 쪽은 409(11410) 다.
 */
export const approveMutation = (options?: Partial<Options<ApproveData>>): UseMutationOptions<ApproveResponse, DefaultError, Options<ApproveData>> => {
    const mutationOptions: UseMutationOptions<ApproveResponse, DefaultError, Options<ApproveData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await approve({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 영상 공개 범위 전환
 *
 * 본인 영상의 공개 범위를 PUBLIC·PRIVATE·FRIENDS 간 전환한다. 전환된 상태를 반환하며, 같은 값 재전환은 멱등하게 성공한다.
 */
export const setVisibilityMutation = (options?: Partial<Options<SetVisibilityData>>): UseMutationOptions<SetVisibilityResponse, DefaultError, Options<SetVisibilityData>> => {
    const mutationOptions: UseMutationOptions<SetVisibilityResponse, DefaultError, Options<SetVisibilityData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setVisibility({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * 카테고리 수신 토글
 *
 * 카테고리 하나의 수신 여부를 바꾸고 변경 후 전체 상태를 반환한다 — 같은 값 재전환은 멱등. category 가 BADGE/HOTZONE/REMIND(대소문자 무시) 외면 10420 이다. off 는 발송만 막고 off 중 쌓인 알림이 on 복귀 후 재발송되는 일은 없다.
 */
export const updateMutation = (options?: Partial<Options<UpdateData>>): UseMutationOptions<UpdateResponse, DefaultError, Options<UpdateData>> => {
    const mutationOptions: UseMutationOptions<UpdateResponse, DefaultError, Options<UpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await update({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getZonesQueryKey = (options?: Options<GetZonesData>) => createQueryKey('getZones', options);

/**
 * 구역 목록 조회
 *
 * 전체 구역(zone) 목록을 반환한다. FE 가 캐시해 gridId 로 표시명을 로컬 산술하고 구역 오버레이에 쓴다. 시딩 전이면 빈 배열(전 시스템이 행정동 폴백으로 동작).
 */
export const getZonesOptions = (options?: Options<GetZonesData>) => queryOptions<GetZonesResponse, DefaultError, GetZonesResponse, ReturnType<typeof getZonesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getZones({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getZonesQueryKey(options)
});

/**
 * 계정 삭제
 *
 * 내 계정을 즉시·비가역 삭제한다. 연쇄 개인 데이터·영상 S3 객체가 제거되고 전 디바이스 세션이 무효화된다. 같은 이메일·카카오 계정으로 다시 로그인하면 신규 가입이다.
 */
export const deleteMeMutation = (options?: Partial<Options<DeleteMeData>>): UseMutationOptions<unknown, DefaultError, Options<DeleteMeData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<DeleteMeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteMe({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getMeQueryKey = (options?: Options<GetMeData>) => createQueryKey('getMe', options);

/**
 * 내 프로필 조회
 *
 * 소셜 로그인이 자동 저장한 이메일·닉네임을 반환한다. 항상 본인 계정만 — 경로에 대상 식별자가 없다.
 */
export const getMeOptions = (options?: Options<GetMeData>) => queryOptions<GetMeResponse, DefaultError, GetMeResponse, ReturnType<typeof getMeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getMe({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getMeQueryKey(options)
});

export const getTrendingKeywordsQueryKey = (options?: Options<GetTrendingKeywordsData>) => createQueryKey('getTrendingKeywords', options);

/**
 * 인기 검색어 TOP 10
 *
 * 오늘+어제(KST) 검색어 집계를 합산해 상위 10개를 순위·검색어로 반환한다. 동률은 검색어 사전순. 검색 횟수와 장소 정보는 포함하지 않으며(클릭 후 장소 검색 API 재호출), 집계가 없으면 200 + 빈 배열이다.
 */
export const getTrendingKeywordsOptions = (options?: Options<GetTrendingKeywordsData>) => queryOptions<GetTrendingKeywordsResponse, DefaultError, GetTrendingKeywordsResponse, ReturnType<typeof getTrendingKeywordsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTrendingKeywords({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTrendingKeywordsQueryKey(options)
});

export const searchPlacesQueryKey = (options: Options<SearchPlacesData>) => createQueryKey('searchPlaces', options);

/**
 * 장소 검색 (장소명 → 좌표·격자)
 *
 * 카카오 로컬 키워드 검색 결과(정확도순 ≤15건)에 각 좌표의 격자 ID 를 얹어 반환한다. 선택 즉시 lat/lng 지도 이동 + gridId 격자 하이라이트. q 누락 400 / trim 후 빈 q·무매치 200 [] / 카카오 장애·타임아웃 502(developCode 5502).
 */
export const searchPlacesOptions = (options: Options<SearchPlacesData>) => queryOptions<SearchPlacesResponse, DefaultError, SearchPlacesResponse, ReturnType<typeof searchPlacesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await searchPlaces({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: searchPlacesQueryKey(options)
});

export const getRegionGridsQueryKey = (options: Options<GetRegionGridsData>) => createQueryKey('getRegionGrids', options);

/**
 * 행정동 격자 카드 리스트 + 헤더 카운트 조회
 *
 * 그 행정동 격자들 중 전역 공개 콘텐츠(공개·인코딩 완료·타인 영상 포함)가 있는 격자를 카드로 반환한다. 헤더 카운트(gridCount·videoCount)는 limit 무관 전체 기준이라 지도 홈 패널(limit=3)과 전체 보기(limit 생략)가 같은 숫자를 본다. 카드 커버는 격자 대표(cover)와 같은 영상이고 썸네일은 presigned GET URL 이다. 미존재·무콘텐츠 regionCode 는 404 가 아니라 200 + 카운트 0·빈 배열이다.
 */
export const getRegionGridsOptions = (options: Options<GetRegionGridsData>) => queryOptions<GetRegionGridsResponse, DefaultError, GetRegionGridsResponse, ReturnType<typeof getRegionGridsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRegionGrids({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRegionGridsQueryKey(options)
});

export const getStatsQueryKey = (options?: Options<GetStatsData>) => createQueryKey('getStats', options);

/**
 * 내 행정동별 수집률 조회
 *
 * 로그인 사용자가 점령(수집)한 격자를 행정동별로 집계한 수집률 리스트를 반환한다. parentCode 로 시군구를 좁힐 수 있고(실존하지 않는 코드면 404/6404), collectedOnly=false 면 롤백으로 0이 된 행정동도 포함한다. 수집이 없으면 404 가 아니라 200 + 빈 배열.
 */
export const getStatsOptions = (options?: Options<GetStatsData>) => queryOptions<GetStatsResponse, DefaultError, GetStatsResponse, ReturnType<typeof getStatsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getStats({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getStatsQueryKey(options)
});

export const getStatByPointQueryKey = (options: Options<GetStatByPointData>) => createQueryKey('getStatByPoint', options);

/**
 * 현재 위치 행정동 탐험률 (좌표 → 수집률)
 *
 * 도감 갤러리 진입 초기값. 현재 위치 좌표가 속한 행정동 1건의 내 수집률을 반환한다. 그 행정동에 수집이 없어도 0% 로 합성해 반환하고, 어떤 행정동에도 안 속하면(바다·국외) 404 가 아니라 200 + data null. 서비스 범위 밖 좌표는 400(6400).
 */
export const getStatByPointOptions = (options: Options<GetStatByPointData>) => queryOptions<GetStatByPointResponse, DefaultError, GetStatByPointResponse, ReturnType<typeof getStatByPointQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getStatByPoint({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getStatByPointQueryKey(options)
});

export const getStatByGridQueryKey = (options: Options<GetStatByGridData>) => createQueryKey('getStatByGrid', options);

/**
 * 격자 중심 행정동 탐험률 (격자 클릭 → 수집률)
 *
 * 클릭한 격자의 중심점이 속한 행정동 1건의 내 수집률을 반환한다. 귀속 축이 수집률 집계(MSG-155)와 같아 탐험률·라벨이 일치한다. 중심점이 어떤 행정동에도 안 속하거나 gridId 형식이 이상하면 200 + data null(별도 에러 코드 없음).
 */
export const getStatByGridOptions = (options: Options<GetStatByGridData>) => queryOptions<GetStatByGridResponse, DefaultError, GetStatByGridResponse, ReturnType<typeof getStatByGridQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getStatByGrid({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getStatByGridQueryKey(options)
});

export const reverseGeocodeQueryKey = (options: Options<ReverseGeocodeData>) => createQueryKey('reverseGeocode', options);

/**
 * 역지오코딩 (좌표 → 행정동)
 *
 * 좌표를 포함하는 행정동 1건을 반환한다. 포함 행정동이 없으면(바다·국외) 404가 아니라 200 + data null. 서비스 좌표 범위(한국) 밖이면 400(6400).
 */
export const reverseGeocodeOptions = (options: Options<ReverseGeocodeData>) => queryOptions<ReverseGeocodeResponse, DefaultError, ReverseGeocodeResponse, ReturnType<typeof reverseGeocodeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await reverseGeocode({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: reverseGeocodeQueryKey(options)
});

export const getExploreRegionsQueryKey = (options?: Options<GetExploreRegionsData>) => createQueryKey('getExploreRegions', options);

/**
 * 전체 지역 리스트 조회
 *
 * 전역 공개 콘텐츠가 있는 행정동만 격자 수 내림차순으로 반환한다(검색 무입력 드롭다운). 각 항목의 gridCount 는 그 행정동 격자 카드 조회의 gridCount 와 같은 정의다. 전역 공개 콘텐츠가 하나도 없으면 빈 배열이다.
 */
export const getExploreRegionsOptions = (options?: Options<GetExploreRegionsData>) => queryOptions<GetExploreRegionsResponse, DefaultError, GetExploreRegionsResponse, ReturnType<typeof getExploreRegionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getExploreRegions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getExploreRegionsQueryKey(options)
});

export const getPreferencesQueryKey = (options?: Options<GetPreferencesData>) => createQueryKey('getPreferences', options);

/**
 * 알림 설정 조회
 *
 * 카테고리 3종(BADGE·HOTZONE·REMIND) 전부의 수신 상태를 반환한다. 설정을 만진 적 없는 사용자는 전부 true 다 — opt-out 기본 전부 on.
 */
export const getPreferencesOptions = (options?: Options<GetPreferencesData>) => queryOptions<GetPreferencesResponse, DefaultError, GetPreferencesResponse, ReturnType<typeof getPreferencesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPreferences({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPreferencesQueryKey(options)
});

export const getActiveMissionsQueryKey = (options?: Options<GetActiveMissionsData>) => createQueryKey('getActiveMissions', options);

/**
 * 활성 미션 목록 조회
 *
 * 지금 활성인 미션 전부를 유형별 렌더 shape(코스=PATH·구역=REGION·축제=BOX·테마/지속=CELLS)로 반환한다. bbox 없이 전역 목록이며 1h 전역 캐시로 재계산을 흡수한다. 시드 전이거나 활성 미션이 없으면 빈 배열이다(404 아님).
 */
export const getActiveMissionsOptions = (options?: Options<GetActiveMissionsData>) => queryOptions<GetActiveMissionsResponse, DefaultError, GetActiveMissionsResponse, ReturnType<typeof getActiveMissionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getActiveMissions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getActiveMissionsQueryKey(options)
});

export const getHotZonesQueryKey = (options: Options<GetHotZonesData>) => createQueryKey('getHotZones', options);

/**
 * 뷰포트 내 핫구역 조회
 *
 * 지도 화면 bbox(남서~북동 좌표) 안의 핫구역을 핫스코어 내림차순으로 반환한다. 전국 상위 K(50)·최소 임계(3) 판정 후 뷰포트 필터 — 없으면 빈 목록이다.
 */
export const getHotZonesOptions = (options: Options<GetHotZonesData>) => queryOptions<GetHotZonesResponse, DefaultError, GetHotZonesResponse, ReturnType<typeof getHotZonesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getHotZones({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getHotZonesQueryKey(options)
});

export const getOccupiedInViewportQueryKey = (options: Options<GetOccupiedInViewportData>) => createQueryKey('getOccupiedInViewport', options);

/**
 * 뷰포트 내 색칠 격자 조회 (커서 페이지네이션)
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 내가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답의 nextCursor를 다음 요청 cursor에 넣어 이어서 조회한다. bbox 한 변의 span은 최대 0.5도.
 */
export const getOccupiedInViewportOptions = (options: Options<GetOccupiedInViewportData>) => queryOptions<GetOccupiedInViewportResponse, DefaultError, GetOccupiedInViewportResponse, ReturnType<typeof getOccupiedInViewportQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getOccupiedInViewport({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getOccupiedInViewportQueryKey(options)
});

const createInfiniteParams = <K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>>(queryKey: QueryKey<Options>, page: K) => {
    const params = { ...queryKey[0] };
    if (page.body) {
        params.body = {
            ...queryKey[0].body as any,
            ...page.body as any
        };
    }
    if (page.headers) {
        params.headers = {
            ...queryKey[0].headers,
            ...page.headers
        };
    }
    if (page.path) {
        params.path = {
            ...queryKey[0].path as any,
            ...page.path as any
        };
    }
    if (page.query) {
        params.query = {
            ...queryKey[0].query as any,
            ...page.query as any
        };
    }
    return params as unknown as typeof page;
};

export const getOccupiedInViewportInfiniteQueryKey = (options: Options<GetOccupiedInViewportData>): QueryKey<Options<GetOccupiedInViewportData>> => createQueryKey('getOccupiedInViewport', options, true);

/**
 * 뷰포트 내 색칠 격자 조회 (커서 페이지네이션)
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 내가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답의 nextCursor를 다음 요청 cursor에 넣어 이어서 조회한다. bbox 한 변의 span은 최대 0.5도.
 */
export const getOccupiedInViewportInfiniteOptions = (options: Options<GetOccupiedInViewportData>) => {
    const opts = infiniteQueryOptions<GetOccupiedInViewportResponse, DefaultError, InfiniteData<GetOccupiedInViewportResponse>, QueryKey<Options<GetOccupiedInViewportData>>, string | Pick<QueryKey<Options<GetOccupiedInViewportData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetOccupiedInViewportData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    cursor: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getOccupiedInViewport({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getOccupiedInViewportInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

export const getCellQueryKey = (options: Options<GetCellData>) => createQueryKey('getCell', options);

/**
 * 단일 격자 색칠 상태 조회
 *
 * 특정 격자를 내가 점령(색칠)했는지와 내 영상 수를 반환한다. 미점령 격자도 404가 아니라 occupied=false로 응답한다.
 */
export const getCellOptions = (options: Options<GetCellData>) => queryOptions<GetCellResponse, DefaultError, GetCellResponse, ReturnType<typeof getCellQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getCell({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getCellQueryKey(options)
});

export const getGridGlobalVideosQueryKey = (options: Options<GetGridGlobalVideosData>) => createQueryKey('getGridGlobalVideos', options);

/**
 * 격자 전역 영상 목록 조회
 *
 * 그 격자에 쌓인 공개(PUBLIC)·READY 영상을 전역(본인·타인 포함)에서 조회수(viewCount) → 최신(createdAt) 순으로 페이지 조회한다. 비공개·삭제·인코딩 미완 영상은 본인 것이라도 제외한다. 첫 요청은 cursor 없이 부르고, hasNext 가 true 면 응답의 nextCursor 를 다음 요청 cursor 로 넘기면 이어진다. 무효 커서는 400(INVALID_CURSOR)이고, size 는 1~50 밖이면 클램프된다. 후보가 없거나 존재하지 않는 gridId 는 빈 페이지다. 썸네일은 presigned GET URL 로 내려준다.
 */
export const getGridGlobalVideosOptions = (options: Options<GetGridGlobalVideosData>) => queryOptions<GetGridGlobalVideosResponse, DefaultError, GetGridGlobalVideosResponse, ReturnType<typeof getGridGlobalVideosQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getGridGlobalVideos({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getGridGlobalVideosQueryKey(options)
});

export const getGridGlobalVideosInfiniteQueryKey = (options: Options<GetGridGlobalVideosData>): QueryKey<Options<GetGridGlobalVideosData>> => createQueryKey('getGridGlobalVideos', options, true);

/**
 * 격자 전역 영상 목록 조회
 *
 * 그 격자에 쌓인 공개(PUBLIC)·READY 영상을 전역(본인·타인 포함)에서 조회수(viewCount) → 최신(createdAt) 순으로 페이지 조회한다. 비공개·삭제·인코딩 미완 영상은 본인 것이라도 제외한다. 첫 요청은 cursor 없이 부르고, hasNext 가 true 면 응답의 nextCursor 를 다음 요청 cursor 로 넘기면 이어진다. 무효 커서는 400(INVALID_CURSOR)이고, size 는 1~50 밖이면 클램프된다. 후보가 없거나 존재하지 않는 gridId 는 빈 페이지다. 썸네일은 presigned GET URL 로 내려준다.
 */
export const getGridGlobalVideosInfiniteOptions = (options: Options<GetGridGlobalVideosData>) => {
    const opts = infiniteQueryOptions<GetGridGlobalVideosResponse, DefaultError, InfiniteData<GetGridGlobalVideosResponse>, QueryKey<Options<GetGridGlobalVideosData>>, string | Pick<QueryKey<Options<GetGridGlobalVideosData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetGridGlobalVideosData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    cursor: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getGridGlobalVideos({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getGridGlobalVideosInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

export const getGridVideosQueryKey = (options: Options<GetGridVideosData>) => createQueryKey('getGridVideos', options);

/**
 * 격자별 내 영상 리스트 조회
 *
 * 로그인 사용자가 해당 격자에 올린 본인 영상을 최근 업로드 순(createdAt DESC)으로 반환한다. 미점령·타인만 점령한 격자·존재하지 않는 gridId 는 빈 배열이다. 썸네일은 presigned GET URL 로 내려주며 READY 이전이면 null 이다.
 */
export const getGridVideosOptions = (options: Options<GetGridVideosData>) => queryOptions<GetGridVideosResponse, DefaultError, GetGridVideosResponse, ReturnType<typeof getGridVideosQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getGridVideos({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getGridVideosQueryKey(options)
});

export const getGridCoverQueryKey = (options: Options<GetGridCoverData>) => createQueryKey('getGridCover', options);

/**
 * 격자 전역 대표 영상 조회
 *
 * 그 격자를 전역에서 대표하는 영상 1건을 반환한다. 공개(PUBLIC)·READY 영상 중 조회수(view_count) → 최신(createdAt) 순으로 뽑으며, 본인·타인 영상 모두 후보다. 비공개·삭제·인코딩 미완 영상은 제외한다. 후보가 없으면(미점령·비공개만·존재하지 않는 gridId) data 는 null 이다. 썸네일은 presigned GET URL 로 내려준다.
 */
export const getGridCoverOptions = (options: Options<GetGridCoverData>) => queryOptions<GetGridCoverResponse, DefaultError, GetGridCoverResponse, ReturnType<typeof getGridCoverQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getGridCover({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getGridCoverQueryKey(options)
});

export const getFriendsQueryKey = (options?: Options<GetFriendsData>) => createQueryKey('getFriends', options);

/**
 * 친구 목록 조회
 *
 * 수락된 친구 전체를 반환한다 — 누가 먼저 요청했는지와 무관하다. 기본 정렬은 친구가 된 시각 내림차순이고 sort=nickname 이면 닉네임순이다. 친구가 없으면 빈 배열.
 */
export const getFriendsOptions = (options?: Options<GetFriendsData>) => queryOptions<GetFriendsResponse, DefaultError, GetFriendsResponse, ReturnType<typeof getFriendsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getFriends({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getFriendsQueryKey(options)
});

export const getFriendProfileQueryKey = (options: Options<GetFriendProfileData>) => createQueryKey('getFriendProfile', options);

/**
 * 친구 프로필·도감 요약 조회
 *
 * 친구의 프로필(닉네임·프로필 이미지·도감 색상)과 도감 요약(수집 격자 수·영상 총합·방문 동 수), 최근 수집 격자 최대 30개를 한 번에 반환한다. 도감 요약 수치는 그 친구가 자기 도감에서 보는 값과 같다. 썸네일은 그 격자에 재생 가능한 공개 영상이 있을 때만 붙는다. 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendProfileOptions = (options: Options<GetFriendProfileData>) => queryOptions<GetFriendProfileResponse, DefaultError, GetFriendProfileResponse, ReturnType<typeof getFriendProfileQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getFriendProfile({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getFriendProfileQueryKey(options)
});

export const getFriendGridsQueryKey = (options: Options<GetFriendGridsData>) => createQueryKey('getFriendGrids', options);

/**
 * 친구 격자 뷰포트 조회
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 그 친구가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답 형상·검증 규칙·에러는 내 격자 조회(GET /api/grids)와 같다 — 응답의 nextCursor 를 다음 요청 cursor 에 넣어 이어 조회하고, bbox 한 변의 span 은 최대 0.5도다. 격자 색상은 내려주지 않는다(FE 단일색 렌더). 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendGridsOptions = (options: Options<GetFriendGridsData>) => queryOptions<GetFriendGridsResponse, DefaultError, GetFriendGridsResponse, ReturnType<typeof getFriendGridsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getFriendGrids({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getFriendGridsQueryKey(options)
});

export const getFriendGridsInfiniteQueryKey = (options: Options<GetFriendGridsData>): QueryKey<Options<GetFriendGridsData>> => createQueryKey('getFriendGrids', options, true);

/**
 * 친구 격자 뷰포트 조회
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 그 친구가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답 형상·검증 규칙·에러는 내 격자 조회(GET /api/grids)와 같다 — 응답의 nextCursor 를 다음 요청 cursor 에 넣어 이어 조회하고, bbox 한 변의 span 은 최대 0.5도다. 격자 색상은 내려주지 않는다(FE 단일색 렌더). 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendGridsInfiniteOptions = (options: Options<GetFriendGridsData>) => {
    const opts = infiniteQueryOptions<GetFriendGridsResponse, DefaultError, InfiniteData<GetFriendGridsResponse>, QueryKey<Options<GetFriendGridsData>>, string | Pick<QueryKey<Options<GetFriendGridsData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetFriendGridsData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    cursor: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getFriendGrids({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getFriendGridsInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

export const getFriendGridVideosQueryKey = (options: Options<GetFriendGridVideosData>) => createQueryKey('getFriendGridVideos', options);

/**
 * 친구 격자 영상 목록 조회
 *
 * 그 친구가 해당 격자에 올린 영상을 최근 업로드 순으로 반환한다. 친구에게 공개된 영상(전체 공개·친구만 보기)만 담기고 비공개 영상은 포함되지 않으며, 삭제·인코딩 미완 영상도 제외된다 — 목록의 영상은 모두 재생 조회로 바로 진입할 수 있다. 친구가 점령하지 않은 격자·존재하지 않는 gridId 는 빈 배열이다. 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendGridVideosOptions = (options: Options<GetFriendGridVideosData>) => queryOptions<GetFriendGridVideosResponse, DefaultError, GetFriendGridVideosResponse, ReturnType<typeof getFriendGridVideosQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getFriendGridVideos({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getFriendGridVideosQueryKey(options)
});

export const getReceivedRequestsQueryKey = (options?: Options<GetReceivedRequestsData>) => createQueryKey('getReceivedRequests', options);

/**
 * 받은 친구 요청 목록
 *
 * 내가 수신자인 대기 중 요청을 최신순으로 반환한다. 항목의 requesterId 를 수락/거절 경로 변수로 그대로 쓴다.
 */
export const getReceivedRequestsOptions = (options?: Options<GetReceivedRequestsData>) => queryOptions<GetReceivedRequestsResponse, DefaultError, GetReceivedRequestsResponse, ReturnType<typeof getReceivedRequestsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getReceivedRequests({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getReceivedRequestsQueryKey(options)
});

export const previewQueryKey = (options: Options<PreviewData>) => createQueryKey('preview', options);

/**
 * 친구 코드 미리보기
 *
 * 요청을 보내기 전 확인 화면용 — 코드 소유자의 닉네임만 반환한다. 요청 가능 여부 검증(자기 자신·중복 등)은 요청 API 가 수행한다.
 */
export const previewOptions = (options: Options<PreviewData>) => queryOptions<PreviewResponse, DefaultError, PreviewResponse, ReturnType<typeof previewQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await preview({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: previewQueryKey(options)
});

export const getMyFriendCodeQueryKey = (options?: Options<GetMyFriendCodeData>) => createQueryKey('getMyFriendCode', options);

/**
 * 내 친구 코드 조회
 *
 * 가입 시 자동 부여된 고정 8자 코드를 반환한다. 상대에게 임의 채널(카톡 등)로 공유하면 상대가 이 코드로 친구 요청을 보낼 수 있다. 재발급 없음.
 */
export const getMyFriendCodeOptions = (options?: Options<GetMyFriendCodeData>) => queryOptions<GetMyFriendCodeResponse, DefaultError, GetMyFriendCodeResponse, ReturnType<typeof getMyFriendCodeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getMyFriendCode({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getMyFriendCodeQueryKey(options)
});

export const getRegionVideosQueryKey = (options: Options<GetRegionVideosData>) => createQueryKey('getRegionVideos', options);

/**
 * 동 단위 내 영상 조회
 *
 * 행정동(regionCode) 격자들에 올린 로그인 사용자의 영상을 created_at 내림차순으로 반환한다(무커서). regionCode 는 by-grid 응답의 regionCode 를 그대로 넘긴다. 귀속은 격자 축이라 영상 좌표가 옆 동이어도 격자 소속 행정동 기준으로 포함된다. 내 도감이라 PRIVATE·인코딩 중 영상도 포함하며(status ACTIVE 만), 그 행정동에 내 영상이 없거나 미존재 regionCode 면 에러 없이 빈 배열을 받는다.
 */
export const getRegionVideosOptions = (options: Options<GetRegionVideosData>) => queryOptions<GetRegionVideosResponse, DefaultError, GetRegionVideosResponse, ReturnType<typeof getRegionVideosQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRegionVideos({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRegionVideosQueryKey(options)
});

export const getSummaryQueryKey = (options?: Options<GetSummaryData>) => createQueryKey('getSummary', options);

/**
 * 개인 도감 요약 조회
 *
 * 로그인 사용자의 점령한 격자 수·올린 영상 총합·방문한 행정동 수를 한 번에 반환한다. 점령 0건 사용자도 에러 없이 세 값이 모두 0으로 응답한다.
 */
export const getSummaryOptions = (options?: Options<GetSummaryData>) => queryOptions<GetSummaryResponse, DefaultError, GetSummaryResponse, ReturnType<typeof getSummaryQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getSummary({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getSummaryQueryKey(options)
});

export const getCollectionGridsQueryKey = (options?: Options<GetCollectionGridsData>) => createQueryKey('getCollectionGrids', options);

/**
 * 갤러리 격자 목록 조회
 *
 * 로그인 사용자가 최근 수집한 격자를 first_collected_at 내림차순 최대 30개로 반환한다(무커서). 각 항목은 gridId·gridY/gridX·수집/방문 시각·영상 수·cover 영상 ID·cover 썸네일 URL 을 담는다. 점령 0건 사용자는 에러 없이 빈 배열을 받는다.
 */
export const getCollectionGridsOptions = (options?: Options<GetCollectionGridsData>) => queryOptions<GetCollectionGridsResponse, DefaultError, GetCollectionGridsResponse, ReturnType<typeof getCollectionGridsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getCollectionGrids({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getCollectionGridsQueryKey(options)
});

export const findMyBadgesQueryKey = (options?: Options<FindMyBadgesData>) => createQueryKey('findMyBadges', options);

/**
 * 내 뱃지 전체 목록
 *
 * 시딩된 전체 뱃지를 내 획득 상태와 함께 시딩 순(badges.id 오름차순)으로 반환한다 — 미획득 행은 earned false·earnedAt null·isNew false·featuredRank null. 이번 응답에 노출된 미확인(새 뱃지) 행은 자동으로 확인 처리되어 다음 조회부터 isNew false 가 된다.
 */
export const findMyBadgesOptions = (options?: Options<FindMyBadgesData>) => queryOptions<FindMyBadgesResponse, DefaultError, FindMyBadgesResponse, ReturnType<typeof findMyBadgesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await findMyBadges({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: findMyBadgesQueryKey(options)
});

export const redirectToKakaoAuthorizeQueryKey = (options: Options<RedirectToKakaoAuthorizeData>) => createQueryKey('redirectToKakaoAuthorize', options);

/**
 * 카카오 로그인 시작 (인가 진입점)
 *
 * 웹 로그인의 시작점이다. 클라이언트는 이 URL 로 이동하기만 하면 된다(location.href). 서버가 카카오 인가 URL(client_id·response_type=code·scope=openid·nonce 포함)을 조립해 302 로 보내면서 같은 응답에 OAUTH_NONCE 쿠키(HttpOnly, 10분)를 심는다. 그래서 scope=openid 누락이나 nonce 누락이 구조적으로 불가능하고, REST API 키가 클라이언트 코드로 나갈 일도 없다. 응답은 리다이렉트라 공통 응답 포맷을 쓰지 않는다.
 */
export const redirectToKakaoAuthorizeOptions = (options: Options<RedirectToKakaoAuthorizeData>) => queryOptions<unknown, DefaultError, unknown, ReturnType<typeof redirectToKakaoAuthorizeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await redirectToKakaoAuthorize({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: redirectToKakaoAuthorizeQueryKey(options)
});

export const getVideoForReviewQueryKey = (options: Options<GetVideoForReviewData>) => createQueryKey('getVideoForReview', options);

/**
 * 관리자 단건 영상 확인
 *
 * 신고 판단용으로 영상 하나를 확인한다 — 공개범위와 상태(BLINDED 포함)를 무시하고 요청 시점에 재생·썸네일 presigned URL 을 발급하며, 조회수를 올리지 않는다. 처리 상태가 READY 가 아니면 playbackUrl 과 expiresInSec 은 null 이다. 없는 영상과 삭제된 영상은 404(3404) 다.
 */
export const getVideoForReviewOptions = (options: Options<GetVideoForReviewData>) => queryOptions<GetVideoForReviewResponse, DefaultError, GetVideoForReviewResponse, ReturnType<typeof getVideoForReviewQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getVideoForReview({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getVideoForReviewQueryKey(options)
});

export const getReportsQueryKey = (options?: Options<GetReportsData>) => createQueryKey('getReports', options);

/**
 * 신고 목록 조회
 *
 * 상태 필터 기준으로 신고를 접수 최신순 페이지 단위로 조회한다. 기본은 미처리(PENDING) 신고다. 항목에 신고자·영상 소유자 닉네임과 영상 현재 상태가 함께 담겨 목록만으로 판단할 수 있다. 지원하지 않는 status 는 400(11420), page 음수나 size 범위(1~100) 밖은 400(11421) 이다. REVIEWING 은 유효한 값이지만 만드는 경로가 없어 항상 빈 목록이다.
 */
export const getReportsOptions = (options?: Options<GetReportsData>) => queryOptions<GetReportsResponse, DefaultError, GetReportsResponse, ReturnType<typeof getReportsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getReports({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getReportsQueryKey(options)
});

export const getReportsInfiniteQueryKey = (options?: Options<GetReportsData>): QueryKey<Options<GetReportsData>> => createQueryKey('getReports', options, true);

/**
 * 신고 목록 조회
 *
 * 상태 필터 기준으로 신고를 접수 최신순 페이지 단위로 조회한다. 기본은 미처리(PENDING) 신고다. 항목에 신고자·영상 소유자 닉네임과 영상 현재 상태가 함께 담겨 목록만으로 판단할 수 있다. 지원하지 않는 status 는 400(11420), page 음수나 size 범위(1~100) 밖은 400(11421) 이다. REVIEWING 은 유효한 값이지만 만드는 경로가 없어 항상 빈 목록이다.
 */
export const getReportsInfiniteOptions = (options?: Options<GetReportsData>) => {
    const opts = infiniteQueryOptions<GetReportsResponse, DefaultError, InfiniteData<GetReportsResponse>, QueryKey<Options<GetReportsData>>, number | Pick<QueryKey<Options<GetReportsData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetReportsData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    page: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getReports({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getReportsInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

/**
 * 친구 삭제
 *
 * 친구 관계를 해소한다. 어느 쪽이든 삭제할 수 있고 즉시 양쪽 모두에서 사라진다. 대기 중 요청은 대상이 아니다.
 */
export const deleteFriendMutation = (options?: Partial<Options<DeleteFriendData>>): UseMutationOptions<unknown, DefaultError, Options<DeleteFriendData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<DeleteFriendData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteFriend({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
