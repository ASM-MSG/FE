// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AcceptData, AcceptResponses, DeleteData, DeleteFriendData, DeleteFriendResponses, DeleteMeData, DeleteMeResponses, DeleteResponses, FindMyBadgesData, FindMyBadgesResponses, GetActiveMissionsData, GetActiveMissionsResponses, GetCellData, GetCellResponses, GetCollectionGridsData, GetCollectionGridsResponses, GetExploreRegionsData, GetExploreRegionsResponses, GetFriendGridsData, GetFriendGridsResponses, GetFriendGridVideosData, GetFriendGridVideosResponses, GetFriendProfileData, GetFriendProfileResponses, GetFriendsData, GetFriendsResponses, GetGridCoverData, GetGridCoverResponses, GetGridGlobalVideosData, GetGridGlobalVideosResponses, GetGridVideosData, GetGridVideosResponses, GetHotZonesData, GetHotZonesResponses, GetMeData, GetMeResponses, GetMyFriendCodeData, GetMyFriendCodeResponses, GetOccupiedInViewportData, GetOccupiedInViewportResponses, GetPlaybackData, GetPlaybackResponses, GetPreferencesData, GetPreferencesResponses, GetReceivedRequestsData, GetReceivedRequestsResponses, GetRegionGridsData, GetRegionGridsResponses, GetRegionVideosData, GetRegionVideosResponses, GetStatByGridData, GetStatByGridResponses, GetStatByPointData, GetStatByPointResponses, GetStatsData, GetStatsResponses, GetSummaryData, GetSummaryResponses, GetTrendingKeywordsData, GetTrendingKeywordsResponses, GetZonesData, GetZonesResponses, IssuePresignedUrlData, IssuePresignedUrlResponses, LoginData, LoginResponses, LogoutData, LogoutResponses, OauthLoginData, OauthLoginResponses, PreviewData, PreviewResponses, RegisterData, RegisterResponses, ReissueData, ReissueResponses, RejectData, RejectResponses, ReplaceData, ReplaceFeaturedData, ReplaceFeaturedResponses, ReplaceResponses, ReportData, ReportResponses, RequestData, RequestResponses, ReverseGeocodeData, ReverseGeocodeResponses, SearchPlacesData, SearchPlacesResponses, SetVisibilityData, SetVisibilityResponses, SignupData, SignupResponses, SocialLoginData, SocialLoginResponses, UnregisterData, UnregisterResponses, UpdateData, UpdateNicknameData, UpdateNicknameResponses, UpdateResponses, UploadData, UploadResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * 영상 삭제
 *
 * 영상을 삭제한다. 해당 격자의 내 영상이 모두 사라지면 점령이 롤백(색칠 해제)된다.
 */
export const delete_ = <ThrowOnError extends boolean = false>(options: Options<DeleteData, ThrowOnError>): RequestResult<DeleteResponses, unknown, ThrowOnError> => (options.client ?? client).delete<DeleteResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/{videoId}',
    ...options
});

/**
 * 단건 영상 재생 조회
 *
 * 영상 하나의 표시용 메타와 재생본 presigned GET URL을 발급한다. 소유자·타인 모두 조회할 수 있으나 삭제·블라인드(타인)는 404, 비공개(타인)·친구만 공개(비친구)는 403이다. READY가 아니면 playbackUrl은 null이다.
 */
export const getPlayback = <ThrowOnError extends boolean = false>(options: Options<GetPlaybackData, ThrowOnError>): RequestResult<GetPlaybackResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetPlaybackResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/{videoId}',
    ...options
});

/**
 * 영상 교체
 *
 * 기존 영상을 새 파일로 교체한다. 좌표를 생략하면 격자를 유지하고 파일만 교체하며, 좌표를 보내면 기존과 같은 격자여야 한다(다르면 거부). 교체 직후 상태는 UPLOADED다.
 */
export const replace = <ThrowOnError extends boolean = false>(options: Options<ReplaceData, ThrowOnError>): RequestResult<ReplaceResponses, unknown, ThrowOnError> => (options.client ?? client).put<ReplaceResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/{videoId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 닉네임 수정
 *
 * 닉네임(2~20자)을 교체하고 변경 후 프로필을 반환한다. 중복 닉네임은 허용된다.
 */
export const updateNickname = <ThrowOnError extends boolean = false>(options: Options<UpdateNicknameData, ThrowOnError>): RequestResult<UpdateNicknameResponses, unknown, ThrowOnError> => (options.client ?? client).put<UpdateNicknameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/users/me/nickname',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 대표 뱃지 집합 교체
 *
 * 획득한 뱃지 중 최대 2개를 대표로 교체 지정한다(멱등). 배열 순서 = 표시 순서(rank 1·2), 빈 배열은 전부 해제. 미획득·미존재 뱃지는 7403, 중복 id 는 7400, 3개 이상은 400 이다.
 */
export const replaceFeatured = <ThrowOnError extends boolean = false>(options: Options<ReplaceFeaturedData, ThrowOnError>): RequestResult<ReplaceFeaturedResponses, unknown, ThrowOnError> => (options.client ?? client).put<ReplaceFeaturedResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/badges/featured',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 영상 메타데이터 저장 (업로드 확정)
 *
 * S3 업로드 완료 후 영상 메타데이터를 저장하고 좌표로 격자를 매핑한다. 해당 격자에 내 첫 영상이면 점령(occupied=true)된다.
 */
export const upload = <ThrowOnError extends boolean = false>(options: Options<UploadData, ThrowOnError>): RequestResult<UploadResponses, unknown, ThrowOnError> => (options.client ?? client).post<UploadResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 영상 신고 접수
 *
 * 다른 사람의 영상을 사유 5종(INAPPROPRIATE, PRIVACY, SPAM, COPYRIGHT, OTHER) 중 하나와 함께 신고한다. 접수된 신고는 PENDING 으로 쌓여 관리자 처리의 입력이 되며, 접수 자체는 영상 상태를 바꾸지 않는다. 같은 영상 재신고는 409, 자기 영상 신고는 400, 없는 영상·삭제·블라인드 영상은 재생 조회와 같은 404 다.
 */
export const report = <ThrowOnError extends boolean = false>(options: Options<ReportData, ThrowOnError>): RequestResult<ReportResponses, unknown, ThrowOnError> => (options.client ?? client).post<ReportResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/{videoId}/reports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 업로드용 presigned URL 발급
 *
 * 영상 파일을 S3에 직접 올릴 presigned URL을 발급한다. 이 URL로 PUT 업로드한 뒤 메타데이터 저장을 호출한다.
 */
export const issuePresignedUrl = <ThrowOnError extends boolean = false>(options: Options<IssuePresignedUrlData, ThrowOnError>): RequestResult<IssuePresignedUrlResponses, unknown, ThrowOnError> => (options.client ?? client).post<IssuePresignedUrlResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/presigned-url',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * FCM 토큰 해제
 *
 * 본인 소유(user_id 일치) 토큰 행을 삭제한다 — 멱등, 없는 토큰·소유 불일치 해제도 200. 로그아웃은 /api/auth/logout body 의 fcmToken 으로 한 번에 처리하고, 이 API 는 토큰 로테이션 등 로그아웃 외 정리 용도다.
 */
export const unregister = <ThrowOnError extends boolean = false>(options: Options<UnregisterData, ThrowOnError>): RequestResult<UnregisterResponses, unknown, ThrowOnError> => (options.client ?? client).delete<UnregisterResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/notifications/tokens',
    ...options
});

/**
 * FCM 토큰 등록/갱신
 *
 * 디바이스의 FCM 토큰을 현재 계정으로 등록한다(UPSERT). 같은 토큰 재등록은 충돌 없이 user_id·platform·appVersion·last_used_at 이 갱신된다 — 재로그인·계정 전환 포함. platform 이 IOS/ANDROID/WEB(대소문자 무시) 외면 10400 이다.
 */
export const register = <ThrowOnError extends boolean = false>(options: Options<RegisterData, ThrowOnError>): RequestResult<RegisterResponses, unknown, ThrowOnError> => (options.client ?? client).post<RegisterResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/notifications/tokens',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 친구 요청
 *
 * 상대의 친구 코드로 요청을 보낸다. 응답 status 가 PENDING 이면 상대 수락 대기, ACCEPTED 면 상대가 먼저 보낸 요청이 있어 즉시 친구 성립(자동 수락)이다.
 */
export const request = <ThrowOnError extends boolean = false>(options: Options<RequestData, ThrowOnError>): RequestResult<RequestResponses, unknown, ThrowOnError> => (options.client ?? client).post<RequestResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 친구 요청 거절
 *
 * 받은 요청을 거절한다. 보낸 쪽에 통지는 없고, 상대는 다시 요청할 수 있다.
 */
export const reject = <ThrowOnError extends boolean = false>(options: Options<RejectData, ThrowOnError>): RequestResult<RejectResponses, unknown, ThrowOnError> => (options.client ?? client).post<RejectResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/requests/{requesterId}/reject',
    ...options
});

/**
 * 친구 요청 수락
 *
 * 받은 요청을 수락해 친구 관계를 성립시킨다. 요청의 수신자 본인만 가능하다.
 */
export const accept = <ThrowOnError extends boolean = false>(options: Options<AcceptData, ThrowOnError>): RequestResult<AcceptResponses, unknown, ThrowOnError> => (options.client ?? client).post<AcceptResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/requests/{requesterId}/accept',
    ...options
});

/**
 * 이메일 회원가입
 *
 * 이메일/비밀번호/닉네임으로 신규 회원을 생성한다.
 */
export const signup = <ThrowOnError extends boolean = false>(options: Options<SignupData, ThrowOnError>): RequestResult<SignupResponses, unknown, ThrowOnError> => (options.client ?? client).post<SignupResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/signup',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 토큰 재발급
 *
 * 리프레시 토큰(웹=쿠키, 앱=body)으로 새 액세스 토큰과 회전된 새 리프레시 토큰을 발급받는다. 직전 리프레시 토큰은 즉시 무효화되며, 회전된 옛 토큰 재사용 시 세션 체인이 폐기된다.
 */
export const reissue = <ThrowOnError extends boolean = false>(options?: Options<ReissueData, ThrowOnError>): RequestResult<ReissueResponses, unknown, ThrowOnError> => (options?.client ?? client).post<ReissueResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/reissue',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * 소셜 로그인 (OIDC)
 *
 * 소셜 제공자의 ID Token으로 로그인/가입하고 JWT 액세스 토큰과 리프레시 토큰을 발급받는다.
 */
export const oauthLogin = <ThrowOnError extends boolean = false>(options: Options<OauthLoginData, ThrowOnError>): RequestResult<OauthLoginResponses, unknown, ThrowOnError> => (options.client ?? client).post<OauthLoginResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/oauth/{provider}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 로그아웃
 *
 * Authorization 헤더의 액세스 토큰을 무효화하고 해당 디바이스(X-Device-Id)의 리프레시 세션을 삭제한다. X-Device-Id 가 없으면 해당 유저의 모든 디바이스 세션을 삭제한다. 선택 body 의 fcmToken 이 있으면 해당 FCM 푸시 토큰도 함께 정리된다 (MSG-178 logout 통합).
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>): RequestResult<LogoutResponses, unknown, ThrowOnError> => (options?.client ?? client).post<LogoutResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * 이메일 로그인
 *
 * 이메일/비밀번호로 로그인하고 JWT 액세스 토큰과 리프레시 토큰을 발급받는다. 웹(X-Client-Type: web, 기본)은 리프레시가 HttpOnly 쿠키로, 앱(app)은 body 로 내려간다.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>): RequestResult<LoginResponses, unknown, ThrowOnError> => (options.client ?? client).post<LoginResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * [개발용] 소셜 로그인 모의
 *
 * 실제 OIDC ID Token 검증 없이 (provider, oid)로 사용자를 find-or-create 하고 액세스+리프레시 토큰을 발급한다. 리프레시는 body 로 내려간다(앱 모드). 로컬/dev 프로파일에서만 노출.
 */
export const socialLogin = <ThrowOnError extends boolean = false>(options: Options<SocialLoginData, ThrowOnError>): RequestResult<SocialLoginResponses, unknown, ThrowOnError> => (options.client ?? client).post<SocialLoginResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/auth/dev/social-login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 영상 공개 범위 전환
 *
 * 본인 영상의 공개 범위를 PUBLIC·PRIVATE·FRIENDS 간 전환한다. 전환된 상태를 반환하며, 같은 값 재전환은 멱등하게 성공한다.
 */
export const setVisibility = <ThrowOnError extends boolean = false>(options: Options<SetVisibilityData, ThrowOnError>): RequestResult<SetVisibilityResponses, unknown, ThrowOnError> => (options.client ?? client).patch<SetVisibilityResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/videos/{videoId}/visibility',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 카테고리 수신 토글
 *
 * 카테고리 하나의 수신 여부를 바꾸고 변경 후 전체 상태를 반환한다 — 같은 값 재전환은 멱등. category 가 BADGE/HOTZONE/REMIND(대소문자 무시) 외면 10420 이다. off 는 발송만 막고 off 중 쌓인 알림이 on 복귀 후 재발송되는 일은 없다.
 */
export const update = <ThrowOnError extends boolean = false>(options: Options<UpdateData, ThrowOnError>): RequestResult<UpdateResponses, unknown, ThrowOnError> => (options.client ?? client).patch<UpdateResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/notifications/preferences/{category}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * 구역 목록 조회
 *
 * 전체 구역(zone) 목록을 반환한다. FE 가 캐시해 gridId 로 표시명을 로컬 산술하고 구역 오버레이에 쓴다. 시딩 전이면 빈 배열(전 시스템이 행정동 폴백으로 동작).
 */
export const getZones = <ThrowOnError extends boolean = false>(options?: Options<GetZonesData, ThrowOnError>): RequestResult<GetZonesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetZonesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/zones',
    ...options
});

/**
 * 계정 삭제
 *
 * 내 계정을 즉시·비가역 삭제한다. 연쇄 개인 데이터·영상 S3 객체가 제거되고 전 디바이스 세션이 무효화된다. 같은 이메일·카카오 계정으로 다시 로그인하면 신규 가입이다.
 */
export const deleteMe = <ThrowOnError extends boolean = false>(options: Options<DeleteMeData, ThrowOnError>): RequestResult<DeleteMeResponses, unknown, ThrowOnError> => (options.client ?? client).delete<DeleteMeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/users/me',
    ...options
});

/**
 * 내 프로필 조회
 *
 * 소셜 로그인이 자동 저장한 이메일·닉네임을 반환한다. 항상 본인 계정만 — 경로에 대상 식별자가 없다.
 */
export const getMe = <ThrowOnError extends boolean = false>(options?: Options<GetMeData, ThrowOnError>): RequestResult<GetMeResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetMeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/users/me',
    ...options
});

/**
 * 인기 검색어 TOP 10
 *
 * 오늘+어제(KST) 검색어 집계를 합산해 상위 10개를 순위·검색어로 반환한다. 동률은 검색어 사전순. 검색 횟수와 장소 정보는 포함하지 않으며(클릭 후 장소 검색 API 재호출), 집계가 없으면 200 + 빈 배열이다.
 */
export const getTrendingKeywords = <ThrowOnError extends boolean = false>(options?: Options<GetTrendingKeywordsData, ThrowOnError>): RequestResult<GetTrendingKeywordsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetTrendingKeywordsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/search/trending',
    ...options
});

/**
 * 장소 검색 (장소명 → 좌표·격자)
 *
 * 카카오 로컬 키워드 검색 결과(정확도순 ≤15건)에 각 좌표의 격자 ID 를 얹어 반환한다. 선택 즉시 lat/lng 지도 이동 + gridId 격자 하이라이트. q 누락 400 / trim 후 빈 q·무매치 200 [] / 카카오 장애·타임아웃 502(developCode 5502).
 */
export const searchPlaces = <ThrowOnError extends boolean = false>(options: Options<SearchPlacesData, ThrowOnError>): RequestResult<SearchPlacesResponses, unknown, ThrowOnError> => (options.client ?? client).get<SearchPlacesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/search/places',
    ...options
});

/**
 * 행정동 격자 카드 리스트 + 헤더 카운트 조회
 *
 * 그 행정동 격자들 중 전역 공개 콘텐츠(공개·인코딩 완료·타인 영상 포함)가 있는 격자를 카드로 반환한다. 헤더 카운트(gridCount·videoCount)는 limit 무관 전체 기준이라 지도 홈 패널(limit=3)과 전체 보기(limit 생략)가 같은 숫자를 본다. 카드 커버는 격자 대표(cover)와 같은 영상이고 썸네일은 presigned GET URL 이다. 미존재·무콘텐츠 regionCode 는 404 가 아니라 200 + 카운트 0·빈 배열이다.
 */
export const getRegionGrids = <ThrowOnError extends boolean = false>(options: Options<GetRegionGridsData, ThrowOnError>): RequestResult<GetRegionGridsResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetRegionGridsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/{regionCode}/grids',
    ...options
});

/**
 * 내 행정동별 수집률 조회
 *
 * 로그인 사용자가 점령(수집)한 격자를 행정동별로 집계한 수집률 리스트를 반환한다. parentCode 로 시군구를 좁힐 수 있고(실존하지 않는 코드면 404/6404), collectedOnly=false 면 롤백으로 0이 된 행정동도 포함한다. 수집이 없으면 404 가 아니라 200 + 빈 배열.
 */
export const getStats = <ThrowOnError extends boolean = false>(options?: Options<GetStatsData, ThrowOnError>): RequestResult<GetStatsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetStatsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/stats',
    ...options
});

/**
 * 현재 위치 행정동 탐험률 (좌표 → 수집률)
 *
 * 도감 갤러리 진입 초기값. 현재 위치 좌표가 속한 행정동 1건의 내 수집률을 반환한다. 그 행정동에 수집이 없어도 0% 로 합성해 반환하고, 어떤 행정동에도 안 속하면(바다·국외) 404 가 아니라 200 + data null. 서비스 범위 밖 좌표는 400(6400).
 */
export const getStatByPoint = <ThrowOnError extends boolean = false>(options: Options<GetStatByPointData, ThrowOnError>): RequestResult<GetStatByPointResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetStatByPointResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/stats/by-point',
    ...options
});

/**
 * 격자 중심 행정동 탐험률 (격자 클릭 → 수집률)
 *
 * 클릭한 격자의 중심점이 속한 행정동 1건의 내 수집률을 반환한다. 귀속 축이 수집률 집계(MSG-155)와 같아 탐험률·라벨이 일치한다. 중심점이 어떤 행정동에도 안 속하거나 gridId 형식이 이상하면 200 + data null(별도 에러 코드 없음).
 */
export const getStatByGrid = <ThrowOnError extends boolean = false>(options: Options<GetStatByGridData, ThrowOnError>): RequestResult<GetStatByGridResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetStatByGridResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/stats/by-grid',
    ...options
});

/**
 * 역지오코딩 (좌표 → 행정동)
 *
 * 좌표를 포함하는 행정동 1건을 반환한다. 포함 행정동이 없으면(바다·국외) 404가 아니라 200 + data null. 서비스 좌표 범위(한국) 밖이면 400(6400).
 */
export const reverseGeocode = <ThrowOnError extends boolean = false>(options: Options<ReverseGeocodeData, ThrowOnError>): RequestResult<ReverseGeocodeResponses, unknown, ThrowOnError> => (options.client ?? client).get<ReverseGeocodeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/reverse-geocode',
    ...options
});

/**
 * 전체 지역 리스트 조회
 *
 * 전역 공개 콘텐츠가 있는 행정동만 격자 수 내림차순으로 반환한다(검색 무입력 드롭다운). 각 항목의 gridCount 는 그 행정동 격자 카드 조회의 gridCount 와 같은 정의다. 전역 공개 콘텐츠가 하나도 없으면 빈 배열이다.
 */
export const getExploreRegions = <ThrowOnError extends boolean = false>(options?: Options<GetExploreRegionsData, ThrowOnError>): RequestResult<GetExploreRegionsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetExploreRegionsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/regions/explore',
    ...options
});

/**
 * 알림 설정 조회
 *
 * 카테고리 3종(BADGE·HOTZONE·REMIND) 전부의 수신 상태를 반환한다. 설정을 만진 적 없는 사용자는 전부 true 다 — opt-out 기본 전부 on.
 */
export const getPreferences = <ThrowOnError extends boolean = false>(options?: Options<GetPreferencesData, ThrowOnError>): RequestResult<GetPreferencesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetPreferencesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/notifications/preferences',
    ...options
});

/**
 * 활성 미션 목록 조회
 *
 * 지금 활성인 미션 전부를 유형별 렌더 shape(코스=PATH·구역=REGION·축제=BOX·테마/지속=CELLS)로 반환한다. bbox 없이 전역 목록이며 1h 전역 캐시로 재계산을 흡수한다. 시드 전이거나 활성 미션이 없으면 빈 배열이다(404 아님).
 */
export const getActiveMissions = <ThrowOnError extends boolean = false>(options?: Options<GetActiveMissionsData, ThrowOnError>): RequestResult<GetActiveMissionsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetActiveMissionsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/missions/active',
    ...options
});

/**
 * 뷰포트 내 핫구역 조회
 *
 * 지도 화면 bbox(남서~북동 좌표) 안의 핫구역을 핫스코어 내림차순으로 반환한다. 전국 상위 K(50)·최소 임계(3) 판정 후 뷰포트 필터 — 없으면 빈 목록이다.
 */
export const getHotZones = <ThrowOnError extends boolean = false>(options: Options<GetHotZonesData, ThrowOnError>): RequestResult<GetHotZonesResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetHotZonesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/hotzones',
    ...options
});

/**
 * 뷰포트 내 색칠 격자 조회 (커서 페이지네이션)
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 내가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답의 nextCursor를 다음 요청 cursor에 넣어 이어서 조회한다. bbox 한 변의 span은 최대 0.5도.
 */
export const getOccupiedInViewport = <ThrowOnError extends boolean = false>(options: Options<GetOccupiedInViewportData, ThrowOnError>): RequestResult<GetOccupiedInViewportResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetOccupiedInViewportResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/grids',
    ...options
});

/**
 * 단일 격자 색칠 상태 조회
 *
 * 특정 격자를 내가 점령(색칠)했는지와 내 영상 수를 반환한다. 미점령 격자도 404가 아니라 occupied=false로 응답한다.
 */
export const getCell = <ThrowOnError extends boolean = false>(options: Options<GetCellData, ThrowOnError>): RequestResult<GetCellResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetCellResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/grids/{gridId}',
    ...options
});

/**
 * 격자 전역 영상 목록 조회
 *
 * 그 격자에 쌓인 공개(PUBLIC)·READY 영상을 전역(본인·타인 포함)에서 조회수(viewCount) → 최신(createdAt) 순으로 페이지 조회한다. 비공개·삭제·인코딩 미완 영상은 본인 것이라도 제외한다. 첫 요청은 cursor 없이 부르고, hasNext 가 true 면 응답의 nextCursor 를 다음 요청 cursor 로 넘기면 이어진다. 무효 커서는 400(INVALID_CURSOR)이고, size 는 1~50 밖이면 클램프된다. 후보가 없거나 존재하지 않는 gridId 는 빈 페이지다. 썸네일은 presigned GET URL 로 내려준다.
 */
export const getGridGlobalVideos = <ThrowOnError extends boolean = false>(options: Options<GetGridGlobalVideosData, ThrowOnError>): RequestResult<GetGridGlobalVideosResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetGridGlobalVideosResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/grids/{gridId}/videos',
    ...options
});

/**
 * 격자별 내 영상 리스트 조회
 *
 * 로그인 사용자가 해당 격자에 올린 본인 영상을 최근 업로드 순(createdAt DESC)으로 반환한다. 미점령·타인만 점령한 격자·존재하지 않는 gridId 는 빈 배열이다. 썸네일은 presigned GET URL 로 내려주며 READY 이전이면 null 이다.
 */
export const getGridVideos = <ThrowOnError extends boolean = false>(options: Options<GetGridVideosData, ThrowOnError>): RequestResult<GetGridVideosResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetGridVideosResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/grids/{gridId}/my-videos',
    ...options
});

/**
 * 격자 전역 대표 영상 조회
 *
 * 그 격자를 전역에서 대표하는 영상 1건을 반환한다. 공개(PUBLIC)·READY 영상 중 조회수(view_count) → 최신(createdAt) 순으로 뽑으며, 본인·타인 영상 모두 후보다. 비공개·삭제·인코딩 미완 영상은 제외한다. 후보가 없으면(미점령·비공개만·존재하지 않는 gridId) data 는 null 이다. 썸네일은 presigned GET URL 로 내려준다.
 */
export const getGridCover = <ThrowOnError extends boolean = false>(options: Options<GetGridCoverData, ThrowOnError>): RequestResult<GetGridCoverResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetGridCoverResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/grids/{gridId}/cover',
    ...options
});

/**
 * 친구 목록 조회
 *
 * 수락된 친구 전체를 반환한다 — 누가 먼저 요청했는지와 무관하다. 기본 정렬은 친구가 된 시각 내림차순이고 sort=nickname 이면 닉네임순이다. 친구가 없으면 빈 배열.
 */
export const getFriends = <ThrowOnError extends boolean = false>(options?: Options<GetFriendsData, ThrowOnError>): RequestResult<GetFriendsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetFriendsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends',
    ...options
});

/**
 * 친구 프로필·도감 요약 조회
 *
 * 친구의 프로필(닉네임·프로필 이미지·도감 색상)과 도감 요약(수집 격자 수·영상 총합·방문 동 수), 최근 수집 격자 최대 30개를 한 번에 반환한다. 도감 요약 수치는 그 친구가 자기 도감에서 보는 값과 같다. 썸네일은 그 격자에 재생 가능한 공개 영상이 있을 때만 붙는다. 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendProfile = <ThrowOnError extends boolean = false>(options: Options<GetFriendProfileData, ThrowOnError>): RequestResult<GetFriendProfileResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetFriendProfileResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/{userId}/profile',
    ...options
});

/**
 * 친구 격자 뷰포트 조회
 *
 * 지도 화면 bbox(남서~북동 좌표) 안에서 그 친구가 점령한 격자를 (grid_y, grid_x) 오름차순으로 반환한다. 응답 형상·검증 규칙·에러는 내 격자 조회(GET /api/grids)와 같다 — 응답의 nextCursor 를 다음 요청 cursor 에 넣어 이어 조회하고, bbox 한 변의 span 은 최대 0.5도다. 격자 색상은 내려주지 않는다(FE 단일색 렌더). 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendGrids = <ThrowOnError extends boolean = false>(options: Options<GetFriendGridsData, ThrowOnError>): RequestResult<GetFriendGridsResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetFriendGridsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/{userId}/grids',
    ...options
});

/**
 * 친구 격자 영상 목록 조회
 *
 * 그 친구가 해당 격자에 올린 영상을 최근 업로드 순으로 반환한다. 친구에게 공개된 영상(전체 공개·친구만 보기)만 담기고 비공개 영상은 포함되지 않으며, 삭제·인코딩 미완 영상도 제외된다 — 목록의 영상은 모두 재생 조회로 바로 진입할 수 있다. 친구가 점령하지 않은 격자·존재하지 않는 gridId 는 빈 배열이다. 친구가 아닌 사용자·본인·존재하지 않는 사용자 조회는 모두 같은 404 다.
 */
export const getFriendGridVideos = <ThrowOnError extends boolean = false>(options: Options<GetFriendGridVideosData, ThrowOnError>): RequestResult<GetFriendGridVideosResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetFriendGridVideosResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/{userId}/grids/{gridId}/videos',
    ...options
});

/**
 * 받은 친구 요청 목록
 *
 * 내가 수신자인 대기 중 요청을 최신순으로 반환한다. 항목의 requesterId 를 수락/거절 경로 변수로 그대로 쓴다.
 */
export const getReceivedRequests = <ThrowOnError extends boolean = false>(options?: Options<GetReceivedRequestsData, ThrowOnError>): RequestResult<GetReceivedRequestsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetReceivedRequestsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/requests/received',
    ...options
});

/**
 * 친구 코드 미리보기
 *
 * 요청을 보내기 전 확인 화면용 — 코드 소유자의 닉네임만 반환한다. 요청 가능 여부 검증(자기 자신·중복 등)은 요청 API 가 수행한다.
 */
export const preview = <ThrowOnError extends boolean = false>(options: Options<PreviewData, ThrowOnError>): RequestResult<PreviewResponses, unknown, ThrowOnError> => (options.client ?? client).get<PreviewResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/preview',
    ...options
});

/**
 * 내 친구 코드 조회
 *
 * 가입 시 자동 부여된 고정 8자 코드를 반환한다. 상대에게 임의 채널(카톡 등)로 공유하면 상대가 이 코드로 친구 요청을 보낼 수 있다. 재발급 없음.
 */
export const getMyFriendCode = <ThrowOnError extends boolean = false>(options?: Options<GetMyFriendCodeData, ThrowOnError>): RequestResult<GetMyFriendCodeResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetMyFriendCodeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/code',
    ...options
});

/**
 * 동 단위 내 영상 조회
 *
 * 행정동(regionCode) 격자들에 올린 로그인 사용자의 영상을 created_at 내림차순으로 반환한다(무커서). regionCode 는 by-grid 응답의 regionCode 를 그대로 넘긴다. 귀속은 격자 축이라 영상 좌표가 옆 동이어도 격자 소속 행정동 기준으로 포함된다. 내 도감이라 PRIVATE·인코딩 중 영상도 포함하며(status ACTIVE 만), 그 행정동에 내 영상이 없거나 미존재 regionCode 면 에러 없이 빈 배열을 받는다.
 */
export const getRegionVideos = <ThrowOnError extends boolean = false>(options: Options<GetRegionVideosData, ThrowOnError>): RequestResult<GetRegionVideosResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetRegionVideosResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/collections/videos',
    ...options
});

/**
 * 개인 도감 요약 조회
 *
 * 로그인 사용자의 점령한 격자 수·올린 영상 총합·방문한 행정동 수를 한 번에 반환한다. 점령 0건 사용자도 에러 없이 세 값이 모두 0으로 응답한다.
 */
export const getSummary = <ThrowOnError extends boolean = false>(options?: Options<GetSummaryData, ThrowOnError>): RequestResult<GetSummaryResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetSummaryResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/collections/summary',
    ...options
});

/**
 * 갤러리 격자 목록 조회
 *
 * 로그인 사용자가 최근 수집한 격자를 first_collected_at 내림차순 최대 30개로 반환한다(무커서). 각 항목은 gridId·gridY/gridX·수집/방문 시각·영상 수·cover 영상 ID·cover 썸네일 URL 을 담는다. 점령 0건 사용자는 에러 없이 빈 배열을 받는다.
 */
export const getCollectionGrids = <ThrowOnError extends boolean = false>(options?: Options<GetCollectionGridsData, ThrowOnError>): RequestResult<GetCollectionGridsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetCollectionGridsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/collections/grids',
    ...options
});

/**
 * 내 뱃지 전체 목록
 *
 * 시딩된 전체 뱃지를 내 획득 상태와 함께 시딩 순(badges.id 오름차순)으로 반환한다 — 미획득 행은 earned false·earnedAt null·isNew false·featuredRank null. 이번 응답에 노출된 미확인(새 뱃지) 행은 자동으로 확인 처리되어 다음 조회부터 isNew false 가 된다.
 */
export const findMyBadges = <ThrowOnError extends boolean = false>(options?: Options<FindMyBadgesData, ThrowOnError>): RequestResult<FindMyBadgesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<FindMyBadgesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/badges',
    ...options
});

/**
 * 친구 삭제
 *
 * 친구 관계를 해소한다. 어느 쪽이든 삭제할 수 있고 즉시 양쪽 모두에서 사라진다. 대기 중 요청은 대상이 아니다.
 */
export const deleteFriend = <ThrowOnError extends boolean = false>(options: Options<DeleteFriendData, ThrowOnError>): RequestResult<DeleteFriendResponses, unknown, ThrowOnError> => (options.client ?? client).delete<DeleteFriendResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/friends/{userId}',
    ...options
});
