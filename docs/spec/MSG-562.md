# MSG-562: [모바일] 이벤트 3단계 — 현장 영상 상세 · 도움돼요 · 댓글 (웹 MSG-520 이식)

## 기획 요약

MSG-560이 옮긴 위치 상세 시트의 영상 카드는 지금 일반 재생 화면(`/video/[videoId]`)으로 빠진다. 웹 MSG-520의 이벤트 영상 미니 패널(재생 + 도움돼요 + 댓글)을 **같은 시트 안의 "현장 영상 상세" 단계**로 옮겨 칩 → 목록 → 개요 → 위치 → 영상 흐름을 끝까지 잇는다. 동작·표시값·에러 문구는 웹 `apps/web/src/features/event` 코드 + hey-api 생성물 + 서버 실응답으로 확정했고, 앱 고유(시트 단계·back 체인·재생 표면·하단 입력·토스트 위치)만 이 스펙이 정한다. 웹·`packages` 변경 0.

## 0. 착수 전 실측 (2026-09-03, 워크트리 `FE-MSG-562` = origin/develop f26d3b5)

### 0-1. 정본 파일 (결정 근거로 인용)

| 파일 | 역할 |
|---|---|
| `docs/spec/MSG-520.md` | 웹 AC 13개·확정 결정·오탐 방지 7항·작업 로그(대안 기각 사유) |
| `apps/web/src/features/event/model/event-video-view.ts` | 제목 폴백 `eventVideoTitle`(:17-26) · 메타 줄 `eventVideoMetaLine`(:29-33) · 댓글 판정 `canSubmitComment`/`trimmedCommentContent`(:39-45) · 페이지 병합 `mergeCommentPages`(:52-67, 중복 commentId 방어) · 커서 `nextCommentsCursor`(:73-80 — `extraPages.at(-1)`) · 에러 문구 `eventInteractionErrorMessage`(:86-96 — 13422/13406/기타) · 재생 속도 `PLAYBACK_RATE_OPTIONS`(:99-104, **제외 범위**) |
| `…/model/use-event-video-detail-query.ts` | `getVideoDetailOptions` + `unwrapEnvelope` + `refetchOnWindowFocus/Reconnect: false`(:28-33) · 조회수 부작용 주석(:17-22) · `retry = refetch`(:39) |
| `…/model/use-event-comments-pages.ts` | 첫 페이지 상세 내장 + `queryClient.fetchQuery(getCommentsOptions({path, query:{cursor}}), retry:false)` 로컬 축적(:52-76) · 영상 교체 시 리셋(:45-48) · 요청 중 교체 폐기(:65-69) · 실패 `onLoadError`, 목록 유지(:70-72) |
| `…/api/use-event-video-mutations.ts` | `helpfulByMe ? removeHelpful : addHelpful`(:73-77) · 응답 `{helpfulCount, helpfulByMe}` `setQueryData` seed(:78-84) · 위치 영상 목록 `_id` 부분 키 invalidate(:50-55) · 댓글 append(중복 방어)+`commentCount+1` seed(:114-128) · 상세 invalidate 금지(:21-23) · 콜백은 훅 레벨(:24-25) |
| `apps/web/src/features/event/model/event-room-store.ts` | `videoId` 슬롯(:28) · 같은 방 재open은 videoId 유지(:67) · `selectLocation`은 videoId 리셋(:79) · `back()` 영상 → 위치 → 방(:83-91) |
| `apps/web/src/pages/map-home/ui/EventVideoMiniPanel.tsx` | 구성 순서(닫기 → 재생 표면 → 제목/메타 → 도움돼요 버튼(aria-pressed)+댓글 pill(비버튼 :192-196) → 댓글 목록 → 입력) · 초안 리셋(:63-68) · 제출 videoId 대조(:86-88) · 잠금 가드(:107·117) · 토스트(:225-229) |
| `EventVideoComments.tsx` | 빈 문구 `아직 댓글이 없어요. 첫 댓글을 남겨보세요`(:27) · `Avatar size sm fallback 첫 글자`(:37-41) · 닉네임+상대시간+본문(:43-53) · `더 보기`/`불러오는 중` secondary sm(:58-66) |
| `EventVideoCommentInput.tsx` | `maxLength 500`(:42) · placeholder `댓글을 입력해주세요` / 잠금 `종료된 행사라 댓글을 남길 수 없어요`(:44-46) · 전송 비활성 `locked \|\| submitting \|\| !canSubmitComment`(:53) |
| `apps/web/src/test/event-video-fixture.ts` | 상세·댓글 픽스처 — 모바일 parity 픽스처의 원본 |
| 앱 `features/event/**`(557·560) · `features/video-playback/ui/video-player-screen.tsx`(재생 블록 :53-58·:81-115) · `features/upload/ui/blur-preview.tsx`(:26-30 expo-video 재생성 실측) · `features/video-actions/model/use-auto-dismiss-toast.ts` · `features/ai-route/ui/route-toast-host.tsx`(인라인 토스트 선례) · `map-home/ui/{home-sheet,sheet-header,sheet-scroll-view,sheet-status-view}.tsx` · `map-home-screen.tsx:246-262`(BackHandler·스냅) | 수정 대상·재사용 |

### 0-2. 앱 확장점 주석 실측

| 티켓 서술 | 실측 |
|---|---|
| `use-event-home.ts` 영상 미니 패널·도움돼요·댓글 | 일치 — `:159-161` "후속 확장점: … 영상 미니 패널·도움돼요·댓글(웹 MSG-520 `getVideoDetail`)" |
| `event-selection.ts` video 슬롯 추가 자리 | `:13-15` — location 슬롯 JSDoc만 있고 video 언급 없음(확장점 표기 없음). 웹 store `videoId`(:28)를 같은 형태로 붙인다 |
| `event-location-sheet-content.tsx` 카드 탭 → 기존 재생 화면 | 일치 — `:92` `onPress={() => goToVideoPlayback(video.videoId, false)}` |
| `event-sheet-switch.tsx` | `:20-27` location case — 그 앞에 video case 추가 |

### 0-3. 서버 계약 — hey-api 생성물에 **전부 있다**, 재생성 불필요 (`apps/web/src/shared/api/generated`, 모바일 `shared/api/{sdk,query-options}.ts`가 `export *` 재수출)

| 용도 | SDK / 쿼리 옵션 | 익명 curl 실측 (2026-09-03) |
|---|---|---|
| 영상 상세 | `getVideoDetail` `GET /api/event-videos/{videoId}`(sdk :1624) · `getVideoDetailOptions`(react-query.gen :2734) · `getVideoDetailQueryKey`(:2723) | 240347 → 200. `occurrenceId 5 · LIVE · locationId 11 · locationName "서면 목데이터 포토존" · zoneName "전포" · zoneCell "I-2" · regionName "부산광역시 부산진구 전포1동" · playbackUrl(presigned) · durationSec 5 · recordedAt 09-01T01:39:10Z · createdAt 09-01T01:39:12Z · uploaderNickname "강정만두" · interactionLocked false · helpfulCount 1 · helpfulByMe false · commentCount 2 · comments{2건, hasNext false, nextCursor null}`. **이 curl 1회가 조회수를 1 올렸다**(목데이터) |
| 도움돼요 | `addHelpful` PUT(:183) · `removeHelpful` DELETE(:168) `/api/event-videos/{videoId}/helpful` · `addHelpfulMutation`(:270)·`removeHelpfulMutation`(:247) → `EventVideoHelpfulResponseDto{helpfulCount, helpfulByMe}`(types :200) | 익명 PUT → **401**(auth-pipeline 처리 영역, 앱은 로그인 필수라 정상 경로 없음) |
| 댓글 목록 | `getComments` GET(:514) `query?: {cursor?, size?}`(types :5391) · `getCommentsOptions`(:769) | 240347 → 200 상세 내장분과 동일 2건, hasNext false |
| 댓글 작성 | `createComment` POST(:529) body `{content}` · `createCommentMutation`(:827) → `EventVideoCommentResponseDto{commentId, authorId, authorNickname, content, createdAt}`(types :1108) | 미호출(쓰기) |
| 위치 영상 목록(무효화 대상) | `getLocationVideosInfiniteQueryKey`(:867) — 앱 `use-location-videos-query.ts:36`이 infinite 옵션 사용 | id 5/loc 11 → `[{videoId 240347, durationSec 5, helpfulCount 1, commentCount 2}]`, hasNext false |

`EventVideoDetailResponseDto`(types :3594)에 **viewCount·프로필 이미지 URL·영상 제목 없음** → 조회수·아바타 이미지·"불꽃 시작 직전"류 제목은 표시 불가(웹 오탐 방지 ①④와 동일).

### 0-4. 실데이터 — 핵심 경로 실기 재현 가능

`occurrenceId 5` "서면 목데이터 축제" LIVE(~9/30) · `locationId 11` 포토존 · `videoId 240347`(5초, ♥1 댓글2, 업로더 강정만두, 댓글 2건 모두 강정만두). 아카이브(`interactionLocked true`)·`hasNext true`(댓글 21건+)·13422·13406은 실데이터 없음 → vitest.

### 0-5. Figma 15794:822 "4. 현장 영상 상세" — 구조 (`get_metadata`·스크린샷 실측, 색·문구는 웹 코드 우선)

시트 y148(1단계) · 핸들 · 패널: `title-header`(‹ `header-title` ✕ — **⋯ 없음**) → `player` 348×196(16:9) → `info`(제목 "광안리 H-6 · 불꽃 시작 직전" / meta "@gwangalli.night · 2분 전" + 우측 "조회 1,840" / `event-badge` "🎆 부산 불꽃축제 · 광안리 해수욕장") → `actions` 2열(♡ 도움돼요 18 | 댓글 6) → `comments-header` "댓글 6" → `comments`(아바타 28 · 닉네임 · 상대시간 우측 · 본문) → **`comment-input`(y556, 시트 하단·탭바 위 고정, 목록 위에 겹침)** → `bottom-nav`. 스크린샷 헤더 제목은 "광안리 해수욕장"(= 위치명).

### 0-6. 앱 코드 대조

- **재생 표면 후보**: `video-player-screen.tsx`는 `AppHeader` 포함 전면 화면이라 시트 안에 못 넣는다(티켓 [제외 범위] "기존 재생 화면 무수정"). 재생 블록 자체는 `useVideoPlayer(uri, i => uri !== null && i.play())` + `VideoView nativeControls contentFit="contain"` + 로딩 `ActivityIndicator` + 실패 문구/다시 시도(:53-115) — `expo-video ~57.0.2`는 이미 dev client에 포함(재빌드 불필요, `upload-video-preview.tsx` JSDoc). `useVideoPlayer`는 소스 변경 시 플레이어를 재생성하고 언마운트 시 해제한다(`blur-preview.tsx:26-30` 실측) → 영상 교체·시트 이탈 시 정지는 마운트 수명으로 성립. `upload-video-preview.tsx`(features/upload)는 자동재생·실패 분기가 없어 부적합.
- **시트 셸**: `HomeSheet`는 콘텐츠를 모르고(`children(context)`), 1단계에서 시트 높이 = `containerH - positions[1]`이 바텀 내비 바로 위까지 찬다(`home-sheet.tsx:197`) → 콘텐츠 트리의 마지막 형제(스크롤 밖)가 곧 "탭바 위 고정 입력창"이 된다(절대 배치 불요). `SheetScrollView`(`flex:1`)·`SheetHeader`(‹·badge·✕)·`SheetStatusView` 재사용.
- **키보드**: `app.config.js`에 `softwareKeyboardLayoutMode` 미지정 → Expo Android 기본 `resize`(윈도우 축소). 앱 어디에도 `KeyboardAvoidingView` 0건. 시트 컨테이너가 축소되면 `handleLayout`(`home-sheet.tsx:118-124`)이 현 단계 위치로 재배치 → 입력창이 키보드 위로 따라온다(예상, 실기 확인 항목 R2).
- **뒤로가기**: `map-home-screen.tsx:246-254` `BackHandler → event.handlers.back() || goBack()` — `stepBackEvent`가 소비하므로 화면 무수정으로 video 단이 붙는다.
- **토스트**: 홈 화면 컨텍스트에서 시트 콘텐츠가 인라인 `Toast`를 그린 선례 `grid-detail-screen.tsx:253-259`·`route-toast-host.tsx`(pointerEvents none). `ActionToast`(RN Modal)는 3초간 화면 전체 터치를 삼켜(:19-20) 재생 중 조작을 막으므로 부적합.
- **테스트 인프라**: `renderHook` 없음(vitest 정책) → 쿼리·뮤테이션은 "옵션 팩토리 + 얇은 훅" 2파일 + `MutationObserver` 구동(`marketing-consent-mutation.test.ts` 선례). parity는 웹 원본 동적 import(`event-location.parity.test.ts:16-19`).
- **Hermes**: 이식 대상 웹 함수 중 `.oxlintrc.json:86-121` 금지 API 0건. `nextCommentsCursor`의 `extraPages.at(-1)`(:77)은 금지 목록 밖이나 `extraPages[extraPages.length - 1]`로 치환(비용 0, parity 출력 동일).
- **브랜치**: `feat/MSG-562-mobile-event-video-detail` 로컬 존재(HEAD f26d3b5), 원격 없음 → 그대로 사용.
- **현재 위치 슬롯**은 `EventLocationSelection`(name 포함) — 상세 도착 전 헤더 제목 폴백 재료.

## 1. 확정 결정 목록 (근거 = 웹 파일:줄 / 서버 계약 / 앱 고유)

| # | 결정 | 근거 |
|---|---|---|
| D1 | **선택 슬롯**: `event-selection.ts`에 `video: number \| null`(videoId). `selectEventVideo(videoId)` · `selectEventLocation`은 video 리셋 · `withEventRoom` 다른 행사면 리셋(같은 행사 재탭은 유지) · `deactivateEvent` 전부 리셋 · `eventBack` **5단**: 영상 → 위치 → 개요 → 목록 → 해제 · `eventSheetStage`는 room 있으면 1(무변경 — 영상도 1단계) | 웹 store `:28`(videoId) `:67`(sameRoom 유지) `:79`(selectLocation 리셋) `:83-91`(back 영상 우선). 스냅 1단계 = Figma y148 + 560 D1 |
| D2 | **상세 조회** `api/use-event-video-detail-query.ts`: `getVideoDetailOptions({path:{videoId}})` + `select: unwrapEnvelope` + `refetchOnWindowFocus: false` + `refetchOnReconnect: false`, staleTime 전역 30초, `retry = refetch`. **invalidate·refetch 금지** — 뮤테이션 결과는 seed만 | 웹 `use-event-video-detail-query.ts:28-33·39`. 조회수 부작용 = 티켓 [동작 요구] 10 + DECISIONS 2026-08-31 MSG-520. `expiresInSec` 부재로 TTL 유도 staleTime 불가(같은 결정) |
| D3 | **재생 표면** = 신규 `ui/event-video-player.tsx`(feature 로컬): `useVideoPlayer(uri, i => { if (uri !== null) i.play() })` 자동재생 · `VideoView nativeControls contentFit="contain"` · `aspect-video bg-foreground` · uri null+pending → `ActivityIndicator` · 상세 실패 → `영상을 불러오지 못했어요` + `다시 시도`(= 상세 refetch). **`video-player-screen.tsx` 무수정**(재생 블록을 미러) | 티켓 [동작 요구] 1 "기존 재생 표면 재사용, 실패 시 재시도 안내" + [제외 범위] "기존 재생 화면 무수정". 웹은 `VideoPlaybackSurface` 추출(MSG-520 재사용 항목)이지만 앱 재생 화면은 `AppHeader` 결합 전면 화면이라 추출이 곧 수정이다(추정 A1). 자동재생·컨트롤 방식은 `video-player-screen.tsx:54-58·83-88`(승인 추정 1·5) 그대로 |
| D4 | **시트 구성**(1단계, Figma 15794:822 순서): `SheetHeader`(‹ **위치명** ✕ — ⋯ 없음) → 플레이어(D3) → 제목 `eventVideoTitle(detail)` → 메타 `eventVideoMetaLine(uploaderNickname, createdAt)` → 배지 pill `{행사명} · {위치명}`(event-tint/primary, 560 유형 pill 규격) → 액션 행: **도움돼요 버튼**(♡/♥ + `도움돼요 {helpfulCount}`, `accessibilityState.selected = helpfulByMe`, 눌림 시 primary 테두리·글자) + **`댓글 {commentCount}` pill(비버튼)** → `댓글 N` 섹션 라벨 → 댓글 목록 → `더 보기` → **입력창 footer(스크롤 밖, 시트 마지막 자식)**. 상세 도착 전엔 헤더+플레이어 로딩만(웹 `detail !== null &&` 게이트 :166). 조회수 **미표시** | 웹 패널 `EventVideoMiniPanel.tsx:122-223` 구성·게이트 · 제목/메타 `event-video-view.ts:17-33` · 도움돼요 `aria-pressed`(:181) → RN `accessibilityState.selected` · 댓글 pill 비버튼(:192-196, 웹 추정 3) · 조회수 부재 = 오탐 방지 ①. 헤더 제목=위치명·배지 pill·섹션 라벨·footer 배치는 Figma 구조(0-5) — 앱 고유. 배지 문구 형식은 `eventUploadLabel`(`event-upload-target.ts:19-22`)과 같은 `{행사명} · {위치명}` — 이모지 없음 |
| D5 | **댓글 목록**: 상세 내장 첫 페이지(오래된순 20건) → `Avatar size="sm" fallback={닉네임 첫 글자}` + 닉네임(body-strong) + 상대시간(caption, 우측) + 본문. 0건이면 `아직 댓글이 없어요. 첫 댓글을 남겨보세요`. `hasNext`면 `더 보기`(secondary sm 전폭, 진행 중 `불러오는 중` 비활성) → `getComments` 커서 로컬 축적, 실패는 토스트 + 받은 목록 유지 | `EventVideoComments.tsx:24-67` · `use-event-comments-pages.ts:35-84`. 웹 오탐 방지 ③④(오래된순·아바타 폴백) 그대로 |
| D6 | **댓글 입력 footer**: `ui-native Input`(`maxLength 500`, placeholder `댓글을 입력해주세요`, `returnKeyType="send"`, `onSubmitEditing` = 전송) + 전송 아이콘 버튼(`accessibilityLabel "댓글 전송"`, `disabled = locked \|\| submitting \|\| !canSubmitComment(draft)`). 전송 본문 = `trimmedCommentContent`. 성공 → 목록 맨 아래 append + `commentCount+1` seed + 입력 비움(**제출 videoId === 현재 videoId일 때만**) + 위치 목록 무효화. 초안은 영상 교체 시 리셋 | `EventVideoCommentInput.tsx:37-55` · 패널 초안 리셋 `:63-68` · videoId 대조 `:86-88`(codex P2) · 뮤테이션 seed `use-event-video-mutations.ts:114-128`. 전송 버튼 형태(아이콘)는 Figma `send`(15796:503) — 웹 `등록` 텍스트 버튼 대신 앱 고유 |
| D7 | **도움돼요 토글**: `helpfulByMe ? removeHelpful : addHelpful` → 응답 `{helpfulCount, helpfulByMe}`를 상세 캐시(`getVideoDetailQueryKey`) `setQueryData` seed(봉투 유지, 캐시 부재면 no-op) + 위치 영상 목록 무효화. **비낙관** — 진행 중 재탭 무시(`isPending` 가드) | `use-event-video-mutations.ts:68-89` · 티켓 [동작 요구] 4 |
| D8 | **위치 목록 무효화** = `getLocationVideosInfiniteQueryKey({path:{occurrenceId, locationId}})` 정확 키(상세 DTO에 두 id가 있다) — 웹의 `_id` 부분 키 대신 정확 키 1개 | 웹 `:50-55`는 패널이 위치 id를 몰라 부분 키를 썼다. 앱 상세 DTO `occurrenceId·locationId`(0-3)로 정확 키가 가능하고 `invalidate-event-surfaces.ts:20-22`와 같은 키 생성. 위치 목록(`getLocations` videoCount)은 변하지 않으므로 무효화 안 함 |
| D9 | **잠금** `interactionLocked === true`: 도움돼요 버튼 `disabled`(opacity-50 + `accessibilityState.disabled`) · Input `disabled` + placeholder `종료된 행사라 댓글을 남길 수 없어요` · 전송 버튼 disabled. 카운트·댓글은 계속 표시. 판정 주체는 서버 필드(FE 날짜 계산 0) | `EventVideoMiniPanel.tsx:182·212` · `EventVideoCommentInput.tsx:44-46` · 웹 AC 10 |
| D10 | **실패 안내** = `eventInteractionErrorMessage(error)` 포팅(parity): 13422 `종료된 행사라 더 이상 참여할 수 없어요` · 13406 `더 이상 볼 수 없는 영상이에요` · 기타 `요청에 실패했어요. 잠시 후 다시 시도해 주세요.` → 시트 안 인라인 `Toast`(dark, footer 바로 위, `pointerEvents="none"`) + `useAutoDismissToast` 3초. 실패 시 캐시·플레이어 무접촉(재생 지속) | 문구 `event-video-view.ts:86-96`(**웹 원문 그대로 — "행사" 단어 포함, parity 대상**. 티켓의 "종료된 이벤트"는 분기 설명이지 문구 지정이 아님) · 토스트 3초 `use-auto-dismiss-toast.ts:4` · 인라인 방식은 앱 고유(0-6, `ActionToast` Modal 기각) |
| D11 | **닫힘·해제**: `‹`·하드웨어 백 = `stepBackEvent`(D1 5단) · `✕` = `deactivateEvent` · 다른 위치 선택(개요 행·지도 셀 탭) = `selectEventLocation`이 video 리셋 · 다른 행사 카드 = `withEventRoom` 리셋. 영상 단이 벗어나면 `EventVideoSheetContent` 언마운트 → `useVideoPlayer` 해제 = 재생 정지 | 티켓 [동작 요구] 2·11 · 웹 store `:79·83-91` · expo-video 수명 `blur-preview.tsx:26-30` |
| D12 | **카드 탭 배선**: `event-location-sheet-content.tsx:92` `goToVideoPlayback(...)` → `handlers.selectVideo(video.videoId)`. 카드 접근명 `행사 영상 재생 — {상대시간}` 유지 | 웹 `EventRoomBodySwitch.tsx:96` `onSelect={() => onVideoSelect(video)}` · 티켓 [동작 요구] 1 |
| D13 | **훅 소유**: `use-event-home.ts`는 `video: { videoId, locationName, occurrenceTitle } \| null` + `handlers.selectVideo`만 노출(≈12줄). 상세 조회·댓글 페이지·뮤테이션·초안·토스트는 **시트 콘텐츠가 소유**(`ui/event-video-sheet-content.tsx` + 배선 훅 `api/use-event-video-sheet.ts`) | 웹도 패널 컴포넌트가 쿼리·뮤테이션을 소유(`EventVideoMiniPanel.tsx:54-95`). `use-event-home`은 560에서 +195줄이라 더 얹지 않는다(앱 고유 분할) |
| D14 | **헤더 제목** = `detail?.locationName ?? location.snapshot.name` (상세 도착 전 폴백) · `onBack`·`onClose` 기존 `SheetHeader` 라벨 그대로 | Figma 헤더 "광안리 해수욕장"(위치명) · 560 위치 시트 헤더와 동형. 웹 패널에는 헤더 제목이 없어(닫기·⋯만) 앱 고유 |
| D15 | 영상 단 진입·이탈에 **카메라·스냅 재명령 없음** — `eventSheetStage` 1 유지, 지도 강조·라벨은 위치 슬롯 그대로(video는 강조 근원 아님) | 560 D16·D17 유지, 웹에 카메라 이동 없음 |
| D16 | 로그인 게이트 없음 — 서버 401은 auth-pipeline. `helpfulByMe`는 응답값 그대로 | 티켓 [제외 범위] 2 · 제품 규칙 "앱은 로그인 필수" |

## 2. 수용 기준

| # | 기준 | 유형 | 검증 방법 |
|---|------|------|----------|
| 1 | 위치 상세의 영상 카드 탭 → 같은 시트(1단계)가 현장 영상 상세로 바뀌고 `getVideoDetail`의 playbackUrl로 자동재생을 시도한다(네이티브 컨트롤). 상세 조회 실패 시 `영상을 불러오지 못했어요` + `다시 시도`(refetch). 실데이터 240347: 헤더 `‹ 서면 목데이터 포토존 ✕` | 화면 | 3-B ① |
| 2 | `‹`·하드웨어 백이 영상 → 위치 → 개요 → 목록 → 해제 5단, `✕`는 전부 해제. `selectEventLocation`·다른 행사 open·`deactivateEvent`가 video를 리셋하고 같은 행사 재open은 유지. `eventSheetStage`는 영상 단에서 1 | 로직+화면 | vitest(`event-selection.test.ts` 확장) + 3-B ④(‹ 1회) |
| 3 | 제목 폴백 zone 라벨 > 행정동명 > 위치명 > `영상`, 메타 `@닉네임 · 상대시간`(createdAt), 배지 `{행사명} · {위치명}`, 조회수 미표시. 실데이터: `전포 I-2` / `@강정만두 · N일 전` / `서면 목데이터 축제 · 서면 목데이터 포토존`. `eventVideoTitle`·`eventVideoMetaLine`은 웹 원본과 동등 | 로직+화면 | parity(`event-video-view.parity.test.ts`) + 3-B ① |
| 4 | 도움돼요 버튼 탭 → `helpfulByMe`에 따라 PUT/DELETE, 응답 `helpfulCount·helpfulByMe`가 버튼 상태·카운트에 seed(비낙관), 상세 쿼리는 **재조회되지 않고** 위치 영상 목록 정확 키가 무효화된다. 진행 중 재탭 무시. 실데이터: `♡ 도움돼요 1` → 탭 → `♥ 도움돼요 2` → 재탭 → `1` | 로직+화면 | vitest(`event-video-mutations.test.ts` — MutationObserver: 메서드 선택·seed·상세 fetch 횟수 불변·목록 invalidate·onError) + 3-B ② |
| 5 | 댓글 목록: 상세 내장 첫 페이지가 아바타 폴백(첫 글자)+닉네임+상대시간+본문 행으로 오래된순, 0건 문구, `hasNext`면 `더 보기`가 `getComments` 커서로 아래에 잇고 실패는 토스트+목록 유지, 영상 교체 시 축적 리셋. `mergeCommentPages`·`nextCommentsCursor`는 웹 원본과 동등(중복 commentId 1회) | 로직+화면 | parity + `event-comments-pages.test.ts`(fetchQuery 경로, QueryClient 직접 구동) + 3-B ①(2행) — `더 보기`는 실데이터 없음 → vitest |
| 6 | 댓글 작성: trim 1~500자만 전송 가능(빈 입력·공백은 버튼 비활성), 성공 시 목록 맨 아래 append·`commentCount+1`·입력 비움(제출 videoId 일치 시)·목록 무효화. `canSubmitComment`·`trimmedCommentContent`는 웹 원본과 동등 | 로직+화면 | parity + vitest(mutations seed·dedup) + 3-B ③ |
| 7 | `interactionLocked=true`: 도움돼요·입력·전송 비활성(`accessibilityState.disabled`), 잠금 placeholder, 카운트·댓글은 계속 표시 | 로직 | vitest(잠금 파생 순수 함수 `eventVideoInteraction(detail) → {helpfulDisabled, inputDisabled, placeholder}`) — **실기 확인불가**(서버 데이터 없음) |
| 8 | 도움돼요·댓글·더 보기 실패 → `eventInteractionErrorMessage` 문구 토스트 3초(13422/13406/기타), 재생 중 영상 무접촉(캐시·플레이어 무변경). 함수는 웹 원본과 동등 | 로직 | parity + vitest(onError 경로) — 실기 미실행(범위 축소) |
| 9 | 도움돼요·댓글 성공 후 `‹`로 위치 시트 복귀 시 카드 `♥ N · 댓글 M`이 재조회로 갱신된다. 실데이터: `♥ 1 · 댓글 2` → `♥ 2 · 댓글 3`(또는 도움돼요 원복 시 `♥ 1 · 댓글 3`) | 로직+화면 | vitest(invalidate 키 단정) + 3-B ④ |
| 10 | 상세 쿼리는 `refetchOnWindowFocus/Reconnect: false`, 뮤테이션 후 invalidate 0회(fetch 호출 1회 유지) — 조회수 부작용 억제 | 로직 | vitest(옵션 단정 + AC 4 fetch 횟수) |
| 11 | 시트 `✕`·다른 위치 선택·행사 교체 시 video 슬롯이 비고 플레이어가 언마운트된다(재생 정지) | 로직+화면 | vitest(AC 2 리셋) + 3-B ④(‹ 후 소리·화면) |
| 12 | 접촉면 감사: `use-event-home.ts` ≤15줄 · `event-sheet-switch.tsx` ≤8줄 · `event-location-sheet-content.tsx` 카드 onPress 1줄+import 교체 · `map-home-screen.tsx`·`grid-map.tsx`·`home-sheet*`·`video-playback/**`·`apps/web/**`·`packages/**`·`metro.config.js`·`vitest.config.ts` **0줄** · `pnpm openapi-ts` 미실행 · 새 네이티브 의존성 0 | 로직 | git diff 감사 |
| 13 | 게이트: `pnpm --filter mobile test`·`typecheck`, 루트 lint(Hermes 금지 API 0건)·format·중복(parity 사본은 baseline 등재 사유 기록) 통과 | 로직 | 게이트 |

**검증 프로파일**: **화면** — 신규 시트 단계 1종(플레이어·반응·댓글·입력) + Figma 15794:822 대조. 실동작은 page-verification **3-B**(핵심 경로 1회 · 스크린샷 ≤6장 · 15분). 로직 기준(2·7·8·10·12·13)은 vitest·감사.

## 3. 구현 계획

- **브랜치**: `feat/MSG-562-mobile-event-video-detail` (로컬 존재, 사용).
- **재사용(신규 코드 0)**: `ui-native` `Input`·`Button`·`Avatar`·`Toast`·`cx` / `map-home/ui` `SheetHeader`·`SheetScrollView`·`SheetStatusView` / `features/video-actions/model/use-auto-dismiss-toast` / `shared/format.formatRelativeTime` / `shared/api` `sdk`(타입)·`query-options`(`getVideoDetailOptions`·`getVideoDetailQueryKey`·`addHelpfulMutation`·`removeHelpfulMutation`·`createCommentMutation`·`getCommentsOptions`·`getLocationVideosInfiniteQueryKey`)·`envelope`·`api-error` / `expo-video` `useVideoPlayer`·`VideoView` / `design-tokens` `semantic` / lucide `Heart`·`MessageCircle`·`Send`(신규 import, 패키지 기존) / `features/event` 557·560 산출물 전부 / `src/test/envelope-response.ts`.
- **`packages/ui-native/src/index.ts` diff 0**. 승격 후보: 없음.

### 3-1. 파일 단위

```
apps/mobile/src/features/event/
  model/event-selection.ts             [확장] video 슬롯 · selectEventVideo · selectEventLocation/withEventRoom/deactivate 리셋 · eventBack 5단 (D1)
  model/event-selection.test.ts        [확장] 5단 · 리셋 매트릭스 · 같은 행사 유지 · 스테이지 1
  model/event-video-view.ts            [포팅 + parity] eventVideoTitle · eventVideoMetaLine · trimmedCommentContent · canSubmitComment · mergeCommentPages · nextCommentsCursor(.at(-1) → 인덱스) · eventInteractionErrorMessage(ApiError는 앱 shared/api/api-error). PLAYBACK_RATE_* 미포팅(제외 범위)
  model/event-video-view.parity.test.ts  웹 원본 동적 import 대조 — 픽스처는 웹 event-video-fixture 형상 + 실DTO 240347
  model/event-video-cache.ts           [모바일 고유 — 웹은 뮤테이션 파일 내부 비공개] seedHelpful(detail, res) · appendComment(detail, comment)(중복 commentId 방어 + commentCount+1) · eventVideoInteraction(detail) → {helpfulDisabled, inputDisabled, placeholder} (D7·D6·D9)
  model/event-video-cache.test.ts
  api/use-event-video-detail-query.ts  [포팅] eventVideoDetailQueryOptions(videoId)(순수 팩토리 — refetchOn* false) + 얇은 훅 {detail, isPending, isError, retry} (D2)
  api/event-video-detail-query.test.ts [unit] 옵션 플래그 단정 (AC 10)
  api/event-video-mutations.ts         [옵션 팩토리, MSG-426 구조] toggleHelpfulMutationOptions(queryClient, {onError}) · createCommentMutationOptions(queryClient, {onCreated, onError}) — mutationFn은 생성 팩토리 `.mutationFn!`(웹 :29-31), onSuccess = seed(setQueryData 봉투 유지) + invalidateQueries(정확 infinite 키) (D7·D8)
  api/event-video-mutations.test.ts    [MutationObserver] PUT/DELETE 선택 · seed · 상세 fetch 횟수 불변 · 목록 invalidate · onError · 댓글 append dedup
  api/use-event-video-mutations.ts     [얇은 훅] useToggleHelpful · useCreateComment
  api/use-event-comments-pages.ts      [포팅] 로컬 축적 + fetchCommentsPage(queryClient, videoId, cursor)(분리 — fetchQuery retry:false) (D5)
  api/event-comments-pages.test.ts     [unit] fetchCommentsPage — QueryClient + fetch 스텁
  api/use-event-video-sheet.ts         [뷰-레이어 배선 훅] 상세·댓글 페이지·뮤테이션·초안(영상 교체 리셋)·토스트를 묶어 시트 콘텐츠에 넘긴다 (D13)
  api/use-event-home.ts                [≤15줄] selection.video 구독 → `video: {videoId, locationName: location.name, occurrenceTitle: title} | null` · handlers.selectVideo(videoId) = selectEventVideo. 확장점 주석 :159-161 해소
  ui/event-video-player.tsx            [신규] 16:9 다크 플레이어 — useVideoPlayer 자동재생 · VideoView nativeControls · 로딩 · 실패 문구+다시 시도 (D3)
  ui/event-video-sheet-content.tsx     [신규] SheetHeader(‹ 위치명 ✕) · 플레이어 · SheetScrollView(제목·메타·배지·액션 행·댓글 라벨·댓글 목록·더 보기) · 인라인 Toast · footer 입력 (D4~D6·D9·D10·D14)
  ui/event-video-comment-row.tsx       [신규] Avatar sm + 닉네임 + 상대시간 + 본문 (D5) — 목록이 짧으면 sheet-content 내부 map으로 대체 가능(builder 재량)
  ui/event-video-comment-input.tsx     [신규] Input + Send 아이콘 Pressable (D6)
  ui/event-sheet-switch.tsx            [≤8줄] `if (event.video) return <EventVideoSheetContent … />` — location case 앞
  ui/event-location-sheet-content.tsx  [1줄+import] onPress → handlers.selectVideo(video.videoId), goToVideoPlayback import 제거 (D12)
```

미접촉(AC 12 감사): `map-home-screen.tsx` · `grid-map.tsx` · `home-sheet*` · `sheet-*` · `use-event-room-query` · `use-location-videos-query` · `use-event-heartbeat` · `video-playback/**` · `upload/**` · `shared/navigation.ts` · `apps/web/**` · `packages/**`.

### 3-2. 웹 원본 대응표

| 웹 파일 | 처리 |
|---|---|
| `model/event-video-view.ts` | 7함수 포팅 + parity (재생 속도 2종 제외) |
| `model/use-event-video-detail-query.ts` | 옵션 팩토리 + 얇은 훅 |
| `model/use-event-comments-pages.ts` | 포팅 (fetch 부분 분리) |
| `api/use-event-video-mutations.ts` | 옵션 팩토리 + 얇은 훅 2파일 · seed 함수는 `model/event-video-cache.ts`로 분리(테스트 가능) · 무효화는 정확 키(D8) |
| `event-room-store.ts` `videoId`·`back` | 패턴 이식 → `event-selection.ts` |
| `EventVideoMiniPanel.tsx` · `EventVideoComments.tsx` · `EventVideoCommentInput.tsx` | 참조본 → RN 재작성 3파일 |
| `widgets/video-mini-panel/VideoPlaybackSurface.tsx` | 미포팅 — `event-video-player.tsx`는 앱 재생 화면 블록 미러(D3) |
| `EventVideoUtilityMenu.tsx` · `PLAYBACK_RATE_OPTIONS` · 로그인 모달 게이트 | 제외 범위 |
| `test/event-video-fixture.ts` | parity 픽스처 형상 참조(웹 파일 직접 import 없음 — 웹 0줄) |

### 3-3. 확장점 (후속 — 이번엔 주석만)

- 댓글 수정·삭제(`updateComment`·`deleteComment` 생성물 존재, 웹 MSG-520 추정 6과 동일하게 미탑재).
- 재생 유틸리티(속도·PIP·다운로드) — 앱 재생 화면 도입 시 함께.

## 4. 범위 밖으로 짚은 항목

| 항목 | 이번 처리 | 근거 |
|---|---|---|
| 헤더 ⋯ 유틸리티 메뉴 | 없음 | 티켓 [제외 범위] 1 · Figma 프레임에도 없음 |
| 비로그인 게이트 | 없음 | [제외 범위] 2 · 제품 규칙 |
| 홈·도감 재생 화면 | 무수정 | [제외 범위] 3 · D3 |
| 일반 영상 좋아요·댓글 | 없음 | 서버 미제공 |
| 댓글 수정·삭제 | 없음 | 웹 동일(MSG-520 추정 6) |
| 영상 단 진입 시 스냅 재명령·카메라 | 없음 | D15 |
| `X-Viewer-Session`·heartbeat 변경 | 없음 | 560 그대로 |

## 5. 추정 (사용자 승인 대상 — 웹에 대응 없음, 앱 고유)

| # | 추정 | 권장안 | 대안 |
|---|---|---|---|
| A1 | **재생 표면**: 신규 `event-video-player.tsx`(≈45줄, 재생 화면 블록 미러) | 채택 — 티켓 "기존 재생 화면 무수정"과 양립. nose가 재생 화면과의 ~10줄 사본을 잡으면 MSG-556 선례대로 baseline 등재 사유 기록 | `video-player-screen.tsx`에서 `VideoPlaybackSurface` 추출(웹 선례) — 렌더 테스트 없는 앱에서 기존 화면 수정 = 실기만이 안전망, 티켓 문구 위배 |
| A2 | **입력창 배치**: 시트 콘텐츠 마지막 형제(스크롤 밖) — 1단계에서 탭바 바로 위 | 채택 — 절대 배치·KeyboardAvoidingView 없이 Figma 위치 성립(0-6). 키보드는 Android `resize` 기본으로 시트 컨테이너가 줄어 따라올 것으로 예상 → 3-B ③에서 확인, 안 되면 `KeyboardAvoidingView`(RN 내장) 1겹 | 화면 레벨 절대 오버레이 — `map-home-screen.tsx` 접촉(0줄 예산 위배) |
| A3 | **헤더 제목 = 위치명**(D14), 상세 도착 전 스냅숏 이름 폴백 | 채택 — Figma 스크린샷·560 헤더 동형 | 제목을 `eventVideoTitle`로 — 본문 제목과 중복 |
| A4 | **배지 pill `{행사명} · {위치명}`** — 티켓 요구, 웹 패널엔 없음 | 채택, 이모지 없음, 560 유형 pill 규격 | — |
| A5 | **`댓글 N` pill 비버튼 + 섹션 라벨 `댓글 N`** | 채택 — 웹 추정 3(비버튼) + Figma `comments-header` | 섹션 라벨 생략(Figma와 차이) |
| A6 | **토스트 = 시트 안 인라인**(footer 위, 3초, 터치 통과) | 채택 — `ActionToast`(Modal)는 터치를 삼킨다 | — |
| A7 | **문구는 웹 원문 유지**("종료된 행사라…" 등 "행사" 단어) — parity 대상 | 채택 — 560이 같은 규칙(`행사 영상 재생 —`·`행사 격자 N개`). 용어 변경은 웹과 동시 후속 | 앱만 "이벤트"로 교체 — parity 제외 필요 |
| A8 | **무효화 = 정확 infinite 키 1개**(D8) | 채택 | `invalidateEventSurfaces` 재사용 — 위치 목록까지 재조회(불필요 GET 1) |

**사용자 질문: 없음** — 나머지는 전부 웹 코드·서버 실측·앱 기존 구조 근거.

## 6. 리스크

- **R1 실기 쓰기 잔존**: 3-B ③ 댓글 전송은 서버에 실댓글을 남긴다(목데이터 축제 id 5 — 수용). 도움돼요는 재탭으로 원복(3-B ②). 댓글 정리는 `deleteComment`(생성물 존재)로 가능하나 bearer 필요 — 리포트에 남긴 댓글 id 기록.
- **R2 키보드**(A2): `resize` 모드에서 `HomeSheet.handleLayout`이 현 단계 위치로 재배치하므로 입력창이 따라올 것으로 예상하나 미실측. 실패 시 `KeyboardAvoidingView` 1겹(RN 내장, 의존성 0). iOS는 이번 범위 밖.
- **R3 조회수 부작용**: 상세 조회 1회 = 조회수 +1(웹 동일, DECISIONS). 실기·curl 횟수를 리포트에 기록. staleTime 30초 경과 후 같은 영상 재진입은 새 시청 1회(웹 결정).
- **R4 스냅 유지**: 사용자가 시트를 2단계로 내린 채 카드를 탭하면 스테이지가 이미 1이라 재스냅 명령이 없다 — 560 위치 전이와 같은 기존 동작(D15), 결함 아님. 필요 시 후속.
- **R5 댓글 21건+ 경계**: 작성 seed가 첫 페이지 끝에 붙어 "더 보기" 병행 시 중간 삽입 가능 — 웹 DECISIONS 수용, 실데이터 2건이라 미재현.
- **R6 헤더 ✕ 접근명 "테마 닫기"**: `SheetHeader` 기존 라벨 — 이 티켓 무수정(공용 헤더 문구는 별도).
- **R7 실데이터 시간 의존**: id 5는 9/30까지, 240347 삭제 시 AC 1~9 실기 불가(curl 원문 보관). 아카이브·hasNext·13422/13406은 vitest만 — 리포트 "확인불가(서버 데이터)".
- **R8 이중 오디오 없음**: 앱은 재생 표면이 시트 하나뿐이라 웹 발견 사항(홈·행사 패널 동시 재생)은 해당 없음.
- **Figma 오탐 방지** (page-verifier 판정 기준):
  1. `조회 1,840` **미표시** — DTO에 viewCount 없음(0-3).
  2. 제목 `광안리 H-6 · 불꽃 시작 직전`의 **" · 불꽃 시작 직전" 없음** — 영상 제목 필드 부재, `eventVideoTitle` 폴백만(실데이터 `전포 I-2`).
  3. 배지 **이모지(🎆) 없음**, 문구는 `{행사명} · {위치명}` 실데이터.
  4. 댓글 정렬 **오래된순**(서버 계약) — Figma "1분 전 → 방금 전 → 3분 전"은 플레이스홀더. 새 댓글은 맨 아래.
  5. 댓글 아바타 **폴백 첫 글자** — 프로필 이미지 URL 없음.
  6. `더 보기` 행은 Figma에 없으나 `hasNext`면 렌더(실데이터 미노출).
  7. 입력 placeholder는 웹 `댓글을 입력해주세요`(Figma `댓글을 입력하세요` 아님), 전송은 아이콘 버튼(Figma).
  8. 플레이어 컨트롤은 **expo-video 네이티브 컨트롤**(Figma 커스텀 바 `0:05 / 0:24`·전체화면 아이콘은 재현 안 함 — 재생 화면 승인 추정 1).
  9. 잠금 상태·토스트·로딩·실패는 프레임에 없음 — 웹 문구 기준.
  10. `@gwangalli.night`·`감정밍`·`18`·`6`·불꽃 썸네일은 플레이스홀더 — 하드코딩 금지.

## 7. 검증 계획 요약 (page-verification 3-B — 핵심 경로 1회 · 스크린샷 ≤6 · 15분)

1. 게이트: `pnpm --filter mobile test`·`typecheck`, 루트 lint(Hermes grep 0)·format·중복(parity 사본 baseline 등재 사유). 신규 테스트 예상: parity 1(7함수) + unit 5(`event-selection` 확장·`event-video-cache`·`event-video-detail-query`·`event-video-mutations`·`event-comments-pages`).
2. 감사(AC 12): `git diff --numstat origin/develop -- apps/mobile/src/features/map-home apps/mobile/src/features/video-playback apps/mobile/src/features/upload apps/web packages` → 0줄, `features/event` 접촉 4파일 줄 예산.
3. 실기(에뮬레이터, 로그인 세션 필요 — 도움돼요·댓글은 bearer. `_workspace` 심링크 금지·`--skip-build`): 서면 진입 → `이벤트` 칩 → 카드 → 개요 → 포토존 행 → 영상 카드 탭 → **① 영상 상세**(재생 중 프레임 · `전포 I-2` · `@강정만두 · N일 전` · 배지 · `♡ 도움돼요 1` · `댓글 2` · 댓글 2행 · 하단 입력창) → 도움돼요 탭 → **② `♥ 도움돼요 2`** → 재탭(원복 1) → 입력창 탭 → **③ 키보드 위 입력창**(R2) → `MSG-562 검증` 전송 → 목록 맨 아래·`댓글 3`·입력 비움(③에 포함 또는 별도 1장) → `‹` → **④ 위치 시트 카드 `♥ 1 · 댓글 3`**(무효화 동기화, 재생 정지) → 하드웨어 백 → 개요 → `✕`. `uiautomator dump`로 접근명(`댓글 전송`·도움돼요 `selected`). Figma 15794:822 구조 대조 1장. 총 ≤6장.
4. 미실행(범위 축소)·확인불가 예상: AC 7 잠금(데이터 없음) · AC 8 실패 토스트(13422/13406 재현 불가) · AC 5 `더 보기`(hasNext false) · AC 2 5단 전체(‹ 1회 + 하드웨어 백 1회만) — 전부 vitest·코드 대조 판정.

## 승인 기록 (2026-09-03)

- 사용자 원문: **"웹 방식대로 진행해"** — 수용 기준 13개·추정 A1~A8 권장안 전건 승인. 사용자 질문 없음.
- A7(웹 원문 문구의 "행사" 단어 유지)·R1(실기 댓글 전송이 목데이터 축제 id 5에 실댓글을 남김)은 승인 요약에 명시 후 승인.
- 브랜치 `feat/MSG-562-mobile-event-video-detail` (워크트리 `FE-MSG-562`, origin/develop f26d3b5 기점 — 기존 브랜치 없어 하네스가 생성).


## 작업 로그 (2026-09-03 — 구현·검증 완료 후 승격)

### 사용자 결정 (원문)
- "일단 1번티켓파고 진행해" → MSG-562 발행(하네스). "웹 방식대로 진행해" → 추정 A1~A8 전건 승인, 질문 0. "빌드 끝나면 검증까지 이어서 진행해".

### 실측 동작 · 변경 파일
- 신규 17(`features/event/` model 2+테스트 2 · api 6+테스트 3 · ui 4 · `src/test/event-video-fixture.ts`), 수정 5(`use-event-home.ts` +10/−2 · `event-sheet-switch.tsx` +4/−1 · `event-location-sheet-content.tsx` +1/−3 · `event-selection.ts` +17/−8 · `event-selection.test.ts` 상태 형상에 `video: null`) + `nose.baseline.json` 재등재(605 → 625 패밀리, 무관 흡수 0) + `DECISIONS.md` 2행.
- 0줄: `map-home/**`·`home-sheet*`·`video-playback/**`·`upload/**`·`shared/**`·`apps/web/**`·`packages/**`·metro·vitest.config. 새 의존성 0, 생성물 재생성 0(getVideoDetail·addHelpful·removeHelpful·getComments·createComment 전부 기존).
- 게이트: vitest 194 파일/**1240**(1210 → +30, RED 26건 확인 후 GREEN) · typecheck · oxlint(Hermes 금지 API 0) · format · check:duplication 통과.

### 빌드 리포트 스펙 이슈 3건 (동작 동일 — 형상 조정)
1. 무효화 키 단정 `toEqual` → `toMatchObject`(생성 키가 `baseUrl` 내부 필드를 실음, `invalidate-event-surfaces.test` 관례).
2. D13: `EventHome.video` 파생을 `videoSheetInput`으로 내리고 `‹`/`✕`는 배선 훅이 `stepBackEvent`/`deactivateEvent`를 묶어 반환 — `video`+`handlers` 두 prop이면 스위치 9줄(≤8 위반).
3. D8: 무효화용 `occurrenceId·locationId`는 seed 직후 상세 캐시에서(입력은 웹 형상 유지).

### 검증 (3-B 실기 9분 · 스크린샷 5장 `_workspace/MSG-562/screenshots/`)
- 통과: AC 1(자동재생 5초 영상 완주 관측)·2(5단 백 실기 전부)·3(parity+실기 `전포 I-2`/`@강정만두 · 2일 전`/배지)·5·8(실패 토스트 실기 부수 관측)·10·11(언마운트, 플레이어 노드 0)·12(접촉면 실측)·13.
- 로직 통과 / 실기 확인불가: AC 4·6·9 — 탭 시 요청 발행 후 기타 문구 토스트. 프로필 탭이 mock 스텁이고 부트 게이트가 없어 로그인 증거가 없으며 `auth-pipeline.ts:188-190`(저장 refreshToken 없는 401 원반환) 경로가 가장 유력 → 에뮬레이터 로그인 세션 부재 추정. 실댓글 잔존 없음.
- vitest만: AC 7(interactionLocked — 서버 필터).
- 확인불가: R2 키보드 따라오기 — AVD 하드웨어 키보드 모드라 IME 패널 높이 0(`ADJUST_RESIZE`는 실측). 후속: `hw.keyboard=no` 또는 실기.
- **재작업 1회(RW-1)**: ① 입력 footer가 카메라 FAB(y≈2127~2265)에 하단 42px 덮임 → footer 래퍼 `pb-9`(SheetScrollView 하단 여유와 동일) ② 도움돼요 26dp·전송 40dp → `hitSlop` 10/4. typecheck·lint·format 재통과. 실기 재촬영 미실행(1~2줄 스타일, 560 RW-1 선례).

### 검토한 대안 (기각 사유)
- 재생 표면을 기존 재생 화면에서 추출(웹 `VideoPlaybackSurface` 방식) — 앱 재생 화면은 `AppHeader` 결합 전면 화면이라 추출 = 수정, 티켓 "기존 재생 화면 무수정"과 충돌 → 45줄 미러(A1).
- 토스트 `ActionToast`(Modal) — 3초간 터치를 삼킴 → 시트 안 인라인 `absolute bottom-full` + `pointerEvents="none"`(A6).
- `handlers` prop 주입 — 스위치 예산 초과 → 훅 반환(DECISIONS 기록).
- `_id` 부분 키 무효화 폴백 — 상세 DTO에 id 2개가 있어 정확 키 1개로 족함(A8).

### 확인불가 · 미실행 · 후속 (지라 코멘트 환류 대상)
- 로그인 기기에서 AC 4·6·9 성공 경로 1회(실댓글 commentId 기록) + R2 동시 확인.
- 탭 왕복 후 이벤트 시트가 2단계로 재마운트돼 footer가 화면 밖(560 D15·R4 기존 동작) — 후속 후보.
- `ExpoVideo … picture-in-picture` E 로그(플레이어 해제마다 3줄) — expo-video 플러그인 설정, 기존 재생 화면과 공유, 범위 밖.
- nose baseline에서 `start-ready-refresh.test`↔`settle-upload-success.test` 패밀리 1건이 빠짐(이 티켓 무관) — 재검출 시 재등재.
- 검증 에이전트 API 529 중단 2회 — 리포트는 완성돼 있어 메인 스레드가 이어받음.
