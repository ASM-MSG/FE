# MSG-581: [모바일] 광역 줌에서 테마 칩 미션 목록 0개 — 경로선 미표시·클러스터/시트 수 불일치

> 원문: https://soma17-msg.atlassian.net/browse/MSG-581 (하네스 생성, 2026-09-07 — 사용자 QA "경로추천 최대 줌아웃 안 보임" / "팝업스토어 클러스터 16개·하단 0개" / "지역축제도 같은 증상")
> **수정 범위: `apps/mobile/src/features/map-home/model/mission-query-args.ts`(+test) 1쌍 + `api/active-missions-query.test.ts` 케이스 1건 갱신.** `apps/web`·`packages/**`·`viewport-query.ts` 0줄.

## 기획 요약
최대 줌아웃에서 경로추천 경로선이 사라지고, 팝업스토어·지역축제 시트가 0개("…가 없어요")인데 집계 마커는 16을 보인다. 세 칩 모두 같은 `useActiveMissionsQuery`라 원인·수정이 하나다.

## 0. 착수 실측 (develop `b65ecc4`)

| 사실 | 실측 | 함의 |
|---|---|---|
| 미션 조회 게이트 | `mission-query-args.ts` → `viewportQueryArgs(bounds)` → `canQueryGrids`: 한 변 0.5° 초과면 `enabled=false` | 최대 줌아웃은 항상 초과 |
| 빈 목록 | MSG-579가 비활성 쿼리의 placeholder를 `undefined`로 → `missions=[]` (이전엔 줌인 때 목록이 잔존해 증상이 가려짐) | 목록 0개 |
| 경로선 | `use-home-overlays.ts` `routes = courseRoutesOf(courseViews)` — 목록 파생 | 목록 비면 라인·마커 소멸 |
| 집계 마커 | `grid-aggregation-query.ts`는 `clampBoundsToSpan(bounds, AGGREGATION_SPAN_CAP_DEG[unit])`로 클램프해 어떤 줌에서도 발사 | 마커 16 / 시트 0개 |
| 선례 | MSG-579 작업 로그 "후속 후보: 0.5° 초과 구간 빈 문구가 집계 마커와 모순" | 예고된 결함 |
| 웹 | 칩 활성 시 1km 고정 줌(`chip-zoom.ts`) + 확정 영역 bbox — 경계에 닿지 않음 | 모바일 전용 |

## 1. 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | `activeMissionsQueryArgs`에서 bounds를 **`clampBoundsToSpan(bounds, MAX_VIEWPORT_SPAN_DEG)`로 잘라** 요청하고, `enabled = bounds !== null && chip !== null`(span 게이트 제거) | 집계 조회와 같은 패턴. 상한 초과 400 경로 없음. 한 줄 diff |
| D2 | `viewportQueryArgs`·`canQueryGrids` 무수정 | `/api/grids`·`/api/hotzones`가 공유 — 저줌은 집계 조회가 담당(MSG-410 설계) |
| D3 | 확대 안내 UI·헤더 문구 없음 | 마커와 목록이 함께 보이면 모순 문구가 사라진다. 목록=중앙 0.5° 창, 마커=행정 단위 총계라 수가 다를 수 있음 — 허용(티켓 명시) |
| D4 | MSG-579 테스트 L3("0.5° 초과면 undefined")는 **"bounds null이면 undefined"** 로 치환 | 초과 bbox가 더는 비활성이 아님. 칩 전환·null 게이트·인증 게이트는 그대로 |
| D5 | 웹식 1km 고정 줌·확정 영역 미도입 | 티켓 제외 범위 |

## 2. 수용 기준

| # | 기준 | 유형 | 검증 |
|---|---|---|---|
| L1 | 한 변 1° 뷰포트 → 요청 bbox가 중심 기준 0.5°×0.5°이고 `enabled=true` | 로직 | `mission-query-args.test.ts` |
| L2 | 상한 안 뷰포트는 bbox 그대로(회귀 없음), 칩 null·bounds null은 `enabled=false` | 로직 | 〃 |
| L3 | `activeMissionsQueryOptions`: 넓은 bbox + 같은 type이면 placeholder가 직전 데이터를 돌려주고, bounds null이면 undefined | 로직 | `active-missions-query.test.ts` |
| L4 | 게이트: 모바일 vitest·typecheck·lint·format:check·nose 통과, `apps/web`·`packages` diff 0 | 게이트 | 명령 |
| S1 | 에뮬레이터 최대 줌아웃 + 경로추천 칩: 경로선·번호 마커가 보이고 시트에 코스 카드가 있다 | 화면 | 실기 + 스크린샷 ① |
| S2 | 최대 줌아웃 + 팝업스토어 칩: 시트 목록이 0개가 아니다(집계 마커와 수는 달라도 됨). 지역축제 동일 | 화면 | 실기 + 스크린샷 ② |
| S3 | 광안리 zoom 16 경로추천=해파랑길, 칩 전환 시 직전 칩 카드 없음(MSG-579 회귀 없음) | 화면 | 실기 |
| S4 | logcat `ReactNativeJS` 에러 0, 네트워크 400 없음 | 화면 | 실기 |

**검증 프로파일**: 경미(로직 한 줄 + 테스트) — 경량 실행(메인 스레드). 실기 3-B ≤4장·15분, 워크트리 Metro 8082 + 런북 1-D.

## 추정 및 질문
- A1 시트 수와 마커 총계 불일치 허용(D3) — 대안 "확대 안내" 상태 신설은 별도 티켓(MSG-579 후속 후보 그대로)
- Q: 없음

## 리스크
- 최대 줌아웃에서 중앙 0.5° 창 밖의 미션은 목록에 없다 — 사용자가 마커 16 vs 목록 N을 결함으로 볼 수 있음. 안내 UI가 필요해지면 후속
- 경로추천 최대 줌아웃 시 코스 수십 개의 라인이 한 화면에 — `courseRoutesOf`는 클리핑 없음(ponytail 주석). 현재 서버 코스 수 한 자리라 성능 영향 없음

## 구현 계획
- 브랜치 `fix/MSG-581-mission-bbox-clamp` (신규 — 기존 0건), 워크트리 `../FE-MSG-581`
- `model/mission-query-args.ts`: import `clampBoundsToSpan`(aggregation-unit)·`MAX_VIEWPORT_SPAN_DEG`·`toGridsRequest`(viewport-query); `viewportQueryArgs` 호출 제거
- 테스트 갱신 2파일(test-first: L1 RED 확인 후 구현)
- nose: 접촉 패밀리만 교체(MSG-572 레시피)

## 작업 로그 (2026-09-07)

- 변경 3파일: `model/mission-query-args.ts`(`viewportQueryArgs` → `toGridsRequest(clampBoundsToSpan(bounds, MAX_VIEWPORT_SPAN_DEG))`, `enabled = bounds !== null && chip !== null`) · `model/mission-query-args.test.ts`(L1 클램프 케이스 신설, L2 null 게이트 분리 — test-first, RED 확인) · `api/active-missions-query.test.ts`(MSG-579 L3 "0.5° 초과 → undefined"를 "넓은 bbox 같은 type → 유지" + "bounds null → undefined" 2건으로 치환).
- 게이트: 모바일 vitest 1327/1327(203 파일) · typecheck · oxlint · oxfmt · nose 0 families(exit 0). `apps/web`·`packages` diff 0.
- 실기(런북 1-D — `emulator-5554`·8081·8082가 전부 타 세션 것이라 `FillMap_Pixel8_verify`(5556) + 워크트리 Metro **8083**, APK는 5554에서 pull, dev 메뉴 Bundle Location `10.0.2.2:8083`, 번들 grep으로 내 코드 확증): 최대 줌아웃(16km 단) 경로추천 4개 + 번호 마커(S1) · 팝업스토어 16개 = 집계 마커 "부산 16", 지역축제 1개 = "부산 1"(S2) · 수영구/광안리 줌인 경로추천=해파랑길 1코스 + 경로선, 칩 전환 시 직전 카드 없음(S3) · logcat `ReactNativeJS` 에러 0·4xx 0(S4). 스크린샷 4장, 실기 약 9분. iOS 미실행(환경).
- 검토한 대안: (a) `canQueryGrids` 상한 완화 — `/api/grids`·`/api/hotzones` 공유라 기각. (b) "확대 안내" 시트 상태 신설 — 마커·목록이 함께 보이면 모순이 사라지므로 기각, 필요해지면 후속(리스크 참조).
- 실기 부작용 관찰: 검증 종료 시점에 타 세션 Metro 8082(FE-MSG-578)가 내려가 있었다. 내 정리 명령은 8083·5556 한정(`pkill -f "--port 8083"`, `emu kill` on 5556)이라 인과는 확인 안 됨 — 사용자 보고에 기재.
