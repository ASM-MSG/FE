# MSG-574: [모바일] 지도 홈 줌아웃 하한 완화 — 시(SIDO) 단위 집계 마커 구간까지 열기

> 원문: `_workspace/MSG-574/00_ticket.md` (하네스 생성, 2026-09-05) · https://soma17-msg.atlassian.net/browse/MSG-574
> **수정 범위: `apps/mobile/src/features/map-home/model/` 4파일(상수 값·주석·테스트).** `apps/web`·`packages/**`·네이티브·ui 0줄.

## 기획 요약

앱 지도 홈이 축척 8km 단(zoom 10)에서 줌아웃을 멈춰 시 전체를 볼 수 없다. 시 단위 집계 마커 구간이 보이도록 하한을 내린다. 서버·웹 무수정.

## 0. 착수 실측 (origin/develop `b211011`)

| 사실 | 실측 | 함의 |
|---|---|---|
| 하한 정의 | `model/aggregation-unit.ts` `MAP_MIN_ZOOM = MAP_SCALE_8KM_ZOOM`(=10), 소비처 `ui/map-home-screen.tsx` `minZoom={MAP_MIN_ZOOM}` 1곳 | 상수 값만 바꾸면 끝 |
| SIDO 경로 | `aggregationUnitForZoom` 6~9→`"SIDO"`, `AGGREGATION_SPAN_CAP_DEG.SIDO=10`, `drillInZoomForUnit("SIDO")=4km 단(11)`, `abbreviateSidoName`, `MARKER_MERGE_PX.SIDO=92` 전부 웹 parity로 동결 | 시 구간 신규 로직 0 |
| 웹 | `MapCanvas.tsx` `minZoom` 0건 → SDK 기본 6 | 티켓 초안은 "웹과 동일(6)" |
| **서버 제약(실기에서 발견)** | 집계 `GET /api/grids/aggregation`은 현재 지역 판정을 위해 **bbox 중심**을, 역지오코딩은 중심 좌표를 `KoreaCoordinates.isOutOfService`(위도 33~39·경도 124~132)로 검사해 밖이면 400(6400). 앱 지도는 전체 화면이고 시트가 아래를 가려 카메라 중심이 보이는 중심보다 약 500px 남쪽 | zoom 6에서 중심이 태평양(위도 ~26)으로 내려가 집계 토스트 + 시트 실패. 하한은 중심 오프셋이 한국 범위 안에 남는 줌이어야 한다 |
| 오프셋 환산 | 500px 남쪽 = zoom 9 약 1.1° · zoom 8 약 2.2° · zoom 6 약 9°. 부산 남해안 위도 35.0 → zoom 9 중심 33.9(안) · zoom 8 32.8(밖) | **하한 = 16km 단(zoom 9)** |
| 요청 폭 | zoom 9 세로 약 5.4° < SIDO 상한 10° | 클램프 미개입 |
| 깨질 테스트 | `aggregation-unit.parity.test.ts` L6(`MAP_MIN_ZOOM===10`, 10~21에 SIDO 없음) | 16km 단 경계 단정으로 교체 |
| 브랜치 | `*MSG-574*` 없음 | `feat/MSG-574-mobile-map-zoom-floor` 생성 |

## 1. 코드로 확정한 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | `MAP_MIN_ZOOM = MAP_SCALE_16KM_ZOOM`(=9, `map-scale`에 역산 상수 신설). 웹처럼 하한을 없애지 않는다 | 서버 한국 범위 검사 + 시트 뒤 중심 오프셋(위 실측). zoom 6~8은 부산 어디서든 400이 난다 |
| D2 | `GridMap.minZoom` prop·`map-home-screen` 배선 무수정(값만 바뀐다) | 접촉면 최소 |
| D3 | 시트 뒤 중심 오프셋 자체(지도 contentPadding)는 손대지 않는다 — 제주 남해안 등 하한과 무관하게 오늘도 나는 결함이라 별도 후속 | 범위 밖, 카메라 명령 전반에 영향 |
| D4 | `MAP_SCALE_8KM_ZOOM` 유지(집계 단위 경계) | parity 표 동등성 |

## 2. 수용 기준

**검증 프로파일: 경미** — 상수 1개 + 테스트. 대상이 `apps/mobile`이라 3-B 실기 1회 필수. 시간 예산: 핵심 경로 1회 + 스크린샷 ≤6장 + 15분.

| # | 기준 | 유형 | 검증 방법 |
|---|---|---|---|
| L1 | `MAP_MIN_ZOOM === MAP_SCALE_16KM_ZOOM === 9`, `aggregationUnitForZoom(9) === "SIDO"`, `aggregationUnitForZoom(10) === "SIGUNGU"` | 로직 | vitest L6 교체 2건 |
| L2 | 웹 parity 전건(L2~L5) 그대로 그린 | 로직 | vitest |
| L3 | `pnpm lint`·`pnpm typecheck`·`pnpm --filter mobile test run`·`format:check`·duplication 그린. `apps/web`·`packages/**`·android/ios diff 0 | 게이트 | 게이트 + `git diff --stat` |
| S1 | 홈에서 핀치 줌아웃을 끝까지 하면 8km 단을 지나 16km 단에서 멈추고, 부산 전역(김해~기장)이 한 화면에 들어오며 시 마커("부산" + 격자 수)가 보인다. 집계 실패 토스트 없음 | 화면 | 실기 [스샷: 하한] (`emu-pinch.sh`) |
| S2 | 시 마커 탭 → 구 구간으로 들어간다(웹 `drillInZoomForUnit` 동일) | 화면 | 실기 [스샷] |
| S3 | `adb logcat` FATAL·`ReactNativeJS` 에러 0 | 화면 | 실기 logcat |
| S4 | 구·동·격자 구간(zoom ≥10) 동작 불변 — 구 마커 탭 → 500m 단, 줌인해 격자가 돌아온다 | 화면 | 실기 [스샷] |

> 정합성: L1이 D1, S1이 [동작 요구] 1·2·5, S2가 3, S4가 4, S3가 7. [동작 요구] 1의 "SDK 기본 zoom 6"은 서버 제약으로 **16km 단으로 확정**(티켓 [참고] 첫 항목의 조이는 안). [동작 요구] 6(AI 경로 화면)은 `minZoom` 배선이 지도 홈 화면에만 있어 정적으로 성립.

## 3. 구현 계획

- **브랜치**: `feat/MSG-574-mobile-map-zoom-floor`
- **변경**
  - `model/map-scale.ts` — `MAP_SCALE_16KM_ZOOM` 신설
  - `model/aggregation-unit.ts` — `MAP_MIN_ZOOM` 값·JSDoc(서버 제약·오프셋 근거)
  - `model/region-cluster-overlay.ts` — 주석
  - `model/aggregation-unit.parity.test.ts` — L6 교체
- **재사용**: 기존 SIDO 경로 그대로. 신규 컴포넌트·훅 0
- **승격 후보**: 없음

## 4. 추정 및 질문

- 없음 — 하한 값은 실기·서버 코드로 확정.

## 5. 리스크

- 시 구간에서 부산 밖 시도 마커(서울 서초 등 실데이터)가 함께 보인다 — 서버 응답 그대로, 티켓 제외 범위.
- 시트 뒤 중심 오프셋으로 제주 남해안 등에서는 zoom 9~10에서도 6400이 날 수 있다(D3, 기존 결함) — 후속.
- iOS 미실측(환경).

---

## 작업 로그 (2026-09-05)

**실측 동작**
- 변경 4파일(`features/map-home/model/`): `map-scale.ts` `MAP_SCALE_16KM_ZOOM` 신설 · `aggregation-unit.ts` `MAP_MIN_ZOOM = MAP_SCALE_16KM_ZOOM`(=9) + JSDoc 근거 교체 · `region-cluster-overlay.ts` 주석 · `aggregation-unit.parity.test.ts` L6 교체(하한=9·`SIDO`, 10=`SIGUNGU`). `GridMap.minZoom` prop·`map-home-screen` 배선 무수정. `nose.baseline.json`은 develop 기준 + 접촉 패밀리만 교체(removed 8 · added 7).
- 게이트: vitest 1317 · typecheck · lint · format · duplication 통과. web·packages·native 0줄.
- 실기(emulator-5554, 약 14분 · 스크린샷 7장): 핀치 줌아웃 끝 → 16km 단에서 정지, 부산 전역(김해~기장) 한 화면 + 시 마커 "부산 1", 집계 토스트 없음 · 시 마커 탭 → 구 구간 · 구 마커 탭 → 500m 단 · 줌인 시 격자·점령 채움 복귀 · logcat 0. iOS 미실행(환경).

**재작업 1회 — 티켓 초안(하한 삭제, 웹 동일 zoom 6) 기각**
- zoom 6 실기에서 "지역 집계를 불러오지 못했어요" + 시트 "불러오지 못했어요". BE 실측: 집계 `getOccupiedAggregatesWithCurrentRegion`이 bbox 중심을, 역지오코딩이 중심 좌표를 `KoreaCoordinates.isOutOfService`(위도 33~39·경도 124~132)로 검사해 400/6400. 앱 지도는 전체 화면 + 시트가 하단을 가려 카메라 중심이 보이는 중심보다 약 500px 남쪽(zoom 9 ≈ 1.1° · zoom 8 ≈ 2.2° · zoom 6 ≈ 9°). 부산 남해안(35.0)에서 zoom 8부터 중심이 범위 밖 → 16km 단이 부산 전역에서 안전한 하한. 요청 폭 5.4° < SIDO 10°라 클램프 미개입.

**검토한 대안**
- 하한 삭제(웹 동일) — 기각: 위 서버 제약.
- 32km 단(zoom 8, 티켓 [참고] 안) — 기각: 오프셋 2.2°로 부산 남해안에서 400.
- 지도 contentPadding으로 중심 오프셋 제거 — 기각(D3): 카메라 명령 전반(내 위치·검색 복귀·드릴인)에 영향, 별도 티켓.
- 클램프 부동소수 여유(ε) — 불필요: 1,095개 중심 표본에서 `(c+5)-(c-5) > 10` 0건, 그리고 16km 단에서는 클램프 자체가 안 걸린다.

**WAIVED · 확인불가**
- 없음. 미실행은 iOS(환경)만.

**후속 후보(DECISIONS 기록)**
- 시트 뒤 카메라 중심 오프셋(제주 남해안 등 zoom 9~10에서도 6400 가능) · 현재 위치 점이 시·구 마커 개수를 가림(z-order) · 드릴인 목적지가 시트 뒤 · 핀치 스크립트 연타 유실·슬롯 미해제 시 지도 입력 무시(런북 함정 10 보강).
