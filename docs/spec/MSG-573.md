# MSG-573 스펙 — 홈 시트 액션 행 공유·저장 칸 제거

검증 프로파일: **경미** (로직 변경 없음 — 표시 전용 마크업 2칸 삭제). 메인 스레드 직접 실행.

## 수용 기준
| # | 기준 | 검증 방법 |
|---|------|----------|
| AC1 | 홈 시트 격자 상세 액션 행에 "영상 추가"만 있고 "공유"·"저장" 텍스트·아이콘이 없다 | 코드 diff + 실기 스크린샷 1장 |
| AC2 | 핫구역 동 요약 시트 액션 행도 AC1과 같다 (같은 컴포넌트 — 호출부 무수정) | 코드 diff (호출부 2곳 동일 컴포넌트) · 실기는 핵심 경로 밖 → "미실행(범위 축소)" 허용 |
| AC3 | "영상 추가" 탭 → 업로드 화면 진입 회귀 없음 | onPress 배선 무수정 diff 확인 + 실기 1회 |
| AC4 | 게이트: `pnpm --filter mobile typecheck` · lint · vitest 통과, `Share2`·`Bookmark` 미사용 import 잔존 0 | 명령 실행 |

## 구현 계획
- `cell-action-row.tsx`: 공유·저장 View 2개 + `Share2`·`Bookmark` import 삭제, `semantic.muted` 사용처 소멸 확인. 헤더 주석을 "영상 추가 단일 행"으로 갱신하고 MSG-573 근거 기록.
- 호출부·테스트·웹 무수정.

## 추정
- 단일 칸이 된 뒤에도 테두리 행(`rounded-md border`) 유지 — Figma 재확인 없이 최소 diff. 시각 조정은 실기에서 판단.

## 리스크
- 없음 (스냅샷·vitest 미참조 확인).

## 작업 로그 (2026-09-05)
- 변경: `apps/mobile/src/features/map-home/ui/cell-action-row.tsx` 한 파일 — 공유·저장 표시 전용 칸 2개 + `Share2`·`Bookmark` import 삭제(4+/13−). 호출부 2곳·웹·테스트 무수정.
- 검증: typecheck·oxlint·oxfmt·vitest(202 files/1305 tests)·nose 전부 통과. 실기 에뮬레이터 — 격자 상세(서면 I-7)에 "영상 추가"만 남음, 탭 시 업로드 화면 진입 확인. 핫구역 요약은 같은 컴포넌트라 실기 미실행(범위 축소).
- 검토한 대안: ① 칸을 남기고 disabled 스타일만 — 눌리지 않는 버튼이 그대로 남아 QA 혼란의 원인이 유지돼 기각. ② 웹 `CellActionRow`도 함께 삭제 — 모바일 티켓 웨이브는 웹 수정 금지 원칙(2026-09), 웹은 별도 판단으로 제외.
- 잔여: 웹 `CellActionRow` 공유·저장 no-op 버튼 동일 상태 · 레거시 `features/grid-detail` 스텁 화면의 공유 원형 버튼(홈 시트 경로 아님).
