# MSG-566: [모바일] 네이버 지도 커스텀 스타일 적용 — My Style ID 발행본을 지도 홈 지도에 적용

> 원문: `_workspace/MSG-566/00_ticket.md` (하네스 생성, 2026-09-04) · https://soma17-msg.atlassian.net/browse/MSG-566
> **수정 범위: `apps/mobile/src/features/map-home/ui/grid-map.tsx` 1파일.** `apps/web`·`packages/**`·네이티브(android/ios) 0줄.

## 기획 요약

NCP Map Studio에서 발행한 커스텀 지도 스타일(`990d8dd2-8d6d-41e6-ae17-6f7e7a594730`, 발행 버전 `20260904102914`)을 모바일 앱 지도에 적용한다. 웹은 범위 밖. 스타일 로드 실패는 지도 기능을 막지 않아야 하고(기본 스타일 폴백), 기존 격자·마커 오버레이는 그대로 보여야 한다.

---

## 0. 착수 실측 (develop `3f36934`)

| 사실 | 실측 | 함의 |
|---|---|---|
| 래퍼 지원 | `@mj-studio/react-native-naver-map` 2.9.0 `NaverMapView` props: `customStyleId?: string` · `onCustomStyleLoaded?: () => void` · `onCustomStyleLoadFailed?: ({message}) => void`. Android `RNCNaverMapViewManager.kt:676` `map.setCustomStyleId(value, callback)` / iOS `RNCNaverMapView.mm:247` 동일 | **JS prop 1개로 끝난다.** 네이티브 패치·재빌드 불필요(기존 dev client에 이미 컴파일돼 있음) |
| 네이버 SDK | Android `map-sdk:3.23.2`(래퍼 gradle.properties) — 커스텀 스타일은 3.17+ | 지원 범위 안 |
| 적용 지점 | `NaverMapView` 인스턴스는 앱 내 `grid-map.tsx` 1곳(grep). AI 경로 화면(MSG-556)도 `GridMap`을 재사용하므로 자동 승계 | 상수·prop 배치는 `grid-map.tsx`(SDK 격리 경계) |
| 실패 동작 | 네이티브 SDK는 로드 실패 시 기본 스타일 유지, 래퍼는 `onCustomStyleLoadFailed`로 메시지만 올림 | 폴백은 SDK가 이미 해 준다 — 코드는 로그 1줄만 |
| Version 값 | SDK API는 ID만 받는다(`setCustomStyleId(String, callback)`). 콘솔 "Version"은 발행 이력 식별자 | 코드에는 주석으로 기록 |
| 웹 | `apps/web` `customStyleId` 사용 0 | 웹 무수정이 곧 "웹 제외" 충족 |
| 테스트 인프라 | vitest 순수 모델만. `grid-map.tsx`는 RN import라 vitest 대상 아님 | 신규 로직 0 → 신규 테스트 0. 게이트 + 실기 |
| 기준 스크린샷 | `_workspace/MSG-561/screenshots/02_cold_start_home.png` (2026-09-03, 기본 스타일 홈) | "적용 전" 비교본으로 재사용 — 실기 1회로 끝냄 |
| 브랜치 | `*MSG-566*` 로컬·원격 없음 | `feat/MSG-566-mobile-map-custom-style` 생성 |

---

## 1. 코드로 확정한 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | 상수 `NAVER_MAP_CUSTOM_STYLE_ID`를 `grid-map.tsx` 모듈 스코프에 둔다 (JSDoc에 발행 Version 기록). 별도 모델 파일·환경변수 만들지 않음 — 공개 식별자이고 소비처 1곳 | 티켓 [동작 요구] 4 · 지도 SDK 격리 경계 규칙 |
| D2 | `onCustomStyleLoadFailed` → `console.warn("[GridMap] custom style load failed", message)` 1줄. 사용자 노출 UI(토스트 등) 없음 — SDK가 기본 스타일로 이미 폴백하고, 사용자가 취할 행동이 없다 | 티켓 [동작 요구] 2 |
| D3 | `onCustomStyleLoaded` 미배선 — 스플래시 해제 신호(`onLoaded`, MSG-445)를 스타일 로드에 묶지 않는다. 스타일이 늦거나 실패해도 스플래시는 타일 기준으로 내려간다 | MSG-445 계약 유지 |
| D4 | `GridMap` prop으로 노출하지 않는다(호출처가 끄거나 바꿀 이유 없음) | YAGNI |

---

## 2. 수용 기준

**검증 프로파일: 경미** — 로직 0, prop 1개 + 상수. 단 대상이 `apps/mobile`이라 3-B 실기 1회는 필수(MOBILE_RUNBOOK). 시간 예산: 핵심 경로 1회 + 스크린샷 ≤2장 + 15분.

| # | 기준 | 유형 | 검증 방법 |
|---|---|---|---|
| L1 | `grid-map.tsx`에 `customStyleId={NAVER_MAP_CUSTOM_STYLE_ID}` 배선, 상수값 `990d8dd2-8d6d-41e6-ae17-6f7e7a594730`, JSDoc에 Version `20260904102914` | 로직(정적) | grep |
| L2 | `onCustomStyleLoadFailed`가 `console.warn`으로 메시지를 남기고 그 외 부수효과 0 | 로직(정적) | 코드 대조 |
| L3 | `pnpm lint`·`pnpm typecheck`·`pnpm --filter mobile test run`·`check:duplication --fail-on new` 그린. `apps/web`·`packages/**`·`apps/mobile/android|ios` diff 0 | 게이트 | 게이트 + `git diff --stat` |
| S1 | Android dev client로 홈 진입 시 커스텀 스타일 지도가 보인다 — `02_cold_start_home.png`(기본 스타일)과 나란히 놓았을 때 배경·도로·라벨 색이 다르다 | 화면 | 실기 [스샷 1: 홈 커스텀 스타일] + 기준본 병기 |
| S2 | 같은 화면에서 격자 셀·클러스터 마커(줌아웃 1회)·점령 채움 등 기존 오버레이가 스타일 위에 종전과 같이 그려진다 | 화면 | 실기 [스샷 2: 줌아웃 클러스터] |
| S3 | `adb logcat -s ReactNativeJS:V` 에 `custom style load failed` 0건, FATAL 0 | 화면 | 실기 logcat |

> 정합성: L1·L2가 D1·D2, S1이 [동작 요구] 1, S2가 3, S3가 2(실패 경로의 부재 확인 — 실패 유도 실기는 하지 않음: ID를 깨서 폴백을 보는 건 SDK 동작 확인이라 범위 밖)를 덮는다.

---

## 3. 구현 계획

- **브랜치**: `feat/MSG-566-mobile-map-custom-style` (신규 — 기존 없음 확인)
- **재사용**: 래퍼 내장 prop. 신규 컴포넌트·훅·모델 0
- **변경**: `apps/mobile/src/features/map-home/ui/grid-map.tsx`
  - 모듈 상수 `NAVER_MAP_CUSTOM_STYLE_ID` + JSDoc(Style ID·Version·콘솔 출처·재발행 시 ID 불변)
  - `<NaverMapView customStyleId={NAVER_MAP_CUSTOM_STYLE_ID} onCustomStyleLoadFailed={…console.warn} />`
- **라우트**: 없음
- **승격 후보**: 없음
- **테스트**: 신규 0 (순수 로직 없음 — vitest 대상 파일 무변경)

## 4. 추정 및 질문

- (추정) 스타일이 앱 NCP Application(`EXPO_PUBLIC_NAVER_MAP_CLIENT_ID`)과 **같은 NCP 계정**에서 발행됐다고 본다. 다른 계정 스타일이면 로드 실패 → 기본 스타일 + warn. S3에서 드러난다.
- (추정) 웹 후속 티켓은 이 티켓에서 만들지 않는다 — 사용자가 "모바일만"이라 명시.

## 5. 리스크

- iOS 미실측 — 래퍼 iOS 구현 동일하나 실기 없음. 리포트에 "미실행(환경)"로 표기.
- 스타일 로드는 네트워크 의존 — 최초 표시 직후 몇백 ms 기본 스타일이 보였다가 전환될 수 있음(SDK 동작, 결함 아님).
- **Figma 오탐 방지**: Figma 노드 없음. 대조 대상은 콘솔 발행 스타일 자체이며 실기 스크린샷이 정본.

---

## 작업 로그 (2026-09-04)

**실측 동작**
- 변경 파일 1개: `apps/mobile/src/features/map-home/ui/grid-map.tsx` (+12) — 모듈 상수 `NAVER_MAP_CUSTOM_STYLE_ID`(JSDoc에 발행 Version `20260904102914`) + `<NaverMapView customStyleId onCustomStyleLoadFailed(console.warn)>`. 신규 테스트 0(순수 로직 없음 — vitest 대상 파일 무변경).
- 게이트 6종 그린(vitest 1249 · typecheck · lint · format · duplication 0 · openapi 드리프트 없음).
- 실기(emulator-5554, `--skip-build`, 약 4분·스크린샷 2장): S1 통과 — 기본 스타일(MSG-561 기준본) 대비 단색 팔레트·간선도로 무채색·상점 POI 숨김이 명확. S2 격자선·점령 채움·핫구역 강조 정상 + **2차 실기(사용자 "에뮬레이터로 해보셈")에서 `adb root`+`sendevent` 핀치로 줌아웃 → 클러스터 말풍선 `부산진구 1` 스타일 위 정상 렌더** (런북 함정 10 신설·`scripts/emu-pinch.sh`). S3 logcat `custom style load failed`·에러 0.
- iOS 미실행(환경) — 래퍼 iOS 구현 동일 prop 코드 확인으로 갈음.

**검토한 대안**
- 상수를 `model/map-style.ts`로 분리 — 기각: 소비처 1곳, 테스트할 로직 없음.
- `onCustomStyleLoaded`로 스플래시 해제 연동 — 기각(D3): 스타일 지연·실패가 스플래시를 잡아둔다.
- 실패 시 사용자 토스트 — 기각(D2): SDK가 기본 스타일로 이미 폴백, 취할 행동 없음.
- 환경변수화 — 기각(D1): 공개 식별자, 빌드별로 달라질 이유 없음.

**WAIVED · 확인불가**
- 없음. 미실행은 iOS(환경)만.
