# MSG-558: [모바일] 줌아웃 행정 단위 집계 마커 말풍선 리디자인 (웹 MSG-475 대응)

## 기획 요약

앱 지도 홈의 저줌(zoom < 16) 행정 단위 집계 마커는 MSG-428 시점의 **파란 원형(동 68 / 구 80, 흰 링 3px)** 그대로다. 웹은 MSG-475에서 같은 마커를 **말풍선(라운드 10 · 패딩 6/12/7 · 링 없음 · 그림자 · 2줄 지역명/개수 · 크기 단일)** 으로 바꿨다. 이 티켓은 앱 `features/map-home`의 집계 마커 **렌더만** 웹 구현과 같은 형태로 교체한다. 집계 단위 전환·탭 드릴인·겹침 병합·격자 복귀는 기존 동작 그대로다.

**플랫폼: `apps/mobile` 전용. `apps/web`·`packages/ui-web`은 읽기만 하고 한 줄도 수정하지 않는다.** 디자인 정본은 Figma가 아니라 **웹 코드**(`apps/web/src/pages/map-home/ui/MapCanvas.tsx` `clusterMarkerContent`, 사용자 지시 2026-09-02)다 — Figma 탐색은 지시에 따라 생략했다.

---

## 코드 현황 대조 (STATUS.md + 소스 실측, 2026-09-02)

### 웹 정본 실측 — `MapCanvas.tsx:358` `clusterMarkerContent` (네이버 SDK `Marker.icon.content` HTML 문자열)

```html
<div class="flex -translate-x-1/2 -translate-y-1/2 flex-col items-center whitespace-nowrap
            rounded-[10px] px-3 pt-1.5 pb-1.75 {bg-secondary | THEME_FILL_CLASS[theme]} shadow-raised">
  <span class="text-fm-label text-primary-foreground/85">{name}</span>      <!-- name null이면 생략 -->
  <span class="text-fm-heading font-bold text-primary-foreground">{formatClusterCount(count)}</span>
</div>
```
마커 옵션: `title={clusterMarkerTitle}`(접근명 `"{name} 점령 격자 N개"`), `onClick=zoomToCluster`(`drillInZoomForUnit`). `zIndex`·충돌 옵션·선택 상태·꼬리(pseudo-element)·테두리 **전부 없음**(grep `after:|before:|tail|selected` 0건). 크기 3단(`CLUSTER_UNIT_SIZE_CLASS`)은 MSG-475에서 **삭제** — 폭은 지명 길이에 hug(브라우저 실측 DONG 63×48 · SIGUNGU 66×48, 높이 동일). 겹침 처리는 렌더 전 순수 함수 `mergeOverlappingMarkers`(`MARKER_MERGE_PX` 68/80/92, 값 불변 — 주석만 "재보정은 RN 렌더 교체 티켓과 함께")뿐이다.

### 웹 클래스 → 토큰 → 앱 매핑

앱의 지도 마커 서브뷰는 nativewind 클래스가 아니라 **토큰 값을 `style`로 직접 주입**한다(`cluster-marker.tsx`·`grid-map.tsx` 경로 번호 마커 관례 — 네이티브 마커 서브뷰라 className 적용이 불확실). 따라서 매핑 대상은 클래스가 아니라 `@fillmap/design-tokens` 값이다.

| 웹 클래스 | 토큰(`packages/design-tokens`) | 값 | 앱 RN `style` |
|---|---|---|---|
| `bg-secondary` | `semantic.secondary`(= `cyan-500`) | `#0071E3` | `backgroundColor: semantic.secondary` (현행 `semantic.primary` `#0066CC`에서 이동) |
| `rounded-[10px]` | 토큰 없음(radius sm 8 / md 12 사이 — 웹도 임의값 예외) | 10 | `borderRadius: 10` 리터럴 + 주석(웹과 같은 예외) |
| `px-3` | `spacing.sm` | 12 | `paddingHorizontal: spacing.sm` |
| `pt-1.5` / `pb-1.75` | 토큰 없음(웹 숫자 스케일) | 6 / 7 | `paddingTop: 6`, `paddingBottom: 7` 리터럴(웹 스케일 값 그대로) |
| `shadow-raised` | `shadow.raised` = `0 2px 10px 0 rgba(25,31,40,.12)` | — | 기존 `RAISED_SHADOW` 상수 재사용(iOS `shadowColor: semantic.textPrimary`·offset 0/2·opacity .12·radius 10, Android `elevation: 3`) — **RN은 CSS 그림자 문자열을 못 받아 분해가 유일한 대안**. Android 한계는 리스크 R3 |
| `text-fm-label` | `typography.label` | 500 / 12 / 15 | `fontSize: 12, lineHeight: 15, fontWeight: "500"` (현행 caption 400/11/14에서 이동) |
| `text-primary-foreground/85` | `semantic.onPrimary` + 알파 85% | `#FFFFFF` ×.85 | `color: semantic.onPrimary` + `opacity: 0.85`(hex 리터럴 없이 알파 표현) |
| `text-fm-heading font-bold` | `typography.heading` + weight 700 | 16 / 20, 700 | `fontSize: 16, lineHeight: 20, fontWeight: "700"` (현행 body-strong 600/13/17에서 이동) |
| `text-primary-foreground` | `semantic.onPrimary` | `#FFFFFF` | `color: semantic.onPrimary` |
| `whitespace-nowrap` | — | — | `<Text numberOfLines={1}>` |
| `-translate-x-1/2 -translate-y-1/2`(중심 앵커) | — | — | `NaverMapMarkerOverlay anchor={{x:.5,y:.5}}` (현행 유지) |
| `title` | — | — | `accessibilityLabel`(현행 문구 유지) |
| `THEME_FILL_CLASS[theme]` | `theme-*` 토큰 | — | **해당 없음** — 앱 마커에는 `theme` 필드가 없고(모바일 `RegionClusterMarker`에 `theme` 없음, 미션·핫 집계 훅 없음) 칩 활성 중 마커는 억제된다(MSG-428 승인 Q4). 파랑 단일 |
| hug 폭 | — | — | **RN 마커는 `width`·`height` 필수**(라이브러리 주석 "되도록 모두 width, height prop 사용") → 순수 추정 함수 `clusterBubbleSize`로 박스 크기 산출 (구현 계획 2) |

### 웹 MSG-428 원형 → 웹 MSG-475 말풍선 → 앱 현행 차이 (항목별)

| 항목 | 앱 현행(MSG-428 `cluster-marker.tsx`) | 웹 정본(MSG-475) | 이 티켓 |
|---|---|---|---|
| 형태 | 원(`borderRadius: size/2`) | 라운드 사각 10 | 교체 |
| 크기 | 단위별 68/80(/92) 정사각 = `CLUSTER_MARKER_PX = MARKER_MERGE_PX` | 단일, 내용 hug(≈63~66×48) | 3단 폐기, 추정 크기 박스 + 내부 hug |
| 테두리 | 흰 링 3px(`RING_WIDTH`) | 없음 | 제거 |
| 채움 | `semantic.primary` #0066CC | `secondary` #0071E3 | 교체 |
| 그림자 | `RAISED_SHADOW`(raised 분해) | `shadow-raised` | 유지 |
| 지역명 | caption 400/11/14, 불투명 | label 500/12/15, 85% | 교체 |
| 개수 | body-strong 600/13/17 | heading 16/20 bold 700 | 교체 |
| 개수 서식 | `formatClusterCount`(정규식 콤마, Hermes Intl 회피) | `toLocaleString("ko-KR")` | 유지(동등성은 `cluster-format.test.ts`가 고정) |
| 접근명 | `"{name} 점령 격자 N개"` | 동일 | 유지 |
| 겹침 | `mergeOverlappingMarkers` 68/80/92 | 동일 | 유지(parity 리터럴) |
| 탭 | `onTap` → `drillInZoomForUnit` | `onClick` 동일 | 유지 |
| 테마 색 분기 | 없음(칩 활성 시 마커 억제) | 축제/팝업/핫 채움 분기 | 대상 아님 |
| 자식 root `key` | 없음 | (해당 없음 — HTML 재생성) | **추가**(라이브러리 계약, R4) |

### 네이버 지도 RN 마커 렌더링 방식 (`@mj-studio/react-native-naver-map@2.9.0` 소스 실측)

`apps/mobile/node_modules/@mj-studio/react-native-naver-map/src/component/NaverMapMarkerOverlay.tsx` `image` prop JSDoc — 마커 종류 5가지: ① SDK 기본 심벌 ② 로컬 이미지(`require`, 캐싱 ✅) ③ 네이티브 리소스 ④ 네트워크 이미지 ⑤ **커스텀 React View(`children`, 캐싱 ❌)**. ⑤의 계약:
- `children`은 1개 이하(`nAssert`), 뷰들에 `collapsable={false}`, **`width`·`height` prop 필수**("되도록 모두 사용 — 없으면 debug/release 크기가 다르게 나옴")
- **"마커의 생김새를 바꿔야 한다면 그 의존성들을 제일 상위 자식의 `key`로 전달해야 한다"** — Android는 RN Shadow Node를 커스텀해 실제 `View`를 삽입, iOS는 `UIView`를 `UIImage`로 캔버스에 그린다
- **"이 타입은 많이 생성될 시 성능에 굉장히 영향 — 단순하게 쓰거나 되도록 이미지 권장"**, 마커당 리소스 큼
- 충돌 옵션 `isHideCollidedMarkers`·`isHideCollidedCaptions`·`isForceShowIcon`·`zIndex`, 텍스트 `caption`/`subCaption`(색·크기·정렬만, **굵기 불가**) 존재

**앱 현행 원형 마커 = ⑤ 커스텀 뷰**(`grid-map.tsx:447~459` — `NaverMapMarkerOverlay width/height={CLUSTER_MARKER_PX[unit]} anchor 0.5/0.5 onTap` + `<ClusterMarker>`). 경로 번호 마커(MSG-298)·미션 이름표(MSG-427)도 같은 방식이다.

**판단 — 권장안: ⑤ 커스텀 뷰 유지.** 말풍선(라운드 사각 + 2줄 텍스트 + 그림자)은 `View`+`Text` `style`만으로 그려지고 꼬리가 없어 SVG 불필요. 성능 제약은 MSG-428이 같은 방식·같은 마커 수로 이미 수용한 리스크(R6)이고, 마커 수는 뷰포트 × 행정 단위 × 병합으로 눌린다(동 단위 부산 전역 ≈ 200여 동이지만 뷰포트가 1° 상한 클램프 + 화면 안만 응답). 실기 검증에서 프레임 저하가 보이면 리스크 R5 대안으로 내려간다.
**대안: ② 이미지 + `caption`/`subCaption`.** 말풍선 PNG 1장(캐싱 ✅) 위에 네이티브 캡션 2줄(`align: "Center"`) — 마커당 리소스가 가장 작다. 대가: 캡션 굵기 지정 불가(개수 bold 불가), 폭 hug 불가(`width`로 늘리면 라운드 10이 왜곡 — 9-patch 미지원), 85% 알파는 `color` rgba로 가능. 정본 재현도가 떨어지므로 성능 실측에서 필요할 때만 채택.

### 앱 핫구역 집계·SIDO 확인 (티켓 [제외 범위] 조사 결과)

- **핫구역 행정 단위 집계 — 앱에 없다.** `apps/mobile/src`에서 `getHotZoneAggregates`·`hotzones/aggregation` 0건. 앱은 개별 조회 `api/use-hotzones-query.ts`(`GET /api/hotzones`, 로그인 게이트)와 `use-home-overlays.ts`의 `hotGridIds` 격자 낱개만 있다. 생성 SDK(`getHotZoneAggregates`)는 `shared/api/sdk.ts`가 웹 생성물을 재수출하므로 **호출 가능한 상태**지만 훅·마커 파생·게이트가 전부 없다. 게다가 앱은 칩 활성 중 집계 마커를 억제(MSG-428 Q4)하므로 핫 칩 저줌은 격자 낱개 그대로 → 웹 MSG-475 ③ 대응은 **별도 티켓**(훅 + `toHotZoneClusterMarkers` 포팅 + `hotCellsVisible` 게이트 + 칩 택일 규칙).
- **SIDO — 앱 `aggregation-unit.ts`는 웹과 같은 3단(`DONG|SIGUNGU|SIDO`) 테이블**이고, `MAP_MIN_ZOOM = 10`으로 SIDO 구간(zoom 6~9) 도달만 막는다(MSG-428 승인 Q2). 이 티켓이 크기 3단을 폐기하면 마커 렌더에 unit 분기 자체가 사라지므로 SIDO 말풍선은 **코드상 자동 지원**(별도 작업 없음), 도달 불가는 그대로다. 티켓 조건 "웹과 같으면 포함"에 해당 → 포함(추가 구현 0).

### 병렬 티켓과의 접촉면

- 이 티켓의 수정 파일: `ui/cluster-marker.tsx`(전면), `model/cluster-bubble-size.ts`(+test, 신설), **`ui/grid-map.tsx` 3줄**(`:33` import 1줄, `:452~453` `width`/`height` 2줄). `map-home-screen.tsx`·`use-home-overlays.ts`·api·model 나머지 **무수정**.
- MSG-556(AI 경로 — 지도 번호 마커·연결선)·MSG-557(행사 — 상단 칩·행사 위치 표시)은 `grid-map.tsx`의 props 인터페이스(`:90~145`)와 마커 렌더 구간(`:400~495`)에 새 층을 붙일 가능성이 높다. 충돌 지점은 **import 블록 `:33`과 클러스터 블록 `:447~459` 인접 줄**. 이 티켓의 diff가 3줄이라 어느 쪽이 나중에 리베이스하든 손 병합은 사소하다 — 리스크 R1.

---

## 수용 기준

### A. 로직 (vitest — `apps/mobile` 순수 모델)

| # | 기준 | 유형 | 검증 방법 |
|---|---|---|---|
| L1 | `clusterBubbleSize(marker)`가 name 있는 마커에 높이 **48**(= 6 + 15 + 20 + 7 → `paddingTop + label.lineHeight + heading.lineHeight + paddingBottom`, 토큰에서 산출)을, `name === null` 마커에 **33**(6 + 20 + 7)을 돌려주고, 폭은 `2×spacing.sm + max(지역명 폭 추정, 개수 폭 추정)`이며 지역명 글자 수에 단조 증가한다 | 로직 | `cluster-bubble-size.test.ts` |
| L2 | 폭 추정은 **과소 추정 금지** 방향이다: 웹 브라우저 실측 표본("부전2동"→63, "부산진구"→66)보다 작지 않고, 한글 1자 ≥ fontSize·숫자/콤마 ≥ 0.6×fontSize로 센다(박스는 투명이라 과대는 비용 0, 과소는 잘림) | 로직 | `cluster-bubble-size.test.ts` |
| L3 | 박스는 그림자 번짐 여백(`SHADOW_BLEED`)을 양쪽에 더한 값이고, 마커 `width`/`height`는 unit과 무관하게 이 함수 하나로 정해진다 — `CLUSTER_MARKER_PX` export가 사라진다 | 로직 | 테스트 + `grep -rn CLUSTER_MARKER_PX apps/mobile/src` 0건 |
| L4 | `MARKER_MERGE_PX`(68/80/92)·`mergeOverlappingMarkers`·`toRegionClusterMarkers`·`aggregationUnitForZoom`·`gateFillCells` 포팅본이 **무수정**이고 parity 4종(`region-cluster-overlay`·`aggregation-unit`·`cluster-overlay`·`map-scale`)이 그대로 통과한다 — 루트 `pnpm test`(웹 스코프만 돌리지 않는다, MSG-476 교훈) | 로직 | vitest(루트) + `git diff --stat apps/mobile/src/features/map-home/model` = 신설 2파일만 |
| L5 | `formatClusterCount` 무변경 — 말풍선 개수 줄이 `1234 → "1,234"`(상한 캡 없음) 그대로다 | 로직 | 기존 `cluster-format.test.ts` |

### B. 화면 (Android dev client 실기 — 에뮬레이터 `FillMap_Pixel8`, page-verification 3-B)

줌 값을 기기에서 직접 읽을 수 없으므로 **마커 지명 접미로 축척 단을 판정**한다: 격자선·점령 채움이 보이면 zoom ≥ 16, `…동` 이름 말풍선이면 동 단(zoom 14~15, 축척 250m~500m), `…구` 이름 말풍선이면 구 단(zoom 10~13, 축척 1km~8km). 스크린샷은 `adb exec-out screencap -p > _workspace/MSG-558/screenshots/{이름}.png`.

| # | 기준 | 유형 | 검증 방법 |
|---|---|---|---|
| S1 | 로그인 · 칩 없음 · 서면 중심에서 핀치 아웃해 격자가 사라지는 첫 축척(동 단)부터 집계 마커가 **원이 아니라 라운드 사각 말풍선**으로 보인다: 파란 채움(#0071E3), 흰 링 없음, 위 지역명(작고 85% 흰색) / 아래 개수(크고 굵은 흰색). 스크린샷 `dong-bubble.png` | 화면 | 실기 + 웹 markup 표 대조 |
| S2 | 더 핀치 아웃해 구 단이 되면 지명이 `…구`로 바뀌고(예: 부산진구·동구) 말풍선 **높이는 동 단과 같다**(크기 3단 없음 — 폭만 지명 길이에 따라 다름). 스크린샷 `sigungu-bubble.png` | 화면 | 실기 |
| S3 | 긴 지명(5자 이상, 예: `…제1동`류)과 4자리 이상 개수(`1,234`)가 있으면 말풍선 안에서 **잘리거나 줄바꿈되지 않는다**(폭 추정 과소 없음). 실데이터에 없으면 스텁 응답으로 1건 확인 | 화면 | 실기(스텁 허용) |
| S4 | 말풍선 주변에 그림자가 보인다 — Android에서 안 보이면 **실패가 아니라 R3 기록**(elevation이 마커 비트맵에 안 실리는 플랫폼 한계) | 화면 | 실기 |
| S5 | 동 말풍선 탭 → zoom 16(격자) 이동, 구 말풍선 탭 → 동 단 이동 — MSG-428 S4 그대로(MSG-445 실기 통과분 회귀 없음) | 화면 | 실기 |
| S6 | 서로 가까운 지역은 **하나의 말풍선**(앵커 지역명 + 개수 합산)으로 병합돼 글자가 겹쳐 뭉개지는 마커가 없다 — 웹과 같은 `mergeOverlappingMarkers` 결과. 구 단(1km~8km)에서 부산 전역 스크린샷 `merge.png` | 화면 | 실기 |
| S7 | 격자 줌(zoom ≥ 16)으로 돌아오면 말풍선이 전부 사라지고 격자선·점령 채움이 복귀한다(두 층 동시 노출 순간 없음 — MSG-428 S1 회귀 없음) | 화면 | 실기 |
| S8 | 테마 칩(핫구역·축제·팝업·경로) 활성 중에는 말풍선이 뜨지 않고, 해제하면 다시 뜬다(MSG-428 S9 유지 — 테마 색 말풍선은 이 티켓 범위 아님) | 화면 | 실기 |
| S9 | 같은 지역의 개수가 재조회로 바뀌면(예: 격자 점령 후 줌아웃) 말풍선 숫자가 **갱신**된다 — 자식 root `key` 계약(R4) 실증. 재현이 어려우면 스텁으로 count만 바꿔 확인 | 화면 | 실기(스텁 허용) |
| S10 | 동 단 부산 전역 뷰포트(마커 수십 개)에서 핀치·드래그가 눈에 띄게 끊기지 않는다 — 끊기면 R5 대안 판단 | 화면 | 실기(관찰) |

### C. 구조 감사

| # | 기준 | 유형 | 검증 방법 |
|---|---|---|---|
| G1 | `apps/web/**`·`packages/ui-web/**`·`packages/design-tokens/**` diff **0줄** | 로직 | `git diff --stat` |
| G2 | `packages/ui-native/src/index.ts` diff 0줄(MSG-420 합의) | 로직 | `git diff --stat` |
| G3 | `grid-map.tsx` diff ≤ **4줄**, `map-home-screen.tsx`·`use-home-overlays.ts`·`api/**` diff 0줄(병렬 556·557 충돌면 최소화) | 로직 | `git diff --stat` |
| G4 | `cluster-marker.tsx`에 hex 리터럴 0건 — 색은 `semantic.*`, 타이포 크기는 `typography.*`, 가로 패딩은 `spacing.sm`; 리터럴은 `borderRadius 10`·`paddingTop 6`·`paddingBottom 7`·`SHADOW_BLEED`·글자 폭 계수뿐이며 각각 주석으로 웹 원본 클래스를 가리킨다 | 로직 | 코드 감사 |
| G5 | `.oxlintrc.json` Hermes 규칙 통과(`toSorted` 등 0건) · `pnpm --filter mobile test` · `typecheck` · `lint` · 루트 `pnpm test` 전부 exit 0 | 로직 | 자동 게이트 |

**검증 프로파일**: **화면** — 마커 형태·색·타이포가 시각 결과이고 웹 markup이 대조 정본. 3-B 실기 필수(프로파일로 건너뛸 수 없음). Figma 대조 단계는 사용자 지시로 **웹 코드 대조**로 대체한다.

---

## 구현 계획

- **브랜치**: `feat/MSG-558-cluster-bubble-marker` — `git branch --list "*MSG-558*"`·`git ls-remote --heads origin "*MSG-558*"` 모두 없음(신규). target `develop`. 커밋 prefix `MSG-558 feat:`
- **재사용**: `formatClusterCount`·`RegionClusterMarker`·`MARKER_MERGE_PX`(병합만, 렌더 소비 중단)·`drillInZoomForUnit`·`RAISED_SHADOW`(cluster-marker 내 기존 상수)·`semantic`/`typography`/`spacing` 토큰·`NaverMapMarkerOverlay` ⑤ 커스텀 뷰 관례(`collapsable={false}`)
- **신규 로직**(test-first): `apps/mobile/src/features/map-home/model/cluster-bubble-size.ts` + `.test.ts` — 순수 함수 `clusterBubbleSize(marker: Pick<RegionClusterMarker,"name"|"count">): { width; height }`. 모바일 고유(웹은 CSS hug라 parity 앵커 없음 — `cluster-format` 선례와 같은 "모바일 고유 model"). 폭 = `2×spacing.sm + max(estimateTextWidth(name, label.fontSize), estimateTextWidth(formatClusterCount(count), heading.fontSize))`, 글자 폭 계수는 한글/기타 1.0 · ASCII 0.6(`ponytail:` 주석 — 실측 폰트 메트릭 아님, 과대 방향 상수. 잘림이 보고되면 계수만 올린다). 여기에 `SHADOW_BLEED`(추정: 12px, 그림자 radius 10 + offset 2)를 4면에 더한 값이 마커 박스. 지도 SDK·RN import 없음 → Node에서 테스트
- **수정**:
  - `ui/cluster-marker.tsx` — 원형 → 말풍선. 바깥 `View`(박스 크기 = `clusterBubbleSize`, 투명, `alignItems/justifyContent: center`, `collapsable={false}`, **`key={`${name}/${count}`}`** 라이브러리 계약) 안에 말풍선 `View`(`alignSelf: center`, 위 매핑 표 style) + `Text` 2개. `CLUSTER_MARKER_PX`·`RING_WIDTH` 삭제, `export { clusterBubbleSize }` 재수출 없이 grid-map이 model에서 직접 import. 파일 상단 SOURCE 주석을 "웹 `MapCanvas.tsx clusterMarkerContent`(MSG-475)"로 교체
  - `ui/grid-map.tsx` — `:33` `import { CLUSTER_MARKER_PX, ClusterMarker }` → `ClusterMarker`만 + `clusterBubbleSize` import, `:452~453` `width/height={CLUSTER_MARKER_PX[cluster.unit]}` → `const size = clusterBubbleSize(cluster)` 값. 나머지(anchor·onTap·key) 무변경
- **라우트**: 변경 없음
- **승격 후보**: **없음** — 웹 마커는 HTML 문자열, 앱은 RN 마커 서브뷰라 공유 단위가 성립하지 않는다(MSG-475 스펙 판단 승계). `ClusterMarker`는 화면 로컬 유지(두 번째 사용처 없음)
- **문서**: `docs/STATUS.md` apps/mobile 섹션 `MSG-558:` 한 줄 · `docs/spec/MSG-558.md`(승인 스펙 + 작업 로그) · `MARKER_MERGE_PX` 주석의 "cluster-marker.tsx가 이 표를 마커 지름으로 그대로 읽는다" 문장은 **값 불변 조건으로 주석만 갱신**(웹 MSG-475가 웹 쪽 주석을 같은 방식으로 갱신)
- **검증 순서**: `pnpm --filter mobile test` → 루트 `pnpm test` → `typecheck` → `lint` → `format:check` → 3-B 실기(S1~S10, 스크린샷 4장 이상) → push 전 codex 리뷰 1회

---

## 추정 및 질문 (사용자 승인 대상)

| # | 항목 | 제안 | 성격 |
|---|---|---|---|
| Q1 | **마커 박스 폭을 글자 수로 추정**(L1·L2) | RN 마커는 `width`/`height`가 필수인데 텍스트 실측은 비동기(`onLayout`)라 첫 프레임에 못 쓴다. 순수 추정 + 과대 방향 상수로 간다. 대안: `onLayout` 측정 후 state 갱신(마커당 2회 렌더, 깜빡임) — 기각 권고 | **추정** |
| Q2 | **`MARKER_MERGE_PX`(68/80/92) 불변** | parity 테스트가 리터럴로 고정하고 웹 수정 금지라 재보정 불가. 말풍선(≈63~80×48)과 원 지름 근거가 어긋나 **긴 지명 동 말풍선 둘이 68~80px 거리에서 가로로 살짝 겹칠 수 있다**(웹도 같은 상태로 승인). 웹과 동일하게 수용. 대안: 네이티브 `isHideCollidedMarkers` 추가(겹치면 작은 쪽을 **숨김** — 개수가 화면에서 사라져 총합이 안 맞고 웹과 달라짐) — 기각 권고 | **추정** |
| Q3 | 채움 `semantic.secondary`(#0071E3) | 웹 MSG-475 승인 결정 ② 그대로(현행 `primary`에서 이동). 앱만 `primary` 유지할 근거 없음 | 추정 |
| Q4 | 타이포 = `label`(500/12/15) + `heading`(16/20) weight 700, 지역명 `opacity .85` | 웹 클래스의 토큰 대응 그대로. 웹은 `text-fm-heading`(500)에 `font-bold`를 얹어 700 — 앱도 `fontWeight: "700"` | 추정 |
| Q5 | `SHADOW_BLEED` 12px | 그림자 radius 10 + offset 2를 박스 여백으로. Android는 elevation이라 번짐이 다르지만 투명 여백이라 과대 무해 | 추정 |
| Q6 | 자식 root `key` 추가(S9) | 라이브러리 JSDoc 계약("생김새 의존성을 최상위 자식 key로"). 현행 원형 마커에는 없어 **같은 지역 재조회 시 개수 미갱신 가능성**이 있다(MSG-445 실기는 드릴인만 확인). 이 티켓에서 함께 고친다 | 추정(결함 보수 겸) |
| Q7 | 테마 색 말풍선(축제/팝업/핫) | **범위 밖** — 앱은 칩 활성 중 마커 억제 + 미션/핫 집계 훅 없음. 파랑 단일 | 확인 |
| Q8 | 핫구역 행정 단위 집계 | **별도 티켓** — 위 조사 결과(훅·파생·게이트 전부 부재) | 확인 |

---

## 리스크

- **R1 (병렬 충돌)**: MSG-556·557이 `grid-map.tsx` props/마커 구간을 함께 고칠 가능성. 이 티켓의 `grid-map.tsx` 접촉은 `:33` import 1줄 + `:452~453` 2줄(G3). 나중에 리베이스하는 쪽이 손 병합 — import 블록은 줄 추가, 클러스터 블록은 이 티켓 판을 취하면 된다. `map-home-screen.tsx`는 건드리지 않는다.
- **R2 (폭 추정 오차)**: 시스템 폰트(앱은 Inter 미로드 — `useFonts` 0건, Android Roboto/Noto CJK)의 실제 글리프 폭이 계수와 다르면 잘림(과소) 또는 여백(과대). 과대 방향 상수로 잘림만 막고(S3), 실기에서 잘림이 보이면 계수 상향.
- **R3 (Android 그림자)**: 커스텀 뷰 마커는 Android에서 뷰를 비트맵으로 캡처해 그리므로 `elevation` 그림자가 마커에 **안 실릴 수 있다**(MSG-428 원형도 같은 상수를 썼으나 실기 S3 확인불가로 남아 있음). S4는 이를 실패가 아닌 기록으로 처리. 대안(필요 시 후속): 그림자 포함 PNG 배경(② 이미지 방식) 또는 `react-native-svg` 필터 — 새 의존은 이 티켓에서 안 든다.
- **R4 (라이브러리 계약 미준수 현행)**: 자식 root `key` 부재(Q6). 이 티켓이 보수하되, S9로 실증한다.
- **R5 (성능)**: 커스텀 뷰 마커 "많이 생성 시 성능 영향" 경고. 마커 수 상한 근거 = 뷰포트 응답 + 1°/4° 클램프 + 병합. S10에서 끊김이 보이면 ② 이미지+캡션 대안(굵기·hug 손실)으로 내리는 판단을 사용자에게 올린다 — 이 티켓 안에서 자동 전환하지 않는다.
- **R6 (실기 환경)**: 에뮬레이터 `FillMap_Pixel8`을 556·557 검증과 공유한다. 병렬 워크트리 3곳이 패키지명 `kr.fillmap.app`을 공유해 dev client가 남의 번들을 무는 함정(MSG-427 §9.8) — 검증 시 Metro 포트·번들 출처를 교차 확인하고 필요하면 순번 대기.
- **R7 (문서-코드)**: `MARKER_MERGE_PX` 주석("cluster-marker.tsx가 이 표를 지름으로 읽는다")이 이 티켓 후 거짓이 된다 — 값은 두고 주석만 고친다(parity는 값만 본다).
- **Figma 오탐 방지**(page-verifier 대조 기준 — 정본은 웹 markup 표):
  - Figma 노드(15221:20097 등)는 사용자 지시로 **대조 대상이 아니다**. 앱 Figma의 원형 마커 `14599:7041`(68/80/92)은 **구 디자인**이라 대조하면 안 된다
  - 폰트 패밀리: 웹 Inter/system-ui ≠ 앱 시스템 폰트 — 전역 정책, 결함 아님
  - 말풍선 폭이 웹 실측(63/66)보다 몇 px 넓을 수 있다(과대 추정 상수) — 잘림만 결함
  - Android 그림자 부재는 R3 기록 대상, 결함 아님
  - 테마 색 말풍선·핫구역 집계 말풍선은 앱에 없는 것이 정상(Q7·Q8)
  - 시(SIDO) 말풍선은 `MAP_MIN_ZOOM` 때문에 화면에서 볼 수 없다 — 미노출이 결함 아님


## 작업 로그 (2026-09-02)

**실측 동작 (빌드 리포트 요약)** — 변경 5파일, 전부 `apps/mobile/src/features/map-home/`. 신설 `model/cluster-bubble-size.ts`(순수 크기 추정: `bubble` = 2×`spacing.sm` + max(지명 폭, 개수 폭) × 높이 6+15+20+7, `box` = bubble + 4면 `SHADOW_BLEED` 12) + `model/cluster-bubble-size.test.ts`(6케이스, test-first). `ui/cluster-marker.tsx` 전면 교체 — 원형 3단(`CLUSTER_MARKER_PX` 68/80/92 + 흰 링 3px) → 말풍선 단일(radius 10 · 패딩 6/12/7 · `semantic.secondary` · 링 없음 · 지명 길이 hug). `ui/grid-map.tsx` +4−3(import 분리 + `width`/`height`). `model/region-cluster-overlay.ts` +2−2(JSDoc만, 값 무변경 — `MARKER_MERGE_PX` 68/80/92는 렌더와 분리돼 불변). 테스트 매핑: L1~L3 ↔ 신규 6케이스 1:1, L4·L5는 기존 parity·format 테스트가 보존을 단정.

**검토한 대안 (기각 이유)** — ① **`onLayout` 텍스트 실측**: RN 마커는 첫 프레임에 width/height가 있어야 해서 실측값이 도착하기 전에 박스가 확정된다. 기각. ② **`packages/ui-native` 승격**: 지도 마커라는 도메인 형태가 강하고 사용처가 지도 홈 하나뿐 — 승격은 두 번째 사용처가 생길 때. ③ **`className`(nativewind) 스타일링**: 네이티브 마커 서브뷰라 토큰 값을 `style`로 직접 주입하는 쪽이 확실하다(MSG-298 경로 번호 마커 선례). ④ **`toLocaleString("ko-KR")` 그대로 쓰기**: Hermes의 Intl 가용성이 빌드 설정에 좌우돼 런타임 서식이 조용히 갈릴 수 있다 — MSG-428의 정규식 구현을 그대로 유지(L5).

**커밋 전 웹 실측이 잡은 결함 1건 (계수 0.6 → 0.65)** — 검증 통과 후, 프로덕션 `fillmap.kr`에 웹 클래스 문자열 그대로 요소를 주입해 computed style을 다시 쟀다(지도 idle이 필요 없는 경로 — 자동화 탭이 hidden이어도 유효). 디자인 값은 전 항목 일치했으나(radius 10 · padding 6/12/7/12 · `rgb(0,113,227)` · label 12/15/500@85% · heading 16/20/700 · shadow `0 2px 10px rgba(25,31,40,.12)`), **폭 추정에서 과소 케이스**가 나왔다: Inter 숫자 advance가 0.6375em이라 ASCII 계수 0.6은 모자라고, **이름 없는 버킷(병합·미판정) + 콤마 없는 개수**에서만 드러난다 — "58"이 실측 44.4 vs 추정 44. 이름이 있으면 한글 폭이 max를 먹어 가려지는 창이다. 0.65로 상향하고 해당 케이스 2건을 "과소 추정 금지" 테스트에 추가했다(전 케이스 여유 양수 — 최소가 그 "58" 케이스의 +0.6이고 나머지는 +5.5~+11.3). 방향은 박스가 커지는 쪽이라 실기 재촬영 불필요 — 게이트만 재실행(mobile 988 통과).

**WAIVED · 수용된 편차**
- **S4 Android 그림자(R3 수용, 실패 아님)**: `RNCNaverMapMarker.updateCustomView()`가 서브뷰를 `draw(canvas)`로 소프트웨어 비트맵(`ARGB_4444`)에 그려 `OverlayImage.fromBitmap`으로 올린다 — `elevation` 그림자는 RenderNode 단계라 이 경로에 실리지 않는다. 실측도 하단 1dp 옅은 띠(RGB 218→243)뿐. 값 조정으로 풀리는 종류가 아니라 렌더 경로를 바꿔야 한다(후속 후보: `react-native-svg` 필터, 배경 그라디언트 레이어).
- **폰트 패밀리 편차**: 웹 Inter vs 앱 시스템(Roboto/Noto CJK) — 플랫폼 기본값이며 디자인 대조 오탐 방지 목록에 기록.
- **테마 채움 미대응**: 웹은 칩 선택 시 `THEME_FILL_CLASS`로 채움이 갈리지만 앱 `RegionClusterMarker`에는 `theme` 필드가 없다(격자 점령 집계 전용). 디자인 불일치가 아니라 데이터 모델 범위 차이 — 앱에 칩 집계가 생기면 그때 따라간다.

**확인불가** — 없음. `pnpm check:duplication`은 서브에이전트 셸 PATH에 `/opt/homebrew/bin`이 없어 검증자가 "미설치"로 오판했으나(nose 0.20.0 설치돼 있음), 사용자가 직접 실행해 **exit 0 · 신규 중복 0 families**로 통과 확정.

**검증 (03_verify_report 요약)** — 실패 0 · 확인불가 0 · 참고 4 → 커밋 가능. 3-B Android 실기(에뮬레이터 `FillMap_Pixel8`)로 S1~S10 전건. dev 계정에 점령 격자가 없어 실서버 집계가 `items: []`라 **스펙이 허용한 로컬 스텁 프록시**(8090 + `EXPO_PUBLIC_API_BASE_URL` 프로세스 오버라이드, 소스·`.env` 무변경)로 확인 후 원상 복구. 말풍선 64×48dp(웹 63×48), 채움 픽셀 `(0,113,227)` 정확 일치, 병합 "중구 12 + 서구 19 → 서구 31" 총합 보존, 스텁 160개 응답에서 `dumpsys gfxinfo` 열화 없음.

**참고 (판정 무관, 후속 재료)**
1. **S9 key 계약의 실효 범위**: Android는 `ViewChangesTracker`가 40ms마다 `updateCustomView()`를 돌려 key 유무와 무관하게 갱신된다 — 이번 실증은 "Android 스테일 없음"까지이고, key가 실제로 필요한 iOS(UIView→UIImage 1회 캡처)는 미검증.
2. `mergeOverlappingMarkers`가 뷰포트 밖 항목까지 병합·마운트한다(160개 응답 전부). S10 실측에선 열화가 없었으나 저사양 실기에서 완화가 필요하면 뷰포트 필터가 가장 싸다 — 웹 parity 함수라 이 티켓 범위 밖.
3. 마커 `accessibilityRole` 부재는 MSG-428부터 동일. Android 커스텀 뷰 마커는 비트맵이라 TalkBack 노드가 아니어서 접근명이 실제 낭독 경로도 아니다 — 기록만.
4. 검증 중 사고 2건: `openapi-ts` 재생성 창(생성물 삭제→재생성)에 Metro 번들 요청이 걸려 일시 resolve 실패(재생성 diff 0), 워크트리 루트에 `_workspace` 심링크를 두자 Metro 파일 맵 크래시(`TreeFS`) — 둘 다 3-B 중 금지 사항으로 확정.

## 리뷰 반영 (2026-09-02, PR #122)

**지적**: `grid-map.tsx`에서 `clusterBubbleSize(cluster)`를 `width`/`height`에 각각 불러 마커당 2회(+ `ClusterMarker` 내부 1회 = 총 3회) 계산한다. `GridMap`은 `onCameraChanged`가 `setCells(buildVisibleCells(...))`로 **매번 새 배열**을 넣어 카메라 프레임마다 리렌더되는 구조이고(같은 파일이 `occupiedPolygons`·`themePolygons`·`hatchLines`를 `useMemo`로 감싼 이유), 클러스터 블록만 그 관례에서 빠져 있었다.

**확인**: 전제 전부 사실. 특히 `showCellGrid`는 기본값 true이고 `false`를 넘기는 곳은 `grid-detail-screen.tsx:136` 하나뿐이라, 말풍선이 뜨는 지도 홈 저줌 화면이 정확히 리렌더가 도는 쪽이다(옆의 `setBelowGridZoom`은 불리언이라 `Object.is`로 걸러지지만 `setCells`는 아니다).

**반영**: `clusters?.map` 콜백을 블록 바디로 바꿔 `const { width, height } = clusterBubbleSize(cluster).box;` 한 번만 계산해 쓴다. `ClusterMarker` 내부 호출은 남긴다 — 없애려면 `box`를 prop으로 받아야 하고, 그건 "마커가 자기 크기를 스스로 안다"는 컴포넌트 계약을 바꾸는 일이라 리뷰어도 범위 밖으로 봤다.

**G3 이탈(기록)**: 이 반영으로 `grid-map.tsx` diff가 +4−3 → **+20−14**가 되어 수용 기준 G3의 "≤ 4줄"을 넘겼다. 블록 바디 전환이 JSX 들여쓰기를 밀어서 생긴 증가이고 논리 변경은 한 줄이다. G3의 목적인 **병렬 레인 충돌면**은 재측정으로 유지 확인 — MSG-556의 마지막 헝크가 develop 좌표 420~430, 558의 클러스터 헝크가 444~462로 **14줄** 떨어져 있다(git 컨텍스트 3줄). 숫자는 깨졌고 목적은 지켜졌다는 판단으로 채택했다.

## 확장 스코프 (2026-09-02 사용자 지시)

**왜 다시 서는가.** 위 스펙은 Q7(테마 색 말풍선)을 범위 밖, Q8(핫구역 집계)을 별도 티켓으로 판정하고 S8을 "칩 활성 중 말풍선 없음"으로 세워 그대로 검증·커밋(c6419e83, 3f0967e5 / PR #122)까지 마쳤다. 사용자 확인 결과 **핫구역·지역축제·팝업스토어 칩에서도 클러스터링을 하기로 되어 있었고**, "2번(558에 포함)으로 진행" 지시가 내려왔다. 이 섹션은 위 승인 내용을 지우지 않고 **증분**으로 얹는다 — 뒤집히는 항목은 아래 "무효화 목록"에서 명시적으로 폐기·대체한다. 정본은 종전과 같이 **웹 코드**이며, 결정은 되묻지 않고 웹 프론트·백엔드 계약을 읽어 확정했다(근거를 항목마다 남김). `apps/web`·`packages/**` 수정 금지는 그대로다.

### 0. 무효화 목록 (위 본문 중 이 섹션이 뒤집는 것)

| 대상 | 종전 | 이후 |
|---|---|---|
| **S8** | 테마 칩 활성 중 말풍선이 뜨지 않는다 | **폐기 → S8'** (§3): 핫·축제·팝업 칩 활성 중에는 **그 칩의 집계 말풍선**이 뜨고, 경로추천 칩만 집계 없음 |
| **Q7** | 테마 색 말풍선 범위 밖 | **범위 안** — 채움색을 `THEME_META[theme].color`로 갈고 접근명 주어를 칩 라벨로 바꾼다 |
| **Q8** | 핫구역 행정 단위 집계 별도 티켓 | **범위 안** — 핫·미션 집계 훅 2종 + 마커 파생 + 칩 택일을 이 브랜치에 신설 |
| **L4** | `region-cluster-overlay.ts` 포팅본 **무수정** | **폐기 → L4'** (§2): `theme` 필드·`toMissionClusterMarkers`·`toHotZoneClusterMarkers`·병합 시 테마 승계를 웹과 같게 **재포팅**하고 parity 테스트를 확장. `MARKER_MERGE_PX` 값·`toRegionClusterMarkers` 출력은 불변 |
| **G3** | `map-home-screen.tsx`·`use-home-overlays.ts`·`api/**` diff 0 | **폐기 → G3'** (§4): 예산 재설정 — `grid-map.tsx` **추가 접촉 0**, `map-home-screen.tsx` ≤ 1줄, `use-home-overlays.ts` ≤ 25줄, `api/**`는 이 확장의 주 작업 영역 |
| 웹 클래스 매핑 표 `THEME_FILL_CLASS` 행 | "해당 없음 — 파랑 단일" | `backgroundColor: marker.theme ? THEME_META[marker.theme].color : semantic.secondary` |
| 리스크 "Figma 오탐 방지" 마지막 두 항목 중 "테마 색·핫구역 집계 말풍선은 앱에 없는 것이 정상" | — | **삭제**. 이제 있는 것이 정상 |
| 검증 리포트 S8 통과 근거(`chip-active.png` 말풍선 0) | 통과 | 이 확장 이후에는 **정반대가 통과**다 — 재검증 시 종전 스크린샷을 근거로 쓰지 않는다 |

### 1. 웹 계약 실측 (원본 직접 확인, 2026-09-02)

**집계 소스 택일 — `apps/web/src/widgets/map-shell/MapShell.tsx:100~131`** (오케스트레이터 요약과 일치 확인):
```ts
if (activeTheme === "hot")      return mergeOverlappingMarkers(hotClusters, viewportZoom);
if (activeTheme === "festival" || activeTheme === "popup")
                                 return mergeOverlappingMarkers(missionClusters, viewportZoom);
if (activeTheme !== null)        return [];                       // 경로추천 칩 — 집계 층 없음
return aggregationUnit !== null ? mergeOverlappingMarkers(toRegionClusterMarkers(items, unit), zoom) : [];
```
이어 `gateRouteClusters(clusters, pathname)`(`cluster-route-gate.ts`)가 `pathname === ROUTES.aiRoute`이면 `[]`. **웹에 이 게이트가 필요한 이유는 MapShell이 전 섹션이 공유하는 지속 지도라서**다 — 홈에서 파생한 마커가 AI 경로 섹션까지 따라가기 때문.

**훅 게이트 — `use-mission-aggregation-query.ts` / `use-hotzone-aggregation-query.ts`** (+ 각 `.test.tsx` 실측):
- 미션: `enabled = theme !== null && unit !== null && bounds !== null` — theme는 `"festival" | "popup"`만(호출부가 그 외를 null로 넘김). `type = missionTypeParam(theme)` → `EVENT` / `POPUP`. **인증 게이트 없음**(MSG-454 익명 허용 — 테스트 "비로그인에서도 조회한다").
- 핫: `enabled = active && unit !== null && bounds !== null`, `active = activeTheme === "hot"`. **인증 게이트 없음**(AC 8).
- 둘 다 `AGGREGATION_SPAN_CAP_DEG[unit]`로 bbox 클램프, `mapQueryPolicy` 펼친 뒤 `placeholderData`만 덮음 — 미션은 **같은 unit && 같은 type**일 때만 직전 데이터 유지, 핫은 **같은 unit**일 때만. `enabled=false`·응답 전·unit null이면 **항상 같은 `EMPTY_MARKERS` 참조**.
- 응답 봉투 `data`는 **배열 직접**(`ApiResponseDtoListMissionRegionAggregateResponseDto.data: Array<…>`, 핫 동일) — 점령 집계의 `{ currentRegion, items }`와 다르다. `unwrapEnvelope(data)`가 곧 items.
- 미션 DTO에 `missionIds`, 핫 DTO에 `gridIds`가 더 있으나 마커 파생은 싣지 않는다(웹 MSG-475 결정 ③).

**마커 파생 — `apps/web/src/features/map-home/model/region-cluster-overlay.ts:30~130, 220~238`**:
- `RegionClusterMarker.theme?: ClusterMarkerTheme`(`Extract<ThemeId, "hot"|"festival"|"popup">`), `MissionMarkerTheme = Extract<ThemeId, "festival"|"popup">`.
- 내부 `toClusterMarkers(items, unit, keyPrefix, theme?)` — id `${keyPrefix}-${unit}-${regionCode}` / `…-unassigned-${seq}`, SIDO 축약, `...(theme ? { theme } : {})`(**키 자체를 안 만든다** — `theme: undefined` 아님). `toRegionClusterMarkers` = prefix `agg`, theme 없음 / `toMissionClusterMarkers(items, unit, theme)` = prefix `mission-${theme}` / `toHotZoneClusterMarkers(items, unit)` = prefix `hot`, theme `"hot"`.
- `mergeOverlappingMarkers` 병합 결과에 `...(anchor.theme ? { theme: anchor.theme } : {})` — "빠뜨리면 합쳐진 미션 마커만 도감 색으로 렌더된다".

**렌더 — `MapCanvas.tsx:114~127, 358~380`**: 말풍선 markup은 위 본문 표와 **완전히 동일**하고 `${theme ? THEME_FILL_CLASS[theme] : "bg-secondary"}`로 **채움만** 갈린다. `THEME_FILL_CLASS`(`theme-classes.ts`) = `bg-theme-hot|festival|popup` → 토큰 `palette["theme-hot"]` `#FF3B30` / `theme-festival` `#AF52DE` / `theme-popup` `#FF9500`(`packages/design-tokens/src/colors.ts:34~36`). 접근명 `clusterMarkerTitle`: `subject = theme ? THEME_META[theme].label : "점령 격자"` → `"{name} {subject} {count}개"` / `"{subject} {count}개"`. 글자색·타이포·패딩·radius·그림자는 테마와 무관하게 동일.

**저줌 격자 낱개 게이트 — `occupancy-visibility.ts:36~53` + `use-home-overlays.ts:78~93`**: 웹은 칩 활성 저줌에서 **격자 낱개를 걷고 집계 마커가 그 자리를 대신한다**. `missionCellsVisible({ zoom, hasDetailTarget }) = hasDetailTarget || zoom >= GRID_MIN_ZOOM`(축제·팝업 — 상세를 연 미션은 예외), `hotCellsVisible({ zoom }) = zoom >= GRID_MIN_ZOOM`(핫 — 예외 없음). 경로추천 칩(코스)은 게이트 대상이 아니다("코스는 라인 표시라 대상이 아니다 — 1km 화면 묶음 유지"). MapShell 주석 원문: "격자 쪽은 use-home-overlays의 hotCellsVisible이 걷는다". **이 게이트 없이 마커만 얹으면 zoom 14~15에서 100m 테마 셀과 말풍선이 공존한다** — 웹이 MSG-451/475에서 명시적으로 벗어난 상태라 이식 범위에 넣는다(확정 근거 §5 C4).

### 2. 앱 현행 재확인 (소스 실측)

| 항목 | 실측 | 이 확장에서 |
|---|---|---|
| `api/grid-aggregation-query.ts:79~92` 게이트 | `isAuthenticated && hydrated && unit && !themeActive && bounds` | **유지** — 칩 활성 중 점령 집계는 웹과 같이 배타(웹 `visibleOccupiedCells` + 훅 게이트). 이 파일 무수정 |
| `api/use-grid-aggregation-query.ts` | 얇은 훅 — `useAuth`·`useSelectedTheme`·`viewport` 읽어 팩토리에 넘김. `{ clusters, isError, retry }` | **택일 배선 자리**(§4) — 이미 테마를 읽고 있고 화면이 `aggregation.clusters`를 그대로 `clusters` prop에 꽂는다. 556·557 무접촉 파일 |
| `model/region-cluster-overlay.ts` | `theme` 없음. `toRegionClusterMarkers`·`mergeOverlappingMarkers`·`MARKER_MERGE_PX`(주석만 갱신됨) | 웹 `:30~130, 220~238` 재포팅(§4) |
| `model/region-cluster-overlay.parity.test.ts` | `toRegionClusterMarkers`·`MARKER_MERGE_PX`·`mergeOverlappingMarkers` 웹 동적 import 대조 | 케이스 확장(L4') |
| `model/themes.ts` | `ThemeId = "hot"|"festival"|"popup"|"route"`, `THEME_META[id] = { id, label, color }` — 라벨 핫구역·지역축제·팝업스토어·경로추천, 색 `palette["theme-*"]`. 웹 `theme.ts`와 값 동일 | 채움·접근명 출처로 **재사용**(신규 표 없음) |
| `model/theme-selection.ts` | 모듈 싱글턴 + `useSelectedTheme(): ThemeId \| null` | 재사용 |
| `model/mission.ts:36` `missionTypeParam(chip: MissionChip)` | `festival→EVENT`, `popup→POPUP`, `route→COURSE` — 웹과 동일 | 재사용 |
| `shared/api/sdk.ts`·`query-options.ts` | 웹 생성물 재수출 — `getMissionAggregatesOptions`·`getHotZoneAggregatesOptions`·DTO 2종 **이미 import 가능** | 재사용(생성물 무수정) |
| `ui/cluster-marker.tsx` | `backgroundColor: semantic.secondary` 고정, 접근명 `"점령 격자"` 고정 | 테마 분기(§4) |
| `ui/grid-map.tsx` | `clusters?: RegionClusterMarker[]` 소비, 마커당 `clusterBubbleSize(cluster).box` + `<ClusterMarker marker>` | **무수정** — `theme`는 optional 필드라 타입·렌더 경로 그대로 통과 |
| `ui/use-home-overlays.ts` | `themeGridIds`: hot → `hotGridIds` 그대로 / 축제·팝업·경로 → 뷰포트 클리핑. **줌 게이트 없음**(`zoom` 입력 자체가 없다) | 웹 `missionCellsVisible`/`hotCellsVisible` 포팅 적용(§4). 556·557 무접촉 파일 |
| `model/cluster-overlay.ts` | 테마 층은 저줌에서도 남긴다(MSG-403 AC 7 대응) — **점령 채움 게이트만** 담당 | 무수정. 테마 낱개 게이트는 웹과 같이 **상류(use-home-overlays)** 에서 건다 — 두 파일의 역할 분담이 웹과 동일해진다 |
| `ui/map-home-screen.tsx` | `aggregation = useGridAggregationQuery(viewport)`(:136) → `clusters={aggregation.clusters}`(:344), `useHomeOverlays({ themeId, bounds, hotGridIds, missions, occupiedGrids })`(:153~159) | `useHomeOverlays` 인자에 `zoom` 1줄만(§4) |
| AI 경로추천 라우트 | 556 브랜치: `src/app/ai-route.tsx` 신설 + `features/ai-route/ui/ai-route-screen.tsx`가 **자체 `GridMap`** 마운트(`themeColor=theme-route`, `route=` 전달, **`clusters` prop 미전달** — grep 0건). 바텀 내비 2번째 탭이 친구 스텁 → AI 추천으로 교체 | **`gateRouteClusters` 대응 불필요**(§5 C3) |
| 557 `map-home-screen.tsx` 헝크(develop 좌표) | 13~18(import) · 49~55 · 230~257 · 322~327 · **336~343**(`themeCells`/`themeColor` 줄) · 352 · 382 | 이 확장의 접촉 :153~159(`useHomeOverlays` 인자 블록) — 최근접 557 헝크(230)와 **71줄** 거리, 인접 아님 |

### 3. 수용 기준 (증분)

#### 3-A. 로직 (vitest, `apps/mobile` 순수 모듈)

| # | 기준 | 검증 방법 |
|---|---|---|
| **L4'** (L4 대체) | `region-cluster-overlay.ts`가 웹 원본과 다시 전건 동등하다: ① `toRegionClusterMarkers` 출력 **불변**(theme 키 부재 — `toStrictEqual`로 고정) ② `toMissionClusterMarkers(items, unit, "festival"|"popup")` id 접두 `mission-${theme}`·`theme` 필드 ③ `toHotZoneClusterMarkers(items, unit)` 접두 `hot`·`theme: "hot"` ④ 병합 마커가 앵커 `theme`을 승계(테마 없는 입력은 키 부재) ⑤ `MARKER_MERGE_PX` 68/80/92 불변. 웹 원본 동적 import 대조 | `region-cluster-overlay.parity.test.ts` 확장 |
| **L6** | `clusterSourceForTheme(theme: ThemeId \| null)`이 `null→"occupied"`, `"hot"→"hot"`, `"festival"|"popup"→"mission"`, `"route"→"none"`을 돌려준다 — 웹 MapShell 택일 분기의 순수 표현 | `theme-aggregation-query.test.ts` |
| **L7** | `missionAggregationQueryArgs({ theme, bounds, zoom })`: `enabled = source==="mission" && unit!==null && bounds!==null`(**인증 축 없음**), `query.type`이 festival→`EVENT`·popup→`POPUP`, `unit`은 `aggregationUnitForZoom(zoom)`, bbox는 `AGGREGATION_SPAN_CAP_DEG[unit]` 클램프. `hotZoneAggregationQueryArgs`는 `source==="hot"` 축만 다르고 `type` 없음. 비활성 시 자리채움 query(0 좌표, `EVENT`/`DONG`) | 동일 파일 |
| **L8** | `placeholderData` 규칙: 미션은 직전 쿼리 `unit`·`type`이 **둘 다** 같을 때만, 핫은 `unit`이 같을 때만 직전 데이터를 돌려준다(웹 훅 `:60~70` 그대로) | 동일 파일 |
| **L9** | `selectThemeClusters({ theme, unit, zoom, missionData, hotData })`: source별로 `unwrapEnvelope(data)`(배열 직접) → `toMissionClusterMarkers`/`toHotZoneClusterMarkers` → `mergeOverlappingMarkers(·, zoom)`. `source`가 `occupied`·`none`이거나 unit null·data 미도착이면 **항상 같은 `EMPTY_CLUSTERS` 참조** | 동일 파일 |
| **L10** | 포팅 `occupancy-visibility.ts`: `missionCellsVisible({ zoom, hasDetailTarget }) = hasDetailTarget \|\| zoom >= GRID_MIN_ZOOM`, `hotCellsVisible({ zoom }) = zoom >= GRID_MIN_ZOOM` — zoom 6~21 × hasDetailTarget 양쪽 전건 웹 동일. `visibleOccupiedCells`는 포팅하지 않는다(웹 전용 `StyledCellOverlay` 입력이고 앱은 점령 배타를 훅 게이트로 이미 구현) | `occupancy-visibility.parity.test.ts` 신설 |
| **L11** | `clusterMarkerLabel(marker)`(모바일 고유, `cluster-format.ts`): `theme` 있으면 주어가 `THEME_META[theme].label`, 없으면 `"점령 격자"`; `name` null이면 이름 생략 — 웹 `clusterMarkerTitle`과 같은 4경우 | `cluster-format.test.ts` 확장 |
| **L12** (회귀) | `grid-aggregation-query.ts` **무수정** — `!themeActive` 게이트가 그대로 통과한다(칩 활성 중 점령 집계 미발사) | 기존 `grid-aggregation-query.test.ts` |

#### 3-B. 화면 (Android dev client 실기, page-verification 3-B — **예산: 핵심 경로 1회 + 스크린샷 6장 이내 + 15분**)

축척 판정 규칙은 본문 §B 그대로. 실데이터에 축제·팝업 집계가 비면 종전 검증과 같이 **로컬 프록시 스텁**(`/api/missions/aggregation`·`/api/hotzones/aggregation`만 스텁, 나머지 실서버 전달)을 허용한다.

| # | 기준 | 스크린샷 |
|---|---|---|
| **S8'** (S8 대체) | 핫구역 칩 활성 + 동 단(zoom 14~15): 말풍선이 **뜨고** 채움이 `#FF3B30`(`theme-hot`)이다. 형태·타이포·2줄 구성은 S1과 동일하고 흰 글자·라운드 10·패딩 6/12/7 그대로. 100m 핫 격자 낱개는 **보이지 않는다**(L10 게이트) | `hot-bubble.png` |
| **S11** | 지역축제 칩: 말풍선 채움 `#AF52DE`(`theme-festival`), 요청 `type=EVENT`(프록시 로그). 미션 격자 낱개·이름표는 저줌에서 안 보인다(상세를 연 미션이 없을 때) | `festival-bubble.png` |
| **S12** | 팝업스토어 칩: 채움 `#FF9500`(`theme-popup`), 요청 `type=POPUP` | `popup-bubble.png` |
| **S13** | 같은 뷰포트에서 축제 → 팝업 칩 전환: 축제 말풍선이 전부 사라지고 팝업 말풍선만 남는다(**두 층 동시 노출 없음** — 택일). 칩 해제 → 파란 점령 말풍선 복귀(S1 회귀) | `chip-switch.png` |
| **S14** | 경로추천 칩 활성 + 저줌: 집계 말풍선 **0개**(웹 `activeTheme !== null → []`), 코스 격자·라인은 종전대로 남는다(L10 게이트 대상 아님) | `route-chip-none.png` |
| **S15** | 테마 말풍선 탭 → `drillInZoomForUnit` 이동(동→16 / 구→동), 근접 마커 병합 시 앵커 지명 + 합산 + **앵커 테마 색** 유지(L4' ④의 실증). 스크린샷은 병합 컷 1장으로 S6 회귀까지 겸한다 | `theme-merge.png` |
| **S16** | 접근명: 테마 말풍선 최상위 뷰 `accessibilityLabel`이 `"{name} 지역축제 N개"` 등 칩 라벨 주어다(코드 감사 — Android 커스텀 뷰 마커는 TalkBack 노드가 아니라 실기 낭독 검증 대상 아님, 종전 참고 3) | 코드 감사 |

**6장 배정**: `hot-bubble` · `festival-bubble` · `popup-bubble` · `chip-switch` · `route-chip-none` · `theme-merge`. S1~S7·S9·S10 회귀는 별도 촬영 없이 S13의 "칩 해제 복귀" 관찰과 자동 게이트로 갈음한다(§3-D).

#### 3-C. 구조 (증분)

| # | 기준 | 검증 방법 |
|---|---|---|
| **G1·G2** (유지) | `apps/web/**`·`packages/**` diff **0줄** | `git diff --numstat develop -- apps/web packages` |
| **G3'** (G3 대체) | `grid-map.tsx` diff = **이미 커밋된 558 헝크(:30, :444~462) 그대로, 추가 접촉 0** · `map-home-screen.tsx` ≤ **1줄 삽입**(`useHomeOverlays` 인자 `zoom`, develop :153~159 블록 안) · `use-home-overlays.ts` ≤ 25줄 · `cluster-overlay.ts`·`grid-aggregation-query.ts`·`visible-grid.ts` 0줄 | `git diff --numstat develop -- apps/mobile/src/features/map-home/ui` |
| **G6** | 신규 파생·게이트·택일은 전부 **순수 파일**(`api/theme-aggregation-query.ts`, `model/occupancy-visibility.ts`, `model/region-cluster-overlay.ts`, `model/cluster-format.ts`)에 있고 훅·화면에는 배선만 있다 — 훅 파일에 `if (theme === …)` 분기 0건 | 코드 감사 |
| **G7** | `cluster-marker.tsx` hex 리터럴 0건 유지 — 테마 채움은 `THEME_META[theme].color`(토큰 `palette["theme-*"]` 경유) | 코드 감사 |
| **G8** | 웹 포팅 순수 모듈 2종(`region-cluster-overlay` 확장분, `occupancy-visibility`)에 `*.parity.test.ts` 동반(MSG-428 관례). 모바일 고유 2종(`theme-aggregation-query`, `clusterMarkerLabel`)은 일반 테스트 | 파일 존재 + 루트 `pnpm test` |

#### 3-D. 회귀 보존 대상 (이미 통과한 기준 — 확장 후에도 유지)

`03_verify_report.md`에서 통과 판정된 기준 중 이 확장이 건드리지 않아야 하는 것:
- **L1·L2·L3·L5** — `cluster-bubble-size`·`cluster-format` 무변경(테마는 크기·서식에 영향 없음). 기존 테스트가 그대로 고정
- **S1~S7·S9·S10** — 칩 없음 경로의 말풍선(파랑 `#0071E3`)·높이 48·잘림 없음·그림자(R3 기록)·드릴인·병합·격자 복귀·개수 갱신·성능. 재촬영하지 않고 S13의 "칩 해제 → 파랑 복귀" 관찰 1회 + 자동 게이트로 갈음(실기 예산 15분)
- **G1·G2·G4·G5** — 그대로. G4의 허용 리터럴 목록에 추가되는 것은 없다(테마 색은 토큰)
- 종전 **S8은 회귀 대상이 아니다**(폐기)

**검증 프로파일**: **화면**(유지) — 칩별 채움색이 시각 결과. 3-B 실기 필수이되 위 예산 안에서 **핵심 경로 1회**: 로그인 → 서면 → 핀치 아웃(동 단) → 핫 칩 → 축제 칩 → 팝업 칩 → 경로 칩 → 칩 해제 → (구 단으로 더 아웃해 병합 1컷). 순서대로 6장.

### 4. 구현 계획 (증분)

- **브랜치**: `feat/MSG-558-cluster-bubble-marker` 그대로, PR #122에 커밋 추가. prefix `MSG-558 feat:`
- **재사용**: `THEME_META`(`model/themes.ts`) · `useSelectedTheme` · `missionTypeParam`(`model/mission.ts`) · `aggregationUnitForZoom`·`AGGREGATION_SPAN_CAP_DEG`·`clampBoundsToSpan` · `mapQueryPolicy` · `mergeOverlappingMarkers` · `unwrapEnvelope` · `getMissionAggregatesOptions`/`getHotZoneAggregatesOptions`(`shared/api/query-options`) · `gatedQueryStatus`(`shared/api`, 2인자) · `ClusterErrorNotice`(기존 배선) · `GRID_MIN_ZOOM`(`model/visible-grid`)
- **웹 포팅 + parity** (test-first):
  1. `model/region-cluster-overlay.ts` — 웹 `:30~37`(타입 2종), `:75~130`(내부 `toClusterMarkers(items, unit, keyPrefix, theme?)` + `toRegionClusterMarkers`를 그 위에 재정의 + `toMissionClusterMarkers` + `toHotZoneClusterMarkers`), `:235`(병합 theme 승계) 재포팅. `ClusterAggregateItem` 최소 형태 인터페이스로 DTO 3종 공통부 수용. **`...(theme ? { theme } : {})` 조건부 스프레드 그대로** — `toStrictEqual` parity가 `theme: undefined`를 잡는다. `[...markers].sort` Hermes 대체는 유지. parity 테스트에 L4' ①~⑤ 케이스 추가(웹 `toMissionClusterMarkers`·`toHotZoneClusterMarkers` 동적 import)
  2. `model/occupancy-visibility.ts`(신설) — 웹 `:36~53` 2함수만. `+ occupancy-visibility.parity.test.ts`
- **모바일 고유 순수 로직** (test-first):
  3. `api/theme-aggregation-query.ts`(신설, `grid-aggregation-query.ts`와 같은 "옵션 팩토리 + 얇은 훅" 구조 — 훅 렌더 테스트 인프라가 없어 웹 `.test.tsx`를 그대로는 못 옮기고, 게이트·인자·placeholder·select를 팩토리로 내려 같은 계약을 순수 테스트로 덮는다): `clusterSourceForTheme` · `missionAggregationQueryArgs`/`hotZoneAggregationQueryArgs` · `missionAggregationQueryOptions`/`hotZoneAggregationQueryOptions`(placeholderData 포함) · `selectThemeClusters`. `+ theme-aggregation-query.test.ts`(`vi.stubEnv` + 동적 import 관례). 웹 테스트 케이스(축제 EVENT·팝업 POPUP·1km→SIGUNGU·격자 줌 미발사·칩 없음 미발사·뷰포트 null·상한 클램프·unit 전환 시 직전 데이터 폐기·같은 unit bbox 이동 유지)를 팩토리 단정으로 1:1 옮긴다
  4. `model/cluster-format.ts` — `clusterMarkerLabel(marker)` 추가 + 테스트
- **배선 수정** (로직 없음):
  5. `api/use-grid-aggregation-query.ts` — `useQuery` 2개 추가(미션·핫, 각 팩토리 옵션). 반환 `clusters = themeActive ? selectThemeClusters(...) : toClusterMarkers(...)`(useMemo). `isError`/`retry`는 **활성 소스 하나**만 본다(`enabled && isError`, 종전 규칙 유지 — 칩 화면에서 점령 집계의 옛 실패가 올라오지 않는다). 훅 이름은 그대로 둔다 — 이름 변경은 화면 :18(import)·:136을 건드리는데 :18은 557 import 헝크(13~18)와 **인접**해 충돌 확률이 높다. JSDoc에 "MSG-558 확장부터 칩 택일 저줌 집계 훅"을 적고 이름은 후속 정리 후보로 남긴다(리스크 R10)
  6. `ui/cluster-marker.tsx` — `backgroundColor: marker.theme ? THEME_META[marker.theme].color : semantic.secondary`, `accessibilityLabel={clusterMarkerLabel(marker)}`. 나머지 스타일 무변경. 최상위 `key`는 `${name}/${count}` 유지 — 칩 전환은 id 접두(`agg-`/`mission-*`/`hot-`)가 달라 `grid-map`의 오버레이 key 자체가 바뀌므로 리마운트된다
  7. `ui/use-home-overlays.ts` — 입력에 `zoom: number | null` 추가. `themeGridIds` useMemo에서 `hot` → `hotCellsVisible({ zoom }) ? hotGridIds : EMPTY`, 축제·팝업 → `missionCellsVisible({ zoom, hasDetailTarget: (selectedCourse ?? selectedMission) !== null })`이 거짓이면 `EMPTY`, 경로 칩·zoom null은 게이트 없음(지도 준비 전은 bounds null이라 이미 빈 배열). `classification`·`gridIdIndex`·`missionLabel`은 `themeGridIds`를 타므로 추가 분기 없음
  8. `ui/map-home-screen.tsx` — `useHomeOverlays({ …, zoom: viewport?.zoom ?? null })` **1줄**(develop :153~159 블록)
  9. `ui/grid-map.tsx` — **무수정**
- **라우트**: 변경 없음. AI 경로추천 라우트 대응 없음(§5 C3)
- **승격 후보**: 없음(종전 판단 유지)
- **문서**: `docs/spec/MSG-558.md`에 이 섹션 동일 append + 작업 로그 · `docs/STATUS.md` apps/mobile `MSG-558:` 행에 "칩 택일 집계(핫·미션) 신설, 테마 채움" 보강 · `docs/decisions/DECISIONS.md` 한 줄("558 S8 역전 — 칩에서도 집계, 웹 MapShell 택일 이식") · PR #122 본문에 스코프 확장 단락
- **검증 순서**: `pnpm --filter mobile test` → 루트 `pnpm test` → `typecheck` → `lint` → `format:check` → `check:duplication` → 3-B 실기(§3-B 경로 1회, 6장, 15분) → push 전 codex 리뷰 1회

### 5. 확정 사항 (되묻지 않고 웹/백엔드 계약으로 결정 — 근거 병기)

| # | 결정 | 근거 |
|---|---|---|
| C1 | 미션·핫 집계 **인증 게이트 없음**(비로그인도 조회) | 웹 훅 JSDoc "인증 게이트 없음 — MSG-454 익명 허용" + 테스트 "비로그인에서도 조회한다". 백엔드 명세도 익명 허용. 점령 집계만 로그인 게이트(보호 API 401) |
| C2 | 채움색 = `THEME_META[theme].color`, 접근명 주어 = `THEME_META[theme].label`, 그 외 스타일 동일 | `MapCanvas.tsx:363` `${theme ? THEME_FILL_CLASS[theme] : "bg-secondary"}` — 채움 외 클래스 동일. `:369~380` `clusterMarkerTitle` |
| C3 | **`gateRouteClusters` 대응 안 함** | 웹은 지속 지도(MapShell)라 홈 파생 마커가 `/ai-route`까지 따라가 게이트가 필요했다. 앱은 556에서 `/ai-route`가 **자체 GridMap**을 마운트하고 `clusters`를 넘기지 않는다(`ai-route-screen.tsx` grep 0건). 홈 안의 경로추천 **칩**은 L6의 `"route"→"none"`으로 웹 `activeTheme !== null → []`과 동치. 556 미머지 상태에서도 결론이 같다 — 현행(develop)에는 AI 경로 라우트 자체가 없다 |
| C4 | 칩 활성 저줌에서 **테마 격자 낱개를 걷는다**(핫: 예외 없음 / 축제·팝업: 상세 대상 있으면 유지 / 경로: 게이트 없음) | 웹 `occupancy-visibility.ts` + `use-home-overlays.ts:78~93`, MapShell 주석 "격자 쪽은 hotCellsVisible이 걷는다". 이 게이트가 없으면 마커와 100m 셀이 공존 — 웹 MSG-451 AC 2·3, MSG-475 AC 11이 폐기한 상태 |
| C5 | 택일·게이트 배선 위치 = **기존 `useGridAggregationQuery` 훅 내부**, 화면 `clusters` 줄 무수정 | 화면 :344 `clusters={aggregation.clusters}`는 557 헝크(336~343)와 **1줄 차**로 인접 — 화면에서 택일하면 충돌면이 생긴다. 훅은 이미 테마를 구독하고 556·557 무접촉 |
| C6 | 테마 집계 실패도 기존 `ClusterErrorNotice`로 노출(활성 소스 것만) | 웹 훅은 오류 UI가 없지만 앱은 MSG-428 Q7로 Toast가 이미 있어 배선 비용 0. 노출 조건은 종전 `enabled && isError` 규칙 그대로 |
| C7 | `MARKER_MERGE_PX` 불변, 테마 마커도 같은 임계로 병합 | 웹 MapShell이 세 소스 전부 `mergeOverlappingMarkers(·, viewportZoom)` 동일 호출 |
| C8 | 응답 `data`가 배열 직접인 점을 `selectThemeClusters`가 흡수(점령의 `{items}`와 다름) | 생성 타입 `ApiResponseDtoListMissionRegionAggregateResponseDto.data: Array<…>` + 웹 테스트 주석 |
| C9 | 상세를 연 대상 판정 `hasDetailTarget = (selectedCourse ?? selectedMission) !== null` | 웹은 `selectedMission !== null`이지만 앱 `use-home-overlays`는 이미 `selectedCourse ?? selectedMission`을 대상으로 그린다(코스는 어차피 게이트 대상 아님이라 결과 동일) |

### 6. 리스크 (증분)

- **R8 (병렬 충돌면, 수치)**: `grid-map.tsx` 추가 접촉 **0** — 556 헝크(65·73·121·298·404~430) vs 558 커밋분(30·444~462) 그대로. `map-home-screen.tsx` 접촉 1줄(:153~159) vs 557 최근접 헝크 230 — **71줄** 거리, 비인접. `use-home-overlays.ts`·`use-grid-aggregation-query.ts`·`cluster-marker.tsx`·`region-cluster-overlay.ts`는 556·557 diff에 없다. 558 확장 커밋 뒤 556·557이 리베이스할 때 이 티켓 쪽 손 병합은 **없다**.
- **R9 (칩 저줌 데이터 부재)**: dev 계정·부산 실서버에 축제/팝업/핫 집계가 비어 있을 수 있다(종전 검증도 점령 집계가 `items: []`라 스텁). S8'·S11~S15는 프록시 스텁 허용 — 색·택일·게이트·병합 판정은 데이터 출처와 무관.
- **R10 (훅 이름 부채)**: `useGridAggregationQuery`가 칩 택일 집계 전체를 돌려주게 된다. 이름 변경은 557 import 헝크와 인접해 이번엔 안 한다 — 556·557 머지 후 `useClusterMarkersQuery`류로 리네임하는 후속 1커밋 후보.
- **R11 (핫 칩 격자 낱개 소실 체감)**: 앱 핫 칩은 `useHotZonesQuery`(로그인 필요)로 "이 동의 핫 격자"를 그렸다. C4 게이트로 zoom < 16에서는 낱개가 사라지고 집계 말풍선(익명 가능)만 남는다 — 웹과 같은 동작이지만 앱 사용자에게는 **표시 방식 변경**이다. `hot-bubble.png`에 낱개 부재를 함께 남겨 의도임을 기록.
- **R12 (마커 리마운트 비용)**: 칩 전환 시 id 접두가 달라 `NaverMapMarkerOverlay` 전부 언마운트→마운트(커스텀 뷰 마커 캐싱 없음, 본문 R5). 뷰포트 수십 개 수준이라 S10 실측(열화 없음) 범위 안이지만, 칩을 빠르게 연타하면 순간 비는 프레임이 있을 수 있다 — 웹도 HTML 마커 재생성이라 동일.
- **R13 (parity 엄격도)**: `toStrictEqual`로 `theme` 키 부재를 고정하면 웹이 나중에 `theme: undefined`로 바꿀 때 모바일 parity가 깨진다 — 의도된 드리프트 감지이므로 유지.
- **Figma 오탐 방지(증분)**: 테마 말풍선의 채움은 웹 `THEME_FILL_CLASS`(토큰 `theme-*`) 대조가 정본. 칩별 말풍선 Figma 노드는 존재하지 않는다(웹 MSG-451 ① "Figma에 칩별 색 변형 노드가 없어 채움만 바꾼 것이 정본") — Figma 미존재를 결함으로 보지 않는다. 핫 칩 저줌에서 100m 핫 격자가 사라지는 것은 C4 의도(웹 동일), 결함 아님.


## 확장 스코프 작업 로그 (2026-09-02)

**경위** — 1차 스코프가 PR #122까지 간 뒤 사용자가 "팝업/핫구역/지역축제 클러스터링 하기로 했잖아"로 되돌렸다. 1차 스펙은 Q7(테마 색 말풍선) 범위 밖·Q8(핫 집계) 별도 티켓으로 판정하고 S8을 "칩 활성 중 말풍선 없음"으로 세워 검증까지 통과시켰는데, 그 판정이 "웹 방식대로"라는 일괄 승인과 정반대였다. 사용자 결정으로 **558에 포함**(옵션 2), 스펙 확장 → 재구현 → 재검증.

**실측 동작 (빌드 리포트 확장 절 요약)** — 신설 4: `api/theme-aggregation-query.ts`(`clusterSourceForTheme` 택일·미션/핫 쿼리 인자·placeholderData·`selectThemeClusters`) + 테스트 26케이스(웹 `.test.tsx` 1:1), `model/occupancy-visibility.ts`(웹 포팅, 칩 활성 저줌에서 테마 격자 낱개 게이트) + parity. 수정 8: `region-cluster-overlay.ts`에 `theme?` 필드·`toMissionClusterMarkers`·`toHotZoneClusterMarkers`·병합 시 테마 승계(parity 확장 +134), `cluster-format.ts`에 `clusterMarkerLabel`(접근명 주어 = 칩 라벨), `use-grid-aggregation-query.ts`에 useQuery 2개 + 택일 배선(실패·재시도는 활성 소스 하나만), `cluster-marker.tsx` 채움만 `THEME_META[theme].color` 분기(형태 무변경), `use-home-overlays.ts` +19 −1(zoom 입력 + 낱개 게이트), `map-home-screen.tsx` **+1**, `grid-map.tsx` **0줄**. `nose.baseline.json`은 웹 포팅 복제 3패밀리 등재(MSG-428 선례) — develop(556) 병합 후 사용자가 563 패밀리로 재생성.

**검토한 대안 (기각 이유)** — ① 택일을 화면(`map-home-screen.tsx`)에서: `clusters` 줄(:344)이 557 헝크와 1줄 인접이라 충돌면이 생긴다 → 기존 훅 안에서 택일. ② `gateRouteClusters` 포팅: 556의 `/ai-route`는 자체 `GridMap`을 띄우고 `clusters`를 안 넘긴다(grep 0건) → 홈 경로 칩은 택일 `"route"→"none"`으로 동치, 미포팅. ③ 훅 리네임(`useGridAggregationQuery` → 집계 통합명): 557 import 헝크와 인접 → 후속(R10). ④ `selectThemeClusters`에 `enabled` 축 추가: bounds null ⇔ viewport null ⇒ zoom 16 ⇒ unit null이라 placeholder 누출 경로에 도달 불가 → 불필요.

**수용된 표시 변경(R11)** — 핫 칩 저줌에서 종전엔 로그인 기반 핫 격자 낱개가 보였는데, 확장 후엔 걷히고 익명 집계 말풍선만 남는다. 웹과 동일한 동작이고 사용자가 스펙 승인 시 확인했다. 미션(축제·팝업)도 같은 규칙 — 팝업 프레임에서 실데이터 15건이 저줌에 셀 0으로 실증.

**검증 (03_verify_report 확장 절 요약)** — 실패 0 · 확인불가 0 · **미실행(범위 축소) 1**: S14 후반 "코스 격자·라인 유지"는 그 뷰포트에 코스가 0개("경로추천 · 0개")라 관찰 대상이 없었다(전반부 말풍선 0 + 집계 요청 0건은 통과). 3-B 실기 **7분 32초**(예산 15분), 스크린샷 6장(`hot-bubble`·`festival-bubble`·`popup-bubble`·`chip-switch`·`route-chip-none`·`theme-merge`). 실서버 칩 집계가 빈 배열이라 스펙 R9의 스텁 프록시(8090, 집계 3경로만) 사용 후 원상 복구. 채움 픽셀 21×21 표본 전건 단일색 — 핫 `#FF3B30`·축제 `#AF52DE`·팝업 `#FF9500`·해제 후 `#0071E3`·병합 앵커 `#FF3B30`(S15). 프록시 로그 `type=EVENT`/`POPUP` 발사, 칩 활성 구간 `grids/aggregation` 0건(L12), 축제→팝업 전환 프레임에 두 층 동시 노출 없음(S13). `check:duplication` 신규 0, baseline set-diff 추가 18/제거 13 전부 556·558 파일(무관 흡수 0). 병합 후 게이트(오케스트레이터 재실측): vitest 169 files / **1069** tests · tsc 0 · lint 0.

**참고** — `hot-bubble.png`의 "100m 핫 낱개 없음"은 실서버 `/api/hotzones`가 `hotZones: []`라 데이터 부재다(핫 게이트는 L10 parity + 배선 감사로 판정). 에뮬레이터 조작 함정 2건(칩 탭 후 시트 확장으로 `−` 좌표가 시트에 떨어짐 / 접힌 시트 핸들 스와이프가 지도 팬이 됨)은 검증 리포트 절차 메모.
