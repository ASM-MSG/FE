# 결정 기록 (Decision Log)

트레이드오프 판단, 테스트가 실제 버그를 잡은 순간, 스펙 해석 결정을 한 줄씩 기록한다.
형식: `| 날짜 | 티켓 | 결정/발견 | 근거 |`

| 날짜 | 티켓 | 결정/발견 | 근거 |
|------|------|----------|------|
| 2026-07-15 | MSG-108 | 발견: `cn()`(tailwind-merge)이 커스텀 토큰 클래스(`p-xs` vs `p-xl` 등) 충돌을 병합하지 못함 — vitest 셋업 첫 스모크 테스트가 발견. `extendTailwindMerge`로 토큰 스케일 등록 필요(별도 티켓 권장, ui-web의 cn도 동일 이슈) | 기본 twMerge 설정은 Tailwind 기본 스케일만 인식. 토큰 충돌 시 둘 다 DOM에 남아 CSS 순서가 승자를 결정하는 잠재 버그 |
| 2026-07-15 | MSG-108 | 테스트 전략: 로직 레이어(훅·스토어·스키마·유틸)만 test-first, 뷰는 브라우저 실동작 검증 | 뷰는 기획·디자인 변경으로 스펙이 자주 바뀌어 테스트 유지비가 회수율을 초과. 로직은 안정적이고 RN 재사용 대상이라 투자 가치 높음. 뷰-로직 분리로 뷰 테스트 추가는 필요 시점에 저비용 가능(되돌리기 쉬운 결정) |
| 2026-07-16 | MSG-112 | 결정: `shared/geolocation.ts`가 `entities/cell`의 `LatLng`를 import하지 않고 구조 호환 `GeoCoords`를 로컬 정의 | FSD 최하위 layer인 shared가 상위 entities에 의존하면 layer 규칙 위반. 두 타입은 `{lat,lng}`로 구조 호환이라 대입 가능 — 결합 없이 재사용성 유지 |
| 2026-07-16 | MSG-112 | 결정: 카카오맵 SDK 로드 실패 재시도(S3)를 `attempt` state 키로 하위 뷰 remount하여 `useKakaoLoader`를 재실행 | 로더는 멱등·캐시라 동일 위치 재호출로는 재로딩 안 됨. remount가 SDK 경계 안에서 재시도를 트리거하는 가장 단순한 방법 |
| 2026-07-16 | MSG-112 | 발견: 카카오맵 SDK가 `http://localhost` 출처의 요청을 503으로 거부(콘솔 도메인 등록과 별개) — dev에서 지도를 보려면 https 서빙 필요 | 검증 중 네트워크 계측으로 확인: 같은 키로 https·무Referer 요청은 200, `Referer: http://localhost:5173` 요청은 503. SDK가 프로토콜 상대 URL을 써서 http 페이지에선 http로 요청됨. dev https화(예: vite basic-ssl)는 별도 티켓 권장 |
| 2026-07-17 | MSG-113 | 결정: 지도를 `MapHomePage` 소유에서 `MapShell`(라우트 상위 지속 셸)로 이관, `/`·`/explore`는 그 위에 오버레이만 스위칭 | 티켓이 "탐색 패널은 지도 위 오버레이, 지도는 뒤에서 계속 보임"을 요구했는데 기존 라우터는 `/explore` 전환 시 `MapHomePage`가 언마운트돼 지도가 사라짐. 지도 소유권을 셸로 올리면 홈↔탐색 왕복에도 지도 중심·줌이 유지됨(검증 단계에서 회귀 없음 확인) |
| 2026-07-17 | MSG-113 | 결정: 카드 클릭 시 지도 "이동"만 구현, 지도 위 격자 "강조" 렌더링은 범위 밖으로 분리 | 티켓 문구는 강조를 "기존 로직 재사용"이라 했으나 실제 `MapCanvas`엔 강조 렌더링 자체가 없었음(이동만 존재) — 없는 것을 재사용할 수 없어 신규 구현 여부를 사용자에게 확인, 범위 확대 대신 후속 티켓으로 분리하기로 결정 |
| 2026-07-17 | MSG-114 | 결정: 지역 필터와 검색어 필터를 상호 배타(단일 필터)로 처리 — 지역 선택 시 `query=""`, 검색 커밋 시 `selectedRegion=null` | 두 필터를 동시 유지하면 "마포구 + 이전 검색어 '성수'" 같은 stale 교집합이 빈 결과를 내 사용자를 혼란시킴. 지역 브라우징과 텍스트 검색은 별개의 진입 동작이라 한쪽 선택이 다른 쪽을 리셋하는 것이 자연스러움. `selectExploreCells`는 두 필터 교집합을 지원하되(AC 16), 스토어가 배타 정책을 강제 |
| 2026-07-17 | MSG-114 | 결정: 검색 패널 입력창을 공용 `SearchBar` 재사용 대신 로컬 surface 필 조합으로 구현 | Figma 패널 입력은 flat surface 필(#f5f5f7)로 `SearchBar`(elevated bg-background+shadow, 장식용 아이콘)와 시각·구조가 다르고, D2가 "검색 아이콘 클릭 커밋"을 요구하는데 `SearchBar`의 아이콘은 클릭 불가. 얇은 로컬 input+버튼 조합이 요구를 정확히 충족(탐색 패널 트리거 SearchBar는 그대로 재사용) |
| 2026-07-18 | MSG-114 | 폐기(위 행): 검색이 드롭다운(`SearchBox`)으로 재구성되며 공용 `SearchBar`를 재사용하게 됨. `SearchBar`에 선택적 `onSearch`를 추가해 아이콘을 클릭 가능한 버튼으로 만들어 D2("아이콘 클릭 커밋")를 SearchBar 재사용으로 충족 | 리팩터로 로컬 input+버튼이 사라지며 아이콘 클릭 커밋이 누락됐다는 코드리뷰 지적을 반영. 별도 로컬 조합 대신 공용 컴포넌트를 확장하는 편이 재사용성·일관성에 유리하고, 장식용 사용처는 onSearch 미지정으로 하위호환 유지 |
| 2026-07-20 | MSG-115 | 결정: 우측 컬럼 상세 시트를 공용 `BottomSheet` 재사용 대신 신규 `CellDetailSheet`로 구현 | `BottomSheet`는 하단 도킹 전용(rounded-t·shadow-sheet)이라 리스트 옆에 붙는 우측 컬럼 레이아웃과 구조가 안 맞음. 강제 재사용 시 불필요한 하단 전용 스타일을 오버라이드해야 해 오히려 결합도만 높아짐. `VideoRow`·`Button`은 그대로 재사용 |
| 2026-07-20 | MSG-115 | 결정: 격자 카드 클릭이 기존 지도 이동(MSG-113 S7)과 신규 상세 시트 열기 두 역할을 겸함 | 티켓 문구가 지도 이동을 언급하지 않아 스펙 단계에서 사용자에게 확인 — 기존 동작 유지 + 상세 선택 추가로 확정. 별도 클릭 영역 분리는 과설계로 판단 |
| 2026-07-20 | MSG-116 | 결정: `ModalCard`(ui-web)에 `confirmDisabled?: boolean` 후위호환 prop 추가 | 신고 모달의 "사유 미선택 시 제출 비활성"(S6)을 충족하려면 확인 버튼 disabled가 필요한데 기존 `ModalCard`는 미지원. 로컬 재구현 대신 공통 컴포넌트를 확장해 다른 확인형 모달에도 재사용 가능하게 함 — 사용자 승인 후 진행 |
| 2026-07-20 | MSG-116 | 결정: 신고 사유 선택 UI(Radix `Select`)를 ui-web으로 승격하지 않고 페이지 로컬(`ReportReasonSelect.tsx`)로 구현 | 소비처가 이 화면 1곳뿐이라 디자인 시스템의 "재사용될 때 승격" 규칙에 미달 — 조기 추상화 대신 두 번째 소비처가 생기면 승격 |
| 2026-07-20 | MSG-116 | 발견(보류): `ModalCard`의 `confirmVariant="danger"`(`bg-error` + `text-primary-foreground`)가 WCAG AA 대비 기준(4.5:1) 미달 — 계산상 약 3.76:1, `fm-title`(15px/600)은 large-text 완화 기준 미충족. 코드리뷰봇 지적, 검증 결과 사실 | 이 조합은 기존 `Button`의 `danger` variant가 먼저 쓰던 색 조합을 그대로 따른 것으로, 이번 PR이 만든 문제가 아니라 디자인 시스템에 이미 있던 이슈(둘 다 이전엔 미사용). 이번 PR에서 로컬로 색만 바꾸면 `Button`과 `ModalCard`의 danger 색이 갈라져 일관성이 깨지므로, `color/error` 토큰 자체를 어둡게 조정하는 디자인 시스템 결정을 후속 티켓으로 분리하기로 사용자와 합의 |
| 2026-07-20 | MSG-117 | 결정: 위치 태그 pill을 `Chip`/`CellBadge` 재사용 대신 feature-local 인라인으로 구현 | 스펙은 두 컴포넌트를 Figma 확인 후 택1 후보로 뒀으나, Figma(13399:1555) 태그는 `bg-primary/10` + primary 텍스트 + MapPin 아이콘의 위치 표시 pill로, 필터 토글용 `Chip`(h-32 solid-active)·격자 배지용 `CellBadge`(solid primary bg + 흰 텍스트)와 시각·역할이 모두 다름. 강제 재사용 시 스타일 오버라이드로 결합만 커져 역할 겹침이 없는 로컬 마크업으로 둠(정적·비동작 표시라 도메인 로직 없음) |
| 2026-07-20 | MSG-117 | 결정: 파일 검증(`upload-validation`)을 플랫폼 `File`이 아닌 중립 `{name,size}`(`UploadCandidate`)로 받는 순수 함수로 설계, DOM `File`→중립 변환은 `UploadDropzone`(UI 경계)에서 수행 | RN 경계 규칙 — 모델 레이어가 웹 전용 `File`/DOM에 의존하면 RN 재사용 시 전면 리팩터. 이름·용량만으로 판정 가능하므로 중립 타입으로 내려 확장을 파일 이동 수준으로 유지 |
| 2026-07-20 | MSG-118 | 결정: `UploadDropzone.onSelectFile`에 원본 `File` 인자를 추가(candidate와 별도로 전달) — 스펙의 "UploadDropzone 변경 없음" 대신 스펙 리스크 항목의 "duration 캡처 경로 신규 추가"를 채택 | duration 실측(loadedmetadata)·미리보기(createObjectURL)가 모두 플랫폼 File을 요구하는데 기존 candidate는 RN-safe `{name,size}`뿐. candidate 타입을 오염시키지 않기 위해 File을 두 번째 인자로 UI 경계(부모)에 전달하고 판정은 순수 함수에 위임 |
| 2026-07-20 | MSG-118 | 결정: `use-video-duration`/`use-highlight-selection`의 상태 초기화를 effect가 아닌 "이전 값 비교(setState during render)" 패턴으로 구현 | 레포의 `react-hooks` flat-recommended가 React 19 컴파일러 규칙(`set-state-in-effect`)을 error로 강제 — effect 동기 setState가 금지됨. objectURL 생성/revoke만 effect에 남기고 상태는 loadedmetadata 콜백(비동기)에서만 세팅해 규칙을 준수하면서 File 교체 시 stale 값 노출을 막음 |
| 2026-07-20 | MSG-118 | 결정: SegmentTrimmer의 모든 포인터 상호작용을 트랙 요소에 직접 바인딩하고 `event.currentTarget`으로 좌표 측정(핸들별 커링 핸들러·trackRef 헬퍼 제거) | `react-hooks` `refs` 규칙이 커링된 핸들러 팩토리 안의 ref 접근을 "렌더 중 ref 접근"으로 오판. 좌표 측정을 ref 대신 이벤트의 currentTarget(=트랙)에서 얻고 드래그 상태만 이벤트 핸들러 내부 ref로 관리해 규칙을 충족 |
| 2026-07-21 | MSG-119 | 결정(스펙 해석): `BlurStep`이 `toBlurConfirmResult`로 확인 payload를 만들고 실제 `console.log`는 부모(`UploadModal`)가 주입한 `onConfirm(result)`이 수행하도록 분리 | 스펙이 BlurStep prop을 `onConfirm(콘솔 로그)`로, UploadModal 배선을 `onConfirm={…콘솔 로그}`로 동시에 기술해 로그 위치가 모호. 목업 감지 데이터·payload 조립은 BlurStep(도메인 소유)에 두고 "확인 시 무엇을 하는가"(임시 완료 지점=로그, 이후 4/4 전환)는 부모가 결정하도록 위임 — MSG-118 HighlightStep의 `onNext` 위임 패턴과 정합 |
| 2026-07-21 | MSG-163 | 결정(7/15 테스트 전략 부분 수정): 뷰 테스트 전면 비보존 → "2회 이상 재검증되는 안정 흐름은 `*.smoke.test.tsx`로 커밋" 정책 도입. 첫 자산으로 업로드 위저드 흐름 스모크 4케이스 승격 (`upload-wizard-flow.smoke.test.tsx`) | MSG-117~120 검증마다 동일한 임시 RTL 하네스를 재작성·폐기(4연속) — 같은 작성 비용으로 커밋하면 회귀 자산이 되고 CI에서도 돈다(testing-trophy: 통합 대역이 ROI 최적). 7/15 결정문 스스로 열어둔 "필요 시점에 저비용 추가" 조항의 발동. 문구·스타일 단정은 계속 제외해 유지비를 통제한다 |
| 2026-07-21 | MSG-120 | 결정(스펙 해석): 스펙이 `PreviewStep` props로 `duration`을 열거했으나 4개 카드(미리보기·하이라이트·블러·위치) 어디서도 소비하지 않아 prop에서 제외 | `duration`을 받아 미사용하면 `@typescript-eslint/no-unused-vars`(error)에 걸리고, 소비처 없는 dead prop은 수술적 변경(고아 회피) 원칙 위반. 어떤 화면 AC(S1~S11)도 duration 표시에 의존하지 않음 — 렌더 게이트(`step==="preview" && duration!==null`)는 유지해 메타데이터 로드 상태만 보장. 표기 이원화(2·3단계 `formatTimecode` m:ss vs 4단계 `formatSelectionRange` "Ns")는 스펙 리스크대로 이번 티켓 유지·보고만 |
| 2026-07-22 | MSG-123 | 발견(재발)·수정: `cn`(기본 twMerge)이 타이포 토큰 `text-fm-caption`을 색상 그룹으로 오분류 — 함께 넘긴 `text-foreground(-muted)`가 크기 클래스를 밀어내 라벨이 16px로 렌더(검증자 DOM 실측이 발견, MSG-108 기록 함정의 첫 실전 재발). BadgeTabBody 라벨을 cn 없는 순수 문자열 조합으로 수정 | 조건부가 색뿐이면 병합이 불필요 — twMerge를 안 태우는 것이 재배치(순서 바꾸면 색 소실)보다 근본적. 정적·조건부 간 그룹이 겹치는 cn 사용은 `extendTailwindMerge` 토큰 등록(MSG-108 후속 티켓 권장분) 전까지 회피 패턴이 정본. 동일 함정의 UploadModal 53·61행은 티켓 범위 밖이라 후속 환류 대상 |
| 2026-07-22 | MSG-123 | 결정(스펙 해석): 뱃지 "전체 보기" 링크·하단 버튼 노출을 `hasMore && !expanded`로 통일 — 카탈로그가 프리뷰 8개 이하면 링크도 표시하지 않음 (AC 2의 "표시된다"를 확장 가능 상태 전제로 해석) | 확장할 내용이 없는 링크는 no-op 함정 UI. 갤러리 "전체 보기"(MSG-122 A4 — >9개일 때만 표시) 선례와 동일 정책으로 도감 내 일관성 유지. mock 카탈로그는 12개(>8)라 실제 화면에서는 항상 표시된다 |
| 2026-07-22 | MSG-122 | 결정(데이터): mock·기본값의 MVP 지역을 서울 → 부산 서면 일대로 확정 이전(사용자 확정) — 폴백 상수 `SEOUL_CITY_HALL` → `SEOMYEON_CENTER`(서면역 35.1579, 129.0594), 디폴트 지역 "중구" → "부산진구", 8구·17셀은 역할 보존 매핑(마포구→부산진구 등, 매핑표는 02_build_report ③). 셀 id 체계(A-14 등)는 유지, 탐험률 맵은 9키→8키(디폴트가 REGIONS에 포함됨), 빈 상태 테스트 지역은 "중구"→"사상구" 승계 | id는 업로드(UploadModal)·도감 시드가 참조하는 안정 키라 지명과 분리 유지가 안전. 폴백 좌표→역지오코딩→디폴트 지역명 3축 수렴(서면→부산진구)이 불변식이라 상수 리네임 없이 값만 바꾸면 이름이 거짓말이 됨. 디폴트 "부산진구"는 수집이 있는 구라 빈 상태 재현 역할은 셀 없는 사상구로 이관 |
| 2026-07-22 | MSG-122 | 결정(스펙 해석): 스펙 A6의 명시값(A-14 2→4, B-08 3→5)을 따르되, 스펙이 파생 수치로 적은 "마포 13"은 산술 오류로 판단(실제 4+5+1+1=11) — mock을 13에 맞추지 않고 승인된 명시값 + 불변식(마포구 합계 > 9)을 정본으로 구현 | AC 6의 검증 기준이 "합계 > 9"(프리뷰 제한 시연 가능)라는 불변식이고 11로 충족됨. 13에 맞추려면 승인 게이트에서 확정된 개별 videoCount를 임의 변경해야 해 더 큰 일탈. AC 13 문구 "mock 13개 중 9개"는 "11개 중 9개"로 읽어야 함 — 빌드 리포트 스펙 이슈로 보고 |
| 2026-07-22 | MSG-121 | 결정: `MapCanvas`의 오버레이 prop 타입(`MapCellOverlay`)을 `features/dex`에서 import하지 않고 컴포넌트 로컬로 정의 — dex의 `CellOverlay`와 구조 호환으로 연결 | 지도 캔버스는 전 섹션 공유 코드(R3)인데 dex feature를 import하면 홈·탐색 지도가 도감 도메인에 결합됨. `{id, bounds}` 순수 데이터 계약은 구조적 타이핑으로 충분 — `shared/geolocation`의 `GeoCoords` 로컬 정의 선례와 동일 근거. 탭 경로 기저(`"/dex"`)도 같은 이유로 `dex-tab.ts`가 로컬 소유(features→app 역참조 회피) |
| 2026-07-22 | MSG-121 | 결정(개정 D4 구현): 최근 수집 행을 단일 버튼 → 형제 버튼 2개(이동+X 제거) 구조로 재편, X 클릭에 stopPropagation 유지. X로 전부 제거한 빈 목록에는 "수집 0건" 빈 상태 안내를 띄우지 않음(hasCollected 분기) | HTML은 버튼 중첩을 금지해 기존 "행 전체 버튼" 안에 X를 넣을 수 없음 — 형제 구조면 실전파도 없지만 조상 리스너 추가 등 구조 변화 대비로 차단을 명시(AC 25). "아직 수집한 격자가 없어요"는 수집 0건의 의미라 사용자가 방금 지운 상태에 띄우면 오정보 — 두 상태를 구분(스모크로 고정) |
| 2026-07-22 | MSG-121 | 결정(개정 A12 해석): `formatExploredPct` 1% 미만은 `toPrecision(2)` 원문 유지(0.5→"0.50%"), 0은 "0%" 특례 | "유효숫자 2자리"의 문자 그대로 구현 — 후행 0 제거는 유효숫자 규칙 위반이고 규칙에 예외를 늘림. 0은 toPrecision이 "0.0"을 만들어 신규 사용자 화면("전체 지도 0.0% 탐험")이 어색해 정수 표기 특례. 1% 미만/이상 경계·반올림 케이스는 테스트로 고정 |
| 2026-07-22 | MSG-121 | 결정(테스트 기법): AC 19 오류 스모크를 훅 목킹 대신 QueryClient 캐시 주입으로 재현 — 실패하는 `fetchQuery`로 error 엔트리를 만들고 `retryOnMount/refetchOnMount: false`로 고정한 뒤, 재시도 클릭이 실제 `refetch`(mock 성공 queryFn)를 태워 데이터 화면으로 복구되는 것까지 단정 | `useDexQuery`를 통째로 목킹하면 react-query 배선(옵저버·refetch)이 검증에서 빠지고, ESM 라이브 바인딩 탓에 `fetchDex` spyOn은 신뢰 불가. 캐시 주입은 공개 API만 사용해 회귀에 강하고, 오류→재시도→복구 전체 경로가 통합으로 검증됨(업로드 스모크의 "seam만 목킹" 원칙과 정합) |
| 2026-07-22 | MSG-124 | 결정: `MOCK_PROFILE`의 닉네임·스트릭을 리터럴 복사 대신 `MOCK_DEX.summary` 직접 참조로 구성 | AC 5(도감-프로필 mock 정합)를 구조적으로 보장 — 도감 mock 수치가 바뀌어도 두 화면이 갈라질 수 없다. 같은 층(entities) 교차 import는 mock-dex→MOCK_CELLS 선례를 따름. 불변식 테스트는 참조가 끊겨도 잡도록 별도 유지 |
| 2026-07-22 | MSG-124 | 결정: `formatJoinedDate`를 `Date` 파싱 없이 ISO 문자열 날짜부 슬라이스로 구현 | date-only ISO("2026-01-12")를 `new Date()`로 파싱하면 UTC 자정으로 해석돼 UTC 서쪽 타임존에서 하루 밀린 표기가 됨. 가입일은 "표기 날짜"가 정본이므로 문자열 기반이 타임존 무관·결정적 — 제로 패딩은 padStart로 처리(AC 3 테스트 고정) |
| 2026-07-29 | MSG-254 | 결정(A5·R1 구체화): 지도 실패 감지를 2계로 분리 — 스크립트 로드 실패는 `MapLoadErrorBoundary`(useNavermaps가 Suspense `use()` throw로 표면화), 인증 실패는 `window.navermap_authFailure` 전역 훅(경계 파일 내 effect 등록/해제) | react-naver-maps 0.2.2는 kakao의 `[loading, error]` 훅이 없음. 인증 실패 시 SDK가 지도 청크를 안 내려 로드 promise가 영원히 pending — Suspense/ErrorBoundary 어느 쪽도 못 잡으므로 전역 훅이 유일한 신호. 두 경로 모두 기존 attempt remount 재시도 패턴에 수렴 |
| 2026-07-29 | MSG-254 | 결정: 초기 중심 이동을 controlled `center` prop 대신 uncontrolled `defaultCenter` + center 변경 effect의 `map.panTo()`로 구현 | react-naver-maps의 controlled center는 `panTo(duration 0)` 즉시 점프로 동작 — 카카오 `isPanto`의 부드러운 이동(geolocation 해소 시)과 체감이 달라짐. effect 경유 panTo(기본 트랜지션)가 기존 동작 등가이고, 로드 전 center 변경은 defaultCenter가 최신값으로 생성돼 자연 수렴 |
| 2026-07-29 | MSG-254 | 발견(R6): react-naver-maps 0.2.2의 `useNavermaps` promise 캐시가 실패(reject·영구 pending)한 promise를 퇴거하지 않음 — attempt remount 재시도가 같은 세션에서는 캐시된 실패 promise를 다시 받아 스크립트 재로드가 일어나지 않음(새로고침으로만 복구) | 캐시 키(`키ID:서브모듈`) 변조 우회는 SDK 로더 내부 동작(중복 서브모듈 로드)에 기대는 핵이라 배제. 재시도 UI·remount 패턴은 스펙(A5)대로 보존하고 한계는 검증·지라 환류로 보고 — 업스트림 수정 또는 후속 티켓 대상 |
| 2026-07-23 | MSG-46 | 결정: 카카오 라벨 검정을 Tailwind 기본 `text-black` 대신 원시 토큰 `kakao-black`(#000000) 신설로 처리, `kakao-yellow`(#FEE500)와 쌍으로 palette에 추가 | 규칙 1 "색상은 토큰 클래스만"의 정본을 유지 — 기본 `black`은 design-tokens에 없는 색이라 쓰면 토큰 밖 색상 선례가 됨. 브랜드 규정색이라 시맨틱 매핑 없이 원시 토큰으로만 사용(Figma 변수 kakao/logo와 대응). 버튼 fill #FEE500은 get_design_context로 확정(스펙 추정 2 해소) |
| 2026-07-23 | MSG-46 | 결정: 카카오 버튼 라벨 타이포를 `text-fm-heading`(16/20) + `font-bold` 조합으로 재현 — 전용 토큰 신설 안 함 | Figma 라벨은 Pretendard Bold 16/20인데 토큰에 bold 16 스타일이 없음. 소비처 1곳(브랜드 버튼)뿐이라 타이포 토큰 신설은 과설계 — 가장 가까운 토큰에 표준 weight 유틸 오버라이드가 최소 변경. tracking -0.32px은 폰트 차이(Inter) 허용 원칙(스펙 오탐 방지 1)에 따라 생략 |
| 2026-07-23 | MSG-125 | 결정(PR #22 리뷰 반영): [변경] 트리거의 aria-pressed 불일치를 Chip 유지 + `aria-pressed={undefined}` 전달로 해소(속성 미노출), 일반 버튼 재구현·Button 오버라이드는 기각. 스프레드 순서 의존은 스모크 단정(속성 부재)으로 고정 | 일반 버튼은 칩 스타일을 페이지에 복제해 중복 UI 규칙 위반, Button은 secondary sm 기준 9속성 오버라이드가 필요하고 `text-fm-*`↔`text-primary`가 twMerge 색상 그룹에서 충돌(MSG-123 재발 함정)해 더 취약. Chip의 `{...props}`가 `aria-pressed={active}` 뒤라 undefined 전달로 속성이 제거됨 — 시각 불변·재사용 유지의 최소 변경 |
| 2026-07-23 | MSG-46 | 발견(후속 2): 후속 1(04 스펙 F1~F4) 산출물이 워킹 트리에 없음 — 05 리포트는 "워킹 트리에 남김"이라 했으나 브랜치가 d5f0cd5(본편)에서 clean. 후속 2 착수 시 F1~F4를 스펙·리포트 그대로 재구현 | 세션 간 미커밋 워킹 트리 산출물은 유실될 수 있음 — 파이프라인 산출물은 최소 스테이징+커밋까지 이어져야 안전. 재구현은 05 리포트의 판단(분기 우선순위·persist 부재 단정)을 그대로 계승 |
| 2026-07-23 | MSG-46 | 결정(후속 2): 로그인 모달 카드 셸에 `ModalCard` 미사용 — Radix Dialog 프레이밍 관례 + ModalCard와 동일 토큰의 로컬 셸로 구현 | ModalCard는 가시 title 행(좌측 h2 + ✕)이 필수인데, G2가 요구하는 콘텐츠는 페이지와 동일한 중앙 정렬 타이틀·서브카피(LoginContent 단일 소스)라 타이틀이 중복·좌우 분열됨. 셸 시각 4클래스만 공유 — ModalCard에 headerless(title 미표시) 옵션 승격은 후속 판단 대상으로 리포트에 기록 |
| 2026-07-23 | MSG-46 | 결정(후속 2 G7): /login 라우트 제거로 `NAV_ROUTES`/`ROUTES` 분리를 단일 `ROUTES`로 환원, 로그인 모달 접근성 타이틀은 sr-only 중복 대신 `Dialog.Title asChild`를 LoginContent의 가시 h2에 주입 | 분리는 "네비 밖 라우트(login)" 구분용이었는데 그 라우트가 사라져 이중 상수는 죽은 구조. sr-only Title은 스모크에서 동명 헤딩 중복(h2 2개)으로 실증 — 가시 헤딩을 그대로 접근성 이름으로 쓰는 것이 중복 없는 최소 구조 |
| 2026-07-23 | MSG-46 | 결정(PR #23 리뷰 반영): ① R1 `/profile` 가드는 진입(마운트) 시점 1회 판정(`RequireAuth`, 스토어 비구독) — 반응형 가드 기각 ② R2 Radix Dialog 프레이밍 4곳 중복을 ui-web `DialogShell`로 승격(크롬 없는 셸: Root/Portal/Overlay/Content + srTitle·scrollable) ③ R3 로그인 모달 ✕는 absolute 현행 유지 + 의도 주석 | ① 반응형이면 /profile에서 로그아웃 클릭이 즉시 홈 추방으로 오폭 — "로그아웃 후 패널 잔류"(후속 F2)가 검증 확정 의도라 직접 진입(주소창·새로고침·뒤로가기)만 막는 것이 정확. ② 프레이밍은 도메인 무관 재사용 UI라 shared/ 금지 규칙상 ui-web이 유일한 공유 레이어(radix-ui 기존 의존) — 시각 클래스 불변으로 4개 모달 스모크 그린 유지. ③ 셸에 ✕ 슬롯을 표준화하면 ModalCard의 onClose ✕와 이중이 되고, title-row 전환은 LoginContent 단일 소스 시각을 깨므로 리뷰어 허용 범위(현행+주석)를 택함 |
| 2026-07-23 | MSG-46 | 결정(후속 3 P1): 목 인증 영속화는 sessionStorage 아닌 localStorage + `shared/storage.ts` 어댑터(zustand `StateStorage`) 경유. 스토어는 `createAuthStore` 팩토리로 export해 테스트가 "새 로드 = 새 스토어 재수화"를 재현 | model 레이어의 localStorage 직접 참조는 RN 경계 lint가 차단 — lint 메시지가 지정한 shared 어댑터 경유가 컨벤션 정답(RN에선 어댑터만 AsyncStorage로 교체). localStorage 선택 근거: "재로그인은 카카오 버튼뿐, 새로고침 리셋 없음"이라는 확정 데모 흐름은 탭·브라우저 재시작을 넘는 보존이 자연스럽고, 실인증 전환 시의 토큰 보존 의미론과도 일치. 팩토리는 codex 지적(수동 상태 설정이 비영속을 가림)을 해소하는 새 로드 의미론 테스트의 최소 장치 |
| 2026-07-23 | MSG-46 | 발견(후속 3): Node 22+의 실험적 `localStorage` getter가 `--localstorage-file` 플래그 없이 undefined를 반환하며 vitest jsdom 환경의 localStorage를 가림(sessionStorage는 Node 내장 구현이 있어 정상인 비대칭) — `src/test/setup.ts`에 Storage 계약 in-memory 심 추가로 해소 | P1 테스트 첫 실행이 발견. jsdom 자체는 localStorage를 제공하나 Node 전역 getter가 우선함. 프로덕션 경로(shared/storage → 실제 localStorage)는 불변, 심은 테스트 환경 보정 전용이며 테스트 파일별 환경 격리로 파일 간 오염 없음 |
| 2026-07-29 | MSG-254 | 결정(재작업 1): 지도 폴백 전환을 `MapLoadErrorBoundary` 내부 children 교체로 변경 — 경계가 마운트를 유지한 채 실패 시 폴백을 렌더 | 인증 실패 시 SDK 하위 트리 언마운트에서 라이브러리 cleanup(`naver.maps.Event.removeListener`)이 throw하는데, 기존 구조는 경계째 언마운트라 예외가 라우터 루트까지 전파돼 앱 전체가 죽음(검증 2회 재현). 경계를 남기면 삭제 단계 예외를 흡수 — jsdom 스모크로 크래시 재현(RED) 후 수정 |
| 2026-07-29 | MSG-254 | 결정(재작업 2, R6 해소): 프리플라이트 로더 신설(`naver-sdk-loader.ts`) — SDK 컴포넌트 마운트 전에 경계가 직접 스크립트를 주입·로드하고, 준비(jsContentLoaded) 후에만 SDK 하위 트리를 마운트. 실패 시 태그 제거+재주입 = 진짜 네트워크 재시도 | react-naver-maps 0.2.2 훅 캐시는 실패 promise를 퇴거하지 않고(위 R6 발견) exports 제한으로 내부 loadScript 딥 임포트도 불가. 준비 후 마운트되는 라이브러리 로더는 전역을 감지해 즉시 resolve하므로 실패 promise가 캐시에 들어갈 경로 자체가 사라짐 |
| 2026-07-29 | MSG-254 | 발견(재작업 2): 인증 실패 시 네이버 SDK의 전역 잔존 상태는 "null화"가 아니라 시점별로 다름 — 언마운트 시점엔 null 참조로 cleanup이 throw하지만, 이후 naver.maps가 내부만 깨진 채 남고 서브모듈 로드·jsContentLoaded=true까지 진행됨(브라우저 계측). 전역 존재·jsContentLoaded 어느 것도 오염 판별에 불충분 | 유일하게 신뢰 가능한 신호는 `navermap_authFailure` 훅 — 수신자(MapCanvas)가 `markNaverMapsPoisoned`로 로더에 보고하고, 오염·stale 상태의 재로드는 잔존 네임스페이스를 무효화(naver=undefined 대입, var 선언 대비 delete 회피)한 뒤 재주입. 재시도가 SDK 전체 재실행이 됨을 브라우저 계측(MutationObserver)으로 확인 |
| 2026-07-29 | MSG-254 | 결정(재작업 2회차): 재시도 리마운트(attempt ≥ 1)에서 Provider `submodules`에 시도 마커(`retry-${attempt}`)를 붙여 react-naver-maps 훅 캐시 키를 우회 — 성공 후 인증 실패 경로에서 캐시가 오염된 이전 naver.maps로 resolve된 promise를 계속 돌려주는 결함 수정. 실제 fetch되는 프리플라이트 URL에는 마커 미포함 | 훅 캐시 키는 `${ncpKeyId}:${submodules 정렬 join}`이고 성공 promise는 퇴거 없음 — 같은 키로는 프리플라이트가 전역을 재구축해도 이전 객체 수령. 마커 URL 유출 없음: SDK는 프리플라이트 ready 후에만 마운트되므로 캐시 미스의 라이브러리 loadScript는 전역 존재로 주입 없이 즉시 resolve(라이브러리 소스 확인). jsdom 스모크로 "재시도 지도 생성이 새 네임스페이스를 탄다" RED 재현 후 수정 |
| 2026-07-30 | MSG-252 | 결정(R4): `map-overlay-store`를 features/dex → widgets/map-shell로 이동(+ 스타일드 셀·경로 슬롯 확장), `cellToBounds`·`CELL_SIDE_METERS`·`CellOverlay`를 features/dex/cell-overlay → entities/cell/cell-geometry로 이동 | 홈(MSG-252)이 두 번째 게시자가 되면서 features/dex 소유를 유지하면 홈→도감 feature 간 결합 발생. 게시자(pages/dex·pages/map-home)가 모두 위젯을 import할 수 있는 FSD 방향이라 소비자인 셸 위젯의 계약(sidebar-store 선례)으로 이동. 기하는 도감·홈 공용이라 entities로(shared/format 선례). 기존 테스트는 경로만 교체(이동은 삭제 감사 예외) |
| 2026-07-30 | MSG-252 | 결정(R1): 빗금(테마 셀 ∩ 내 점령)은 SVG GroundOverlay 대신 순수 함수 `buildHatchLines`(반대각 평행선 절단 기하) + 사선 Polyline 묶음으로 렌더 | 네이버 Polygon은 패턴 채움 미지원(스펙 R1 확인). Polyline 방식은 기하를 vitest로 검증 가능하고(끝점 경계 내·평행 기울기), GroundOverlay의 이미지 스케일링·해상도 의존이 없음. 시각 결과는 디자인 근사치(R1 명시) |
| 2026-07-30 | MSG-252 | 결정(스펙 해석): 칩 토글은 어느 방향이든(해제·전환·비활성→활성) 열려 있던 셀 상세를 닫는다 — AC 9-1의 "해제·전환"을 activeTheme 변경 전체로 일반화 | 상세의 배지·영상 목록·버튼이 활성 테마에서 파생되므로(AC 9 vs 10) 테마가 바뀐 채 상세를 유지하면 표시 기준이 어긋난 stale 상세가 남음. AC 10 상세(비활성)에서 칩 활성화 시에도 닫는 것이 일관 규칙 |
| 2026-07-30 | MSG-252 | 결정(검증 기각 반영): 교집합 빗금(테마 셀 ∩ 내 점령)은 경로추천 포함 4테마 공통 규칙 — 빌드 시 해석("AC 7 문언이 3테마 한정이라 route는 빗금 없음", `hatchable = activeTheme !== "route"` 게이트)을 폐기하고 게이트 제거 | 검증 Figma 대조에서 경로추천 정본(13848:8440)에 경로 셀 ∩ 내 점령 위치의 초록 빗금 셀 확인(03_verify_report 판정 2) — AC 7 문언은 route 빗금을 금지한 게 아니라 언급하지 않았을 뿐이며 디자인 정본이 우선. 기존 "route 빗금 없음" 테스트를 새 정본으로 재설계, RED 재확인 후 수정 |
| 2026-07-30 | MSG-252 | 결정(AC 14 해석): 칩·셀 상세 초기화 시점 = 홈 이탈(MapHomePage 언마운트) — 스토어에 `reset()` 추가, 뷰 useEffect cleanup에서 호출. 사이드바 접힘은 리셋하지 않음 | 복귀 시 리셋과 사용자 관찰 결과가 동일해 기존 관례(오버레이 clear 언마운트 클린업)를 따름. 모든 사이드레일 섹션이 MapShell Outlet 자식이라 언마운트가 전 섹션 커버. 배선을 뷰에 둬 스토어의 라우터 미참조(RN 경계) 유지. 접힘은 display:none(비언마운트)으로 상태 유지가 셸의 기존 계약이라 AC 14 문언(다른 섹션 이동) 밖 |
| 2026-07-30 | MSG-253 | 결정(스펙 해석): `CellVideo.uploaderHandle`을 필수가 아닌 선택 필드로 추가 | 필수로 하면 이전 티켓(explore·dex)의 불변 테스트 픽스처가 `CellVideo` 리터럴을 만들어 컴파일 단계에서 전부 갱신을 강요당함 — 스펙 리스크 항목("필드는 추가만, 다른 feature 영향 없음")과 이전 티켓 테스트 불변 원칙을 동시에 지키는 유일한 형태. mock 표본은 전부 핸들을 주입해 상세 화면(AC 10)에는 항상 표시된다 |
| 2026-07-30 | MSG-253 | 결정(고아 정리): 스펙은 `HomeCellDetail`에서 `location` 제거만 명시했으나 `videoCount`도 함께 제거 | 서브타이틀이 "위치 · 영상 N개" → `subtitle` 문자열로 교체되며 `videoCount`의 유일 소비처(구 서브타이틀)가 사라짐 — 이 변경이 만든 고아는 정리한다는 수술적 변경 원칙 적용. 셀 자체의 `Cell.videoCount`는 불변 |
| 2026-07-30 | MSG-263 | 결정(데이터): 부산 경계는 southkorea-maps의 `geo_simple`이 아니라 원해상도 `skorea_provinces_geo.json`에서 추출해 자체 Douglas-Peucker(0.0022°)로 간소화 — 미세 도서 5개·미세 홀 1개 제거, 수영만 홀(광안대교 폐합 내수면)은 유지 | `geo_simple`은 부산이 단일 링 67점으로 뭉개져 영도·가덕도 해안·수영만이 소실됨(스펙 "영도·가덕도 유지" 위반). 원해상도판은 두 섬이 교량 연결로 주 링에 포함돼 간소화 후에도 유지됨을 좌표 판정으로 검증(busan-boundary.test). mapshaper 등 도구 의존 없이 재현 절차를 파일 주석에 기록 |
| 2026-07-30 | MSG-263 | 결정(구조): `GRID_ORIGIN`(부산 bbox SW, D2)을 entities/cell/grid.ts에 리터럴로 하드코딩하고 entities/region의 `BUSAN_BBOX.sw`와의 정합은 busan-boundary.test가 단정 | 격자 지오메트리(cell)가 경계 데이터 모듈(region)을 import하면 entities 간 역방향 결합이 생김 — 값 복제 + 테스트 단정으로 드리프트를 막으면서 단방향 의존 유지. 서버 격자 체계 도입 시 원점 자체가 교체될 전제(D2) |
| 2026-07-30 | MSG-263 | 결정(구조): 점령 셀의 격자 스냅·경계 필터·occupied 스타일 부여를 `buildOccupiedGridCells`(grid-overlay) 한 곳에 두고, `buildHomeOverlayCells`(theme-overlay)의 base 분기가 이를 위임 호출 | 스펙이 두 함수 모두에 점령 오버레이 파생을 기술해 그대로 두면 스냅·필터 로직이 이원화됨 — 단일 출처로 합치고 theme-overlay는 테마 셀 분기(색·빗금·1회 게시)만 소유. grid-overlay→theme-overlay 방향은 타입 전용 import라 런타임 순환 없음 |
| 2026-07-30 | MSG-263 | 결정(개정 2 구조): 상시 점령 셀 파생(`buildOccupiedGridCells(MOCK_DEX.collectedCells)`)을 MapShell 모듈 스코프 상수로 1회 계산 | mock이 정적 데이터라 컴포넌트 스코프 useMemo가 불필요 — 섹션 전환·리렌더 재계산 0회. 점령이 서버 상태가 되는 시점(실 API 전환)에 쿼리 구독 + useMemo로 옮기는 전제를 주석에 남김 |
| 2026-07-31 | MSG-277 | 결정(구조): 홈 좌측 패널 Escape 닫기 배선(입력 타깃 무시 계약 포함)을 `pages/map-home/ui/use-escape-close.ts` 뷰-레이어 훅으로 추출 — HomeCellDetailPanel·ThemeFeedPanel 공용 | 테마 피드에 Escape를 배선(추정 6)하면 동일 10줄 effect가 두 패널에 중복됨 — 두 번째 사용처 발생 시 추출 규칙 적용. MSG-252 리뷰 반영으로 고정된 "input/textarea 타깃 무시" 계약이 한 곳에 남아 패널 간 드리프트를 막음 (기존 Escape 스모크 2케이스 GREEN 불변) |
| 2026-07-31 | MSG-277 | 결정(2차 스펙 해석): 시간대 그래프의 집계 표본을 화면 피드와 동일한 `myVideos+otherVideos`로 통일 — 비테마 점령 상세는 내 영상만 집계 | AC 9(순수 함수 계약)는 `cell.videos` 합 단정, AC 10(화면)은 "N = 피드 표본 수"를 요구 — 비테마 상세는 피드가 내 영상만 나열하므로 cell.videos 전체를 집계하면 그래프 합과 "영상 N개 기준" 문구가 어긋남. 함수는 입력 중립(테스트는 cell.videos로 계약 단정)이고 표본 선택은 뷰가 피드와 공유해 문구-그래프 정합 유지 |
| 2026-07-31 | MSG-277 | 결정(3차 스펙 해석): 미니 패널 videoSrc 목 소스를 CC0 샘플 2종 순환으로 채택(스펙 허용 범위 2~3종의 하한) — `mdn.github.io/shared-assets`의 flower.mp4·friday.mp4, 3종 후보였던 sintel-short.mp4는 제외 | MDN cc0-videos 세트에서 실로드 확인(HTTP 206, video/mp4)된 mp4는 2종뿐이고 sintel은 CC-BY 3.0(Blender)이라 티켓의 "CC0 샘플" 정책 위배. webm 추가는 Safari 재생 신뢰성을 깎아 제외 — 순환 검증 테스트(mock-cells.test)는 "2종 이상"만 단정해 소스 추가에 열려 있음 |
| 2026-08-01 | MSG-289 | 결정: 목 videoId(number 전환, 추정 3)의 번호 체계를 "격자 순번 (index+1)*100 + 영상 순번"의 결정적 블록으로 채택하고, dex 수집 영상은 `cellById(gridId).videos[i].videoId`를 직접 참조 | 임의 난수는 스냅샷 비결정성을, 전역 연번은 격자-영상 대응의 가독성을 해침. dex가 cell의 videoId를 참조하면 "수집 영상 = 그 격자 영상의 일부" 불변식(② B3, mock-dex.test)이 번호 체계 중복 없이 구조적으로 보장됨 |
| 2026-08-01 | MSG-289 | 결정(스펙 해석): 명세 정렬은 entities의 스펙 대응 타입(CellVideo·CollectedCell·CollectedVideo·DexBadge·DexSummary)까지만 — 뷰 모델(DexView.collectedCellCount, HomeCellDetail.cellId, ThemeFeedSection.cellId)과 Cell.id 의미의 콜백 파라미터명은 유지 | 뷰 모델은 명세 대응이 아닌 FE 표시 계약이라 satisfies 검출 범위 밖이고, 이름까지 바꾸면 수술적 변경 원칙(스펙 소급 불가 diff)을 위반. 소스 필드 읽기(`summary.totalGridCount`)만 치환해 렌더 불변 유지 |
| 2026-08-03 | MSG-307 | 결정(A1 확정): NativeWind 4.2.6 + Tailwind v3.4(apps/mobile·ui-native 로컬 devDep)로 진행 — NativeWind 5는 5.0.0-preview.4로 여전히 preview라 스펙의 "preview/RC 금지" 조건에 걸림. preset v3 호환은 tailwindcss v3 CLI 컴파일 스모크로 검증(bg-primary=#0066CC, text-fm-title=15/19/600, shadow·opacity modifier 전부 생성) | 웹 v4.3.2와 버전 분리는 pnpm 워크스페이스의 패키지별 독립 해석으로 안전. preset은 theme.extend 순수 데이터라 양쪽 동일 값 |
| 2026-08-03 | MSG-307 | 결정: `@fillmap/tailwind-preset/native` 서브패스 신설 — 웹 preset을 그대로 포함(presets 중첩)하고 Tailwind v3에 없는 숫자 스케일 스텝(0.75·5.5·7.5·15·19.5·21·22·26·35·120)만 n×4px로 보충. metro `inlineRem: 16`으로 rem 기준도 웹과 통일 | v4는 숫자 스케일을 동적 생성하지만 v3는 고정 목록이라 ui-web과 동일한 클래스(w-22, size-7.5 등)가 v3에서 생성되지 않음. 임의값 우회는 토큰 규칙(1조) 위반이라 preset 보충이 정도. 웹 preset은 불변(웹 동작 무영향) |
| 2026-08-03 | MSG-307 | 결정: ui-native 프레스 피드백은 웹 `active:brightness-[0.86]` 대신 `active:opacity-80`(back 버튼은 웹과 동일 `active:bg-surface`) — RN은 CSS filter 미지원. transition-* 계열도 제거(RN 전환은 reanimated 의존이라 스캐폴딩 티켓에서 도입 안 함) | 시각 결과는 다르지만(밝기 감소 vs 투명도) RN 표준 프레스 관례이고 상태 표현력은 동일. 색 오버레이 방식은 variant별 색 파생이 필요해 토큰 규칙과 충돌 |
| 2026-08-03 | MSG-307 | 결정: ui-native 아이콘은 `lucide-react-native`(+react-native-svg 15.15.4, SDK 57 번들판) 채택, 색은 RN에 currentColor 상속이 없어 design-tokens `semantic` 값을 JS로 직접 전달 | 웹 ui-web이 lucide-react라 아이콘 세트 1:1 유지. hex 리터럴 금지(1조)는 토큰 상수 import로 준수 |
| 2026-08-03 | MSG-307 | 결정: on-device Storybook은 addon 없이 구성(ondevice-controls/actions 미도입), 스토리는 variant 매트릭스 렌더 방식. `storybook.requires.ts`는 sb-rn-get-stories로 생성해 커밋(CI typecheck가 앱 엔트리 import 체인으로 이 파일을 요구) | controls addon은 datetimepicker·slider 등 네이티브 peer 5종을 추가로 요구 — 스캐폴딩 티켓에서 과설계. 매트릭스 스토리는 ui-web AllVariants 관례와 동일한 검증력 |
| 2026-08-03 | MSG-307 | 결정: apps/mobile `reactCompiler: false`(SDK 57 템플릿 기본 true에서 변경), pnpm 격리 대응으로 `react-native-css-interop` 0.2.6을 앱 직접 의존성에 명시 | NativeWind 4.x는 jsxImportSource가 css-interop import를 모든 JSX에 주입하는데 React Compiler와의 조합은 검증 사례가 부족해 스캐폴딩 단계 리스크 차단. css-interop은 nativewind의 전이 의존성이라 pnpm 격리에서 앱이 직접 해석 불가(Metro 번들 실패 실측) → 명시 선언이 표준 해법 |
| 2026-08-03 | MSG-307 | 발견(사용자 인수 확인 중 실제 기기 테스트): Expo Go에서 on-device Storybook(`EXPO_PUBLIC_STORYBOOK=1`) 진입 시 앱 시작 직후 네이티브 SIGSEGV로 즉시 크래시 — `react-native-worklets` 네이티브 바이너리가 Expo Go에 고정 번들된 버전과 우리 설치 버전 간 불일치로 추정(버전 자체는 `expo install --check` 통과). 캐시 클리어·에뮬레이터 페이지 크기(4KB/16KB) 무관하게 100% 재현 | 03_verify_report의 "시뮬레이터 확인불가" 잔여 항목이 실제 실기기 테스트에서 크래시로 드러남 — 빌드·검증 단계는 시뮬레이터가 없어 잡지 못했던 결함. Expo Go는 범용 앱이라 네이티브 모듈 버전을 프로젝트가 통제할 수 없는 것이 근본 원인 |
| 2026-08-03 | MSG-307 | 결정: apps/mobile의 `android`/`ios`/`storybook` 스크립트를 `expo start --android`/`--ios`(Expo Go) 대신 custom dev client(`expo run:android`/`expo run:ios`, storybook은 `expo start --dev-client`)로 전환. `app.json`에 `android.package: "com.anonymous.fillmap"` 추가(dev client 빌드에 필수, expo prebuild 자동 생성값) | dev client는 프로젝트가 설치한 정확한 네이티브 모듈 버전을 그대로 앱에 포함해 Expo Go와의 버전 불일치 자체를 제거 — 실제로 크래시 완전 해소 확인(에뮬레이터 재현). Expo 공식 권장 워크플로(reanimated 등 네이티브 모듈 사용 시 dev client 전환)와도 일치. 패키지명은 추후 정식 배포 전 재검토 대상(현재는 자동 생성 placeholder) |
| 2026-08-03 | MSG-307 | 결정(코드 리뷰 반영): `Avatar`/`Thumbnail`(ui-native)의 `errored` state를 `src` 변경 시 리셋 — 리뷰가 제안한 `useEffect(() => setErrored(false), [src])`는 채택하지 않고, 렌더 중 `prevSrc` 비교로 즉시 `setErrored(false)`하는 패턴으로 대체 | 제안 코드 그대로 넣으면 `react-hooks/set-state-in-effect`(effect 내 동기 setState 금지)에 걸려 `pnpm lint` 실패(실제 재현 확인). 렌더 중 비교 패턴은 React 공식 문서 "Adjusting some state when a prop changes" 권장안이자 MSG-118에서 동일 규칙 때문에 이미 채택한 선례(`use-video-duration`/`use-highlight-selection`)와 동일 해법 — 저장소 전체에 일관 |
| 2026-08-03 | MSG-307 | 결정(코드 리뷰 반영): apps/mobile devDependencies에서 미사용 `@gorhom/bottom-sheet` 제거 | 전수 grep 결과 앱·ui-native 어디서도 import 0건, `bottom-sheet.tsx`는 순수 프레젠테이셔널 쉘(드래그/스냅 미구현)이라 필요 없음. DECISIONS.md에 도입 근거 기록도 없어 실수로 남은 의존성으로 판단 — 실제 드래그/스냅이 필요해지는 화면 티켓에서 재도입 권장 |
| 2026-08-03 | MSG-292 | 결정(MSG-307 방침 개정): apps/mobile에 vitest 러너 신설 — 대상은 순수 모델(RN import 없는 로직) 단위 테스트로 한정, RN 컴포넌트 렌더 테스트 인프라(jest-expo·RNTL)는 미도입. AsyncStorage는 vi.mock 인메모리 Map으로 격리, `allowOnly:false`(웹 선례), 루트 `pnpm test`에 mobile 포함 | 사용자 확정 4 — 온보딩 완료 영속(기준 5·6)이 apps/mobile 최초의 로직 레이어라 test-first 게이트가 필요해짐. MSG-307의 "RN은 vitest 대상 아님"은 로직 레이어 부재 시점의 방침이었고, 순수 모델로 한정하면 RN/Expo 모듈 변환 리스크 없이 웹과 동일한 TDD 하네스를 공유할 수 있다 |
| 2026-08-04 | MSG-292 | 결정(코드 리뷰 반영): 온보딩 저장소 접근 실패 처리를 리뷰 제안(뷰 레이어 `.catch`)이 아닌 모델 총함수 계약으로 채택 — `getOnboardingCompleted()`는 실패 시 내부 catch로 `false` resolve(온보딩부터 보여주는 안전 폴백), `setOnboardingCompleted()`는 best-effort(실패해도 resolve). 실패 재현 테스트 2케이스 선행(RED 확인) 후 수정 | 리뷰 지적 자체는 사실(get reject 시 스플래시 영구 멈춤, set 실패 시 온보딩 갇힘). 다만 뷰마다 `.catch`를 얹으면 호출부가 늘 때마다 방어를 반복해야 하고 누락 시 같은 결함이 재발 — 실패 의미론("판정 불가=미완료", "저장은 best-effort")은 도메인 지식이므로 모델이 소유하는 게 RN 경계 규칙(로직은 모델로)과 정합. 총함수가 되면 뷰는 무수정 |
| 2026-08-04 | MSG-292 | 결정(코드 리뷰 반영): ui-native `Button`에 `shape?: "rounded"\|"pill"`(기본 rounded — 기존 렌더 불변) 신설, 모서리 클래스를 컴포넌트 내부에서 단독 결정 — 온보딩 CTA의 `className="rounded-full"` 오버라이드를 대체 | ui-native `cx`는 tailwind-merge 없는 단순 join이라 내부 `rounded-md`와 오버라이드 `rounded-full`이 둘 다 남아 승자 비보장(검증 스크린샷 실측으로 rounded-md 승리 = Figma pill 불일치 확인). 충돌 병합기 도입 대신 충돌 자체가 없는 옵션 API로 해결 — 같은 티켓의 Dots `activeShape` 선례와 관례 통일 |
| 2026-08-04 | MSG-293 | 결정(코드 리뷰 반영): `startKakaoLogin` 스텁 계약 테스트 추가 — 리뷰의 "형식적 계약 테스트" 제안이 실행해 보니 진짜 RED였음: 스텁의 bare `__DEV__` 참조가 vitest node 환경에서 ReferenceError(Metro 주입 전역이라 RN 밖엔 부재). `typeof __DEV__ !== "undefined"` 가드로 수정 후 GREEN | "테스트 없이 진행"의 근거(러너가 미병합 MSG-292에만 존재)가 develop 병합(4e97d77)으로 무효화 — 순수 모델 레이어는 vitest 대상이라는 MSG-292 확정 범주에 정확히 해당. vitest config에 `define: {__DEV__}`를 넣는 대안은 기각 — 모델의 환경 전역 의존을 가리는 것이므로 함수 쪽을 환경-순수하게 만드는 게 "순수 모델" 경계와 정합 |
| 2026-08-04 | MSG-293 | 결정: 로그인 화면 상단 여백을 SafeAreaView + `pt-section`(64px 토큰)으로 처리 — Figma의 126px을 픽셀 고정하지 않음(스펙 오탐 방지 4). `apps/mobile/assets-env.d.ts` 신설 — expo/types(SDK 57)가 `*.png` 모듈 선언을 제공하지 않아 typecheck 실패, 미병합 MSG-292 브랜치의 동명 파일과 **바이트 동일**하게 작성해 병합 시 both-added 충돌이 자동 해소되게 함 | 126px은 상태바 포함 수치라 기기별 SafeArea에 위임하고 잔여분은 토큰 스케일 중 최근접(section 64)을 선택 — 임의값·픽셀 고정 금지 규칙 준수. png 선언을 다른 파일명·타입으로 만들면 MSG-292 병합 시 중복 ambient 선언(중복 default export) 타입 에러가 나는 지뢰가 됨 |
| 2026-08-04 | MSG-294 | 결정: 동등성 테스트(grid.parity)의 웹 원본 로드를 정적 import가 아닌 `new URL(…, import.meta.url)` 변수 경로 동적 import + `typeof mobileGrid` 캐스트로 구현 | 정적 import 시 웹 grid.ts의 타입 의존 체인(./cell → `@/shared/api/…`)이 모바일 tsconfig의 `@/*` paths로 잘못 해석돼 typecheck가 깨짐. 변수 경로면 TS가 모듈 해석을 생략하고, vitest는 런타임에 정상 로드(순수 모듈이라 실행 의존 없음). 파일 이동 시 테스트가 깨지는 드리프트 감지 특성은 유지(스펙 리스크 4) |
| 2026-08-04 | MSG-294 | 결정: `buildVisibleCells`에 줌 게이트(`GRID_MIN_ZOOM=15`, 미만이면 빈 배열) 포함 — AC 3 문면에는 없는 파라미터 | 광역 줌 아웃 시 100m 셀이 수천 개로 폭증해 SDK 폴리곤 렌더가 멈추는 실발생 경로 차단. 웹 `grid-overlay.ts`의 동일 임계(GRID_MIN_ZOOM=15) 선례를 따름 — 스펙이 "웹 grid-overlay 로직 참조"를 명시하므로 스펙 밖 기능이 아닌 참조 로직의 일부로 판단 |
| 2026-08-04 | MSG-294 | 결정: 지도 홈의 "직전 카메라" 추적을 ref가 아닌 `onCameraIdle`(SDK 정착 이벤트) → state로 구현 | 최초 구현(매 프레임 onCameraChanged → ref 기록)은 react-hooks/refs가 제스처 콜백의 렌더 중 ref 접근 가능성을 지적. eslint-disable 우회 대신 idle 시점만 state 반영으로 재설계 — 재렌더는 이동 종료 시 1회뿐이고 "직전 지도 영역"(AC 13) 의미에도 정착 카메라가 더 정합 |
| 2026-08-04 | MSG-294 | 결정: 네이버 지도 키 env 변수를 스펙 정본(`EXPO_PUBLIC_NAVER_MAP_CLIENT_ID`) + 기존 변수 폴백(`EXPO_PUBLIC_NAVER_MAP_NCP_KEY_ID`) 이중 판독으로 구현 | 사용자가 스펙 확정 전 `.env.local`에 후자 이름으로 키를 이미 발급·저장해 둠(리스크 1의 "사용자 발급 대기"가 실은 완료 상태). 정본 이름만 읽으면 이미 있는 키가 무시돼 지도 AC 검증이 막힘 — 폴백 한 줄로 즉시 동작시키고, 이름 이관 후 폴백 제거 가능하게 주석 명시 |
| 2026-08-04 | MSG-294 | 결정(2차): HomeSheet 단계 제어를 부모 prop 동기화(useEffect)가 아닌 시트 내부 상태 + ref 명령(`snapTo`·`restoreIfHidden`)으로 설계 | react-hooks/immutability(v7)가 useEffect에서 쓰인 reanimated 공유값(`translateY`)을 제스처 콜백에서 수정하는 것을 금지 — disable 우회 대신 effect 자체를 제거하는 재설계. 부모 주도 전환(홈 복귀 리셋·홈 탭 재탭)은 grid-map `moveTo`와 동일한 명령형 핸들 관례로 통일, 드래그·전체 보기·명령 모두 `goToStage` 단일 경로를 지남 |
| 2026-08-04 | MSG-294 | 결정(2차): 시트-그리드 제스처 공존을 `simultaneousWithExternalGesture(ref)`가 아닌 `Gesture.Native()` 게스처 객체 참조로 구현 | ref 전달 방식은 react-hooks/refs(렌더 중 ref 접근 가능성) 지적 — RNGH 공식 대안인 Native 게스처로 그리드 스크롤을 RNGH 체계에 편입하면 ref 없이 동일 관계 선언 가능. 그리드 최상단 복귀 후 아래 드래그=시트 축소는 스크롤 오프셋 공유값 + 시작점 재보정(rebase)으로 처리, 헤더 존(y≤92)에서 시작한 드래그는 가드를 우회해 항상 시트를 움직임 |
| 2026-08-04 | MSG-294 | 결정(2차): react-doctor `rn-no-scrollview-mapped-list` 2건(카테고리 칩 4종·피크 썸네일 mock 6건)은 FlatList 전환 대신 규칙 문서의 suppress-when 조항 근거로 `react-doctor-disable-next-line` 억제. `runOnJS`→`scheduleOnRN`(rn-reanimated-4-use-worklets-scheduler)은 정본 수정 적용 | 규칙 문서가 "10행 미만 고정 배열은 가상화 오버헤드가 마운트 비용을 초과하므로 억제하라"고 명시 — 두 목록 모두 고정 배열(칩 4·mock 6)이라 정확히 해당. 시트 2열 그리드는 판정 자체가 없고(mock 6건) API 연동 시 FlatList 전환을 재검토. 억제는 pre-commit 훅(react-doctor --staged) 게이트 통과의 전제 |
| 2026-08-04 | MSG-294 | 결정(3차 검증 환류): `sortGridVideos`의 `Array.prototype.toSorted`를 스프레드 복사+`sort`로 교체 (2차 검증이 잡은 실버그 — Hermes 0.17/RN 0.86 미구현으로 홈 최초 렌더 크래시). RED 사이클 없이 기존 테스트 6케이스 무수정 그린을 완료 조건으로 함 | 엔진(Hermes) 격차는 Node 기반 vitest로 재현 불가 — 보존 단정 예외에 준함(2차 검증 리포트 재작업 지시 1). 동작 계약(원본 불변·새 배열 반환)은 동일. react-doctor `js-tosorted-immutable`은 사유 주석과 함께 억제. 3차 검증에서 실기기 재배열 동작 확증 |
| 2026-08-04 | MSG-294 | 결정(4차 리뷰 반영): 시트 스냅 스프링을 물리 파라미터(damping 28/stiffness 260) 대신 duration 기반 임계 감쇠(`{ duration: 220, dampingRatio: 1 }`)로 교체 — 스펙이 허용한 두 대안(파라미터 튜닝 vs withTiming) 중 전자를 duration 표기로 채택 | 실기기 피드백 "정착이 굼뜨다"(AC 11 4차). dampingRatio 1은 정의상 오버슈트가 없고 duration이 체감 시간을 직접 지정해 튜닝 반복이 불필요. withTiming 전환은 드래그 릴리즈 속도감(스프링 질감)을 잃어 기각. reanimated 4.x 정식 SpringConfig |
| 2026-08-04 | MSG-296 | 결정: 선택 격자 점선 강조를 스펙의 "폴리라인 pattern prop" 대신 dash 세그먼트 파생(`buildDashedRectOutline` 순수 함수, 변당 12칸 슬롯 × 4변의 2점 폴리라인)으로 구현 | SDK 소스 확인 결과 `NaverMapPolylineOverlay`(v2.9.0)가 `pattern` prop을 인터페이스에 선언만 하고 네이티브 컴포넌트로 전달하지 않아(전달 누락 버그) prop 방식은 실선으로 조용히 오동작. patch-package 도입 대신 순수 함수 파생이 테스트 가능하고 SDK 업그레이드 시 되돌리기 쉬움 |
| 2026-08-04 | MSG-296 | 결정: 모바일 vitest에 `@` → 웹 src 별칭 추가 (parity 테스트 전용) | `formatViewCount` 웹 원본(features/explore/model/cell-detail.ts)이 내부에서 `@/shared/format` 별칭 re-export를 포함해 동적 import만으로는 해석 불가. 모바일 소스는 `@` 별칭을 쓰지 않아 자체 해석에 영향 없음 — grid.parity.test.ts의 "별칭 의존 파일 회피" 제약을 별칭 매핑으로 해소 |
| 2026-08-04 | MSG-297 | 결정: 검색 복귀 지도 이동을 홈 라우트 params(lat·lng)로 전달하되 `ts`(요청 시각) 파라미터를 추가 — 같은 구를 연속 선택해도 params가 달라져 이동 effect가 재발화. 초기 `resolveMapCenter()` 이동은 검색 이동 발생 시 ref 가드로 건너뜀 | expo-router `navigate`는 동일 params면 리렌더/재발화가 없어 "지도를 옮긴 뒤 같은 구 재검색" 흐름에서 카메라가 안 움직이는 결함이 됨. ref 가드는 스펙 리스크 2(카메라 경합 — 늦게 도착한 위치 조회가 목적지 이동을 덮는 문제)의 최소 해법 |
| 2026-08-04 | MSG-297 | 결정(리뷰 반영): 구 매칭(matchRegion)을 "부분 일치 첫 매칭(find)"에서 "2자 이상 + 유일 매칭(filter)일 때만 확정"으로 좁힘 — RED 테스트("구"·"부") 선행 후 수정 | 웹 searchCells는 후보 집합 필터일 뿐이지만 모바일은 매칭이 곧 자동 지도 이동 확정이라 의미가 다름 — "구" 입력이 8개 구 전부에 걸리며 첫 매칭(부산진구)으로 조용히 튕기는 결함(PR #37 리뷰). 정확 일치(===)는 "해운대"류 구 생략 자연 입력을 죽여 기각 |
| 2026-08-05 | MSG-298 | 결정: 테마 검색어 바를 feature 로컬 조합 대신 ui-native `SearchBar` 옵트인 확장(`onBack`·`onClear`)으로 구현 — 미지정 시 렌더 경로 완전 불변(기존 onSearch 분기 유지) | 스펙이 두 대안 중 빌더 재량으로 위임. MSG-114 `onSearch` 확장 선례와 동일 패턴 — 로컬 재구현은 검색바 시각 변경 시 이중 수정 지점이 됨. 기존 사용처(홈·검색 화면)는 새 props 미지정이라 회귀 없음 |
| 2026-08-05 | MSG-298 | 결정: 경로추천 번호 웨이포인트를 `NaverMapMarkerOverlay` 커스텀 뷰 children으로 구현 (스펙 리스크 1 스파이크: 소스 레벨 확인) | v2.9.0 소스에서 children이 네이티브 컴포넌트로 실제 전달됨을 확인 — pattern prop(선언만 있고 미전달, MSG-296)과 달리 전달 경로가 존재. Android 서브뷰 평탄화 대비 `collapsable={false}` 지정. 런타임 확증은 검증 단계 시뮬레이터 몫, 실패 시 caption 텍스트 폴백 |
| 2026-08-05 | MSG-298 | 발견: 기존 `GridVideoCard`의 `h-25`·`size-6.5` 클래스가 mock 컴파일 검증 결과 미생성(무효) — v3 스케일·nativeSpacingSteps 어디에도 없는 스텝 (Thumbnail 기본 size-16으로 폴백 렌더 중) | tailwind CLI로 mobile 설정 실컴파일해 확인. 범위 밖 기존 결함이라 수정하지 않고 보고만 (수술적 변경). 신규 사용 스텝(1.75·50)은 nativeSpacingSteps에 등재해 같은 함정 회피 |
| 2026-08-05 | MSG-298 | 결정: 상대시간 mock(uploadedAt 일부)을 모듈 로드 시점 기준 `hoursAgo()`로 동적 생성 | 고정 ISO는 검증 시점에 따라 "N시간 전"이 "M월 D일"로 드리프트해 AC 15 상대시간 케이스를 화면에서 확인 불가. 표시용 mock 한정 비결정성 — 테스트는 포맷 함수에 now 주입으로 결정적 유지 |
| 2026-08-05 | MSG-298 | 결정(재작업 1): 테마 선택 상태를 화면 useState에서 feature 로컬 모듈 상태(`theme-selection.ts` — 모듈 스코프 + `useSyncExternalStore`)로 이전. zustand 등 신규 의존성·AsyncStorage 미도입 | 검증 실측으로 루트 Stack + `router.navigate` 구조에서 홈 스크린이 탭 왕복마다 재마운트됨이 확인(AC 4 실패, 확정 4 전제 반증 → 스펙 수정판). 재마운트를 넘는 최소 보관 위치가 모듈 스코프 — 같은 구조의 다른 홈 로컬 상태(정렬 칩 등)도 탭 왕복 시 리셋되므로 유지가 AC로 요구되면 동일 패턴 적용 |
| 2026-08-05 | MSG-302~305 | 결정(스펙 승인): 업로드 플로우 신규 의존성 expo-image-picker + expo-video 도입 — 갤러리·카메라 모두 image-picker 단일 처리(expo-camera 미도입), 304 재생은 선택 원본을 "블러본 mock"으로 실재생(시각 블러 효과 없음). 305 하이라이트 카드는 선택 구간 사유 동적 반영, 제목 미표시는 Figma 준수 유지 | 사용자 승인 게이트 결정 3건. 단순성 우선 — 촬영·선택이 한 라이브러리로 충분하고, 실재생은 티켓 문구("재생할 수 있다") 충족의 최소 구성 |
| 2026-08-05 | MSG-302~305 | 결정(트레이드오프): ui-native Chip 텍스트 색을 Figma의 파란색 대신 기존 다크 고정 유지, 배경만 `bg-primary/10` 매칭 | 공용 컴포넌트 API(텍스트 색 variant) 변경은 이번 스펙 밖 — 수술적 변경 원칙. 검증자 타당 판정. 파란 텍스트가 필요한 화면이 늘면 variant 신설 티켓으로 승격 권장 |
| 2026-08-05 | MSG-302~305 | 결정: `check:duplication` 게이트 통과를 위해 nose.baseline.json을 `--write-baseline`으로 재작성 — 신규 의도 중복 2건 등재, 해소 가능 1건은 테스트 헬퍼 추출로 코드 해소 | 전면 재직렬화 특성상 무관한 기존 하위 family 22건이 함께 등재됨(diff가 큰 이유) — 도구 특성이며 코드 변경 아님, 검증 리포트에 참고 기록 |
| 2026-08-05 | MSG-306 | 결정: 모바일 MOCK_PROFILE의 nickname·streakDays를 도감 mock 참조 대신 리터럴로 두고, 정합은 mock-profile.parity.test의 단정으로 고정 | 웹은 entities/dex(같은 레이어) 참조로 정합했지만 모바일 도감 mock은 features/dex 소속 — entities→features 참조는 FSD 역방향 의존. 테스트 정합이 참조 정합과 같은 드리프트 감지를 레이어 위반 없이 제공 (스펙 "참조 정합 여부는 빌더 판단") |
| 2026-08-05 | MSG-306 | 결정: ui-native ModalCard 딤 탭 닫기를 `onOverlayPress` 옵셔널 prop으로 확장 — 지정 시에만 오버레이가 Pressable로 승격되고 카드가 responder 선점, 미지정 시 기존 View 렌더 그대로 | 기존 사용처(현재 앱 내 0곳, 스토리만) 회귀 0 보장이 목표 — 조건 분기라 기본 경로의 렌더 트리가 변하지 않는다. 카드 `onStartShouldSetResponder`는 버튼 등 더 깊은 자식이 responder 협상에서 우선하므로 카드 안 인터랙션 불변 |
| 2026-08-05 | MSG-306 | 결정: 편집 화면 아바타 88px는 웹 MSG-125 선례대로 `size="lg"` + `className="size-22"` 오버라이드 | cx는 단순 join이지만 tailwind 생성 CSS에서 size-22가 size-12보다 뒤에 방출돼 상향 오버라이드는 승자가 결정적 — 하향 오버라이드(size-7 등)는 이 방식이 통하지 않으므로 주의. nose 재등재는 웹 미러 4파일의 의도 중복 (parity 선례) |
| 2026-08-05 | MSG-306 | 결정(리뷰 반영): 닉네임 trim을 UI handleSave가 아니라 profileStore.save(단일 쓰기 경로)에 배치 — 공백 선두 닉네임이 avatarFallback 이니셜을 빈 칸으로 만드는 결함의 재현 테스트 선행(RED→GREEN). canSaveProfile은 원본 기준 판단 유지(웹 계약 미러) | 스토어 배치가 향후 호출자 전부를 보호하고 vitest 단정 가능. 리뷰가 잡은 실결함 — 웹은 비영속이라 드러나지 않던 신규 엣지. 함께 반영: ui-native Switch accessibilityLabel 옵셔널 확장(래퍼 View 방식은 switch role 중첩 부작용으로 기각), ReactNode 명시 import 관례 정렬 |
| 2026-08-06 | MSG-317 | 결정(사용자 확정): 영상 0개 격자(미등재 셀 포함)는 지도 홈 격자 탭에서 상세 진입 자체를 차단(no-op, `hasCellVideos` 게이트) — MSG-296 질문 2-a(미등록 셀도 빈 상태로 진입)의 부분 번복. 상세 화면의 빈 상태 분기는 직접 라우트 진입 안전망으로 존치 | 빈 격자에서 더보기 메뉴(수정·삭제·신고)가 대상 없이 노출되는 엣지를 진입 차단으로 원천 제거 — 웹 상세 선택 no-op 관례 정합. 삭제로 목록이 빈 뒤의 세션 내 상태는 기존 빈 상태 분기가 처리 |
| 2026-08-06 | MSG-317 | 결정: `check:duplication` 게이트 통과를 위해 nose.baseline.json `--write-baseline` 재등재 — 신규 1건은 report-types 무결성 테스트의 golden 값 리터럴(소스 배열과 동일 문면) | golden 값 테스트는 리터럴 중복이 목적 자체(상수 공유 추출 시 소스=소스 비교의 동어반복이 됨) — mock-badges 무결성 테스트와 동일 관례. MSG-302~305 재등재 선례의 도구 특성(전면 재직렬화)도 동일 |
| 2026-08-06 | MSG-317 | 결정(리뷰 반영, AC 8 재정의): `deriveCellDetail` 빈 상태 판정을 표본 목록(`remaining.length`)에서 전체 잔여(`videoCount−제외 수`) 기준으로 수정 — 재현 실패 테스트 선행(RED→GREEN). 미리보기는 표본 기준 유지(표본 소진 시 null), `hasCellVideos`도 videoCount 기준으로 정합(mock상 동작 차이 없음 — 의미 정합) | 리뷰가 잡은 실결함: videos는 최근 표본(서면 138개 중 5개)이라 표본 소진 시 통계 "133개"인데 isEmpty=true("영상이 없어요")가 되는 모순. 판정과 통계를 동일 데이터 기준으로 통일. 제외로 전체 잔여 0이 되는 경로는 mock에 전체==표본 셀이 없어 테스트 불가 — 전체 0 격자(범천)·미등재 셀 분기 테스트로 커버 |
| 2026-08-06 | MSG-317 | 결정(사용자, AC 16): 지도 홈 우하단 FAB(+, "기록하기") 제거 — 바텀 내비 중앙 카메라 버튼(→ /upload)과 기능 중복. ui-native Fab 컴포넌트는 존치(다른 사용처 없음 확인, 컴포넌트 삭제는 범위 밖) | MSG-277 계열 스펙 요소였으나 MSG-302에서 바텀 내비 카메라가 업로드 진입점이 되며 중복 발생 — 진입점 단일화(기획 결정). 내 위치 버튼·컨테이너 레이아웃은 유지 |
| 2026-08-06 | MSG-317 | 결정(AC 17): `resolveMapCenter`에 신규 조회 3초 타임아웃 레이스 + 타임아웃·실패 시 getLastKnownPositionAsync → 서면 순 폴백 — 총함수 계약·권한 거부 폴백·성공 경로 불변(기존 테스트 5건 무수정 통과). last-known 선반환 대신 신규 조회 우선을 택함 | 에뮬레이터 재현: `getCurrentPositionAsync({})`가 위치 픽스 없는 환경에서 무기한 대기 → 내 위치 탭이 수 분간 무반응(시스템 lastKnown은 존재). 신규 조회 우선(≤3s)이 신선도를 지키면서 응답 상한을 보장 — last-known 즉시 반환은 stale 좌표로 이동한 채 갱신 경로가 없어 기각 |
| 2026-08-06 | MSG-317 | 결정(리뷰 반영, AC 17 2차): `resolveMapCenter`의 진행 중 신규 위치 조회를 모듈 레벨 single-flight로 공유 — in-flight 프라미스 재사용, settle 시(성공·실패 모두) 클리어, 호출자별 타임아웃 레이스는 공유 프라미스 위에서 수행. 재현 실패 테스트(연타 2회 → 발행 1건 단정) RED→GREEN. `watchPositionAsync`+`remove()`(진짜 취소) 전환은 기각 | 리뷰가 잡은 실결함: getCurrentPositionAsync는 취소 API가 없어 타임아웃 후에도 네이티브 요청이 pending으로 남음 — 픽스 없는 환경에서 내 위치 연타 시 미해결 요청이 탭 횟수만큼 누적. single-flight로 네이티브 요청 최대 1개·늦은 픽스는 다음 탭이 재사용. watch 전환은 이전 티켓 커밋 테스트 5케이스의 mock 대상 교체를 강제해 이전 티켓 테스트 불변 원칙 위배 |
| 2026-08-06 | MSG-323 | 결정: react-doctor에 `ignore.files: apps/web/src/shared/api/generated/**` 추가 — 규칙 2종 전역 비활성 대신 경로 제외(최협소 제어). pre-commit이 생성물 지적 2건(await-in-loop·barrel import)으로 커밋 차단한 건의 해소 | 증거 기반 판정: await-in-loop은 오탐(setAuthParams의 인증 스킴 순차 시도 — Promise.all 전환 시 의미 변경), barrel import는 수정 불가(hey-api 생성 구조, 수기 수정 금지·재생성 복원·CI 드리프트 검사가 잡음). eslint·oxfmt·check:duplication이 generated를 제외하는 기존 정책과 정합 — 두 규칙은 수기 코드에 계속 유효. MSG-307 도입 시점엔 generated가 타입 선언뿐이라 미발현 |
| 2026-08-06 | MSG-323 | 결정(리뷰 반영 2): react-doctor 설정을 `apps/web/doctor.config.json`으로 미러 — CI 액션(project:"*")은 워크스페이스 프로젝트 디렉토리 기준으로 스캔해 루트 설정을 읽지 않음을 로컬 재현으로 확증(apps/web에서 실행 시 생성물 지적 2건 재검출). 루트 설정은 pre-commit(--staged, 루트 실행)용으로 존치 | 설정 이원화는 스캔 컨텍스트가 실제로 두 개(루트 훅·프로젝트 CI)인 데서 오는 구조적 요구 — 경로만 각 기준으로 다르고 의도는 동일(generated 제외). 액션 입력(directory·project)으론 해소 불가(루트는 React 프로젝트 미검출) |
| 2026-08-06 | MSG-324 | 결정: 401 재발급 파이프라인(auth-pipeline)의 원요청 재시도를 Ky 훅 재진입 없이 raw fetch + 전송 전 클론(WeakMap 보관)으로 수행, reissue SDK 호출은 동적 import | 재시도가 httpClient를 다시 타면 재시도의 401이 재발급을 또 트리거하는 루프 여지가 남음 — raw fetch는 구조적으로 재진입 불가(auth 제외 목록과 이중 안전장치). 클론은 fetch가 POST body를 소모해 재전송 불가한 문제의 해소(키가 원 Request라 응답 후 자동 회수). 동적 import는 sdk.gen→client.gen→client-config→http-client→auth-pipeline 정적 순환의 TDZ 회피 (MSG-323 client-config/error-interceptor 분리 선례) |
| 2026-08-06 | MSG-324 | 결정: 인증 훅(use-auth-mutations)은 생성 mutation 옵션의 mutationFn을 위임 호출하는 래핑 패턴 — 변수 고정(dev oid 상수·logout 무인자)·봉투 언랩이 필요한 훅만 래핑하고, 시그니처 그대로인 useEmailLogin은 옵션 spread | 테스트 계약이 mutate() 무인자 호출·언랩된 data를 요구하는데 생성 옵션의 TVariables는 Options<XxxData> 고정 — 옵션 재구성보다 mutationFn 위임(TanStack v5.101부터 필수 2인자 context도 그대로 전달)이 생성물 비수정 원칙과 양립하는 최소 간접화 |
| 2026-08-06 | MSG-324 | 결정(검증 환류): `check:duplication` 신규 6 families 중 봉투 응답 헬퍼 교차 1건만 `apps/web/src/test/envelope-response.ts`로 추출(두 테스트 GREEN 유지)하고, 나머지는 `nose --write-baseline`으로 베이스라인 재등재 — `use-auth-mutations.test.tsx` 내부 3건은 케이스별 renderHook+mutate+waitFor 스캐폴딩, `pages/dex` 스모크 2건은 신규 테스트 추가로 클러스터링이 이동한 부수 발현(파일 무변경) | 봉투 헬퍼는 두 파일에 동일 정의가 실재해 두 번째 사용처 규칙상 추출 대상. 반대로 per-scenario setup 통합은 nose 자신의 권고("consolidate only a genuinely shared fixture/helper, not per-scenario setup")에 반하고 케이스별 단정 가독성을 해침. dex 2건은 코드 변경으로 해소 불가. 베이스라인 전면 재직렬화로 무관 family가 함께 등재되는 것은 도구 특성(MSG-302~305·MSG-317 선례) |
| 2026-08-07 | MSG-324 | 결정(실서버 검증 환류): `authBeforeRequest`가 auth 엔드포인트(login·signup·reissue·dev/social-login)에는 액세스 토큰을 주입하지 않도록 수정 — afterResponse의 제외 목록과 대칭을 맞춤. 재현 테스트 5케이스 RED→GREEN(4건은 미주입 단정, 1건은 logout 계속 주입 단정) | 실서버 실측으로 발견: reissue에 Authorization이 붙으면 서버가 그 토큰을 검증해 401(2401)로 거부한다(헤더 없음 200 / 무효 토큰 401 / 유효 토큰 200). 재발급이 필요한 시점의 액세스 토큰은 정의상 무효라, 붙이면 재발급이 필요한 바로 그 순간에만 실패 — 실 로그인 도입 시 전 사용자가 1시간마다 재로그인으로 튕겼을 결함. vitest 목은 헤더를 검사하지 않아 미검출, 최초 브라우저 검증은 dev 엔드포인트의 쿠키 미발급과 증상이 겹쳐 가려졌다. logout은 Authorization 필수라 제외 목록 밖에 두는 비대칭을 테스트로 고정 |
| 2026-08-07 | MSG-324 | 결정(리뷰 반영): auth 제외 목록에 `/api/auth/oauth/{provider}` 추가 — provider가 경로 파라미터라 정확 일치 Set으로는 안 걸려 접두사 매칭(`AUTH_ENDPOINT_PREFIXES`) 신설. 재현 테스트 2건 RED→GREEN | Codex 리뷰 지적. 실 소셜 로그인 경로에 방금 고친 것과 동일한 결함이 남아 있었다: ① 무효 토큰이 로그인 요청에 주입되고 ② 401(2421 id_token 검증 실패)이 재발급을 트리거해 "로그인 실패"가 "세션 만료"로 오표현된다. MSG-324는 이 경로를 배선하지 않지만 생성 SDK에 존재해 실 OIDC 티켓 착수 시 즉시 발현할 잠복 결함 |
| 2026-08-07 | MSG-324 | 결정(리뷰 반영): 로그인 성공 시에만 `queryClient.clear()`, 로그아웃 시에는 비우지 않음 — React Doctor의 mutation-invalidation 경고 3건 중 로그인 2건만 채택하고 signup·logout은 기각 | 교차 사용자 노출(A 로그아웃 → B 로그인 시 A의 캐시)은 로그인 쪽 clear()로 완결된다(다음 세션은 항상 빈 캐시에서 시작). 로그아웃에서 비우면 화면에 남은 프로필 패널이 로딩→401 오류로 떨어져 "화면 전환 없이 패널에 머문다"는 MSG-124 F2 계약이 깨진다 — 스모크 테스트가 검출. signup은 자동 로그인을 하지 않아(추정 7) 인증 상태 불변이므로 대상 아님 |
| 2026-08-07 | MSG-324 | 결정(리뷰 반영): 재시도 fetch의 throw에 `onSessionExpired`를 붙이자는 제안 기각 — 오류를 그대로 전파하고 그 의미론을 테스트 2건으로 고정 | 이 시점엔 재발급이 이미 성공해 새 토큰이 저장된 상태로 세션은 유효하다. 취소(언마운트 AbortError)나 일시적 네트워크 장애로 로그아웃시키면 오히려 해롭다 — 취소는 Query가 취소로, 네트워크 오류는 shouldRetryQuery 재시도 대상으로 다뤄야 한다. 지적의 유효한 부분(미커버 경로)은 테스트 추가로 해소 |
| 2026-08-07 | MSG-324 | 결정(리뷰 반영): 로그인·로그아웃 버튼에 `disabled`/`aria-busy` pending 가드 추가. 실패 피드백 UI는 후속 화면 티켓 | 연타 시 중복 요청(로그인은 세션 중복 생성) — 검증 리포트가 관찰로만 남긴 항목을 리뷰가 재지적해 승격. `nose.baseline.json` 재등재는 ProfilePanel 2줄 편집으로 기존 에러상태 4중복 family의 줄 번호만 밀린 결과(코드 변경 아님) — 4곳 공용 ErrorState 추출은 타 티켓 소유 페이지 4개를 건드려 수술적 변경 원칙에 어긋나므로 승격 후보로 환류 |
| 2026-08-07 | MSG-324 | 결정(리뷰 반영): react-doctor `query-mutation-missing-invalidation` 경고 2건(useSignup·useLogout)은 억제하지 않고 그대로 둔다 — 의도된 결정임을 JSDoc과 테스트로만 표기 | 억제 수단이 전부 부작용을 동반한다: ① `eslint-disable-next-line react-doctor/...`는 react-doctor가 존중하나(실측 확인) eslint가 "Definition for rule not found"로 에러 → lint 게이트 실패, ② react-doctor 자체 지시자(`doctor-disable-next-line`)는 미지원(실측), ③ 패키지가 eslint 플러그인을 내보내지 않아 규칙 등록 불가(exports는 cli·api뿐), ④ config `rules`에서 off는 전역 비활성이라 MSG-325~329의 진짜 누락을 놓친다(MSG-323의 "최협소 제어" 원칙 위배). 경고는 pre-commit `--blocking warning`을 막지 않음(실측 exit 0) — 비용은 PR 코멘트 노이즈뿐이라 수용 |
| 2026-08-07 | MSG-325 | 결정: FE 격자 계산 규칙을 서버 정본(`GridConstants`: lat 0.0009 · lng 0.00115, 원점 (0,0))으로 교체 — 웹 `entities/cell/model/grid.ts`와 모바일 복제본을 함께 바꾸고 `encodeGridId`·`decodeGridBounds` 신설. `GRID_CELL_METERS`·`GRID_REF_LAT`는 파생 근거가 사라져 제거 | BE 레포 대조로 확인: `.claude/rules/glossary.md`("격자 계산 규칙")가 **FE·BE·모바일이 이 상수를 공유**한다고 규정하는데 FE만 자체 원점(부산 bbox SW)·자체 스텝을 쓰고 있었다. 서면 기준 서버 격자와 남북 19.4m·동서 36.9m 어긋나(셀 한 변의 20~37%) 색칠 격자가 격자선과 맞물리지 않는 상태 — 연동 전 선결 조건. 셀은 정사각형이 아니게 된다(부산 100.2m × 104.7m, 글로서리 명시) |
| 2026-08-07 | MSG-325 | 결정: `/api/grids` 요청 게이트를 bbox span 상한(0.5도)만으로 판정하고 줌 게이트(`GRID_MIN_ZOOM`)는 넣지 않음 — 스펙 기준 3의 "줌 미만에서도 요청하지 않는다"를 실측 근거로 수정 | 줌 게이트를 넣으면 zoom < 15에서 클러스터 마커(MSG-264)가 집계할 점령 격자를 못 받아 빈다 — 이전 티켓 AC 회귀. 실측상 span이 0.5도를 넘는 건 zoom ≤ 11(부산 전역 이상)뿐이라 클러스터 유효 구간(12~14)은 span 게이트만으로 살아 있다. 부산 전역 bbox는 경도 span 0.5106도로 상한 초과 — 한 번에 조회 불가임을 테스트로 고정 |
| 2026-08-07 | MSG-325 | 발견: 명세 서술과 OpenAPI 스키마 불일치 — `/api/grids/{gridId}/cover`·`/api/regions/stats/by-grid`는 후보 없음·무귀속 시 `200 + data: null`을 준다고 description에 적혀 있으나 스키마에 nullable 표기가 없어 생성 타입이 non-null. FE는 `?? null`로 흡수 | 타입만 믿고 `data`를 그대로 쓰면 런타임 null에서 화면이 깨진다. 백엔드에 nullable 표기 요청 후보(환류 대상) |
| 2026-08-07 | MSG-325 | 결정: 오버레이 셀 id 체계를 서버 격자 id로 통일 — 목 테마 셀도 자기 `center`를 `encodeGridId`한 값을 id로 쓴다(`buildHomeOverlayCells`의 3번째 인자를 `OccupiedCell[]` → `occupiedGridIds: string[]`로 변경). 목 데이터 파일은 무수정 | 점령 격자가 API(서버 id)에서 오는데 테마 셀만 목 라벨("A-14")을 쓰면 교집합(빗금) 판정·`excludeSectionCells` 중복 제거가 항상 공집합이 된다. 좌표에서 결정론적으로 파생하므로 목 id 체계를 건드리지 않고 두 층을 같은 키로 맞출 수 있다 |
| 2026-08-07 | MSG-325 | 결정: 셀 상세에서 목 파생을 제거하고 API가 주는 것만 표시(결정 B①) — `upload-hours.ts`+테스트·`CellHourChart.tsx` 삭제, `CellActionRow`의 "몰아보기" 제거, 표시 모델에서 영상 목록·전역 지표(조회·담수율)·마지막 업로드 시각 제거 | 명세 응답에 대응이 없거나 격자 상세 연동 티켓의 제외 범위다. 남긴 항목은 제목(zone 라벨)·내 점령 배지·내 영상 수·행정동 줄·전역 대표 영상 1건. 삭제한 모듈은 그 티켓에서 영상 목록과 함께 복구한다(git 이력 보존) |
| 2026-08-07 | MSG-325 | 결정: 기준 15(로딩·실패·빈 3상태)를 새 UI 없이 기존 `CellSummaryPanel`의 상태 슬롯 재사용으로 이행 — 점령 격자 쿼리의 isLoading·isError를 패널의 기존 로딩·에러(재시도) 분기에 합류시킴 | Figma 지도 홈에 "이 지역 정보"를 대표하는 자리가 이 패널이고 3상태 디자인이 이미 있다. Q1(상단 지역명·수집률)을 디자인 부재로 범위에서 뺀 것과 같은 기준 — 디자인에 없는 UI를 새로 만들지 않는다 |
| 2026-08-07 | MSG-325 | 결정(리뷰 반영): 셀 탭 상세 열림 판정의 테마 id 집합을 `themeCellGridIds`(좌표 유래 서버 gridId)로 파생 — `themeCells.map(c => c.id)` 직접 사용 제거. 재현 테스트 2건 RED→GREEN(① 목 라벨 id로는 판정이 false임을 고정, ② 판정 id 집합 ≡ 게시 id 집합) | Codex 리뷰가 잡은 실결함: 오버레이 게시 id는 좌표 유래 서버 gridId로 통일했는데(기준 7) 탭 판정만 `ThemeCell.id`(목 라벨 "A-14")를 써서, 목 소스 3테마(지역축제·팝업스토어·경로추천)는 강조 셀을 눌러도 상세가 영영 안 열렸다. 핫구역은 API 셀의 id가 이미 gridId라 증상이 가려졌다 — 한쪽 소스만 검증하면 놓치는 유형 |
| 2026-08-07 | MSG-325 | 결정(리뷰 반영 2): 게시 목록과 탭 판정 id를 단일 필터 소스(`renderableThemeCells` = 부산 경계 내부 테마 셀)에서 파생 — `buildHomeOverlayCells`·`themeCellGridIds`가 같은 함수를 거치게 해 "판정 집합 ≡ 게시 집합"을 구성상 보장. 바다 셀 케이스를 불변식 테스트에 추가(RED→GREEN) | Codex 리뷰 2차 지적. 1차 수정이 인코딩 규칙만 맞추고 경계 필터는 게시 쪽에만 남겨, 부산 밖 테마 셀의 gridId가 판정 집합에 잔류했다 — 그 격자가 다른 층으로 렌더되면 "강조된 테마 셀만 상세를 연다"(AC 9)가 깨진다. 현재 전 층이 경계 필터를 거쳐 실현 경로는 없지만, 불변식이 목 데이터가 전부 부산 안이라는 우연에 의존하던 상태였다(테스트가 공허하게 통과) |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영 3): 실 카카오 인가 리다이렉트를 `VITE_KAKAO_LOGIN_ENABLED` 플래그 뒤에 두고 **기본값은 기존 dev 모의 로그인 유지**. 스모크 2케이스로 양쪽 분기 고정 | Codex 리뷰 지적: 카카오 리다이렉트로 dev 로그인을 대체했는데 인가 코드→ID 토큰 교환 엔드포인트가 없어 로그인이 완결되지 않았다 — 기존 기능을 제거하고 대체를 못 세운 회귀. 1차 대응(개발 빌드 전용 버튼 추가)은 프로덕션 회귀를 그대로 둬 재지적됨. 플래그 기본 off면 어떤 환경에 배포돼도 기존 로그인이 그대로 동작하고, 서버 교환이 붙으면 env 한 줄로 전환된다 |
| 2026-08-08 | MSG-325 | 결정: 세션 만료(재발급 실패) 시 로그인 모달을 열지 않는다 — 토큰만 비운다. 배선을 `app/configure-auth.ts`로 분리하고 계약 3케이스 고정(토큰 비움 / 모달 미개방 / 열린 모달 불간섭) | 사용자 보고 결함: 프로필을 누르지 않아도 줌인·줌아웃마다 로그인 모달이 떴다. 원인은 MSG-325가 지도를 실 API로 붙이면서 비로그인 상태의 뷰포트 이동이 매번 401 → 재발급 실패 → onSessionExpired → openModal을 타게 된 것(MSG-324 시점엔 지도가 목이라 미발현). 확정 규칙: 로그인 모달은 **프로필 진입 + 비로그인**일 때만 뜨고, 그 판정은 SideRailNav·RequireAuth 소관 |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영 4): OAuth state 보관소를 `consume()`(읽으며 삭제) → `peek()`/`clear()` 분리. 콜백 판정은 `useState` 초기화에서 peek로 1회 고정하고, 폐기는 `useEffect`에서 수행. StrictMode 렌더 스모크 5케이스 신설(RED→GREEN) | Codex 리뷰가 잡은 실결함: 판정을 `useMemo` 안에서 `consume()`으로 했는데 앱 진입점이 StrictMode라 렌더가 2회 실행된다 — 두 번째 판정에서 state가 이미 지워져 **정상 콜백이 항상 CSRF 실패로 표시**된다(dev 전 구간). 렌더는 순수해야 한다는 원칙 위반이 그대로 발현한 사례라, 테스트도 StrictMode로 렌더해 이중 실행을 고정했다 |
| 2026-08-08 | MSG-325 | 결정: 카카오 로그인을 백엔드 확정 계약으로 재배선 — 프론트는 서버 진입점(`/api/auth/oauth/kakao/authorize`) 이동 1회 + 콜백에서 코드 교환(`/api/auth/oauth/kakao/code`) 1회만 한다. 카카오 SDK·앱 키·nonce·scope는 전부 서버 몫이라 제거(`VITE_KAKAO_JS_KEY`·`VITE_KAKAO_LOGIN_ENABLED` 폐기) | 백엔드가 nonce 쿠키(HttpOnly 10분)를 심고 REST API 키로 교환까지 수행하는 진입점을 배포 — 스펙 조회로 두 엔드포인트 존재를 확인 후 착수. 이 설계는 REST 키가 클라이언트로 나갈 경로 자체를 없애고 scope=openid 누락을 구조적으로 불가능하게 한다. 회귀 방지용 플래그는 실 플로우가 완결되면서 존재 이유가 사라져 함께 제거 |
| 2026-08-08 | MSG-325 | 발견(테스트가 잡은 버그 2건): ① 인가 코드가 StrictMode에서 2회 전송 — `useState` 가드는 mount→cleanup→mount 사이 리렌더가 없어 두 실행이 같은 값을 본다. 코드는 1회용이라 두 번째가 2423으로 실패 → `useRef`(동기 반영)로 교체. ② 성공·실패 콜백 유실 — `mutate`의 per-call 콜백은 관찰자 언마운트 시 버려져 StrictMode 첫 마운트 요청의 콜백이 사라진다(홈 이동·실패 안내가 통째로 미발생) → 훅 레벨 옵션으로 이동 | 둘 다 dev 브라우저에서 "로그인이 되긴 하는데 화면이 안 넘어감/원인 불명 실패"로 나타났을 결함. StrictMode 렌더 스모크가 없었으면 검증에서도 원인 특정이 어려웠다 |
| 2026-08-08 | MSG-325 | 결정: vitest 셋업에서 `registerApiErrorInterceptor()`를 등록 — 앱은 main.tsx에서 1회 등록하는데 테스트는 그 진입점을 타지 않아 실패 응답이 **정규화되지 않은 원시 봉투**로 도착했다 | 화면이 `developCode`로 분기하는 코드를 테스트가 검증하지 못하던 사각. 실제로 2423/2502 안내 분기 테스트가 통과할 수 없었고, 원인 추적에 여러 라운드가 들었다. 앱과 같은 조건으로 맞춰 이후 티켓의 에러 분기 테스트도 유효해진다 |
| 2026-08-08 | MSG-325 | 결정: `X-Device-Id`를 로그인 응답 헤더에서 저장(localStorage)하고 이후 요청에 재주입하되 **로그아웃은 제외** | 재발급이 같은 디바이스 세션을 갱신하려면 기기 식별이 필요하다. 반대로 로그아웃은 "X-Device-Id 미전송 = 전 디바이스 세션 삭제"가 MSG-324 확정 동작이라, 전역 주입하면 현재 기기만 로그아웃되는 조용한 동작 변경이 된다 — 제외 경로를 테스트로 고정. 헤더 접근이 필요해 이 mutation만 생성 옵션 대신 SDK 직접 호출(생성물 비수정 원칙은 유지) |
| 2026-08-08 | MSG-325 | 발견(브라우저 검증): 점령 격자 첫 페이지 요청에 빈 `cursor=`가 실려 서버가 400으로 거부 — **점령 격자가 한 번도 조회된 적이 없었다**. 생성 queryFn이 pageParam을 문자열이면 cursor 쿼리로 싣는데 `initialPageParam: ""`이 빈 문자열이라 그대로 나갔다. 객체 형태(`{ query: viewport }`)로 교체하고, 실요청 쿼리스트링을 단정하는 렌더 테스트 신설(RED→GREEN) | 단위 테스트가 순수 헬퍼(nextGridsPageParam·flattenGridPages)만 봐서 훅이 조립하는 실제 쿼리스트링은 아무도 검증하지 않던 사각. hotzones는 커서가 없어 200이었고 grids만 400이라 "같은 클라이언트인데 하나만 실패"로 드러났다 |
| 2026-08-08 | MSG-325 | 검증(실서버): 401 → 재발급 → 재시도 전 구간 통과. 동시 401 두 건(grids·hotzones)에 재발급이 **1회만** 호출돼 MSG-324의 single-flight가 실서버에서 처음 확증됐다. 응답 헤더로 CORS 자격증명(`Allow-Credentials: true` + 명시 Origin)·리프레시 토큰 회전·`X-Device-Id` expose까지 확인 — MSG-324 인수인계의 미해결 항목(CORS 자격증명)이 해소됐다 | dev 모의 로그인은 Set-Cookie가 없어 재발급 검증이 구조적으로 불가능했다(MSG-324 실측). 실 카카오 로그인이 붙으면서 처음으로 검증 가능해진 경로다 |
| 2026-08-08 | MSG-325 | 결정(사용자 지시): 로그아웃 시 즉시 홈으로 이동 — MSG-124 F2("화면 전환 없이 패널에 머문다")를 폐기. 스모크 단정도 홈 이동·프로필 콘텐츠 소멸로 교체 | 로그아웃 후에도 프로필 패널이 남으면 비로그인 상태로 보호 화면에 머무는 셈이고, 사이드레일 활성 탭도 '프로필'로 남는다. 홈으로 보내면 활성 탭 판정(getActiveNavKey)이 pathname 기반이라 '홈'이 자연히 활성된다. 콜백은 훅 레벨 옵션(useLogout의 onFinished)으로 받는다 — mutate의 per-call 콜백은 화면을 떠나며 언마운트되면 유실되기 때문(같은 세션에서 카카오 콜백이 이 함정에 걸렸다) |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영 5): `nose --write-baseline` 전면 재작성을 폐기하고 **실제 보고된 family만 선별 등재**하도록 정정. 이번 티켓이 등재한 것은 4건뿐 — 에러상태 4중복·썸네일 마크업 3중복·mobile↔web ProfilePanel(전부 줄 번호 이동)과 웹↔모바일 `grid.ts` 복제(MSG-294 D1의 의도된 중복) | Codex 리뷰 지적: `--write-baseline`은 현재 전 family를 통째로 다시 쓰기 때문에 **기본 표면 아래로 보류된 family까지 승인**한다. 실제로 세션 중 두 번의 전면 재작성이 무관한 12건(예: `hatch-pattern.test`↔`theme-overlay.test`, `busan-boundary.test`↔`theme-overlay.test`)을 조용히 등재해, 나중에 그 중복이 표면화돼도 게이트가 놓치는 상태였다. 세션 전 베이스라인으로 되돌린 뒤 보고된 id만 upsert — 파일 조합 기준 순증 0건으로 확인. MSG-324가 "베이스라인 전면 재직렬화는 도구 특성"이라 넘긴 판단을 정정한다 |
| 2026-08-08 | MSG-325 | 발견(CI): 클러스터 줌 게이트 e2e가 깨짐 — 점령 격자 소스를 목(`MOCK_DEX.collectedCells`)에서 실 API로 바꾸면서, 로그인하지 않는 e2e 환경에서는 `/api/grids`가 빈 응답이라 클러스터가 렌더되지 않는다. Playwright `page.route`로 서면 3×3 격자를 스텁해 MSG-264 회귀 스펙을 유지 | 로컬 게이트(`pnpm test`)는 vitest만 돌고 e2e는 `test:e2e`로 분리돼 있어 push 전에 드러나지 않았다. 데이터 소스를 바꾸면 그 데이터에 의존하던 e2e가 함께 깨진다는 것을 놓친 사례 — 스텁은 서버 격자 규칙으로 인코딩해 규칙 변경 시에도 함께 검증된다 |
| 2026-08-08 | MSG-325 | 결정: 루트 라우트에 `errorElement`(RouteErrorBoundary) 추가 — react-doctor error(`react-router-require-root-error-boundary`) 해소. 오류 내용은 표시하지 않는다(사용자가 할 수 있는 일이 없고 내부 정보만 노출) | 라우터를 건드리며 규칙이 발현했고, 없으면 렌더·로더 오류에서 라우터 기본 화면(개발자용 스택)이 사용자에게 그대로 노출된다. 무매칭 경로(404)도 이 화면으로 수렴해 기존 "신규 404 페이지 없음" 결정과 양립한다. 남은 경고 2건(useSignup·useLogout 캐시 무효화)은 MSG-324에서 의도된 결정으로 기록된 항목이라 그대로 둔다 |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영): 격자 상세 조합 훅에서 `placeholderData: keepPreviousData` 제거 — 단일 엔티티 조회용 `entityQueryPolicy`(staleTime만) 신설. 재현 테스트 RED→GREEN(A 격자 열린 상태에서 B 탭 → 새 응답 전까지 null) | 리뷰가 잡은 실결함: keepPreviousData는 "같은 종류 데이터의 연속 갱신"(뷰포트 이동)용인데, 격자 탭은 키가 **다른 엔티티**를 가리킨다. 상세가 열린 채 다른 셀을 탭하면 selectedCellId가 A→B로 직행해 새 응답 전까지 A의 배지·영상수·대표 영상이 B 자리에 그대로 남고, cell·cover·stat이 각자 다른 시점에 도착해 서로 다른 격자의 값이 섞일 수 있다. 응답 gridId 가드 대신 원인(placeholderData)을 제거해 섞임 경로 자체를 없앴다. QA에서 기준 13이 확인불가(점령 격자 0건)였던 구간이라 실측으로는 못 잡았을 결함 |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영): `use-hotzones-query`·`use-grid-detail-query`에 renderHook 테스트 신설 — 순수 헬퍼만 테스트하던 사각을 메움(빈 응답 흡수, center→gridId 왕복, 조합·null 분기). 봉투 목은 MSG-324가 추출해둔 `test/envelope-response`를 재사용(중복 게이트가 신규 clone family로 검출) | 같은 커밋의 `use-occupied-grids-query`는 로직·실요청 테스트를 둘 다 가졌는데 두 훅만 비어 있었다. 빈 `cursor` 결함이 정확히 "훅이 조립하는 실제 동작을 아무도 안 보는" 사각에서 나왔던 만큼, 같은 유형의 공백을 남겨둘 이유가 없다 |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영): 홈 좌측 패널 분기를 `detail` 유무 → **`selectedCellId`(선택 컨텍스트) 기준**으로 변경하고, 상세 로딩 자리(`HomeCellDetailLoading`)를 신설. MapHomePage 레벨 스모크 3케이스 신설(RED→GREEN) | 직전 수정(placeholderData 제거)이 만든 부작용: 상세가 열린 채 다른 격자를 탭하면 `detail`이 잠깐 null이 되는데, 분기가 detail 기준이라 그 사이 **요약 패널로 튀었다가 되돌아온다**. 격자별로 쿼리 키가 달라 캐시 재사용도 없어 네트워크 지연이 있으면 매번 발생한다. 선택 컨텍스트로 분기하면 상세 슬롯을 지키면서 로딩만 알린다. 로딩 상태에도 Escape 닫기를 유지 — 조회 실패로 상세가 영영 안 올 때 빠져나갈 수단이 필요하다. 이 통합 흐름(선택→로딩→상세)을 검증하는 페이지 레벨 테스트가 없어 회귀가 자동으로 안 잡히던 것도 함께 해소 |
| 2026-08-08 | MSG-325 | 결정: 테스트 렌더 스캐폴딩(`QueryClientProvider` + `MemoryRouter`)을 `test/render-with-providers`로 추출 — 중복 게이트가 dex 스모크 3건과의 신규 clone family로 검출. 남은 `<Routes>` JSX 유사성 1건은 nose가 `~0 removable`로 판정해 선별 등재(베이스라인 순증 1건, 제거 0) | 추출은 실제로 4번째 사용처가 생겨 두 번째 사용처 규칙을 충족한다. 반면 라우트 JSX는 추출해도 줄이 줄지 않고 가독성만 해쳐 등재가 맞다. 등재는 전면 재작성이 아니라 보고된 id만 upsert하는 방식(같은 세션의 정정 결정 준용) |
| 2026-08-08 | MSG-325 | 결정(리뷰 반영): 격자 상세 조회 실패를 화면에 실패로 표시 — `useGridDetailQuery`가 `{ detail, isError, retry }`를 반환하고, 상세 슬롯에 `HomeCellDetailError`(문구 + 다시 시도) 추가. 훅 2케이스 + 페이지 1케이스 신설(RED→GREEN) | 직전 수정에서 실패 시에도 `null`을 반환해 **로딩 화면이 영구히 유지**됐다. Escape 탈출구를 넣어 갇히는 것은 막았지만, 화면이 실패를 "불러오는 중"이라고 계속 표시하는 것 자체가 결함이다(리뷰 지적). `isError`는 격자 조회(`getCell`)만 반영한다 — 대표 영상·행정동 실패는 각자 빈 분기라 상세가 성립하므로 실패로 올리지 않는다(테스트로 고정) |
| 2026-08-08 | MSG-325 | 결정: 상세 실패 마크업이 `CellSummaryPanel` 실패 상태와 유사(2 copies, nose `~0 removable`)한 건 선별 등재 — 공용 ErrorState 추출은 MSG-324에서 이미 환류된 승격 후보로, 4개 페이지 소유 파일을 건드려야 해 이 티켓 범위 밖이다. 테스트 내 fetch 스텁 중복은 모드 인자(`ready`/`pending`/`error`)로 실제 통합해 해소 | 등재는 "추출 가치 없음(도구 판정) + 범위 밖"일 때만, 통합 가능한 것은 통합한다는 기준을 유지 |
| 2026-08-10 | MSG-352 | 결정: 블러 서사 전환(개수→전체 블러)으로 `blur-detection.ts`·`blur-detection.test.ts`·`BlurOverlay.tsx`를 통째로 삭제 — 부분 수정이 아니라 모듈 제거 | Figma ver 10에서 얼굴/번호판 개수 서사가 사라지자 감지 목업·집계·개수 포맷·박스 오버레이 전 함수가 고아가 됐다(소비자 0). 남는 문구는 정적 상수뿐이라 로직 레이어에 둘 것이 없다 — 스펙 추정 7("고아가 되면 정리")의 승인 범위. 테스트 삭제는 대상 모듈 삭제에 따른 동반 삭제로, 약화가 아님 |
| 2026-08-10 | MSG-352 | 결정: 개발용 로그인 성공 시 로그인 모달을 닫는다(per-call onSuccess로 `closeModal`) | 스펙에 성공 후 동선 명시가 없으나, 로그인된 채 로그인 모달이 남는 것은 A4("카카오와 동일하게 사용 가능")의 동선과 어긋난다 — 카카오는 리다이렉트로 모달을 벗어난다. per-call 콜백은 패널이 제출 시점에 항상 마운트돼 있어 StrictMode 유실 경로(초기 mount 사이클 중 시작된 요청)에 해당하지 않는다 |
| 2026-08-10 | MSG-352 | 결정: 카카오/이메일 로그인 mutationFn의 X-Device-Id 보관 로직을 `requestSavingDeviceId`로 추출, ModalCard 스토리 4곳의 렌더 스캐폴딩을 `renderCard`로 통합 — 중복 게이트(nose)가 각각 신규 clone family·4번째 copy로 검출. 남은 changed 1건(업로드 카드 div 유사 계열, `~0 removable`)은 내 삭제로 구성이 줄어든 변화라 베이스라인 갱신으로 수용(신규 0 상태에서 재작성) | 두 추출 모두 실사용처 2·4곳으로 두 번째 사용처 규칙 충족. 베이스라인 재작성은 신규 family가 0으로 확인된 상태에서만 — 무관한 중복을 함께 승인하던 전례(MSG-325 정정)와 구별 |
| 2026-08-10 | MSG-352 | 결정: C10 업로드 자동 재개를 `upload-modal-store`의 `pendingAfterLogin` 플래그 + 모듈 레벨 zustand 구독 2개(auth 전이 시 재개, 로그인 모달 취소 시 해제)로 구현 — 의도는 메모리 전용(비영속)이라 카카오 OAuth 리다이렉트는 미생존(dev 로그인만 자동 재개) | 재개·해제 로직을 upload 쪽 구독에 두면 결합이 기존 C9 게이트와 같은 upload→auth 단방향으로 유지된다(auth가 upload를 알면 순환). dev 로그인은 훅 레벨 onSuccess(setAccessToken)가 패널 closeModal보다 먼저라(TanStack Query 순서 보장) 전이 시점 재개가 취소 해제와 경합하지 않는다. 리다이렉트 생존은 스토리지 영속화가 필요해 코디네이터 허용대로 한계 기록(단순성 우선) |
| 2026-08-10 | MSG-352 | 결정: C11 카카오 자동 재개 — 업로드 의도를 `uploadIntentStorage`(sessionStorage, shared/storage 어댑터)로 영속화하고, 복원은 스토어 **모듈 로드 시 hydrate**(`pendingAfterLogin` 초기값)로 통일. stale 방지는 "로그인 모달 열림 = 이전 의도 무효" 구독 신설(게이트는 모달을 먼저 연 뒤 의도 세팅) + 재개·취소 시 영속 의도 소진 | sessionStorage는 oauthStateStorage와 같은 근거(같은 탭 리다이렉트 왕복만 생존, 탭 종료 시 소멸 → 만료 로직 불요·타 탭 미유출); auth-store의 localStorage persist는 기기 단위 영속이라 의도 같은 1회성 값에 과잉. hydrate 방식이면 전이 구독이 dev 로그인과 동일 코드로 수렴하고, 전이 시 스토리지 직접 조회는 메모리 상태와 무관한 스퓨리어스 재개 경로를 만든다(테스트 격리에서도 실증). 모든 로그인 경로(카카오·dev)가 로그인 모달을 거치므로 "모달 열림" 가드가 이탈 복귀 잔존 의도를 다음 로그인 전에 소거하며, 카카오 리다이렉트 이탈은 모달 닫힘 이벤트가 없어 취소로 오판되지 않는다. 가드·소진 테스트 3건은 해제 미구현 중간 단계에서 실제 실패를 관찰해 실효성 확인 |
| 2026-08-10 | MSG-357 | 결정: 모바일 grid 사본(MSG-294 복제+동등성 게이트)은 이번 티켓에서 5179로 마이그레이션하지 않고, `grid.parity.test.ts`를 "의도적 분기 상태 감시"로 재기준화 — ① 모바일 로컬 체계 자기 일관성 ② 웹이 5179 정본을 유지하는지(구 체계 심볼 부재 포함)를 단정한다 | 스펙 범위는 apps/web뿐인데 웹 grid 전환으로 동등성 게이트가 정당하게 깨졌다. 모바일 소비처(grid-map·visible-grid·cell-detail 등 10+파일)가 전부 Bounds 기반이라 마이그레이션은 별도 티켓 규모 — 빌드 리포트 스펙 이슈로 환류. 게이트를 무단 삭제하지 않고 분기 사실 자체를 테스트로 명문화해, 웹 재변경·모바일 마이그레이션 어느 쪽이든 이 파일이 다시 깨져 재조정을 강제한다 |
| 2026-08-10 | MSG-357 | 결정: 격자선 직선 근사(끝점만 역변환)의 5179 경계 대조 허용 오차를 0.5m로 캘리브레이션 — 실측 최대 편차 ~16cm(버퍼 범위 ~3.3km 현의 자오선 수렴 곡률) | 스펙이 직선 근사를 명시(국소 선형, BE 동일 근거)하므로 곡률 오차는 사양이다. zoom 15에서 1px ≈ 4m라 16cm는 화면 비가시. 0.5m 상한은 근사를 허용하면서 구 위경도 스텝 격자(수십 m 어긋남)를 확실히 거른다. 꼭짓점 자체 대조는 별도로 1e-7°(≈1cm, A2) 엄격 유지 |
| 2026-08-10 | MSG-357 | 결정: 격자·채움 표시 게이트 `GRID_MIN_ZOOM` 15→16 상향(사용자 지시 — 축척 100m부터 격자, 250m부터 클러스터), `CLUSTER_WINDOW_CELL_FACTOR` 3.5→1.75 반감, `DEFAULT_ZOOM`·viewport-store 초기 줌 15→16 동반 상향 | 클러스터 윈도 공식이 게이트에 앵커(2^(GRID_MIN_ZOOM−zoom))라 게이트만 올리면 전 줌에서 윈도 지상 폭이 2배가 된다 — 계수 반감이 앵커 이동을 정확히 상쇄해 기존 캘리브레이션(픽셀 등가 ≈90px, tier 3 마커 44px의 2배)이 불변이며 회귀 단정으로 고정. 진입 줌을 함께 올리지 않으면 첫 화면이 클러스터로 열려 기존 진입 경험(격자 노출)이 깨진다 |
| 2026-08-11 | MSG-378 | 결정: 웹 `avatar-fallback.ts`를 스펙(고아 제거)과 달리 존치 — 웹 사용처(ProfileHeader·ProfileEditModal)는 기본 이미지 에셋으로 전환 완료, 파일·테스트만 남김. 대신 파일 JSDoc에 존치 사유 명기 | 스펙의 "사용처 전멸" 판정이 apps/web 범위만 조사 — apps/mobile의 `avatar-fallback.parity.test.ts`가 웹 원본을 동등성 기준으로 직접 import하고 모바일 프로필 화면은 여전히 이니셜 fallback을 사용한다. 삭제 시 모바일 테스트가 깨진다(빌드 리포트 스펙 이슈). 모바일 기본 이미지 전환 후속 티켓에서 양쪽 함께 정리 |
| 2026-08-11 | MSG-378 | 결정: 웹 `MOCK_PROFILE`에 `profileImageUrl: null` 추가에 맞춰 모바일 미러(`profile.ts`·`mock-profile.ts`)에도 같은 필드를 동반 추가 | 모바일 `mock-profile.parity.test`가 웹 mock과 `toEqual` 전체 동등을 단정 — 웹만 바꾸면 패리티가 깨진다. 패리티 테스트를 약화(필드 선별 비교)하는 대신 미러를 동기화하는 것이 게이트의 존재 의의(양앱 mock 정합)에 부합. 모바일 화면 배선은 하지 않음(표시 전환은 후속) |
| 2026-08-11 | MSG-378 | 결정: pre-commit react-doctor(createObjectURL without revoke) 환류 — 미리보기 objectURL 폐기를 `previewUrl` effect cleanup 소유로 재구성(재선택·닫기·언마운트 3경로 수렴)하고 언마운트 revoke를 스모크로 고정(RED 확인). 잔여 검출 1건은 setState 경유 데이터 흐름을 못 쫓는 검출기 한계라 해당 줄만 인라인 억제 | 검출기의 정본 수정(생성·revoke를 한 effect에)은 리포지토리 eslint `react-hooks/set-state-in-effect`(effect 본문 setState 금지)와 충돌 — 기계 게이트 간 충돌은 repo 린트를 우선하고, 오탐 억제는 증거(3경로 cleanup + 언마운트 테스트) 명기 후 라인 단위로만. 원 지적 자체는 진양성(언마운트 누수 실재)이라 코드 수정이 선행됐다 |
| 2026-08-12 | MSG-378 | 결정(범위 확장): 프로필 조회를 getMe 실 API로 전환하면서도 업로드 확정 반영은 invalidate가 아닌 setQueryData 유지 — queryKey `["profile"]`·ProfileData 계약 불변 | 확정 응답(UserProfileResponseDto)이 변경 후 상태의 정본이라 invalidate의 재조회 왕복이 잉여이고, 본체 커밋의 스모크 자산(캐시 병합 단정)도 그대로 유효하다. 커스텀 queryFn(서버+mock 병합)이 필요해 생성 getMeOptions 직결 대신 fetchProfile 내부 교체 — 파일이 스스로 예고한 전환 지점이라 소비처 diff 0 |
| 2026-08-12 | MSG-378 | 결정: codex 리뷰 환류 — `formatJoinedDate`가 시각 있는 입력을 UTC로 취급해 KST(+9)로 옮긴 날짜를 표기하도록 수정(경계 UTC 15:00=KST 자정·연 경계·마커 유무 케이스를 3개 타임존 TZ 매트릭스로 고정). 변환은 Intl 미사용, 고정 +9 상수 오프셋 + UTC getter | 서버 createdAt이 마커 없는 UTC 저장값 그대로라(MSG-373 명세) 날짜부 절취는 KST 00~09시 가입자를 하루 전으로 표기. KST는 DST가 없어 고정 오프셋이 정확하고 RN(hermes Intl 편차)에도 안전. date-only(레거시 mock) 경로는 표기 보존을 유지해 기존 테스트 불변 |
| 2026-08-12 | MSG-326 | 결정: 카드 나열 마크업은 FeedVideoList(pages/map-home/ui)로 추출, 신설 "실패 문구+다시 시도" 상태 블록 2벌(목록 실패·미니 패널 실패)은 공통화하지 않고 nose 베이스라인에 의도 중복으로 등재 | 카드 나열은 격자 상세 피드 복원으로 두 번째 사용처(테마 피드와 동일 10줄)가 생겨 추출 경로 충족(check:duplication 신규 패밀리 검출). 상태 블록 4벌은 공유 0줄에 래퍼·색·레이아웃이 전부 상이해(nose 자체 판정 "데이터 모양이 맞을 때만 공통화") 파라미터 3개+ 강제 추상화라 회피 — 등재는 검증 스킬의 의도 중복 절차 준용 |
| 2026-08-11 | MSG-329 | 결정: 업로드 단계열(presign→S3 PUT→확정)을 순수 상태 머신(`upload-orchestration.ts`) + 주입 이펙트로 구현 — 선분석·게시가 같은 러너를 공유하고, 실패는 단계 표기 `UploadFlowError`(state 보존)로 던져 재시도가 성공 단계를 건너뛴다 | B11(단계별 실패 구분·성공 단계 스킵·presign 만료 재발급)을 훅 안 분기문으로 짜면 선분석/게시 두 곳에 중복되고 테스트가 fetch 목 시퀀스에 결합된다. 순수 머신으로 빼니 만료·스킵 경계를 vi.fn 순서 단정만으로 검증 |
| 2026-08-11 | MSG-329 | 결정: ffmpeg.wasm은 `@ffmpeg/ffmpeg`+`@ffmpeg/core`(단일 스레드)로 도입, 코어는 `?url` 번들 자산(자체 서빙) + dynamic import 지연 로드, `optimizeDeps.exclude` 필요(자체 워커 상대 URL). 끝점 보정 계산(`video-trim.ts`)만 단위 테스트하고 wasm 어댑터는 인터페이스 뒤(스모크는 목킹) — 전체 구간 선택이면 트리밍 자체를 생략해 원본을 그대로 확정 업로드 | 티켓 확정(FE 스트림 카피·지연 로드·CDN 금지). wasm은 jsdom 실행 불가라 어댑터 격리가 유일한 테스트 전략. 코어 32MB는 별도 자산 청크로 분리돼 초기 번들 영향 없음(빌드 실측) |
| 2026-08-11 | MSG-329 | 결정: 확정 성공 invalidate(B13)에서 점령 격자는 생성 키의 `_id`만 남긴 부분 키(TanStack 부분 일치)로 전 뷰포트를 무효화 — 뷰포트 파라미터별로 캐시 키가 갈라져 정확 키 재구성이 불가. 격자 상세 3종(getCell·getGridCover·getStatByGrid)은 응답 gridId로 정확 키 무효화 | 수동 문자열 키 금지 관례를 지키면서(식별자는 생성 키 함수에서 파생) 파라미터화된 캐시 전체를 무효화하는 유일한 경로. 코드베이스 최초의 invalidate 배선(티켓 "유지" 문구는 실측상 신규 — 스펙 리스크 2) |
| 2026-08-11 | MSG-329 | 결정: 웹 MOCK_PROFILE·profile-format 폐기로 모바일 parity 테스트 2건의 앵커가 사라짐 — 웹 원본 동적 import 단정을 제거하고 모바일 단독 불변식 테스트로 전환(리네임). 모바일 profile-edit는 웹이 2~20자로 강화되며 검증 규칙이 갈라졌으나 parity 샘플에 1자가 없어 통과 — 모바일 실 API 티켓에서 정리 필요(환류) | parity의 전제(웹 mock이 정본)가 이 티켓으로 소멸. 모바일 동작 커버리지는 유지하되 죽은 앵커만 제거하는 것이 최소 수술 |
| 2026-08-13 | MSG-386 | 결정: 토큰 규칙(better-tailwindcss 상당)을 oxlint JS 플러그인 자작(`tools/oxlint/no-restricted-classes.mjs`, ESLint 호환 create/visitor API)으로 이식 — eslint-plugin-better-tailwindcss를 jsPlugins로 그대로 로드하는 대안은 기각. RN 경계·react-hooks·react-refresh 상당은 전부 oxlint 네이티브 규칙 사용 | 원본 플러그인은 tailwindcss 패키지 해석·eslint 의존이 있어 jsPlugins(alpha)에서의 동작을 보장 못 하고 eslint 계열 제거 목적과 상충. 자작 규칙은 2패턴(px·색상)만 필요해 ~150줄로 충분, 원본 기본 selector(class/className 속성·cn/cva 등 callee·클래스성 변수) 커버리지를 재현하고 위반 샘플 전/후 실측으로 동등성 확인. 발견: 기존 하이브리드 구성에서 exhaustive-deps가 어느 쪽에서도 검사되지 않던 공백(eslint-plugin-oxlint가 꺼두고 oxlint fast pass는 react 플러그인 미활성) — 전환으로 warn 검사 복원 |
| 2026-08-13 | MSG-328 | 결정: 지역·검색 5개 엔드포인트 익명 호출을 실서버(api.fillmap.kr)로 실측 — 전부 401. 비로그인 홈은 조회 자체를 게이트(enabled=false)하고 패널·검색 드롭다운에 로그인 유도 UI를 표시. "401을 그대로 쏘고 실패 상태 표시" 대안은 기각 | 사용자 위임(구현 중 실측 판단). 익명 401이 auth-pipeline의 재발급 시도 → 실패 → onSessionExpired(로그인 모달 강제 오픈)를 트리거해, 게이트 없이는 홈 진입만으로 모달이 뜨는 오작동이 된다 (스모크로 고정) |
| 2026-08-13 | MSG-328 | 발견: 스펙 삭제 표의 "use-cells-query 소비처가 CellSummaryPanel뿐" 실측이 틀림 — pages/dex/DexPanel(상세 시트용 격자 소스, MSG-122 ② AC 23)이 소비 중이라 존치. cell-viewport.ts만 삭제 | 삭제하면 도감 격자 상세가 깨진다. mock 격자 소스의 실 API 전환은 도감 티켓 몫으로 남김 |
| 2026-08-13 | MSG-328 | 결정: 웹 explore 자산 삭제로 모바일 parity 테스트 2건(regions·recent-history)의 웹 앵커가 소멸 — MSG-329 profile 선례대로 모바일 단독 불변식 테스트로 전환(리네임), format parity는 이동 경로(widgets/cell-detail/model)로 재지정 | parity의 전제(웹 원본)가 소멸. 모바일 검색 화면이 두 모듈을 실사용 중이라 커버리지는 유지하고 죽은 앵커만 제거하는 최소 수술 |
| 2026-08-13 | MSG-328 | 결정: 실패+재시도 행을 pages/map-home/ui/RetryNotice로 추출 — 신규 패널·검색에서 같은 마크업이 5곳으로 늘어 check:duplication 신규 카피가 됐다. 기존 유사 블록(HomeCellDetailPanel·VideoMiniPanel)은 티켓 범위 밖이라 미수정, nose 베이스라인은 삭제·이동으로 변한 4패밀리를 현행화 | 두 번째(이상) 사용처 등장 시 추출 원칙. ui-web 승격은 스펙 승격 후보 없음이라 페이지 로컬 유지 |
| 2026-08-13 | MSG-328 | 발견(리뷰 P2): 표시 지역 채택 후 지도 이동의 reverse-geocode 실패가 무표시 — 실패 시 currentRegion null로 재검색 버튼도 못 떠 복구 경로가 없었다. populated 분기에 RetryNotice(reverse.retry) 추가, 이전 지역 헤더·리스트는 유지. 재현 스모크가 계약 고정 | 실패를 성공분 표시로 위장하지 않는다(MSG-325 결)의 populated 상태 누락 보수. 리스트 전체를 에러로 갈아치우는 대안은 유효 콘텐츠를 가려 기각 |
| 2026-08-14 | MSG-327 | 결정: 동 클릭 → `collections/videos?regionCode=` 호출에 필요한 regionCode를 `GET /api/regions/stats/by-grid`(대표 격자 id → 행정동)로 얻는다. 스펙이 계획했던 `regions/stats` 목록 + regionName 문자열 매칭은 기각 | `CollectionGridResponseDto`에 regionCode가 없다(명세 갭). 이름 매칭은 동명이동(다른 시군구의 같은 행정동명)에서 오매칭하고 목록 선행 조회가 필요한데, by-grid는 격자 하나로 정확한 코드를 준다. 덕분에 "백엔드 환류 필수"였던 항목이 "있으면 조회 1회 절약" 수준으로 내려갔다 |
| 2026-08-14 | MSG-327 | 결정: 도감 진행 바를 `GET /api/regions/stats/by-point`(좌표 → 행정동+수집률)로 전환하고 프론트 역지오코딩 어댑터(`shared/region-lookup.ts`)와 해석 로직(`current-region.ts`)을 폐기 | 서버가 좌표 하나로 행정동 판별과 수집률 계산을 함께 해준다. 구 경로는 네이버 geocoder로 구(區) 이름만 얻고 수집률은 mock 맵이라 실데이터가 불가능했다. 부수 효과로 도감에서 지도 SDK 의존이 사라져 RN 경계가 깨끗해졌다 |
| 2026-08-14 | MSG-327 | 발견(브라우저 실측): 서버 `regionName`이 "부산광역시 부산진구 부전2동" 전체 경로라 스펙 추정("원문 그대로 표시")대로 두니 388px 패널의 모든 행이 잘렸다. 표시만 마지막 토큰으로 축약(`shortRegionName`)하고 묶음 키는 원문 유지. `progressRate`도 6.85처럼 소수 둘째 자리로 와 첫째 자리까지만 표기 | 실 API 붙이기 전에는 알 수 없던 값 형태. 키까지 축약하면 동명이동이 한 행으로 뭉치므로 표시와 키를 분리했다. aria-valuenow·채움 너비는 원값 유지 — 보조기술에는 반올림 안 한 값이 가야 한다 |
| 2026-08-14 | MSG-327 | 결정: `VideoMiniPanel`·`VideoOwnerMeta`를 pages/map-home → `widgets/video-mini-panel/`로 이동 | 사용자 확정(갤러리 영상 클릭 = 미니 패널 재생)을 지키려면 pages/dex가 pages/map-home 컴포넌트를 써야 해 FSD 위반(pages→pages)이 된다. MSG-328이 cell-detail을 widgets로 올린 선례를 따랐다 |
| 2026-08-14 | MSG-327 | 결정: 뱃지 아트를 Figma 시안(14599:11390) 메달 24종 SVG로 export해 `entities/badge/assets/`에 커밋(404KB), `resolveBadgeArt`가 서버 iconUrl > 로컬 에셋 > null(민무늬 원) 순으로 해석 | 백엔드 `badges.icon_url`이 전량 NULL이고 시안 노트가 "SVG export → S3 → icon_url UPDATE"를 후속 과제로 명시했다. 로컬 에셋 없이는 진열장이 빈다. iconUrl 우선 로직이 서버 전환 시 코드 변경 없는 자동 승계를 보장하고, 알 수 없는 code는 임의 기본 메달 대신 민무늬 원으로 떨어뜨려 잘못된 티어를 진짜처럼 보여주지 않는다 |
| 2026-08-14 | MSG-327 | 결정: 도감 로딩·오류 게이트를 화면 골격 3종(users/me·summary·grids)에만 걸고 뱃지·수집률은 부분 실패를 허용 | 실 API 5종을 전부 게이트에 넣으면 뱃지 하나 실패가 패널 전체를 막는다. 뱃지는 탭 로컬 오류 UI, 수집률은 표시 보류(잘못된 0%를 스치듯 보여주는 것보다 낫다)로 분리 |
| 2026-08-14 | MSG-395 | 결정: 미션 shape(CELLS/BOX/PATH/REGION) 판별을 `type`이 아니라 shape의 필드 유무로 한다 | 명세 주석이 네 shape를 THEME·CONTINUOUS / EVENT / COURSE / AREA에만 대응시키고 **POPUP의 shape를 규정하지 않았다**. type 표에 기대면 실 응답의 팝업 109건이 통째로 빈 격자가 된다(실측) |
| 2026-08-14 | MSG-395 | 결정(사용자 렉 보고 환류): 미션 격자를 전개하지 않는다 — 진행도는 "내 수집 격자(≤30)를 미션 도형에 넣어보기"로 방향 반전(`missionCoversGrid`), 지도 타일·코스 라인·이름표는 전부 뷰포트로 클리핑(`missionGridIdsInBounds`·`missionsInBounds`) | 전국 전개 시 BOX 미션 170개 × 900m 사방(9×9=81칸) = 격자 1만 4천 개이고 칸마다 proj4 변환이 붙어 좌표 변환 8만 회 + naver Polygon 1만 4천 개가 올라갔다. `occupiedIds`가 지도 이동마다 바뀌어 줌·이동마다 전량 재계산돼 페이지가 멈췄다(사용자 보고). 수정 후 팝업 칩 활성 + 줌 조작에서 longtask 0건 실측. 칸 수 상한만 두는 대안은 잘린 타일이 데이터를 거짓말하게 만들어 기각 |
| 2026-08-14 | MSG-395 | 발견/수정: `cn`(tailwind-merge)이 커스텀 `text-fm-*`를 **색상으로 분류**해 같은 호출의 `text-<색>`과 충돌시켜 조용히 지우고 있었다 — 폰트 크기가 브라우저 기본 16px/24px로 돌아가 카드가 Figma 87px 대신 107px이 됐다. `extendTailwindMerge`로 `fm-*` 8종을 `font-size` 그룹에 등록(목록은 design-tokens `typography` 키에서 파생) | MSG-108이 첫날 남긴 "extendTailwindMerge로 토큰 스케일 등록 필요(별도 티켓 권장, ui-web의 cn도 동일 이슈)"가 타이포 축에서 실제 버그로 터진 것. 내 컴포넌트만 `cn`을 우회하는 대안은 `text-fm-*` 239곳에 같은 함정을 남겨 기각. 공용 유틸이라 기존 화면의 지워졌던 크기도 함께 되살아난다(도감·프로필 브라우저 회귀 확인 완료) |
| 2026-08-14 | MSG-395 | 발견(실측): `/api/collections/grids`가 30건만 준다 — 같은 계정의 `collections/summary.totalGridCount`는 101이고 `?size`·`?limit` 무시, 명세에도 페이징 파라미터가 `never`다. 반례로 `GET /api/grids/16795_11390`은 `occupied: true`인데 목록에 없다 | FE 미션 진행도가 구조적으로 과소 집계돼 활성 미션 318건 전부가 `내 진행 0/N`으로 뜬다. 같은 격자가 상세에선 `내 점령`, 코스에선 `미방문`으로 한 화면 안에서 어긋난다. FE 계산은 승인된 계약(단일 소스=collections/grids)대로이며 `mission-progress.ts` 한 곳 교체로 해소 가능 — 백엔드 환류 필요 |
| 2026-08-14 | MSG-395 | 결정: 미션 카드 `영상 N`은 서버 `videoCount` 도착 전까지 `missionId` 결정적 목(`mission-video-count.ts`)으로 표시 | 사용자 확정(백엔드 API 개편에 포함 예정). 목록에서 미션마다 격자 영상을 조회하면 요청이 미션 수 × 격자 수로 늘고, `Math.random`은 재렌더마다 숫자가 흔들려 목이라는 사실보다 화면이 고장 난 것처럼 보인다. 미션 **상세**의 `올라온 영상 N개`는 이미 조회한 피드 길이라 실수치 |
| 2026-08-14 | MSG-395 | 결정(사용자 확정): 활발한 시간대 그래프를 Figma(14629-3839)의 행정동 요약에서 **격자 상세**로 옮긴다 | 시간대 API(`/api/grids/{id}/hourly-uploads`)가 격자 단위라 동 전체를 그리려면 동에 속한 격자 수만큼 요청이 나간다. 격자 상세는 이미 격자 하나를 특정한 화면이라 요청 1건으로 끝난다 |
| 2026-08-14 | MSG-395 | 결정: 프로필 "내 활동" 수집률의 모수를 **내가 손댄 행정동의 격자 합**(`/api/regions/stats` 합산)으로 잡는다 — 실측 100/32,433 = 0.3% | MSG-329가 `—`로 비우고 MSG-327로 미뤘던 축을 채운 것. 전국 전체 격자 수를 주는 명세 축이 없고, 있다 해도 분모가 수십만이라 화면에 늘 0%가 뜬다. 도감의 행정동 단위 "탐험률"을 계정 전체로 넓힌 의미이며, 정의 변경은 `activity-summary.ts` 한 곳 |
| 2026-08-14 | MSG-395 | 발견/보수: 세션 정책이 Agent 도구를 막자 파이프라인 Phase 3(검증)이 통째로 누락된 채 커밋까지 갔다 — 하네스는 "검증 절차는 생략하지 않는다"고 적어뒀지만 **위임 불가 시의 대체 경로가 없었고 기계 강제도 없었다**. 스킬에 "메인 스레드가 해당 스킬을 직접 로드해 같은 절차를 밟는다"를 명문화하고, `_workspace/MSG-{n}/01_spec.md`가 있는데 `03_verify_report.md`가 없으면 거부하는 pre-commit 게이트를 신설 | 위임 수단이 사라진 것이지 단계가 사라진 것이 아닌데, 그 구분이 문서에 없어 "구현 중 게이트를 돌린 것"을 검증으로 착각했다. 대가는 실측으로 드러났다 — Figma를 눈대중해 카드 치수를 두 번 틀렸고(사용자 지적), FSD 감사를 안 해 features→features를 codex가 대신 잡았다. 문서 문장만 고치면 같은 사고가 재발하므로 훅으로 막는다(react-doctor·format 게이트 선례) |
| 2026-08-15 | MSG-403 | 결정: 지도 데이터(핫구역·점령 격자·미션) 조회의 bbox 정본을 뷰포트 → **확정 영역**(`region-panel-store.committedBounds`)으로 바꾼다. 갱신은 최초 채택·"장소 불러오기"·칩 활성화 3경로뿐이고, 패널 헤더도 확정 지역명에 고정한다(MSG-328의 헤더 라이브 동기화·`region-header` 폐기) | 사용자 보고: 지도를 조금만 움직여도 격자가 다시 그려지고 지역명이 계속 바뀌어 화면이 안정되지 않는다. 칩 화면에만 적용하는 안은 기본 홈에서 같은 증상이 남아 기각(사용자 승인). 대가로 기본 홈에서도 점령 격자가 이동에 즉시 따라오지 않는다 — 되돌리려면 소비처 3곳의 bounds 인자만 바꾸면 된다 |
| 2026-08-15 | MSG-403 | 결정: 저줌 채움 게이트(`gateFillCells`)에서 **테마 색을 가진 칩 셀만 예외**로 남기고, 클러스터는 점령 셀 전용으로 축소(`selectClusterSource` 삭제) | 칩은 축척 500m·1km로 넓힌 화면에서 데이터를 보여주는 것이 목적인데 `GRID_MIN_ZOOM`(16) 게이트가 그 줌의 타일을 전부 걷어냈다. 칩 셀도 클러스터로 접는 대안은 채움과 배지가 같은 격자에 겹쳐 기각. 판별자를 색으로 둔 것은 점령·강조 전용 셀만 색이 없다는 기존 계약을 그대로 쓰기 위함 |
| 2026-08-15 | MSG-403 | 결정: 미션 진행도·스팟 통계·미션 영상 피드를 신규 서버 API 3종(`/missions/progress`·`/missions/{id}`·`/missions/{id}/videos`)으로 이관하고, MSG-395의 FE 교집합 계산(`missionProgress`)과 격자별 다중 조회 조합을 폐기. `completed`는 보정하지 않는다 | MSG-395가 "서버가 진행 필드를 주면 `mission-progress.ts` 한 곳만 교체"로 설계해 둔 것을 실행한 것. 스탬프 비회수 정책상 `0/1 + 완료`가 서버 정의상 정상이라, 프론트가 모순을 보정하면 스탬프 보유자에게 미완으로 보인다 |
| 2026-08-15 | MSG-403 | 발견/수정: 미션 API는 전역 `bearerAuth`를 상속해 익명 호출이 401(developCode 2403)이다 — MSG-395는 "축제·팝업·코스는 공개 정보"를 전제로 인증 게이트를 걸지 않았다. 미션 훅 3종에 비로그인 미발사 게이트 신설 | 게이트 없이는 비로그인 홈에서 칩을 누를 때마다 401 + auth-pipeline 재발급이 돌아 세션 만료 처리가 오발동한다(MSG-328에서 지역·검색 API로 같은 사고를 겪었다). 전제가 실측과 다르다는 것은 이번 명세 개편을 확인하다 드러났다 |
| 2026-08-15 | MSG-403 | 발견(검증): 칩 진입 시 "줌 반영"을 **영역 참조 변화**로 판정하면 줌이 적용되기 전의 사용자 드래그를 줌 결과로 오인해, "장소 불러오기를 눌러야만 갱신"(AC 11)이 깨진다. 판정을 목표 줌 도달(`zoom === target`)로 바꾸고 이미 목표 줌이면 대기를 걸지 않도록 수정 | 브라우저 검증에서 목록이 1개 → 4개로 조용히 바뀌는 것으로 드러났다. 단위 테스트만으로는 잡히지 않았고(참조 변화 시나리오를 그대로 모사했다), 재현 테스트 4케이스를 추가해 회귀를 고정했다 |
| 2026-08-15 | MSG-403 | 결정(codex 리뷰 반영): 전체 지역 리스트의 선택은 `selectRegion`으로 **격자 리스트만** 바꾸고 확정 영역(지도 데이터 bbox)은 건드리지 않는다 | `/api/regions/explore` 응답에 좌표가 없어 선택 지역으로 지도를 옮길 수 없다. 그런데 선택 시 현재 뷰포트를 함께 확정하면, 지도를 옮겨둔 상태에서 목록의 다른 동을 고르는 것만으로 미션·핫구역·점령이 갱신돼 "장소 불러오기로만 갱신"(AC 11)이 우회된다. 헤더·격자는 선택 지역, 지도는 마지막 확정 영역이라는 분리는 MSG-328의 기존 동작과도 일치한다 |
| 2026-08-15 | MSG-403 | 결정(codex 리뷰 반영): 칩 진입 확정을 "즉시 + 줌 후" 2회에서 **활성화당 1회**(행정동 판별 완료 ∧ 목표 줌 도달)로 단일화하고, 미완 확정은 대기 플래그로 보류한다 | 역지오코딩 디바운스(500ms) 중 칩을 켜면 행정동이 null이라 확정이 통째로 누락되고, 그 칩은 이전 확정 영역을 계속 조회한 채 남았다. 대기 플래그가 없으면 "처리 완료"로 표시된 활성화를 다시 살릴 방법이 없다. 확정 1회화로 중복 요청도 사라진다 |
| 2026-08-17 | MSG-407 | 결정: 동의 화면 체크박스를 ui-web `Checkbox`로 승격하지 않고 화면 로컬 native `input[type=checkbox]`(+`accent-primary`)로 구현 | 사용처가 이 화면 1곳 — "두 번째 사용처에서 승격" 관례(MSG-116 Select·액션시트 선례) 준수. 스펙도 로컬 구현 후 승격을 유효 경로로 명시. native input이라 접근성(role=checkbox·label 연결)도 추가 코드 없이 충족 |
| 2026-08-17 | MSG-407 | 결정(스펙 해석): 동의 화면 CTA 활성 판정을 별도 순수 함수로 빼지 않고 화면 `disabled={!requiredChecked \|\| isPending}` + 컴포넌트 스모크(기준 5·7)로 고정 | 스펙 계획이 "CTA 활성 판정" 로직을 features/profile/model에 들도록 했으나, [필수]만 조건이라 판정이 항등 함수가 됨 — 항등 함수의 ① 순수 테스트는 동어반복(테스트 템플릿 안티패턴)이라 관찰 가능한 화면 계약(④)으로 검증 층을 올렸다. 게이트 판정(3조건 ∧)은 계획대로 순수 함수(`shouldShowConsentGate`)로 분리 |
| 2026-08-17 | MSG-407 | 결정: [저장] 순차 체인(이미지→닉네임→동의)의 "성공분 재요청 없음"은 이미지 단계만 구조적 보장(성공 즉시 선택·예약 폐기), 닉네임·토글은 getMe invalidate로 profile prop이 저장값이 되는 무변경 판정에 의존 | 단계별 완료 플래그 ref를 추가하면 재오픈·prop 갱신과의 동기화 상태가 늘어 복잡도가 커진다. 닉네임·동의 PUT은 멱등이라 invalidate 도달 전의 짧은 창에서 재시도해도 해는 없고, 기존 MSG-329 이미지→닉네임 체인과 같은 보장 수준을 유지한 것 |
| 2026-08-17 | MSG-407 | 결정(codex P1 반영 — 빌드 리포트 대안 5 뒤집음): 동의 게이트 로그아웃을 `auth-store.logout`(로컬 해제만)에서 기존 `useLogout` 뮤테이션(logout API + onSettled 로컬 우선 종료) 경유로 교체 | 로컬 해제만으로는 HttpOnly 리프레시 쿠키가 살아 있어 "로그아웃했다"는 세션이 재발급으로 재인증될 수 있다. 스펙 계획 문구("auth-store.logout 재사용")를 문자적으로 따른 것이 원인 — 레포의 로그아웃 책임 소재는 useLogout이며 ProfilePanel 관례와도 정합. 기준 11과 모순 없음(서버 무효화가 추가되는 강화) |
| 2026-08-17 | MSG-407 | 결정(v3 builder 위임): 토글 접점 제거로 사용처가 사라진 `ProfileData.locationEnabled` 매핑·타입과 `locationStatusLabel`을 서버 실값 유지가 아니라 **삭제**로 정리. 모바일 parity의 해당 케이스는 단독 단정으로 전환 | v3가 "죽은 코드로 방치하지 않는 것"을 조건으로 유지/삭제를 위임 — 게이트는 getMe 봉투의 locationConsent를 직접 읽어 ProfileData를 경유하지 않으므로 필드를 남기면 소비처 없는 죽은 계약이 된다. 철회 UX 후속 티켓이 오면 그 티켓의 스펙으로 다시 넣는 것이 수술적 변경 원칙과 정합. parity 전환은 구 regions·recent-history 선례 |
| 2026-08-17 | MSG-407 | 결정(v4): 게이트 뒤로가기 로그인 중단을 버튼 로그아웃과 **별도 useLogout 인스턴스**로 배선(popstate용은 onFinished 없음 — navigate 금지) + 중복 발화는 ref 가드(리렌더 무관 1회) | 훅 레벨 콜백 관례(MSG-325 — per-call 콜백은 관찰자 언마운트 시 유실) 아래서 한 인스턴스로 "버튼=홈 이동 / 뒤로가기=이동 없음" 두 완료 동작을 나눌 수 없다. per-call 콜백 분기는 게이트가 로그아웃 완료 직후 언마운트되는 이 화면에서 정확히 유실 위험 경로. isPending 가드는 리렌더 전 연타에 구멍이 있어 ref로 보강 |
| 2026-08-17 | MSG-407 | 결정(codex P1·P2 반영): 게이트 popstate 방향 판별을 `history.state.idx`(react-router 7.18.1이 엔트리마다 심는 단조 증가 인덱스 — 소스 실측) **마지막 관찰값 비교**로 구현(커지면 forward=무시), idx 미상은 뒤로가기로 간주. 교차 트리거(뒤로가기·버튼) 중복은 공용 ref 가드 `requestLogoutOnce`로 1회 보장 | 마운트 시점 idx 고정 비교는 forward 무시 후의 back(6→5)을 forward로 오판 — 마지막 관찰값 갱신이 다단 트래버설을 판별한다. 미상 폴백을 뒤로가기로 둔 것은 이탈 수단 보존이 잠금보다 안전하고 라우터 관리 히스토리에선 미상 발생 경로가 없어서. 인스턴스 통합 대신 공용 가드를 택해 훅 레벨 콜백 관례(MSG-325)와 기존 결정(별도 인스턴스)을 유지 |
| 2026-08-17 | MSG-407 | 결정(codex 4차 리뷰 기각): 게이트가 SPA 첫 히스토리 엔트리일 때의 크로스 도큐먼트 뒤로가기(사이트 이탈)는 로그인 중단을 실행하지 않고 세션을 보존한다 — pagehide/unload 로그아웃 경로를 추가하지 않음 | pagehide는 뒤로가기뿐 아니라 새로고침·탭 닫기·외부 링크 이동에서도 발화해 이를 로그아웃 트리거로 쓰면 정상 세션까지 파괴된다(방향 판별 불가·beacon 전송도 비신뢰). 이탈 후 복귀하면 게이트가 다시 전면 차단하므로 미동의 세션 보존은 앱 열람으로 이어지지 않는다 — 기준 2는 same-document popstate로 한정, 검증 4차가 이 엣지를 실측·정상 판정함 |
| 2026-08-17 | MSG-408 | 결정: 셸 상주 자동 동기화 훅(use-push-token-sync)은 생성 mutation 옵션(`mutationFn!` 좁힘) 대신 생성 SDK(`register`·`unregister`) + `throwOnError` 직접 호출 | TanStack Query 5.101의 `MutationFunction`이 두 번째 인자 `MutationFunctionContext`를 필수로 요구 — useMutation 밖(effect)에서는 컨텍스트를 위조해야 해 타입 정직성이 깨진다. useLogin 계열의 SDK 직접 호출 선례와 정합. useMutation 안에서 쓰는 토글 훅은 스펙대로 `mutationFn!` 패턴 유지(context 전달) |
| 2026-08-17 | MSG-408 | 결정: 자동 동기화·포그라운드 구독을 AppLayout 훅 호출이 아니라 `PushNoticeHost` 컴포넌트 내부로 배선 | AppLayout에서 직접 훅을 부르면 위치동의 게이트 early return **앞**에서 실행돼 게이트 표시 중에도 동기화가 도는 AC 3 위반 — 훅은 조건부 호출이 불가하므로, 셸 분기 JSX 내부에 마운트되는 컴포넌트가 유일하게 안전한 수명 경계다 |
| 2026-08-17 | MSG-408 | 결정(추정 2 부분 이탈): 미지원 브라우저의 "알림 받기" 토글 비활성은 SettingRow의 aria-disabled 관례 대신 Radix Switch 네이티브 disabled + 사유 캡션 | ui-web Switch(Radix)는 aria-disabled 노출 prop이 없고 네이티브 disabled만 지원 — 래퍼로 aria-disabled를 흉내 내면 클릭 차단을 별도 구현해야 해 과설계. 탭 순서 이탈은 조작 불가 행이라 실질 손실이 없다 |
| 2026-08-17 | MSG-408 | 결정(codex 리뷰 1 반영 — 스펙 AC 7 정정, 오케스트레이터 승인): 토글 OFF의 DELETE 실패 시 보관을 비우지 않는다 — 성공 시에만 삭제·OFF 반영, 실패 시 보관·ON 유지 + 오류 안내로 재시도 가능 상태 유지 | 실패에 비우면 서버에 토큰이 남아 푸시는 계속 오는데 UI는 OFF가 되고, 자동 동기화가 보관 토큰을 전제하므로 재해제 경로가 영구 소멸한다. "DELETE 멱등이니 비워도 된다"는 스펙 문구는 성공 응답 전제의 낙관이었다 — 네트워크 오류·5xx는 멱등성이 구제하지 못한다 |
| 2026-08-17 | MSG-408 | 결정(codex 리뷰 2 반영 — 빌드 리포트 대안 5 뒤집음): use-push-token-sync 비동기 사슬에 cancelled 가드(cleanup에서 set, 각 단계 후 중단) 추가 | 대안 5의 "언마운트 후 setToken 무해" 판단은 로그아웃 경쟁을 놓쳤다 — 로그아웃이 fcmTokenStorage를 비운 **뒤** 늦은 setToken이 도착하면 비워진 보관이 구 사용자 토큰으로 재오염된다(다음 로그인 사용자의 자동 동기화가 남의 토큰을 재등록하는 경로). 경쟁 테스트로 재현 후 수정 |
| 2026-08-17 | MSG-408 | 결정(codex 리뷰 3 반영): 토글 등록/해제 실패에 오류 안내 토스트("알림 설정에 실패했어요") — denied 안내와 상태 채널 공유(`notice: "denied" | "error" | null`) | 조용한 실패는 사용자가 토글이 안 켜진 이유를 알 수 없게 한다. 별도 상태 2개 대신 단일 채널이 동시 표시 불가(마지막 안내만 유효)라는 실제 요구와 일치하고 ProfilePanel 배선도 단순해진다 |
| 2026-08-18 | MSG-408 | 기각(PR #60 리뷰 — 오케스트레이터 판정): use-push-toggle의 useMutation 성공 시 쿼리 무효화 추가 2건(등록/해제) 미반영 | 등록/해제 대상 서버 상태(알림 토큰 목록·설정)를 읽는 쿼리가 코드베이스에 존재하지 않아 무효화할 캐시 자체가 없다 — 린트 오탐. 토글 표시 정본은 서버 조회가 아니라 로컬(권한 × 보관 토큰)이며, 해당 조회가 생기는 티켓에서 무효화를 함께 도입하는 것이 수술적 변경 원칙과 정합 |
| 2026-08-18 | MSG-408 | 결정(PR #60 리뷰 3 반영): 토글 안내(denied/error)를 ProfilePanel 자체 토스트에서 `push-notice-store`(경량 zustand 단일 슬롯) 경유 PushNoticeHost 단일 스택 렌더로 이행 | 두 컨테이너가 같은 fixed 좌표(bottom-md right-md z-50)라 동시 표시 시 겹침(OFF 실패 토큰 유지 + 포그라운드 수신 조합 실재). 알림 표시 표면을 notifications 도메인 안에서 단일화하면 겹침이 구조적으로 불가능해지고 FSD 응집도 오른다 — 대안(z-index 조정·위치 분리)은 스택 수만 늘리는 대증요법이라 기각 |
| 2026-08-17 | MSG-410 | 결정: 겹침 2차 병합을 그리디 앵커 흡수(count 큰 마커가 앵커, 근접 마커 흡수·합산)로 구현 — 임계 `MARKER_MERGE_PX`=64px(최대 마커 지름과 동일 규모), 병합 위치는 앵커 좌표, 병합 키는 멤버 키 정렬 결합 | count 가중 평균 위치는 바다 위로 표류할 수 있고, 계층 클러스터링류는 마커 수십 개 규모에 과설계. 앵커 좌표는 지배 지역 위에 남아 클릭 줌 인(마커 좌표 중심)이 의미를 유지한다. 임계를 상수로 노출해 캘리브레이션 여지를 남김 |
| 2026-08-17 | MSG-410 | 결정: 집계 훅의 items 반환도 enabled 게이트로 비운다(비활성 시 빈 배열) — TanStack 캐시 잔존 데이터가 칩 활성·고줌 복귀 화면에 마커로 남는 것을 차단 | enabled=false는 재조회만 막고 캐시 data는 그대로 노출된다 — placeholderData(keepPreviousData)와 결합하면 칩 화면에 점령 마커가 남아 MSG-403 AC 1·2 회귀가 된다. 셸에서 거르는 대안은 게이트 판정을 두 곳으로 쪼개 어긋날 수 있어 훅 한 곳에 모았다 |
| 2026-08-17 | MSG-410 | 결정: 구 "99+" count 캡 폐기, 천 단위 구분 서식으로 교체. 마커 크기 3단은 count(tierOf)가 아니라 unit(동 48/구 56/시 64px) 기준 | Figma 14599:7042~7048 variant가 `단위=동/구/시`이고 예시 수치(118·348)가 세 자리를 그대로 노출한다 — 고정 44px 원 전제였던 캡의 근거가 사라짐. 크기 px는 Figma 실측 전 근사값으로, 검증 단계 Figma 대조에서 보정 전제 |
| 2026-08-17 | MSG-410 | 결정(재작업 1): 마커 크기를 Figma 1x 실측으로 보정(동 68/구 80/시 92px + 흰 링 3px `border-3 border-background`), 병합 임계 `MARKER_MERGE_PX`를 단일 64에서 **단위별 지름 Record(68/80/92)**로 재캘리브레이션 | 임계=지름이면 "중심 거리<지름 ⇔ 원이 실제로 겹침"이라 병합 조건이 시각 겹침과 정확히 일치. 단일 최대값(92) 일괄은 동 단위에서 24px 떨어져 보이는 마커까지 합쳐 과병합, 구 64 유지는 전 단위에서 겹친 마커를 방치 — 한 병합 입력은 단일 unit이므로 Record 조회로 충분(분기 비용 0). 링 색은 지도 타일과의 분리가 목적이라 지면색 토큰 border-background(흰 알약 라벨과 동일 관례) |
| 2026-08-17 | MSG-411 | 결정: `invalidateGridQueries`(features/upload)를 features/video-actions가 cross-feature import로 재사용 — 키 목록 복제 대안 기각 | 스펙 리스크 항목의 판단 위임 — features 간 import는 기존 선례가 상당(dex→map-home `map-query-policy`, dex→region `gated-query-status`)해 관례 위반이 아니고, 복제는 격자 무효화 대상이 바뀔 때 두 곳을 고치는 사고 씨앗. 업로드 확정·READY 전이·영상 삭제가 같은 격자 캐시 세트를 무효화하는 것은 동일한 도메인 계약이다 |
| 2026-08-17 | MSG-411 | 결정: 더보기 메뉴 컴포넌트(`VideoMoreMenu`)가 삭제 확인 모달·신고 모달·실패 토스트를 함께 소유하는 복합 구조 — 진입점(도감 갤러리 카드·미니 패널)은 트리거 버튼만 감싼다 | 메뉴·모달·토스트를 진입점마다 따로 조립하면 두 진입점에 상태 배선이 중복되고, Radix 메뉴가 항목 선택 시 닫히는 특성상 토스트·모달은 메뉴 밖에 살아남는 부모가 필요하다. 삭제 확인 모달은 열릴 때만 마운트해 카드 메타 보강용 getPlayback 조회가 모달 오픈 시에만 발사되게 함 |
| 2026-08-17 | MSG-411 | 결정: 미니 패널에서 삭제 성공 시 `onDeleted=onClose`로 패널을 닫는다 — 스펙 AC 4(모달 닫힘+무효화)에는 없는 최소 보강 | 삭제된 영상이 미니 패널에서 계속 재생 중이면 사용자가 삭제 실패로 오인하고, 캐시 무효화 재조회는 404로 귀결된다. 도감 갤러리는 목록 무효화 재조회로 카드가 빠져 후속 동작이 필요 없음 — 진입점별 후속을 optional 콜백으로 분리해 스펙 밖 동작을 최소화 |
| 2026-08-18 | MSG-411 | 결정(codex 커밋 전 리뷰 1 기각): playback 로딩 중(현재값 미확보) 공개 범위 선택의 PATCH 발사는 유지 — "확보 전 항목 비활성" 제안 불채택 | 같은 값 미발사(추정 5)는 알려진 현재값의 불필요 요청 절약이 목적 — 미확보 창에는 비교 대상도 ✓ 표시도 없어 선택이 명시적 설정 의사이고 서버 PATCH는 멱등 명세라 무해. 비활성화는 playback 조회 실패 시 공개 전환을 영구 불가로 만드는 더 나쁜 결함(재생 조회와 전환 가능 여부는 독립이어야 함) |
| 2026-08-18 | MSG-411 | 결정(codex 리뷰 2·3·4 반영): ② 공개 전환 in-flight 중 재발사 무시(훅 mutate 래핑, 선착 우선) ③ 삭제 후속(미니 패널 닫기+playback 캐시 remove)을 useDeleteVideo로 중앙화 — 초판 "onDeleted=onClose" 프롭 배선(위 8/17 행) 폐기 ④ invalidateGridQueries가 gridId null 시 격자 계열을 _id 부분 키로 광역 무효화 | ② 메뉴 재오픈으로 PATCH가 겹치면 늦은 구 요청이 최신 전환을 덮는 순서 역전 — 창이 1 RTT라 무시가 가장 단순한 결정적 동작. ③ 프롭 배선은 도감 진입점 누락을 실제로 냈다(codex 적중) — 양 소비처 onClose가 동일 스토어 close라 훅 중앙화가 진입점 무관 보장, playback은 invalidate가 아닌 remove(재조회 404). ④ 확정 차단 대안은 playback 실패 시 삭제 영구 불가라 기각, 키 목록 복제 대신 기존 함수 확장(단일 정본 유지 — 8/17 결정과 정합). 부수 발견: 재오픈된 Radix 메뉴는 jsdom에서 select 발화 불가, ky fetch는 mutate의 마이크로태스크 뒤(요청 수 단정 전 플러시 필수) |
| 2026-08-18 | MSG-411 | 결정(PR #62 리뷰 반영): ① 자동 소멸 토스트를 `use-auto-dismiss-toast` 공유 훅으로 추출하며 동일 문구 연속 설정 엣지(같은 값 setState 스킵 → 타이머 미재시작)를 내부 `{ text }` 참조 래핑으로 함께 해소 ② `useSetVideoVisibility` 반환에서 무가드 `mutateAsync`를 타입 레벨 제외 — "스킵 시 resolve 반환" 대안 기각 | ① 3곳 중복(두 번째 사용처 규칙 충족)이라 추출 정당, 엣지는 래핑 한 줄로 싸게 고쳐지고 페이크 타이머로 테스트 가능해 "JSDoc 명기 후 현행 유지" 선택지보다 해소가 낫다. ② 스킵된 호출이 성공 Promise로 resolve되면 호출부가 완료로 오인 — 존재하지 않는 API가 가짜 성공보다 정직하고 컴파일 에러로 오용이 조기 차단된다(@ts-expect-error 단정으로 고정) |
| 2026-08-18 | MSG-412 | 결정: ① 입력 중 칸 코드("서면 A"·"서면 A-")는 마지막 토큰을 구역어에서 떼고 버려 구역명 "서면"으로 매칭 — "입력 전체를 구역어로" 대안 기각 ② 격자 선택 줌 보장 헬퍼(`zoomForGridFocus`)를 features/search에 두고 `GRID_MIN_ZOOM`을 features/map-home에서 cross-feature import — 상수 복제 대안 기각 | ① 전체를 구역어로 쓰면 "서면 A"가 어떤 구역명에도 부분 일치하지 않아 칸 코드 완성 전 결과 0건 — AC 7(구역 매치 승인 포함)이 메우려던 UX 공백이 타이핑 중에 재발한다. 마지막 토큰이 A~Z 1자(+`-숫자?`) 꼴이면 칸 코드 진행 중으로 보는 것이 안전(구역명은 한글) ② 임계 복제는 저줌 게이트(`gateFillCells`)와 어긋날 때 하이라이트가 조용히 안 보이는 결함 — 단일 정본 유지가 우선(MSG-411 upload invalidate 재사용과 같은 관례) |
| 2026-08-18 | MSG-409 | 결정: PATCH 성공 응답(전체 상태) 기록 시 아직 왕복 중인 다른 카테고리의 낙관 값을 보존(`recordServerPreferences` in-flight 제외 병합) — "최신 응답 우선(무조건 덮어쓰기)" 대안 기각 | 스펙 리스크 명시 지점: 서로 다른 카테고리 연속 전환 시 먼저 온 응답이 나중 조작의 낙관 반영을 덮으면 토글이 눈앞에서 되돌아간다. in-flight 카테고리는 어차피 자기 응답이 확정하므로 보존이 안전하고, 순수 함수로 분리해 경합 시나리오를 유닛 테스트로 고정했다 |
| 2026-08-18 | MSG-409 | 결정: 카테고리 토글 PATCH 실패 안내는 push-notice-store 기존 "error" 타입·문구("알림 설정에 실패했어요 / 잠시 후 다시 시도해주세요") 재사용 — 신규 안내 타입+문구("설정 변경에 실패했어요") 추가 기각 | 추정 6(스택 재사용, 문구는 구현 시 확정)의 확정: 기존 문구가 알림 설정 변경 실패를 정확히 서술해 의미 왜곡이 없고, 타입 추가는 PushNoticeHost 카피 맵·스토어 계약을 늘리는 잉여 diff다(단순성 우선). 문구 분화가 필요해지면 그 티켓에서 타입을 나눈다 |
| 2026-08-18 | MSG-409 | 결정(검증 중복 게이트): 신규 5 패밀리(전원 테스트 파일 — 스모크 렌더 하네스·mutation 시나리오별 지연 응답 셋업·import 블록)를 nose.baseline.json에 의도 중복으로 등재 — 셋업 헬퍼 통합 대안 기각 | 스모크 하네스는 기존 profile-panel.smoke 관례 답습이고, nose 자체 힌트도 per-scenario setup을 통합 비대상으로 안내. 시나리오별 지연 응답 셋업을 헬퍼로 합치면 각 테스트의 경합 타이밍 의도가 가려져 가독성이 나빠진다 |
| 2026-08-18 | MSG-413 | 결정: 편집모드 선택을 badgeId 배열(순서=랭크=PUT badgeIds 순서) 단일 표현으로 통일 — 별도 rank 객체·직렬화 함수 없음. 해제 시 승격은 배열 제거의 자연 결과 | FeaturedBadgeRequestDto가 "배열 순서 = 표시 순서"라 배열이 곧 와이어 포맷. rank 필드를 따로 들면 승격 로직·직렬화가 추가로 필요해 기각 |
| 2026-08-18 | MSG-413 | 결정(검증 재작업): ① 훅 테스트 반복 셋업을 파일 로컬 `mountReplaceHook`+`mutateAndSettle`로 추출, 성공/실패 거울 케이스(AC 7·8)는 `it.each` 케이스 테이블로 통합 ② `errorEnvelope`를 `@/test/envelope-response`로 승격하고 기존 2파일(report-dialog·use-video-mutations) 로컬 사본을 import로 교체 ③ retry:false 하네스는 통합하지 않고 nose 베이스라인 재기록으로 수용 | ① 중복 게이트 신규 패밀리 2개 해소 — 베이스라인 등재 대안은 추출이 가독성을 해치지 않아 기각(단정은 케이스 데이터로 명시 유지, 테스트 수 1090 불변). ② 3번째 복제 발생 = 추출 트리거(envelopeResponse·stub-fetch 선례), 동작 동일한 기계적 교체라 커밋된 테스트 불변 원칙의 취지(단정 보호) 위반 아님. ③ query-wrapper.tsx의 선행 결정("에러 경로 retry:false 클라이언트는 각자 정의 — 통합하지 않는다")을 존중 — 통합 대안 기각. 베이스라인 갱신분은 전부 기수용 패밀리의 라인 이동 재키잉+MSG-413 파일의 관례 합류(티켓마다 갱신 커밋되는 기존 운영 방식 그대로) |
| 2026-08-18 | MSG-413 | 결정(codex 리뷰 P2 반영): 저장 PUT pending 중 "완료" 버튼 disable(`disabled={save.isPending}`) — 편집모드 이탈 경로를 닫아 늦은 `onSaved`가 재진입한 새 편집 세션을 닫는 레이스 차단. 진입 버튼 disable은 추가하지 않음 | 완료가 막히면 pending 중 편집 종료 자체가 불가라 레이스 도달 경로가 소멸 — 재진입 가드는 발생 경로 없는 방어 코드라 기각. 부수 발견: 재진입 시 `save.reset()`이 in-flight 가드(기준 9)까지 무력화했음(중복 PUT 가능) — 같은 수정으로 함께 해소. 흐름 스모크(PUT 보류 중 완료 disabled)로 RED 재현 후 고정 |
| 2026-08-18 | MSG-415 | 결정: ① 교체 확정 `useReplaceVideo`를 `useConfirmUpload` 미러(finalize만 생성 SDK `replace`)로 신설하고 두 훅을 위저드에 공존시켜 publish에서 replaceTarget 유무로 분기 — "confirm 훅에 모드 파라미터 주입" 대안 기각 ② `replaceTarget`에 스펙 필드(videoId·gridId) 외 zoneName·zoneCell을 추가 ③ 모드 문구는 `wizard-mode.ts` 순수 함수(`wizardModeCopy`·`replaceGridLabel`)로 분리, SelectStep·PreviewStep의 locationLabel을 `string | null`(생략 가능)로 확장 | ① 모드 파라미터 주입은 confirm의 입력 타입·무효화 세트(점령/뱃지 vs playback/도감)가 갈라져 조건 분기 덩어리가 됨 — 미러 훅이 각자 단순하고 orchestration·presign·S3는 이미 공용이라 중복은 finalize 껍데기뿐 ② 교체 모드 위치 라벨(추정 1 — 좌표 미전송이라 지도 중심 대신 대상 격자 라벨)의 전달 채널이 필요한데, 진입점(VideoActionTarget)이 이미 보유한 값의 부분집합 전달이 별도 playback 조회보다 싸다 ③ 라벨 미확보 시 지도 중심 행정동을 그대로 두면 "다른 위치로 교체되는 듯한" 오해를 유발 — 생략이 정직하다 |
| 2026-08-18 | MSG-419 | 결정: 모바일 인증 스토어를 zustand+persist 이식 대신 **모듈 스코프 팩토리 + useSyncExternalStore + 주입식 저장소 포트(`AuthPersistence`)**로 구현 (스펙 문언 "웹 팩토리 이식 + persist"에서 이탈) | apps/mobile은 zustand 미도입이 확립된 관례(search-store·profile-store·dex-tab 3선례)이고, 이 티켓만 의존성을 늘리면 스토어 두 방식이 공존한다. 토큰 저장소가 expo-secure-store(비동기)라 어차피 재수화가 비동기 게이트(`hydrated`)를 요구해 persist의 동기 재수화 이점이 없다. 포트 주입 덕에 네이티브 모듈 없이 순수 모델 테스트가 성립(모바일 vitest 원칙) |
| 2026-08-18 | MSG-419 | 결정: 생성물의 `'../client-config'` 치환을 metro `resolver.resolveRequest`뿐 아니라 **vitest 플러그인으로도 동일 규칙 재현** — 테스트에서 웹 client-config를 쓰거나 파이프라인 테스트를 목으로 우회하는 대안 기각 | 치환을 런타임에만 두면 테스트가 검증하는 배선(웹 Ky·localStorage 체인)이 실제 앱 배선과 달라져 AC 3·6 단정이 무의미해진다. 같은 조건(요청 문자열 + 웹 generated 디렉토리 출처)을 두 곳에 두는 대신 한쪽만 두면 드리프트를 아무도 못 잡는다 |
| 2026-08-18 | MSG-419 | 발견/결정: ky 2.x의 RN 호환(스펙 리스크 1)은 **feature-detect가 모듈 로드 시 1회**라 검증 테스트를 별도 파일로 분리해야 성립 — `AbortSignal.any` 삭제를 같은 파일 안에서 하면 이미 로드된 ky 상수가 그대로라 오탐(`AbortSignal.any is not a function`)이 난다. `ReadableStream` 부재는 node(undici) Response 생성이 그 전역을 요구해 러너에서 재현 불가 | vi.resetModules()가 node_modules 모듈을 되돌리지 않는 것을 실측으로 확인. 분리 후 "AbortSignal.any 없는 런타임에서도 생성 SDK → 모바일 Ky → 401 재발급 → 재시도"가 통과 — 리스크 1의 절반은 자동 게이트로 고정하고, 나머지(실기 Hermes)는 dev client 스모크로 남긴다 |
| 2026-08-18 | MSG-419 | 결정: QueryProvider·부트스트랩을 웹의 `app/` 대응 위치가 아니라 `shared/api/`·`features/auth/model/`에 배치 | expo-router가 `src/app/`을 라우트 전용으로 스캔해 기본 export 컴포넌트가 아닌 파일은 라우트 경고를 낸다. FSD 레이어 의미는 유지하되(프로바이더=인프라, 배선=features가 shared를 소비) 물리 위치만 라우터 제약에 맞춘다 |
| 2026-08-18 | MSG-419 | 발견/결정(재작업 1회차): `_layout.tsx`의 `EXPO_PUBLIC_STORYBOOK` 가드는 **호출만 막고 정적 import 체인을 못 막아** env 없는 Storybook 부팅이 client-config throw로 죽었다(검증 실측). 수정은 `_layout` 동적 import가 아니라 **client-config의 Storybook 분기 + 실패 fetch 가드** — 일반 모드는 부트 throw 그대로, Storybook 모드는 부트 통과·호출 시 throw | 동적 import는 `_layout`만 고쳐선 부족하다: expo-router는 dev(importMode `sync`)에서 `validateRouteTreeExports`로 **모든 라우트 모듈을 eager 평가**해 `app/dev/api-smoke.tsx`의 sdk 정적 import가 같은 체인을 재차 태운다(expo-router 57 `getRoutesCore.js:29-33`). 라우트마다 lazy 래핑을 요구하는 규칙은 MSG-422~431 전체에 지뢰를 남긴다. 반면 client-config 분기는 인터셉트 지점 1곳에 갇히고, 폴백 URL로 조용히 나가는 경로를 fetch 가드가 막아 AC 3의 "폴백 금지"를 유지한다 |
| 2026-08-18 | — | 결정: 앱 패키지명 확정 `kr.fillmap.app`(도메인 fillmap.kr 역순) — MSG-307에서 placeholder로 뒀던 expo 자동생성값 `com.anonymous.*`를 정식 역순 도메인으로 교체. android.package와 함께 그간 누락돼 있던 ios.bundleIdentifier도 같은 값으로 명시 | FCM은 패키지명으로 앱을 식별하고 Firebase는 등록 후 패키지명 변경이 불가능해, 푸시(MSG-418)를 붙이기 전에 확정해야 한다. 플레이 스토어도 `com.anonymous.*` 접두사를 거절한다. 파급: 네이버 지도 NCP 키가 구 패키지명으로 등록돼 있어 콘솔 재등록이 필요하고(미조치 시 안드로이드 지도 타일 인증 실패), 패키지명이 바뀌므로 기존 dev client는 별개 앱으로 설치된다 — 재빌드 필요 |
| 2026-08-18 | MSG-419 | 발견/결정(재작업 2회차, codex 리뷰): 재발급 in-flight 중 로그아웃하면 늦게 도착한 발급이 세션을 되살리고(토큰 디스크 재기록 포함), 재로그인한 새 세션은 옛 발급분에 덮어써진 뒤 만료 처리까지 됐다. 수정은 **세션 세대(generation)** — 스토어가 소유하고 logout이 증가, 파이프라인이 시작 시점 세대를 캡처해 완료 시 대조하고 다르면 `discarded`로 폐기(재시도·만료 처리 모두 없음). 함께 `onTokensIssued`를 `Promise<void>`로 바꿔 저장 완료까지 await하고 저장 실패를 재발급 실패로 취급, `token-storage.saveTokens`만 실패 전파로 전환 | "refreshToken이 null인가" 검사로는 로그아웃→재로그인 케이스를 못 잡는다(새 세션에도 토큰이 있다) — 세션의 **동일성**을 판정해야 해서 단조 증가 세대를 택했다. 시작 시점 refreshToken 값 비교 대안은 회전 토큰이 정상 흐름에서도 바뀌어 오탐이 난다. 저장 실패를 삼키면(기존 `void` 호출) 회전된 refreshToken이 디스크에 없는 채 재시도가 성공해, 앱 재시작 시 서버가 폐기한 옛 토큰만 남아 세션이 끊긴다 — 그래서 저장까지가 재발급 성공의 요건이다. 회귀 4케이스로 RED 재현 후 고정 |
| 2026-08-18 | MSG-414 | 결정: ① 잔디 날짜 산술은 `Date` 파싱 대신 epoch 일수 정수(Date.UTC 산술)로 — KST 라벨 문자열을 TZ 무관 계산, "오늘"은 todayKst 주입(실사용 진입점 `todayKstDate`) ② "가장 활발한 요일" 동률은 주 시작(일요일)부터 앞선 요일 승리, 창 내 업로드 0일이면 null로 두고 모달 부제에서 그 조각만 생략 ③ 월 라벨은 각 열의 일요일이 새 달에 진입하는 열 기준(첫 열 포함) — 시안의 4주 균등 배치 기각 ④ DexPanel의 BadgeErrorState를 message 파라미터화한 `DexTabErrorState`로 통합(기록 탭이 두 번째 사용처) | ① `new Date("YYYY-MM-DD")`는 UTC 자정 파싱이라 비KST 브라우저에서 하루 밀림(스펙 리스크 명시) — 정수 산술은 어느 TZ에서도 결정적이고 테스트도 고정 날짜로 안정 ② 스펙이 동률 규칙 미지정 — 요일 배열 순회의 초과 비교가 만드는 자연 순서를 규칙으로 명문화, 빈 창에 임의 요일을 지어내는 것은 기만 ③ 스펙 "Figma 오탐 방지"가 균등 배치를 플레이스홀더로 판정 ④ 동일 shape 두 번째 사용처 = 추출 트리거(성급한 공통화 아님) |
| 2026-08-18 | MSG-414 | 결정(검증): ① nose 신규 6패밀리 전부 베이스라인 재기록으로 수용 — confirm/replace 미러 훅(386e5205b3)은 MSG-415 의도 중복이 이번 invalidate 추가로 표면 위로 올라온 것, 테스트 4패밀리는 시나리오별 셋업 관례 합류+라인 재키잉, GalleryTabBody↔RecordTabBody div(0 removable)는 구조 유사일 뿐 ② AC 10 "재시도 클릭 시 재조회"를 record-tab 스모크에 검증 단계에서 1케이스 추가로 고정(실패→스텁 교체→클릭→본문 회복) | ① nose 자체 힌트가 시나리오별 셋업 통합을 만류 + MSG-413 "티켓마다 베이스라인 갱신 커밋" 운영 선례, 미러 훅 통합은 MSG-415에서 이미 기각된 결정 ② 어느 테스트도 재조회 회로를 단정하지 않아 커버 공백 — 안정 사용자 흐름이라 스모크 승격 기준 부합(page-verification 4단계 예외) |
| 2026-08-18 | ADHOC(헤더 대표 뱃지) | 결정: ① pill 공용 추출(`FeaturedBadgePills`) 시 ul에 `aria-label="대표 뱃지"` 부여 ② badge-tab 스모크의 `findByText("한 달 개근")` 2건을 `findAllByText(...).toHaveLength(2)`로 갱신 ③ `featuredBadgesOf`는 `initialFeaturedSelection` 재사용 대신 독립 순수 함수로 구현 | ① 메타 줄(가입일·기록 요약)을 대체하는 목록이라 익명 list는 SR에서 맥락 없는 항목 나열 — 접근 가능한 이름이 스펙 a11y(ul/li 시맨틱)의 자연 보완이고 role 기반 테스트 쿼리 지점이 됨, 시각 무변화(AC 7) ② 커밋 테스트 불변 원칙의 예외 조건 충족 — 현 스펙(AC 3)이 도감 헤더에 대표 뱃지 이름 노출을 요구해 이름이 화면 2곳(진열장+헤더 pill)이 됨, 단정 취지는 개수 명시(2)로 보존 ③ 재사용 대안(id 배열→find로 name 역참조)은 O(n²) 이중 순회 + 결과 형이 달라 껍데기만 공유 — filter·sort 4줄 중복이 더 읽기 쉬움(badge-showcase 확장이라는 스펙 지시와도 정렬) |
| 2026-08-18 | MSG-420 | 결정: ui-native `ActionSheet`가 홈 인디케이터 인셋을 `bottomInset?: number` prop으로 받고 `react-native-safe-area-context`에 직접 의존하지 않음 — 패키지 의존성 추가 대안 기각 | ui-native는 현재 RN·nativewind만 peer로 두는 순수 프레젠테이션 계층이고, BottomNav ↔ AppBottomNav가 이미 "safe-area 인셋은 앱 위젯이 소유" 선례를 만들었다. 시트 하나 때문에 패키지 의존성(+lockfile)을 늘리면 이후 모든 소비처가 safe-area 구현에 묶인다 |
| 2026-08-18 | MSG-420 | 결정: `tailwind-preset/native.ts` 보충 스케일에 1.25(5px)·6.5(26px) 추가 — 컴포넌트 쪽에서 근사 스텝(py-1·size-6)으로 대체하는 대안 기각 | v3는 동적 스케일이 없어 보충 배열이 유일한 표현 경로다(0.75·1.75 선례). CellBadge는 ui-web과 "동일 클래스 문법 미러"가 요구사항이라 py-1.25를 바꿀 수 없고, Checkbox 원형은 Figma 실측 26px — px 임의값은 금지이므로 스텝 추가가 규칙에 부합하는 유일한 해법 |
| 2026-08-18 | MSG-420 | 결정: StatTile 값 글자에 `text-fm-display`(20px) 사용 — Figma 실측 18px에 대해 임의값·신규 토큰 추가 대안 기각 | 토큰 세트에 18px 단이 없고, 컴포넌트 한 곳을 위해 타이포 토큰을 신설하면 Figma Text Styles 정본과 어긋난다(색·타이포 토큰은 Figma 변수가 정본). 가장 가까운 기존 단(20px)을 쓰고 JSDoc에 근사 사실을 남겼다 |
| 2026-08-18 | MSG-420 | 결정(중복 게이트): 신규 **5** 패밀리를 nose.baseline.json에 의도 중복으로 등재 — 공통 추출 대안 기각. ① ui-web↔ui-native CellBadge 스토리 보일러플레이트 ② VideoCard↔VideoRow 메타 2줄 마크업 ③ 스토리 meta 블록 ④ tab-stub-screen↔toast 중앙 정렬 View ⑤ **video-card↔grid-video-card↔theme-video-card 재생 오버레이**(+action-sheet↔modal-card 오버레이 관례는 기승인 패밀리의 연속 — 승격으로 카피가 1개 줄었다) | ①③은 웹/네이티브 미러와 Storybook CSF 형식 자체라 추출 대상이 아니고, ②는 두 카드(세로 카드 vs 가로 행)의 시각 계약을 묶는 결합, ④는 중앙 정렬 View의 우발 유사다. ⑤는 **스펙 추정 2가 "기존 두 카드의 VideoCard 이관은 이번 범위 밖"으로 승인한 이연 부채** — 소비 화면 티켓에서 이관하면 자연 해소되므로 여기서 통합하지 않는다(등재는 부채 은폐가 아니라 이 근거의 보존이다) |
| 2026-08-18 | MSG-421 | 결정(재작업 1회차로 정정): 히어로 장식은 ① `GridCell`·`Chip` 대신 평범한 `View` 조합으로 그리고 ② 일러스트·보상 행을 각각 `accessible` + `accessibilityLabel` 한 덩어리로 묶어 낭독을 요소 1개로 정리한다. **최초 채택했던 `accessibilityElementsHidden` + `importantForAccessibility="no-hide-descendants"`는 이 스택에서 아무 효과가 없었다** | 실기(`uiautomator dump`, RN 0.86.2 Fabric + NativeWind, emulator Pixel_8_2) 대조 실험 결과 `importantForAccessibility`는 className 유무·노드 종류와 무관하게 **완전히 무시된다**(일반 텍스트 행에 걸어도 트리에서 사라지지 않음) — 코드만 쓰고 실기 확인을 생략한 최초 판단이 틀렸다. 재사용 컴포넌트가 뿌리 원인이었다: `GridCell`·`Chip`은 `accessibilityRole="button"` 내장이라 탭 동작 없는 장식이 가짜 버튼 11개(1장 8 + 3장 3)로 낭독됐다 — 재사용 규칙은 상호작용 컴포넌트를 위한 것이므로 장식 목적에 한해 로컬 `View`로 대체했다(오케스트레이터 승인). 그룹핑은 이 스택에서 실제로 듣는 유일한 수단이며(라벨이 자식 텍스트 연결을 대체하는 것을 덤프로 확인), 결과적으로 1장은 포커스 4개(진행바·건너뛰기·일러스트·CTA), 3장은 4개(진행바·일러스트·보상 행·CTA)로 정리됐고 이름 없는 clickable 노드는 0개다 |
| 2026-08-18 | MSG-421 | 결정: `tailwind-preset/native.ts` 보충 스케일에 **13(52px 테마 격자 셀)만** 추가 — 임의값(`size-[52px]`)·근사 스텝(`size-12`=48px) 대안 기각. 초안에 함께 넣었던 42(168px 미니맵 고정 높이)는 **재작업 1회차에서 철회**했다 | MSG-420 선례와 동일 논리(v3는 동적 스케일 부재라 보충 배열이 유일 경로, 값 추가는 기존 렌더 무영향). 기존 사용처 0건을 grep으로 확인해 MSG-420의 `size-6.5` 같은 부수 효과가 없다. 42가 사라진 이유는 미니맵 고정 높이 자체를 없앴기 때문이다 — `h-42`가 4행째를 통째로 잘라내 정의한 셀 8칸 중 5칸만 보였고(검증 지적), 데이터를 3행으로 줄여 클리핑을 해소하자 고정 높이가 불필요해졌다 |
| 2026-08-18 | MSG-421 | 결정(중복 게이트): 온보딩 히어로가 만든 **신규 패밀리 6개**(families 233→237, members 608→623 = 합계 841→860)를 nose.baseline.json 재기록으로 수용 — 공통 추출 기각. 대표 예: login-screen↔onboarding-screen 헤드라인 텍스트 블록, onboarding-screen 건너뛰기↔search-screen "전체 삭제" 텍스트 Pressable, badge-shelf↔explore-hero-card 작은 pill View(마지막 1개는 재작업의 `Chip`→로컬 View 전환이 만든 pill 패밀리 3카피) | 전부 `similar · 0 removable`(공유 라인 0, 파라미터 1개)로 서로 무관한 feature 사이의 JSX 형태 우연 일치다. 추출하면 사용처 2곳이 각기 다른 도메인인 교차 feature 추상화가 생겨 단순성 원칙에 반하고, 두 화면의 시각 계약이 묶여 한쪽 디자인 변경이 다른 쪽을 깨뜨린다(MSG-413~420의 티켓별 베이스라인 갱신 운영 선례) |
| 2026-08-18 | MSG-421 | 발견/결정: NativeWind v4는 className **문자열 순서**가 아니라 **CSS 캐스케이드(생성 스타일시트 순서)** 로 충돌 승자를 정한다(런타임 `native-interop.js`가 클래스별 룰을 모아 `specificityCompare`로 재정렬, Order = Tailwind 출력 위치 = 후보 클래스명 알파벳순). 따라서 공통 컴포넌트 기본값을 className으로 덮는 3곳(VideoCard 썸네일 `bg-surface`→다크, Chip active `bg-primary`→`theme-hot`, GridCell selected 알파 상향)에 important 수식자(`!bg-…`)를 사용 | 순서 전제(`cx`가 뒤에 붙이니 이긴다)는 **거짓**이다 — `bg-foreground`는 `bg-surface`보다 알파벳이 앞서 스타일시트에 먼저 놓이므로 그냥 덧붙이면 다크 썸네일이 회색으로 렌더된다(승인 Q2의 전제가 무너짐). important 룰은 런타임이 normal 이후에 적용하는 별도 버킷이라 결정적이고, 대안(overlay 슬롯에 `absolute inset-0` 배경 레이어를 넣어 캐스케이드를 우회)은 재생 원 아래에 레이어를 끼워야 해 VideoCard 렌더 순서를 바꾸게 되어 기각. `packages/ui-native/src/lib/cx.ts` 주석("RN은 캐스케이드 충돌 병합이 필요 없다")은 이 사실과 어긋나는 **기존 오기** — 이번 티켓 범위 밖이라 수정하지 않고 발견으로 남긴다 |
| 2026-08-18 | MSG-421 | 결정(재작업 1회차): ui-native `SegmentedProgress` 루트에 `accessible` 추가 — 스펙의 "`accessibilityRole="progressbar"` 내장이라 S11 자동 충족" 전제를 정정 | Android(RN 0.86.2 Fabric)는 `accessible` 없이는 role·value만으로 접근성 노드를 만들지 않아 진행바가 **아예 낭독되지 않았다**(수정 전 덤프: 진행바 자리에 focusable 노드 0개). 수정 후 같은 자리에 focusable 노드가 생기고 TalkBack 실행 시 초기 접근성 포커스 사각형이 진행바 bounds에 정확히 놓이는 것을 확인했다. 값 추가라 모든 소비처의 시각 렌더는 불변이고 `index.ts` diff는 0줄로 유지된다(VideoCard optional props와 같은 성격의 비파괴 확장) |
| 2026-08-18 | MSG-422 | 결정: 동의 항목 카탈로그가 `hasTermsView: boolean` 대신 `termsTitle: string \| null`을 갖는다 — 전문 뷰 헤더에 동의 라벨 문장을 그대로 넣는 대안 기각 | 스펙 추정 8은 "항목명만 헤더에 바꿔 넣는다"였는데, 라벨 정본 문구("개인정보 수집 및 이용에 동의 (필수)")는 `AppHeader`의 1줄(numberOfLines=1, 가용 약 278px)에서 잘린다. 문서명을 카탈로그에 두면 "보기 유무"와 "전문 헤더 제목"이 한 필드로 표현돼 중복 없이 잘림도 사라진다 — AC 8(만 14세 행에만 보기 없음) 단정은 null 판정으로 그대로 유지 |
| 2026-08-18 | MSG-422 | 결정: "모두 동의" 행만 라벨을 `Checkbox` 밖 `Text`(`text-fm-heading` 16px)로 두고, 항목 5행은 `Checkbox`의 내장 `label`(13px)을 쓴다 — 전 행 통일 대안 2종 기각 | Figma는 모두 동의를 16px semibold, 항목을 14px로 그려 위계를 만드는데 `Checkbox` 내장 라벨은 `text-fm-body`(13px) 고정이라 통일하면 위계가 사라진다. 반대로 5행 전부를 밖으로 빼면 정확한 14px은 얻지만 라벨 탭이 죽어 터치 표적이 글리프(hitSlop 포함 44px)로 줄고 접근 가능한 이름을 5번 수기로 잇게 된다 — 강조 1행만 예외를 두는 편이 손실이 가장 작다(Checkbox JSDoc이 두 용법을 모두 정식 API로 명시) |
| 2026-08-18 | MSG-422 | 결정(중복 게이트): 신규 5패밀리를 nose.baseline.json에 의도 중복으로 등재 — 공통 추출 대안 기각. ① `consent-gate.ts`↔웹 `location-consent.ts` ② `use-consent-gate.ts`↔웹 `use-location-consent-gate.ts` ③ `use-update-location-consent.ts`↔웹 `useUpdateLocationConsent` ④⑤ `signup-consent-screen.tsx`의 View 블록이 각각 `login-screen.tsx`·`checkbox.stories.tsx`와 짝을 이루는 신규 2-copy 패밀리(`consent-terms-view.tsx`는 기존 11-copy view 패밀리에 합류, 0 removable) | ①은 스펙이 지시한 **parity 포팅** 그 자체다(웹 원본을 런타임에 import하면 웹 리팩터가 모바일 번들에 직결 — envelope·api-error가 세운 "복제 + parity 테스트" 관례). ②③은 import 경로(모바일 barrel)와 인증 스토어가 달라 공유 불가능한 플랫폼 미러이고, ④⑤는 flex 컨테이너의 우발 유사다 |
| 2026-08-18 | MSG-422 | 발견/결정(재작업 1회차): "로그인 직후 게이트 미표시"의 근본 원인은 게이트 판정이 아니라 **로그인 성공 핸들러의 `queryClient.clear()`**였다 — clear()는 Query를 destroy·제거하는데 그 순간 구독 중이던 옵저버는 재부착이 **자기 다음 렌더**(`setOptions`)에서만 일어나, 로그인 후 재렌더 트리거가 인증 스토어뿐인 루트 게이트가 조용히 취소된 `pending/fetching`에 영구 고정됐다. 수정은 `shared/api/reset-session-cache.ts`(=`resetQueries()`) 신설 후 로그인 경로 교체. 로그아웃 경로는 clear() 유지 | 실측 계측 로그가 결정적: `removed(obs:2)` → `added(obs:0)` → `observerAdded(obs:1)` → `success(obs:1)` — 두 옵저버 중 **다른 이유로 재렌더된** api-smoke 것만 새 Query로 옮겨 갔고 게이트 것은 남았다(프록시의 200도 그 쿼리였다). 대안 기각: ① 게이트 훅에 자체 쿼리 키 부여 — invalidate 기반 자동 해제(AC 18)가 깨지고 요청이 2배 ② 게이트를 캐시 이벤트에 직접 구독 — 증상 배선을 하나 더 얹는 것 ③ 캐시 비우기를 setTokens 앞으로 이동 — 순서 의존이라 다른 화면 옵저버에서 같은 결함이 재발 ④ 로그아웃도 resetQueries로 통일 — 거기서는 재조회가 곧 401이라 clear()가 옳다. `resetQueries`는 Query 객체를 유지한 채 상태만 되돌려 옵저버가 떨어지지 않고 활성 옵저버를 재조회시키므로 "이전 사용자 데이터 폐기 + 화면 갱신" 의도에 정확히 대응한다 |
| 2026-08-19 | MSG-422 | 결정(codex 리뷰 Medium 반영): 로그아웃 정산을 `await authStore.logout(); queryClient.clear();`에서 `const cleared = authStore.logout(); queryClient.clear(); await cleared;`로 재배열 — ① 순서 유지(무시) ② `void authStore.logout()`로 await 자체를 버리는 대안 기각 | `authStore.logout()`은 세대 증가·토큰 null 상태 전이를 **동기로** 끝내고 `persistence.clearTokens()`만 await한다. 그 await를 먼저 기다리면 구독자에게 비로그인이 이미 공표된 뒤 캐시가 비워지기 전까지 이전 사용자 데이터가 렌더되는 창이 남는다(①). await를 버리면 창은 닫히지만 저장소 정리 실패가 미처리 거부로 샌다(②). 반환 promise를 붙잡고 같은 tick에서 clear()를 부르면 두 결함이 동시에 사라진다 — 수행 내용·네트워크 거동·뒤로가기 1회 가드는 전부 불변이고 실행 순서만 바뀐다 |
| 2026-08-19 | MSG-422 | 결정(PR #70 리뷰 반영): 저장 비행 중에는 뒤로가기 이탈을 받지 않는다 — `abortSignup` 1회 가드에 `isSaving` 추가. ① 현행 유지(이탈 즉시 로그아웃) ② PUT을 취소하고 이탈 두 대안 기각 | ①은 이미 발사된 PUT이 뒤늦게 성공할 수 있어 "중단했는데 서버에는 consented:true가 남는" 불확정 상태를 만든다 — 동의 이력이 이 화면의 존재 이유이므로 허용할 수 없다. ②는 TanStack Query의 mutation에 취소 경로가 없고, 서버가 이미 받은 요청을 되돌리지도 못한다. 대기 창은 유한하다(ky 기본 timeout 10초 → isSaving 해제, 성공 시엔 게이트 자체가 닫힘) |
| 2026-08-19 | MSG-422 | 결정(PR #70 리뷰 반영): `resetQueries` 회귀 테스트의 단정을 "조회가 나가는가"에서 "앞 계정의 locationConsent가 다음 사용자 판정에 새지 않는가"로 교체 — 처음 쓴 전이 커버 테스트 유지 기각 | 처음 버전은 `clear()` 구현에서도 통과해 결함을 구분하지 못했다(실행으로 확인). 활성화 직후 **동기 시점**에 이전 세션 데이터 부재를 단정하면 `invalidateQueries`(데이터 보존)에서 실패한다 — 재조회 완료를 기다리는 단정은 어느 구현이든 통과해 결함을 가린다. 테스트가 잡아야 할 것은 요청 발사가 아니라 미동의 사용자에게 게이트가 열리는 누수다 |
| 2026-08-19 | MSG-424 | 결정: 업로드 스텝 전이·실패 분기를 화면이 아니라 `upload-flow-store`가 소유한다 — ① 웹처럼 화면 훅(`use-upload-wizard`) 하나에 상태를 모으는 대안 ② 각 화면이 자기 전이를 판정하는 대안 기각 | 모바일은 스텝이 **라우트로 갈라져** 있어 웹의 단일 모달 훅을 그대로 옮길 수 없고(①), 화면마다 판정을 두면 모바일에 렌더 테스트 인프라가 없는 조건(vitest.config 주석 — 스펙 R7)에서 전이 회귀 안전망이 실기 검증뿐이 된다(②). 스토어로 밀면 스텝 전이 8종·실패 분기 3종이 전부 vitest 단정 대상이 되고 화면은 "스토어가 정한 스텝으로 이동"만 한다 |
| 2026-08-19 | MSG-424 | 결정: 오케스트레이션 진행 상태(presign·s3PutDone)를 훅 `useRef`가 아니라 스토어에 둔다 — 웹 `use-upload-mutations`의 ref 보관 방식을 그대로 옮기는 대안 기각 | 기준 36이 "앱을 벗어났다 돌아와도 진행 상태 유지"를 요구하는데 ref는 화면 언마운트와 함께 사라져 복원 대상이 될 수 없다. 스토어에 두면 AsyncStorage 영속 스냅숏(`toPersisted`)에 자연히 포함되고, 재시도가 성공 단계를 건너뛰는 계약(기준 31·34)이 프로세스 재시작 뒤에도 유지된다 |
| 2026-08-19 | MSG-424 | 결정: presign/PUT/확정 이펙트 조립을 훅에서 분리해 `api/upload-effects.ts` 순수 팩토리로 둔다 — 웹처럼 `useMutation` 안에 인라인하는 대안 기각 | 모바일에는 훅 렌더 테스트 인프라가 없어 인라인하면 "무엇이 어떤 순서로 나가는가"(기준 24 — purpose 미전송·S3 PUT의 httpClient 미경유)를 고정할 수단이 사라진다. 팩토리로 빼면 전역 fetch 스텁 하나로 생성 SDK 경로와 S3 경로를 동시에 관찰할 수 있다 |
| 2026-08-19 | MSG-424 | 결정: picker가 확장자 없는 파일명을 주면 MIME에서 파생해 붙이되, 파일명이 이미 확장자를 가지면 **그대로 둔다** — ① 항상 MIME 기준으로 갈아 끼우기 ② 확장자 없으면 거부 두 대안 기각 | ①은 `clip.avi`를 `clip.avi.mp4`로 만들어 허용 목록 검증(MP4·MOV)을 무력화한다. ②는 안드로이드 `content://` 자산처럼 파일명이 없는 정상 영상을 반려한다. 파생은 "정보가 없을 때만"이라야 검증이 살아 있으면서 오반려도 없다 |
| 2026-08-19 | MSG-424 | 결정(중복 게이트): 신규 15패밀리를 nose.baseline.json에 의도 중복으로 등재 — 웹 모듈 공유(런타임 import) 대안 기각 | 포팅 5종(`upload-wizard`·`upload-validation`·`upload-orchestration`·`highlight-selection`·`processing-poll`)은 스펙이 지시한 **parity 포팅** 그 자체다. 웹을 런타임 import하면 웹 리팩터가 모바일 번들에 직결되고 드리프트를 테스트가 아니라 사고가 먼저 알린다(envelope·api-error·consent-gate가 세운 "복제 + parity 테스트" 관례). `location-label`·`invalidate-grid-queries`·`s3-upload`도 같은 성격의 플랫폼 미러다 |
| 2026-08-19 | MSG-424 | 발견/결정(검증 실패 1 재작업): 실기 S3 PUT 100% 403 `SignatureDoesNotMatch`의 근본 원인은 자격증명·contentLength가 아니라 **Expo winter fetch가 Blob body일 때 호출자의 `Content-Type` 헤더를 blob 자신의 type으로 덮어쓰는 것**이었다. 수정은 body를 `.blob()` → `.arrayBuffer()`로 교체. ① RN Blob에 type 지정해 재생성 ② XHR로 내려가 setRequestHeader 직접 제어 ③ `expo-file-system.uploadAsync` 도입 세 대안 기각 | 소스가 결정적이었다: `expo/src/winter/runtime.native.ts`가 `install('fetch', …)`로 globalThis.fetch를 갈아 끼우고, `RequestUtils.normalizeBodyInitAsync`가 Blob에 대해 `overriddenHeaders: [["Content-Type", body.type]]`을 돌려주며 `overrideHeaders`가 호출자 헤더를 **지운다**. 로컬 파일 읽기 응답에는 content-type이 없어 그 type이 빈 문자열이 됐고, presigned URL의 `X-Amz-SignedHeaders=content-length;content-type;host`라 서명이 어긋났다(웹은 브라우저 fetch가 명시 헤더를 우선해 같은 코드가 동작한다 — 웹 원본을 그대로 옮긴 것이 함정이었다). ArrayBuffer 바디에는 override 자체가 없어 한 줄로 해소되고, `.blob()`이 하던 "네이티브 blob store 복사 + base64 재읽기"(Expo 경고)도 함께 사라진다. ①은 네이티브 blob store를 한 번 더 복사하고 ②는 RN XHR도 같은 정규화를 타며 ③은 새 네이티브 의존성이라 dev client 재빌드·사용자 승인이 필요하다(R3 스트리밍 해소는 후속 과제로 남긴다 — `expo-file-system`은 Expo 툴체인의 전이 의존성으로만 존재해 `apps/mobile`에서 `require.resolve` 불가이므로 직접 의존성 추가와 dev client 재빌드가 선행돼야 한다. **R3(파일 전체를 JS 힙에 올리는 구조)는 잔존하며 현재 방어선은 500MB 사전 차단뿐이다**) |
| 2026-08-19 | MSG-424 | 결정(검증 실패 2 재작업): 저장된 스텝 복원을 **앱 진입점에서 `router.replace("/home")` + `router.push(복원 라우트)`** 로 구현. ① 복원 라우트로 곧장 `<Redirect>` ② 기준 36을 "상태 복원"으로 축소하고 주석만 정정 두 대안 기각 | ①은 스택에 홈이 없어 완료 화면 [확인]의 `dismissTo("/home")`과 [이전 단계로]의 `back()`이 갈 곳을 잃는다(expo-router는 플랫 Stack이라 URL로 하위 스택을 만들지 않는다). 실기에서 복원 후 하드웨어 백이 지도 홈으로 가는 것까지 확인했다. ②는 티켓 요구("업로드 중 앱을 벗어났다 돌아와도 진행 상태가 유지된다")를 절반만 이행한다 — 상태는 살아 있는데 사용자는 그 화면으로 돌아갈 방법이 없다 |
| 2026-08-19 | MSG-424 | 결정(검증 지적 3·6 재작업): 테스트를 "통과하지만 아무것도 못 잡는" 형태에서 결함 재현형으로 교체 — R4 타임아웃은 가짜 타이머로 **실제 만료 시각**을 관측하고, 위치 조회는 조회 옵션 빌더를 별 파일로 분리해 네트워크 경계에서 좌표 전달을 단정한다 | 상수 비교(`toBe(120_000)`)와 인스턴스 비교(`not.toBe(httpClient)`)는 `.extend({})`에서도 통과한다 — 변이 실험으로 실증했다(연장 제거 시 새 테스트만 실패). 위치 조회는 훅 파일이 expo-location을 끌고 와 vitest import 자체가 불가능했으므로 옵션 빌더를 분리하는 것이 유일한 관측 경로였다(token-storage의 포트 분리와 같은 이유). 두 테스트 모두 변이 주입으로 실패를 확인한 뒤 남겼다 |
| 2026-08-19 | MSG-424 | 발견/결정(재검증 2차 실패): `[업로드하기]` 연타가 **서버에 중복 영상을 실제로 생성했다**(프록시 실측 videoId 287+288 / 289+290, 2/2 재현). 원인은 가드가 `confirm.isPending`(비동기 React 상태) 하나뿐이라 같은 틱의 두 번째 탭 시점에 아직 false였던 것. 수정은 스토어에 **동기 잠금**(`beginFlight`/`endFlight`)을 두고 발사 래퍼(`confirm.publish`·`analyze.start`)가 획득, 뮤테이션 `onSettled`가 해제. ① `useRef` 화면 로컬 가드 ② `mutationFn` 진입부 가드 두 대안 기각 | ①은 `analyzing-screen`의 `started`와 같은 결이지만 화면 로컬이라 언마운트·재마운트에 사라지고 스토어가 이미 오케스트레이션 상태를 소유한 구조와 어긋난다. ②는 `mutate()`가 mutationFn을 마이크로태스크로 미뤄 실행하므로 **같은 틱의 두 호출이 둘 다 통과한다** — 가드는 반드시 발사 지점에서 동기여야 한다. 해제를 per-call `onSettled`가 아니라 뮤테이션 옵션에 둔 이유: 화면이 언마운트되면 per-call 콜백은 실행되지 않아 잠금이 남는다. 잠금은 영속 대상에서 제외했다(강제 종료된 잠금이 살아남으면 재실행이 영구히 막힌다) + 진행을 버리는 전이(`clearedFlow`·`reset`·`backToHighlight`)가 함께 해제한다. 변이 주입(동기 판정 제거)으로 새 테스트 2건만 실패함을 실증했고, 실기 연타 3~4회 × 2회차에서 `POST /api/videos`가 각각 1건만 나가는 것을 프록시로 확인했다 |
| 2026-08-19 | MSG-424 | 결정(codex 리뷰 P1): 미리보기 `[이전 단계로]`의 복귀 지점을 `router.back()`(스택 이력)에서 **흐름 상태 기반 명시 이동**(`backRouteFromPreview` → `router.replace`)으로 교체. ① 현행 `back()` 유지 ② `navigate`로 기존 항목에 되돌아가기 두 대안 기각 | 재작업 2가 넣은 복원 경로가 `replace("/home") + push(복원 라우트)`로 스택을 만들기 때문에, **정상 진입과 복원 진입의 스택 모양이 다르다**: 정상은 `[/home, /upload/highlight, /upload/preview]`라 back()이 하이라이트로 가지만, 콜드 스타트로 미리보기가 복원되면 `[/home, /upload/preview]`뿐이라 back()이 **홈으로 나간다** — 스토어는 이미 highlight로 전이시켜 놓은 채로 화면만 플로우 밖으로 빠지는 상태다(①). 이력에 기대는 판정은 두 진입 경로 중 하나에서 반드시 틀리므로, 복귀 지점은 스택이 아니라 흐름 상태가 정해야 한다. ②는 정상 경로에선 깔끔하지만 복원 경로에선 하이라이트를 미리보기 **위에** 쌓아 뒤로가기가 스텝이 지난 미리보기로 떨어진다. `replace`는 두 경로 모두 `[/home, /upload/highlight]`로 수렴해 결정적이다(대가: 정상 경로 왕복 시 스택에 동일 화면 항목이 하나 늘지만 렌더 결과가 같아 사용자에겐 보이지 않는다). 순수 매핑으로 빼 "복원된 미리보기에서 뒤로 = 하이라이트"를 vitest로 고정했고(변이 주입으로 실효성 확인), 실기 콜드 스타트에서도 하이라이트 도달을 관측했다 |
| 2026-08-19 | MSG-424 | 결정(codex 리뷰 P2): `[업로드하기]` 비활성 조건과 발사 가드를 순수 판정 `canPublishUpload`로 단일화하고 좌표 미확보 시 "위치 확인 중…"을 표시. 발사 가드에만 좌표 조건을 두는 현행 유지 기각 | `resolveMapCenter()`가 비동기라 진입 직후 `center`가 null인데 버튼은 활성이었다 — 누르면 `publish()`가 조용히 반환해 **주 동작이 고장난 것처럼 보인다**(무응답 버튼). 비활성 조건과 가드가 서로 다른 규칙을 쓰던 것이 원인이라 한 함수로 모으고, 비활성만으로는 이유가 안 보이므로 라벨로 대기 상태를 노출했다. 회귀는 vitest로 고정(좌표 조건 제거 변이 시 실패 확인) |
| 2026-08-19 | MSG-424 | 결정(codex 2회차 P1-1): 오케스트레이션 진행을 **흐름 종료 시점이 아니라 단계 완료 시점마다** 스토어에 기록한다 — 이펙트 데코레이터 `withFlowProgress`를 `runUploadFlow`에 감싸 주입. ① 현행(반환·catch에서 한 번) 유지 ② 포팅 파일 `upload-orchestration.ts`에 진행 콜백 추가 두 대안 기각 | ①이 부족한 이유가 요점이다: `runUploadFlow`는 단계 진행을 함수 지역 변수에 들고 있다가 반환하거나 throw할 때 한 번만 내보내는데, **프로세스가 죽으면 catch 블록도 실행되지 않는다** — presign·S3 PUT이 끝난 뒤 앱이 종료되면 그 진행이 어디에도 남지 않아 콜드 스타트 재개가 이미 끝난 단계를 다시 실행했다. "재시도가 성공 단계를 건너뛴다"(기준 31·34·36)는 계약이 프로세스 재시작에서만 조용히 깨져 있었고, 3차 검증의 콜드 스타트 확인은 플로우가 정상 종료된 뒤여서 이 창을 못 봤다. ②는 스펙이 "그대로 복제 + parity"로 지정한 포팅 5종의 본문을 바꿔 웹과의 동등성 근거를 흐린다 — 대신 **이펙트 호출 시점이 곧 단계 경계**라는 성질을 썼다(`putToS3(presign)` 인자 = presign 확정 직후, `finalize(s3Key)` 호출 = PUT 완료 직후). 실기 실증: presign 후 S3 PUT 도중 force-stop → 재개 시 **presign이 다시 나가지 않고**(프록시 총 1건) 확정 1건으로 완주했다. **남는 창(의도적)**: 확정 요청 후 응답을 못 본 채 종료되면 재개가 finalize만 다시 쏴 영상이 한 번 더 생길 수 있다 — 클라이언트만으로는 판별 불가하고 서버 멱등 키가 필요하다(환류 대상, `flow-progress.ts` 주석에 명시) |
| 2026-08-19 | MSG-424 | 결정(codex 3회차 P1): 게시 성공 응답을 받은 **그 시점에** 재개 가능한 진행을 정리한다 — `settleUploadSuccess`(뮤테이션 옵션 onSuccess)가 격자 무효화와 함께 `uploadFlowStore.reset()`을 호출. ① 완료 화면 [확인]에서만 정리(현행) ② `completed`를 영속해 재개 시 완료 화면을 다시 보여주기 두 대안 기각 | ①이 남긴 창이 결함이었다: 완료 오버레이는 **화면 로컬 상태**라 그게 떠 있는 동안 앱이 종료되면 스토어에 `step:"preview"`와 `confirm:{presign, s3PutDone:true}`가 그대로 남고, 다음 실행이 미리보기로 재개돼 게시가 다시 활성화된다 — 오케스트레이션은 PUT까지 끝난 상태라 **finalize만 다시 쏴 중복 영상이 생긴다**. 2회차에 남긴 "확정 응답을 못 본 채 종료" 창(서버 멱등 키 필요)과 달리 이건 **응답을 이미 받은** 뒤라 클라이언트가 닫을 수 있다. ②는 영속 스키마에 완료 상태를 추가하고 재개 라우팅에 분기를 더하는데, 업로드가 이미 끝난 마당에 화면을 복원할 실익이 없다(블러 완료는 알림으로 온다 — 티켓 전제). per-call 콜백이 아니라 뮤테이션 옵션에 둔 이유는 endFlight와 같다 — 화면이 언마운트되면 per-call은 실행되지 않는다. 실기 실증: 완료 화면 표시 중 force-stop → 재시작이 지도 홈으로 뜨고 `POST /api/videos`는 1건에 머물렀다 |
| 2026-08-19 | MSG-423 | 결정: 지도 "이동 종료" 판정을 스펙의 `onCameraChanged` + 500ms 디바운스 대신 SDK 내장 `onCameraIdle`로 구현하고, **최초 1회만** `onCameraChanged`로 뷰포트를 씨딩한다 | 스펙 자체가 "라이브러리에 idle 계열이 있으면 그것을 우선"이라 열어둔 경로이고, `@mj-studio/react-native-naver-map` v2.9.0에 `onCameraIdle`이 있다(제스처가 완전히 끝나야 발화 — 요구 5의 "정지 후 재조회"와 정확히 일치). 디바운스 대안은 매 카메라 프레임마다 부모 `setState`를 일으켜 시트·카드 목록 전체를 60fps로 재렌더시킨다. 씨딩 한 줄이 필요한 이유는 idle이 "움직인 뒤 멈출 때"만 오기 때문 — 사용자가 지도를 건드리지 않으면 진입 후 조회가 영영 나가지 않는다. 남은 버스트 억제는 역지오코딩 훅의 500ms 디바운스(웹 원본 그대로)가 백업으로 맡는다 |
| 2026-08-19 | MSG-423 | 결정: 시트 상태 파생 입력을 "쿼리 상태 플래그 + 카드 개수"로만 두고(`deriveSheetState(statuses, cardCount)`), 게이트가 닫힌 쿼리는 `gatedQueryStatus`가 **도착으로 접는다** | 요구 7("실패 시 목 폴백 금지")을 문서가 아니라 **타입으로** 고정하려는 설계다 — 입력에 데이터 배열이 없으면 목 상수를 끼워 넣을 자리 자체가 없고, 실패가 하나라도 있으면 어떤 카드 수에서도 `list`가 나오지 않는다(L7이 실패 표본 전건으로 단정). 게이트를 도착으로 접는 이유는 비로그인·행정동 밖(바다 등)에서 조회를 안 하기로 한 쿼리가 영원히 pending으로 남아 시트가 무한 로딩에 걸리기 때문 — 웹 `gatedQueryStatus`의 isPending 게이트와 같은 문제·같은 해법이다 |
| 2026-08-19 | MSG-423 | 결정: 시트 1열 카드(`VideoCard`)에 `onPress`를 배선하지 않는다 — ① 격자 상세(`/grid/{cellId}`)로 보내기 ② 탭해도 아무 일 없는 버튼으로 두기 두 대안 기각 | ①은 좌표계가 어긋난다: 격자 상세는 구 위경도 셀 id + mock 데이터 기반([제외 범위] "기존 유지")이라 서버 5179 격자에서 유도한 id는 `hasCellVideos`가 전부 false로 떨어뜨려 결국 no-op이고, 그 사이 mock 상세가 열릴 위험만 남는다. ②는 스크린리더에 "가짜 버튼"을 노출해 S11이 금지하는 바로 그 상태다(MSG-421 실측 교훈). 목적지가 정해질 때까지는 카드도 "전체 보기"와 같은 표시 전용으로 두고, 라우팅은 후속 티켓에 넘긴다 |
| 2026-08-19 | MSG-423 | 결정: 실패 상태의 "다시 시도" 버튼을 웹 `RetryNotice`의 `variant="secondary"` 대신 `primary`로 둔다 | 웹 패널은 배경이 `bg-background`라 secondary(같은 흰색)도 텍스트 버튼으로 읽히지만, 모바일 시트 배경은 `bg-surface-elevated`(역시 흰색)이고 ui-native secondary는 테두리가 없어 터치 대상이 배경과 완전히 동일해진다. 토큰만 사용하는 범위에서 affordance를 살리는 최소 변경이며, ui-native `Button`에 outline variant를 추가하는 대안은 `index.ts` 잠금(MSG-420)과 무관하게 이번 티켓 범위를 넘는다 |
| 2026-08-19 | MSG-423 | 결정(중복 게이트): 신규 7패밀리 + 변경 1건을 nose.baseline.json 재기록으로 수용 — 공통 추출 기각 | 전부 이 티켓이 **지시받은 포팅** 그 자체다: `viewport-query`(34줄 공유)·`grid-5179`·`grid-card`·`map-query-policy`는 웹 원본의 복제 + parity 테스트 관례(D1, envelope·api-error 선례)이고, `use-region-grids-query` 등 api 3종은 import 경로·인증 스토어가 플랫폼마다 다른 미러다. 런타임 공유(웹 모듈 직접 import)는 웹 리팩터가 모바일 번들에 직결되는 결합을 만들어 관례가 이미 기각한 방향이다 — 드리프트는 번들이 아니라 parity 테스트가 잡는다 |
| 2026-08-19 | MSG-423 | 결정(codex 리뷰 P2-1 반영): 게이트가 닫힌 쿼리의 `retry`를 no-op으로 만든다 — ① 현행 유지 ② 화면의 `handleRetry`에서 활성 쿼리만 골라 부르기 두 대안 기각 | TanStack Query의 `refetch()`는 `enabled`를 **무시하고** 요청을 보낸다. 시트의 공용 재시도가 세 쿼리를 한꺼번에 부르므로, 하나만 실패해도 `regionCode: ""`(행정동 미확정)인 지역 격자 요청이나 span 상한을 넘긴 뷰포트의 점령 격자 요청 같은 **잘못된 요청**이 함께 나간다(①). ②는 활성 여부 판정이 훅 밖으로 새어 나가 화면이 각 쿼리의 게이트 조건을 다시 알아야 하고, 훅이 늘 때마다 같은 실수가 재발한다 — 게이트를 아는 유일한 지점(`gatedQueryStatus`)에서 막는 편이 호출부 수와 무관하게 성립한다 |
| 2026-08-19 | MSG-423 | 결정(codex 리뷰 P2-2 반영): `toOccupiedCells`의 중복 판정 기준을 `gridId` → **스냅 이후 모바일 셀 인덱스(`col:row`)** 로 교체 — 스펙 L4 문구("같은 gridId 중복은 1건")와 의도적으로 달라진 지점 | 승인 Q2 A안(5179 중심 → 모바일 인덱스 스냅)의 직접적 귀결이다: 5179 셀은 100m 정사각인데 모바일 경도 스텝은 부산 위도에서 약 104m라, 두 체계의 경계가 어긋나는 지점에서 **서로 다른 gridId 둘이 한 모바일 셀로 스냅된다**(서면 일대 20×20 격자 훑기에서 충돌 20쌍 실측 — 예: `16850_11412`·`16850_11413` → `{col:112218,row:39056}`). gridId로만 접으면 같은 인덱스가 통과해 폴리곤이 겹쳐 그려지고 `GridMap`의 셀 key(`col:row`)가 충돌한다. 스펙 문구는 "중복 응답"만 상정했고 스냅 이후의 충돌은 예견하지 못한 것이라, 함수의 **출력 계약**(모바일 셀 목록)에 맞춘 인덱스 기준이 옳다 — 인덱스 기준은 gridId 중복도 함께 흡수하므로 L4의 원 문구도 계속 성립한다 |
| 2026-08-19 | MSG-423 | 결정(PR #72 리뷰 반영): `gatedQueryStatus`에 `settled`(게이트 판정 확정 여부)를 **필수 3번째 인자**로 추가하고 `isResolved: (!active && settled) \|\| data !== undefined`로 바꾼다 — ① 화면에서 `viewport === null`·`hydrated`를 보고 분기 ② `settled` 기본값 `true`로 선택 인자화 두 대안 기각 | 게이트가 "조회하지 않기로 확정"과 "아직 모른다"를 구분하지 못하고 둘 다 도착으로 접어, 카드 0건이 `empty`("영상이 없어요")로 샜다 — 무한 로딩을 막으려던 장치가 정반대의 틀린 상태를 노출한 것이다. 미판정 경로는 둘이다: **경로 ①** 보안 저장소 재수화 전(`isAuthenticated`는 부팅 직후 무조건 false이고 그 갭을 메우라고 `hydrated`가 따로 있다 — 로그인 사용자의 콜드 진입에서 empty→loading→list 깜빡임) **경로 ②** 지도 뷰포트 미확정(`map-home-screen`의 `viewport`가 `useState<Viewport \| null>(null)`이라 **첫 프레임에는 세 게이트가 전부 닫힌다** = 인증과 무관하게 매 콜드 스타트가 empty). 리뷰가 제안한 `settled = hydrated`만으로는 ②가 안 잡힌다 — 재수화가 지도 씨딩보다 먼저 끝나면 `hydrated=true`인데 `viewport`는 여전히 null이라 다시 empty로 떨어지는 경합이 남는다. ①은 판정 책임이 훅 밖으로 새어 후속 티켓에서 훅이 늘 때마다 재발하고, ②는 호출부가 판정을 잊어도 조용히 옛 동작으로 돌아가므로 필수 인자로 강제했다. 훅별 `settled`: occupied=`hydrated && bounds !== null`, geocode=`hydrated && debounced !== null`(디바운스 훅이 초기값을 지연 없이 노출하므로 null은 뷰포트 미확정뿐), region-grids=`hydrated`(regionCode null의 두 사유를 이 훅은 구분 못 하지만, 역지오코딩 미도착이면 그 훅 자신이 미도착이라 시트가 이미 로딩이고 도착 후 null이면 행정동 밖 확정이라 empty가 옳다) |
| 2026-08-19 | MSG-423 | 결정(중복 게이트): PR #72 리뷰 반영이 만든 신규 1 + 변경 1 패밀리(`use-reverse-geocode-query`·`use-region-grids-query`의 웹↔모바일 미러)를 baseline 재기록으로 수용 — 공통 추출 기각 | 기존 수용 근거의 연속이다(D1: 웹 원본 복제 + parity, 런타임 공유는 웹 리팩터가 모바일 번들에 직결돼 기각). 변경분이 뜬 이유는 모바일 사본에 `settled` 계산 3줄이 들어가 지문이 바뀐 것뿐이고, 두 사본은 오히려 **더 갈라졌다** — 웹 auth 스토어는 동기(localStorage)라 재수화 갭이 없어 `settled` 축 자체가 필요 없다. 참고로 웹 `gatedQueryStatus`는 여전히 2인자이며 같은 계열의 미판정 경로가 있는지는 이 티켓 범위 밖이다(웹은 재수화 갭이 없어 경로 ①은 성립하지 않는다) |
| 2026-08-19 | MSG-425 | 결정: 갤러리 2단 조회(`by-grid` → `collections/videos`)를 셸이 아니라 `region-gallery-view`가 소유 — 스펙 셸 구조표의 "셸이 쿼리 소유" 문구를 부분 이탈 | 두 쿼리는 선택된 동에만 의미가 있어 셸이 들면 `selected === null` 구간 내내 비활성 쿼리를 들고 있어야 하고, 스펙이 명시한 "동 변경 시 key 리마운트로 상태 초기화" 계약과도 어긋난다. 웹 선례(`GalleryTabBody`)도 같은 배치이며, 셸이 소유해야 하는 것(스펙 스위치 2줄 계약)은 tab·selected·removed 상태와 상단 3쿼리다 — MSG-430/431 확장 계약은 그대로 성립 |
| 2026-08-19 | MSG-425 | 결정: `dex-format`의 통계 타일 포맷터를 스펙 표의 `formatBadgeCount` 대신 `formatCountTile`·`formatStreakTile` 2종으로 명명 — 이름 그대로 승계 대안 기각 | Figma 통계 타일은 `6개`(수집 격자)·`4개`(획득 뱃지)·`12일`(스트릭) 3칸인데 스펙 표에는 개수 포맷터 1개(`formatBadgeCount`)만 있고 스트릭 포맷터가 없다(추정 A6이 `formatStreak` 폐기와 `12일` 표기를 동시에 요구). `formatBadgeCount`를 격자 타일에도 쓰면 이름이 용도를 속이고, 격자용을 따로 만들면 같은 한 줄이 두 번 정의된다 — 역할 이름 2종이 두 문제를 함께 없앤다 |
| 2026-08-19 | MSG-425 | 결정: 모바일 포팅본에서 `.at(-1)`(shortRegionName)·`toSorted`(gallery-groups)를 인덱스 접근·스프레드 sort로 치환 — 웹 원본 그대로 복제 대안 기각 | Hermes 0.17(RN 0.86)에 `Array.prototype.toSorted` 미구현(`sort-grid-videos.ts` 선례 — 사용 시 런타임 크래시). 문자 단위 동일 복제는 테스트(Node)에서만 통과하고 실기에서 깨진다. 결과 동등성은 parity 테스트가 값으로 고정하므로 구현체가 달라도 드리프트는 감지된다 |
| 2026-08-19 | MSG-425 | 결정: 갤러리 선택 상태 타입으로 `RecentRegion`을 그대로 쓰고 웹의 `GallerySelection`(regionName+gridId) 대응 타입을 신설하지 않음 | `RecentRegion`이 이미 `regionName`과 `sampleGridId`를 둘 다 갖는다. 웹은 지도 오버레이 격자 클릭이라는 두 번째 진입점이 있어 별도 선택 타입이 필요했지만(도감 목록에 없는 격자에서도 열린다), 모바일은 진입점이 동 행 하나뿐이라 타입 하나가 더 생길 이유가 없다(추정 A5로 지도 영역 자체가 없다) |
| 2026-08-19 | MSG-425 | 결정(중복 게이트): 신규 클론 패밀리를 nose.baseline.json에 의도 중복으로 등재 — 공통 추출 대안 기각. `recent-regions`·`gallery-groups`(+`latestCreatedAt`)·`use-region-stat-query`가 웹 원본과 짝을 이룬다 | 앞 둘은 티켓이 지시한 **parity 포팅** 그 자체다(웹을 런타임 import하면 웹 리팩터가 모바일 번들에 직결 — envelope·api-error·consent-gate가 세운 "복제 + parity 테스트" 관례). 쿼리 훅은 생성 옵션 import 경로(모바일 barrel)와 staleTime 정책이 달라 공유 불가능한 플랫폼 미러다 |
| 2026-08-19 | MSG-425 | 결정: `mock-profile.test.ts`의 "스트릭이 도감 mock과 일치" 케이스를 삭제 — 리터럴 12로 치환 대안 기각 | 앵커였던 `MOCK_COLLECTION_SUMMARY`가 이 티켓의 필수 삭제 대상이고, 도감 스트릭이 실 API(`summary.currentStreak`)로 바뀌어 "두 화면이 같은 사용자를 다르게 말하지 않는다"는 불변식의 한쪽이 사라졌다. 리터럴로 남기면 `streakDays === 12`라는 동어반복만 남아 아무것도 검증하지 못한다 |
| 2026-08-19 | MSG-425 | 결정(codex 리뷰 P2 반영): 셸 `ScrollView`에 ref를 달아 `selected`·`tab` 변경 시 `scrollTo({y:0, animated:false})` — ① 현행 유지(무시) ② 갤러리 뷰를 별도 `ScrollView`로 분리 두 대안 기각 | 셸이 `ScrollView` 하나를 계속 들고 본문만 갈아끼우므로 목록 아래쪽 동을 눌러 갤러리를 열면 직전 오프셋이 남아 갤러리가 중간부터 보인다(헤더·첫 영상이 가려짐). ①은 목록 상단 행만 눌러 본 검증에서 드러나지 않은 실결함이고, ②는 셸의 단일 스크롤 컨테이너 구조를 깨 탭 전환·복귀 경로마다 스크롤 상태가 갈린다. 세 경로(갤러리 진입·복귀·탭 전환)를 effect 한 곳에서 처리하면 구조 변경 없이 전부 해소된다. `animated:false` — 화면이 이미 교체된 뒤라 스크롤 연출은 이동으로 읽히지 않는다 |
| 2026-08-19 | MSG-426 | 결정: `알림 받기` 단일 토글 ↔ 서버 카테고리 5종 매핑을 **표시=`some`(하나라도 on이면 ON) / 켜기·끄기=5종 일괄 PATCH / 하나라도 실패하면 스냅숏 전체 롤백**으로 합성. 대안 기각 ① 표시=`every` ② 부분 성공분만 유지(부분 롤백) ③ 백엔드 일괄 엔드포인트 대기 | 서버 preferences에 마스터 스위치가 없어 FE가 합성할 수밖에 없다(형제 티켓 MSG-429가 "세분 설정은 프로필 단일 토글로 갈음"을 명시). ①은 부분 상태를 OFF로 보여 "껐는데 알림이 온다"는 더 나쁜 혼란을 만든다. ②는 화면 토글이 어느 쪽도 아닌 상태를 표현할 수 없어 표시가 거짓이 된다 — 전체 롤백 후 다음 진입의 GET이 정본으로 수렴시키는 편이 정직하다. ③은 티켓을 막는다. 부작용(웹 세분 설정을 앱 토글이 덮어씀)과 일괄 PATCH 엔드포인트 요청은 지라 환류 |
| 2026-08-19 | MSG-426 | 결정: mutation 훅을 **옵션 팩토리 + 얇은 훅**으로 쪼개고 테스트는 `MutationObserver`로 옵션 객체를 직접 구동. 세션 종료(`authStore.logout`)는 팩토리 인자로 주입. 대안 기각 ① `vi.mock`으로 auth-session 대체 ② RN 렌더 테스트 인프라(jest-expo·RNTL) 도입 ③ 훅 테스트 포기 | 모바일 vitest는 순수 모델 한정(MSG-292 확정 4)이라 `renderHook`이 없고, `auth-session`을 import하면 expo-secure-store가 Flow 구문으로 파싱 실패한다(실측: `SyntaxError: Unexpected token 'typeof'`). ①은 통합 신뢰를 잃고(테스트 템플릿이 모듈 mock을 금지) 주입 지점이 코드에 남지 않는다. ②는 티켓 범위 밖의 인프라 결정이다. ③이면 기준 2·3·5·6·10·15가 전부 무검증으로 남는다. 주입은 이미 `setupAuthPipeline({store, goToLogin})`이 세운 RN 경계 관례다 |
| 2026-08-19 | MSG-426 | 결정: `profile-store`(모듈 스코프 mock 스토어)를 폐기하고 닉네임·이메일·가입일·프로필이미지를 `getMe` 실값으로 전환, 로딩·실패에는 `MOCK_PROFILE` 폴백. 대안 기각 ① mock 유지 ② 스토어를 서버값 캐시로 남김 ③ 조회 성공까지 화면 게이트 | ①이면 `profileImageUrl`이 영원히 null이라 기준 12·15("기본 이미지로 되돌리기")가 구조적으로 관찰 불가능한 죽은 코드가 된다 — 티켓 동작 요구 8을 성립시키는 최소 전제다. ②는 같은 값의 정본이 둘(스토어·쿼리 캐시)이 돼 저장 반영 경로가 갈린다. ③은 MSG-422 재작업이 남긴 교훈(게이트가 조회에 묶이면 화면이 통째로 사라진다)을 그대로 반복한다 |
| 2026-08-19 | MSG-426 | 결정(중복 게이트): 신규 6패밀리를 nose.baseline.json에 의도 중복으로 등재 — `profile-format.ts`·`profile-image.ts`(웹 원본 포팅, 티켓 [참고]가 지시)·`use-update-nickname.ts`(웹 훅의 플랫폼 미러)·테스트 봉투 헬퍼 등. 공통 추출 대안 기각 | 웹 모듈을 런타임 import하면 웹 리팩터가 모바일 **번들**에 직결된다 — envelope·api-error가 세운 "복제 + parity 테스트로 드리프트를 번들이 아니라 테스트가 먼저 알린다"는 관례를 따른다(MSG-422 선례와 동일 판단). 훅은 import 경로(모바일 barrel)와 캐시 클라이언트가 달라 공유 불가능하다 |
| 2026-08-19 | MSG-426 | 결정(PR #74 리뷰 반영): 로그아웃 후속(모달 닫기·`goToLogin`)을 per-call `mutate(undefined,{onSettled})`에서 **`useLogout` 훅 레벨 옵션**으로 이관하고, 확인 모달 닫기에 `isPending` 가드 추가. ① 현행 유지 ② 가드만 추가하고 per-call 콜백 존치 두 대안 기각 | per-call 콜백은 관찰자 언마운트 시 실행되지 않는다 — `_layout`에 인증 게이트가 없어 `goToLogin()`이 유일한 이동 경로이므로(D5), 콜백이 유실되면 세션만 끊긴 채 남의 프로필 화면에 머무른다. 같은 PR의 `use-delete-account`·`use-update-nickname`이 이미 훅 레벨로 옮긴 원칙에서 로그아웃 배선만 벗어나 있었다(②가 이 불일치를 남긴다). 가드는 별개 문제를 푼다 — 확인 후에는 서버 성패와 무관하게 로컬 세션이 끊기므로(로컬 우선 종료) 되돌릴 수 없는데, 그때 모달만 닫아 주면 "취소했는데 잠시 뒤 로그인 화면으로 튕기는" 것처럼 보인다. 계정 삭제 모달의 `close()` 가드와 동형으로 맞췄다 |
| 2026-08-19 | MSG-426 | 결정(PR #74 리뷰 반영): 프로필 편집에서 저장 비행 중 이탈 차단을 헤더 `‹`·[취소]·**하드웨어 뒤로가기** 3경로에 건다 — 리뷰가 요청한 헤더만 막는 대안 기각 | 이미지 삭제 → 닉네임 저장이 per-call 콜백으로 이어져 있어 중간 언마운트 시 **이미지는 서버에서 지워졌는데 닉네임 저장만 조용히 누락**된다(오류도 안 보인다). 헤더만 막으면 Android 하드웨어 뒤로가기로 같은 창이 그대로 남는다 — 모바일에서 더 흔한 이탈 경로다. MSG-422 `abortSignup`이 두 트리거를 함께 막은 선례와 동형 |
| 2026-08-19 | MSG-426 | 결정(중복 게이트): `BackHandler` 구독 보일러플레이트 3중 복제(동의 게이트·도감·프로필 편집)를 nose.baseline.json에 의도 중복으로 등재 — 공용 훅 `useHardwareBack` 추출 기각 | 추출을 실제로 구현했다가 되돌렸다. **React가 Effect Event를 다른 훅으로 넘기는 것을 금지한다** — `react-hooks(rules-of-hooks)`가 세 파일 모두에서 "created with React Hook useEffectEvent, and can only be called from Effects and Effect Events in the same component. It cannot be assigned to a variable or passed down" 오류를 냈다. 판정 함수가 각 화면의 최신 상태를 읽어야 해 Effect Event는 필수이므로, 이 6줄은 구조적으로 추출 불가능하다 |
| 2026-08-19 | MSG-428 | 결정: 모바일 `GRID_MIN_ZOOM`을 15 → 16으로 상향(웹 정본 동기화). 대안 기각 ① 15 유지 + 마커 하한을 14로 낮춤 ② 15 유지 + zoom 15에서 두 층 동시 표시 | 모바일 파일 주석이 처음부터 "웹 GRID_MIN_ZOOM 선례와 동일 임계"를 선언하고 있어 값 쪽이 드리프트다(웹은 MSG-357 후속에서 이미 16). ①은 티켓 요구 "축척 250m~500m는 동 마커"와 어긋나고, ②는 격자 채움 위에 마커가 겹쳐 "격자 채움이 사라지고 마커가 표시된다"를 깨뜨린다. 두 층의 상호 배타는 zoom 6~21 전건 단정(L3)으로 고정했다 |
| 2026-08-19 | MSG-428 | 결정: 시(SIDO) 단위를 단위 테이블에서 제거하지 않고 **줌 하한(`MAP_MIN_ZOOM = MAP_SCALE_8KM_ZOOM = 10`)으로 도달 차단**. 대안 기각 ① `AggregationUnit`에서 SIDO 제거 ② 파생 단계에서 SIDO 마커 필터링 | ①은 `aggregationUnitForZoom`·`AGGREGATION_SPAN_CAP_DEG`·`drillInZoomForUnit`·`MARKER_MERGE_PX` 네 시그니처가 웹과 갈라져 parity 전건 비교가 통째로 무너진다 — 이 티켓의 방어선이 드리프트 감지 그 자체다. ②는 "필터가 빠지면 되살아나는" 배제라 런타임 보장이 약하다. 하한은 카메라가 SIDO 구간(zoom 6~9)에 **진입 자체를 못 하게** 만들고, 전국 확장 시 한 줄만 되돌리면 된다 |
| 2026-08-19 | MSG-428 | 결정: 마커 파생 `useMemo`(`toClusterMarkers`)를 화면이 아니라 조회 훅에 두고, 합성 자체는 순수 팩토리로 내려 테스트한다. 대안 기각 ① 스펙 구현 계획대로 화면에 `useMemo` 배치 | ①이면 화면 diff가 프리티어 포맷 기준 10줄을 넘어 수용 기준 G3(map-home-screen 추가 diff ≤10줄)를 깬다 — MSG-427이 같은 파일을 병렬로 고치고 있어 충돌면 최소화가 이 티켓의 확정 제약이다. 훅으로 옮기면 스펙이 함께 요구한 "로직은 전부 model/api에, 화면은 조립만"에 오히려 더 부합하고, 훅 파일은 vitest에서 열리지 않으므로 합성 함수만 팩토리로 내려 계약을 고정했다(실제 화면 diff 9줄) |
| 2026-08-19 | MSG-428 | 결정: `formatClusterCount`를 웹의 `toLocaleString("ko-KR")` 대신 정규식 3자리 구분으로 구현하고, 동등성을 Node `toLocaleString`과의 표본 대조로 고정. 대안 기각 ① 웹과 같은 `toLocaleString` 사용 | Hermes의 Intl 가용성은 빌드 설정(intl 변형 포함 여부)에 좌우돼 ①은 실기에서 서식이 조용히 달라질 수 있다 — 그러면 "31"이 "31"이 아니라 로케일 기본 서식으로 나가는 것을 테스트가 잡지 못한다. 표기 동등성만 테스트로 묶으면 구현 차이는 무해하다 |
| 2026-08-19 | MSG-428 | 결정(중복 게이트): 신규 5패밀리(`map-scale`·`aggregation-unit`·`region-cluster-overlay` 웹 포팅 + 파생)를 nose.baseline.json에 의도 중복으로 등재. 공통 패키지 추출·웹 모듈 직접 import 기각 | 웹 모듈을 런타임 import하면 웹 리팩터가 모바일 **번들**에 직결된다 — envelope·api-error·map-query-policy가 세운 "복제 + parity 테스트로 드리프트를 번들이 아니라 테스트가 먼저 알린다"는 관례를 따른다(MSG-423·426과 동일 판단). 세 모듈 모두 `*.parity.test.ts`가 웹 원본을 직접 import해 전건 대조한다 |
| 2026-08-19 | MSG-428 | 발견(codex 리뷰, 재작업 1회차): 웹 `features/map-home/model/cluster-overlay.ts`(`gateFillCells`)를 통째로 포팅하지 않아 저줌에서 **점령 채움과 클러스터 마커가 공존**(S1 위반). 수용 기준 L3이 이를 못 잡은 이유는 L3이 배경 격자선(`buildVisibleCells`)만 상호 배타로 단정하고, 지도에 **별도 prop으로 가는 점령 채움 층**을 덮지 않았기 때문 — 기준이 요구보다 좁았다. 결정: 게이트를 모바일 `cluster-overlay.ts`로 포팅하고 L3을 채움 층 전체로 확장 | 배경 격자선과 점령 채움은 모바일에서 서로 다른 렌더 경로(`buildVisibleCells` 상태 vs `occupiedCells` prop)라 하나의 게이트가 둘을 덮지 않는다. 웹의 판별자 `cell.color !== undefined`는 병합 목록 전제라 모바일에선 "어느 prop 층인가"로 대응된다(occupied=게이트 대상 / theme·hatch·route=저줌 존치, MSG-403 AC 7) — 이 동치를 `cluster-overlay.parity.test.ts`가 웹 원본 동적 import로 고정한다. 게이트 위치는 화면이 아니라 `grid-map.tsx`: ① 화면 배치는 G3(≤10줄) 예산을 13줄 이상으로 밀고 ② 화면 `viewport`는 idle에만 갱신돼 배경 격자선(매 카메라 이벤트)과 판정 시점이 어긋난다 — 같은 `onCameraChanged` zoom으로 두 채움 층을 함께 판정해야 "동시에 보이는 순간"이 프레임 단위로도 없다 |
| 2026-08-19 | MSG-428 | 결정(재작업 2회차): 공유 컴포넌트 `GridMap`에서 화면 고유 정책을 걷어냈다 — ① `minZoom`을 하드코딩 대신 prop(기본 하한 없음)으로 빼 지도 홈만 `MAP_MIN_ZOOM`을 넘기고 ② 게이트 판정을 연속값 zoom state가 아니라 **판정 결과 불리언**(`isBelowGridZoom`)으로 든다. 대안 기각 ① `minZoom` 하드코딩 존치(설계 흐름 무영향이라는 검증 판정) ② raw zoom state 유지 ③ `setState`를 `showCellGrid` 가드 안으로 이동 | 두 하드코딩 모두 티켓 범위 밖인 `features/grid-detail`로 새어 나갔다 — 상세 화면은 같은 `GridMap`을 쓰므로 줌 하한 10이 함께 걸렸고, 매 프레임 발화하는 `onCameraChanged`의 `setZoom`이 종전엔 없던 60fps 리렌더를 만들었다(MSG-423이 부모 setState 디바운스를 기각한 것과 같은 함정). ①은 "지금은 안 깨진다"에 기대는 판정이라 상세 화면이 바뀌면 조용히 깨진다 — 제약은 그것을 요구한 화면에서 선언한다. ②는 `occupiedPolygons` 재계산까지 프레임마다 끌고 갔고, ③은 무관한 두 prop을 결합시킨다. 불리언은 `Object.is` 동일성으로 리렌더를 걸러내면서 판정을 **같은 카메라 이벤트의 zoom**으로 하는 정확성 근거를 그대로 보존한다(임계 판정은 순수 함수로 노출해 parity 테스트가 고정) |
| 2026-08-19 | MSG-427 | 결정: 미션 영역을 폴리곤 통짜가 아니라 **격자 타일 + 빗금**으로 그리기 위해 `encodeGridId`·`viewportGridRange`(entities/cell/grid-5179)·`pointInPolygon`(entities/region/boundary-geometry) 3종을 웹에서 포팅하고, 타일 생성을 **현재 뷰포트로 클리핑**한다. 대안 기각 ① `NaverMapPolygonOverlay`에 폴리곤 그대로 렌더 ② 미션 전 영역 격자를 미리 펼침 | ①은 "내 점령 ∩ 미션" 대각 빗금이 Figma 4테마 공통 문법인데 폴리곤 한 장으로는 표현할 수 없다. ②는 웹에서 실제 렉 사고를 낸 경로다 — BOX 미션 하나가 900m 사방(9×9=81칸)이고 응답이 170개라 proj4 변환이 수만 번 돌고 폴리곤 1만 개가 지도에 올라간다. 클리핑은 렌더 비용을 화면 크기에 묶는다 (F-12) |
| 2026-08-19 | MSG-427 | 결정: 미션 목록 카드에 **영상 수 배지를 넣지 않는다**. 대안 기각 ① 목록 미션마다 `GET /api/missions/{id}` 호출 ② 웹의 결정적 목 해시(`mission-video-count.ts`) 포팅 | `/missions/active` 응답에 `videoCount`가 없다. ①은 팝업 목록이 Figma 기준 38건이라 상세 요청이 목록 길이에 비례한다. ②는 티켓의 "mock 의존 0건"(F1)과 정면 충돌한다 — 실수치는 상세 시트가 서버 `videoCount`로 보여준다 |
| 2026-08-19 | MSG-427 | 결정: 미션 상세 `올라온 영상`은 서버 `MissionDetailResponseDto.videoCount`, 코스 스팟 행의 `영상 N`은 `spotStats[].videoCount`를 쓴다 — **웹의 미배선 2건을 모바일이 앞질러 고친다**. 대안 기각: 웹과 같은 값(첫 페이지 길이·목 해시) 유지 | 웹은 두 서버 값을 받아 놓고도 UI가 첫 페이지 길이(20에서 포화)와 목 해시를 쓴다. 같은 수치가 웹·앱에서 달라 보이는 비용을 감수하는 이유는 이 티켓이 mock 0건을 요구하기 때문이다 — 웹 보수는 `apps/web` diff 0줄 제약으로 범위 밖이라 별도 티켓 제안으로 남긴다 |
| 2026-08-19 | MSG-427 | 결정: 시트 단계 이동 규칙(`stepBack`·`closeTheme`·`showsCloseButton`·`sheetStageFor`)을 `sheet-navigation.ts` 순수 모델로 뽑아 헤더 `‹`·헤더 `✕`·Android 하드웨어 뒤로가기 3경로가 **같은 함수**를 부르게 했다. 대안 기각: 각 시트/핸들러에서 삼항 사슬로 분기 | 칩 4종 × 상세 2단이라 분기가 세 곳에 흩어지면 한 곳만 어긋나는 사고가 난다(A3·A4·A5가 같은 규칙을 요구). 순수 모델이면 에뮬레이터 없이 vitest로 단계 복귀 전수를 고정할 수 있다 |
| 2026-08-19 | MSG-427 | 결정: 스탯 3분할을 `ui-native/StatTile` 재사용 대신 feature 로컬 `StatTrio`로 구현 | Figma 실측이 **라벨 위 / 값 아래** + `bg-surface-soft` 박스인데 `StatTile`은 값(fm-display 20px) 위 / 라벨 아래로 순서·타이포가 반대이고 props로 뒤집을 수 없다. `stat-tile.tsx`에 옵셔널 prop을 더하는 것 자체는 `index.ts`를 건드리지 않지만(MSG-421 `video-card.tsx` 선례), 순서·값 타이포·3분할 `bg-surface-soft` 컨테이너까지 prop으로 흡수하면 StatTile이 두 얼굴이 된다 — 겹치는 것이 라벨·값 두 문자열뿐이다. 웹도 같은 이유로 로컬 `StatTrio`를 갖는다. 승격 후보로 보고만 남긴다 |
| 2026-08-19 | MSG-427 | 결정: 활발한 시간대를 **동 요약에 유지**하고 핫스코어 상위 5격자의 `hourly-uploads`를 합산해 3시간 단위 8칸으로 접는다 (웹은 24칸·격자 상세). 대안 기각 ① 웹 선례대로 격자 상세로 이동 ② 이번 범위에서 제외 | Figma 모바일 정본과 티켓이 둘 다 동 요약에 두고 막대가 8개다. 요약의 다른 수치(영상 수·조회·최근 24시간)가 이미 같은 5격자 표본 기반이라 근거가 일관되고, 요청 수도 표본 상한에 묶인다. 웹과 파생이 갈리므로 parity 테스트를 값 동등이 아니라 "웹 3칸 합 = 모바일 1칸" 축약 관계로 썼다 |
| 2026-08-19 | MSG-427 | 결정(중복 게이트): 신규 클론 51 + 변경 6 패밀리를 `nose.baseline.json`에 의도 중복으로 등재(MSG-428 머지본 위로 리베이스 후 310→363). 대부분 **웹→모바일 포팅 쌍**(`mission`·`panel-branch`·`mission-status`·`course`·`grid-videos`·`home-cell-detail`·`hot-region-summary`·`boundary-geometry`·`encodeGridId`/`viewportGridRange`·`theme-classes`)과 모바일 `map-home/api` 쿼리 훅 관례 반복 15종이다. 공통 추출 대안 기각 | RN이 웹 모듈을 런타임 import하면 웹 리팩터가 모바일 **번들**에 직결된다 — envelope·grid-5179가 세운 "복제 + parity 테스트로 드리프트를 번들이 아니라 테스트가 먼저 알린다"는 관례를 따랐고(MSG-422·426과 같은 판단), 이 티켓의 포팅 16종에는 전부 `*.parity.test.ts`가 붙어 있다. 훅 반복은 각 훅이 자기 게이트(`active`·`settled`) 의미를 선언하는 것이 요점이라 팩토리로 접으면 그 의미가 사라진다. **감사 결과**: 등재분 중 범위 밖에서 멤버까지 새로운 패밀리는 3건뿐이고(`segment-range-slider` 자체 중복 · `upload-modal-store.test` 자체 중복 · `ui-native↔ui-web bottom-nav` 미러) **전부 내가 건드리지 않은 파일의 기존 중복**이 코퍼스 확대로 기본 표면 위로 올라온 것이다 |
| 2026-08-19 | MSG-427 | 결정(좌시프트): Hermes 미구현 API를 `.oxlintrc.json`의 `apps/mobile`·`packages/ui-native` 한정 규칙으로 차단 — `no-restricted-properties`(`toSorted`·`toReversed`·`toSpliced`·`Object.groupBy`·`Map.groupBy`·`Promise.withResolvers`) + `no-restricted-globals`(`structuredClone`). 대안 기각 ① 주석으로 경고 ② vitest setup에서 해당 메서드를 지워 테스트가 터지게 하기 ③ 별도 티켓으로 미루기 | 실기 검증이 잡은 차단 결함이다 — `hot-region-summary.ts`의 `toSorted`가 지도 홈을 마운트 즉시 크래시시켰고(`useHotRegionSummary`가 칩과 무관하게 항상 호출) 프로덕션 3곳 전부 웹 포팅에서 딸려 왔다. **게이트 6종이 원리적으로 못 잡는다** — vitest는 Node에서 돌아 `toSorted`가 있고 typecheck는 `lib`에 ES2023이 있다. ①은 이미 실패가 실증됐다: 같은 함정이 `gallery-groups.ts:37`·`region-cluster-overlay.ts:109`에 **두 번 문서화돼 있었는데도 재발했다**. ②는 런타임 결함을 테스트 실패로 번역해 원인이 안 보이고 서드파티 코드까지 함께 죽는다. ③은 MSG-428이 같은 포팅 경로로 이어지므로 재발 창을 열어 둔다. MSG-298·386의 "CI가 잡은 것은 로컬 게이트로 내린다" 원칙 적용. 규칙 실효성은 크래시 코드를 되돌려 검출되는 것을 확인해 검증했다(RED→GREEN) |
| 2026-08-19 | MSG-427 | 검증이 잡은 버그: 팝업 미션 상세에서 `period` 한 값을 부제와 스탯에 겸용해 **운영시간이 두 번 나오고 날짜 기간이 화면에서 사라졌다**(D11). 순수 파생 `missionDetailPeriod(theme, dto)`로 부제(테마 무관 날짜 기간)와 스탯(팝업=운영시간 / 축제=축제 기간)을 분리 | 목록 카드가 `formatOperationTime`을 쓰는 것은 D4·Figma 화면 5의 요구라 유지가 정답이었고, 상세만 두 값이 필요했다. 한 변수를 두 자리에 재사용한 것이 원인이라 파생을 객체로 바꿔 자리마다 다른 필드를 읽게 했다. Figma 화면 6의 `… · 7.31~8.14`가 판정 근거 |
| 2026-08-19 | MSG-427 | 결정(codex 리뷰 P1): 상세 시트가 그릴 미션·코스 뷰를 **선택 id가 유지되는 동안 래치**한다(`pickSelectedView` 순수 함수 + 훅의 ref 보관), 뷰가 아예 없는 경우에는 **헤더를 남긴 폴백**(`SheetDetailFallback`)을 그린다. 대안 기각 ① 선택 자동 해제 후 목록 복귀 ② 상세를 `/missions/{id}`로 재설계 ③ 빈 상태 + 뒤로가기만 | 목록(`/missions/active`)이 뷰포트 bbox 기반이라 상세를 연 채 지도를 조금만 움직여도 선택 미션이 응답에서 빠지는데 `selectedMissionId`는 남아 `homePanelKind`가 계속 상세를 가리켰다 → 시트가 `null`을 렌더했고, **헤더(뒤로·닫기)가 상세 콘텐츠 안에 있어 탈출 수단까지 사라졌다**(iOS엔 하드웨어 백도 없다). ①은 사용자가 누르지 않은 이동이 일어나 놀랍다. ②가 더 옳지만(상세 응답에 `mission` 전체가 있다) 카드 탭 직후 상세 도착 전 빈 시트가 번쩍이는 회귀가 생겨 범위 대비 이득이 작다 — 후속 권장으로 남긴다. ③은 래치로 살릴 수 있는 정보를 버린다. **모듈 싱글턴(`mission-selection.ts`)이 탭 왕복을 넘겨 살아남는 경로 때문에 래치만으로는 부족해 ③을 폴백으로 함께 채택했다** |
| 2026-08-19 | MSG-427 | 결정(P1 파생): 래치된 뷰의 진행도를 **미션 상세 응답(`MissionDetailResponseDto.progress`)** 에서 다시 만든다. 대안 기각 ① 목록 진행도의 마지막 값을 그대로 표시 ② 래치 상태를 `progressFailed`로 취급해 전부 `확인 불가` | 목록 진행도(`/missions/progress?missionIds=`)는 목록 id 기반이라 미션이 뷰포트를 벗어나면 함께 빈다 — ①은 낡은 값을 사실처럼 보여준다(D13 취지 위반). ②는 과잉 억제다: 상세 조회는 선택 id로만 구동돼 살아 있어 **방문 여부·스팟 영상 수는 정확한데** 그것까지 `확인 불가`로 지워 코스 상세가 오히려 나빠진다. 상세 응답은 명세상 목록 진행도와 같은 서버 계산이고 이미 조회 중이라 **추가 요청 없이** 정본을 쓸 수 있다 |
| 2026-08-20 | MSG-427 | 리뷰가 잡은 버그: `useGridNamesQuery`의 `isPending`이 계산·문서화만 되고 **소비처가 0곳**이라, 코스 상세를 열 때마다 격자 이름 도착 전까지 모든 포토스팟이 `이름 없는 경유 지점`으로 표시됐다. 판정을 순수 함수 `courseSpotListState(spotCount, namesPending)`로 뽑아 스팟 목록만 로딩으로 게이트 | 웹 `CourseDetailPanel`은 같은 이유로 **패널 전체**를 `DotsLoader`로 막는다("부분 렌더 금지", MSG-403 후속). 모바일은 목록만 막는 쪽을 택했다 — 제목·진행도·거리는 이미 도착해 있고 격자 조회 6~8건을 기다리느라 그것까지 늦추면 모바일 체감이 나빠진다(사용자 승인). 판정을 UI가 아니라 모델에 둔 이유는 모바일에 렌더 테스트가 없어서다(MSG-292 확정 4) — UI에 두면 같은 회귀를 다시 못 잡는다. RED 확인: 게이트를 되돌리자 신규 테스트가 실패 |
| 2026-08-20 | MSG-427 | 보류(후속 티켓): `decodeHtmlEntities`가 범위 초과 숫자 엔티티(`&#999999999;` 등)에 `String.fromCodePoint` `RangeError`를 던져 미션 카드·상세 렌더가 깨질 수 있다. 이번 티켓에서 고치지 않는다 | **같은 코드가 웹 `mission-format.ts`에도 바이트 동일하게 있고**(주석만 다름) parity 테스트가 둘을 대조한다 — 모바일만 고치면 의도적 갈라짐이 부채로 남고, 웹까지 고치면 이 티켓의 `apps/web` diff 0줄 하드 제약이 깨진다. 서버 통제 데이터라 실측 위험이 낮아 웹·모바일을 함께 고치는 후속 티켓으로 넘긴다(사용자 승인). 참고: 리뷰 지적 중 "서로게이트 절반(0xD800~0xDFFF)도 throw한다"는 **사실이 아니다** — 실측상 고립 서로게이트를 정상 반환하고, throw는 `> 0x10FFFF`·음수·비정수뿐이다 |
| 2026-08-20 | MSG-427 | 리뷰가 잡은 버그(ad3749d 후속): `useGridNamesQuery`의 `isPending`을 `query.data === undefined`로 계산해, 격자 이름 조회가 **하나라도 실패하면 게이트가 영원히 안 풀려** 코스 상세가 무한 ActivityIndicator에 갇혔다. 웹 원본과 같이 `query.isPending`으로 교체하고, 파생 전체를 순수 팩토리 `grid-names-query.ts`로 분리해 테스트 7건을 붙였다 | 실패한 쿼리는 status가 error로 확정돼도 `data`가 undefined로 남는다 — pending과 error를 구분하지 못하는 계산이었다. ad3749d가 `isPending`을 처음 소비하게 만들면서 잠재 결함이 실제 결함이 됐다(그 전에는 죽은 필드라 무해). 같은 함정을 `shared/api/gated-query-status`가 이미 문서화하고 있었다("비활성 쿼리는 영원히 pending — 누르지 않으면 갤러리가 영구 스켈레톤"). **훅 계층이라 게이트 6종·parity 어느 것도 못 잡는 유형**이라, MSG-428 `grid-aggregation-query`의 "순수 팩토리 + 얇은 훅" 선례를 따라 테스트 가능한 층으로 내렸다(훅은 `auth-session`이 expo-secure-store를 끌고 와 vitest에서 열리지 않는다). RED 확인: 결함 코드로 되돌리면 실패 전용 테스트 2건이 깨진다. `isError` 노출은 하지 않았다 — 실패 시 격자 코드 폴백이 승인된 동작(E-16)이고 웹도 노출하지 않아 parity가 유지된다 |
| 2026-08-19 | MSG-430 | 결정: 뱃지 메달 24종을 `react-native-svg` `SvgXml` + **문자열 카탈로그**로 이식하되, 원본을 감싸지 않고 Figma 아트보드 잔재 7요소(뷰박스를 덮는 흰 배경 path·페이지 rect 3·카드 rect 2)를 기계 제거한 뒤 `<g id="medal">` + `<defs>`만 남긴다(368KB → 307KB, 참조되지 않는 id 속성 동시 제거). 대안 기각 ① `react-native-svg-transformer` 도입 ② `expo-image` + `require('*.svg')` ③ 서버 `iconUrl`만 + 도형 폴백 ④ SVG→JSX 코드젠 ⑤ PNG 변환 | ①은 새 devDep + `metro.config.js` transformer 3종 수정이 필요한데 그 파일이 `withNativeWind`(자체 babel transformer 소유)로 감싸져 있어 체이닝 실패 시 앱 전체 번들·스타일이 죽는다(화면 1개 이식이 감당할 블라스트 반경 아님, kakao-login-button 선례도 같은 길을 피했다). ②는 `expo-asset`이 `.svg`를 네이티브 번들 대상에서 제외해 dev에서만 뜨고 릴리스에서 깨질 수 있다. ③은 `badges.icon_url`이 전량 NULL이라 티켓 요구("실제 메달 아트")를 못 지킨다. ④는 파일당 path 33개 + mask/gradient라 리뷰 불가. ⑤는 로컬에 변환기가 없고 벡터·아트 소스 단일성을 잃는다(실기 성능 문제 발생 시의 폴백으로만 남긴다). 배경 제거는 필수였다 — 웹은 흰 페이지 배경이라 드러나지 않았지만 모바일은 테마 배경 위에 얹혀 뱃지 뒤 흰 판이 보인다 |
| 2026-08-19 | MSG-430 | 결정: 1년 기록 화면을 `src/app/dex/history.tsx` **라우트 신설**로 둔다(웹은 모달). 대안 기각 ① 오버레이/시트 ② 도감 셸 안의 상태 분기 | 셸(`dex-screen`)이 `AppHeader` 타이틀·`BackHandler`·단일 ScrollView를 소유하므로 오버레이로 가면 셸을 고쳐야 해 "교체 2줄·셸 무수정" 계약이 깨진다. 라우트는 자체 ScrollView를 가져 중첩 세로 스크롤 문제가 원천 소멸하고 하드웨어 뒤로가기·제스처 백을 라우터가 처리한다. expo-router에서 `dex.tsx`(→`/dex`)와 `dex/history.tsx`(→`/dex/history`)가 충돌 없이 공존함을 `getRoutes`로 사전 확인했다(우발 계획인 `dex/index.tsx` 이동은 불필요) |
| 2026-08-19 | MSG-430 | 결정: `resolveBadgeArt`가 웹처럼 문자열 하나가 아니라 `{kind:"uri"|"xml"}` 판별 유니온을 돌려준다 | 웹은 원격 URL과 로컬 에셋이 둘 다 `<img src>`라 문자열로 충분하지만, RN은 원격(`SvgUri`)과 인라인 XML(`SvgXml`)의 렌더 컴포넌트가 갈린다. 소스 우선순위(iconUrl > 로컬 > null)는 웹과 동일하게 유지하고 판별만 얹었다 — 표시 컴포넌트가 문자열을 휴리스틱으로 구분하는 것(`startsWith("<svg")`)보다 계약이 명시적이다 |
| 2026-08-19 | MSG-430 | 결정: 1년 잔디도 기록 탭과 **같은 9px 셀/3px 간격**을 쓰고 폭 초과분만 가로 스크롤로 처리한다(시안은 셀을 7.44px로 축소해 한 화면에 담았다). 웹의 `size:"sm"|"lg"` 셀 규격 분기는 이식하지 않고 `scrollable` 불리언으로 대체 | 셀을 화면 폭에 맞춰 줄이면 토큰 밖 치수가 생기고 탭과 1년 화면의 잔디가 서로 다른 밀도로 보인다. 프롭 이름을 `size`로 유지하면 크기를 바꾸지 않는 `size` 프롭이 돼 계약이 거짓말이 된다 |
| 2026-08-19 | MSG-430 | 발견: 승인 A7("업로드 확정 시 `upload-history` 무효화 1줄 추가")은 **이미 구현돼 있었다** — MSG-424가 `features/upload/api/invalidate-grid-queries.ts`에 `getUploadHistoryQueryKey()` 무효화를 선반영했고 테스트도 있다. 이 티켓에서 그 파일은 무수정 | 스펙이 웹 MSG-414 A8을 근거로 요구한 것을 선행 티켓이 먼저 넣은 경우다. 중복 추가는 no-op 코드만 늘린다 |
| 2026-08-19 | MSG-430 | 검증 발견(실기): **뱃지 메달 아트가 한 개도 렌더되지 않는다.** 스펙 전제 "`badges.icon_url` 전량 NULL"이 스테일 — 운영 API가 이제 24종 전부에 **PNG URL**(`fillmap-static.s3…/badges/*.png`)을 채워 보낸다. `resolveBadgeArt`가 iconUrl을 우선하므로 `badge-medal.tsx`가 `SvgUri`(SVG 전용 파서)로 PNG를 그리려다 null을 렌더 → 메달이 0높이로 사라진다. 웹은 `<img src>`라 같은 전제 변화에도 정상 | 재작업 지시: `kind:"uri"`는 `SvgUri`가 아니라 RN `Image`(또는 확장자·Content-Type 분기)로 그린다. 우선순위(iconUrl > 로컬 > null)와 L6 계약은 유지 |
| 2026-08-19 | MSG-430 | 검증 발견(실기): 잔디 요일 라벨 "월·수·금"이 `h-2.25`(9px) 고정 높이 View 안에서 **세로로 잘려 "월(하단 결손)·스·그"로 읽힌다**(기록 탭·1년 화면 공통). 코드 주석의 "글자는 넘치게 둔다" 전제가 Android에서 성립하지 않는다(기본 클립) | 재작업 지시: 행 높이 대신 `lineHeight`/`textAlignVertical`로 정렬하거나 라벨 열만 셀 stride(12px)에 맞춰 높이를 키운다 |
| 2026-08-19 | MSG-430 | WAIVED: `upload-grass-grid.tsx`의 `rounded-[2px]` 2곳(9px 잔디 셀·범례 스와치)을 임의값인 채로 수용 | `packages/design-tokens/src/radius.ts`에 2px 대응 토큰이 없고 최소값이 `xs:4`라 9px 셀에 과하다. 웹 원본(`UploadGrassGrid.tsx`)도 동일 리터럴이라 플랫폼 간 차이가 생기지 않는다. 토큰 신설은 디자인 시스템 결정이라 별건으로 분리 |
| 2026-08-19 | MSG-430 | 결정(중복 게이트): 검증 단계에서 nose 베이스라인에 20 family upsert · 6 family 제거(교체된 구 id)로 게이트를 그린화. `--write-baseline` 전면 재작성은 하지 않았다(303 중 297 항목 무변경) | 신규분은 전부 웹↔모바일 포팅 미러(`badge-showcase`·`featured-selection`·`upload-grass`·`badge-art`·조회 훅·UI 미러) 또는 parity 테스트 픽스처로, 레포가 세운 "복제 + parity 테스트" 관례의 산물임을 위치·내용으로 직접 확인했다. 유일한 앱 내부 신규분은 텍스트 버튼 `Pressable` 3중(4줄, 추출 가치 0)이라 함께 등재. 빌드 리포트 9절이 적은 id 일부(`e12fcaf8ea`·`15ee0d0261`·`910a4774f6`)는 현재 실행에 존재하지 않아(내용 기반 id 변동) 실제 보고 id 기준으로 등재했다 |
| 2026-08-19 | MSG-430 | 결정(재작업 F1): `BadgeArt`의 원격 갈래를 `kind:"uri"` 하나에서 `"svg-uri"`·`"image-uri"` 둘로 넓히고, 판별을 순수 함수 `isSvgUri`(쿼리스트링·해시 제거 후 경로 확장자, 대소문자 무시)로 분리해 단위 테스트를 붙였다. 대안 기각 ① 원격을 무조건 RN `Image`로 ② `kind:"uri"` 유지 + 표시 컴포넌트에서 URL 분기 ③ HEAD로 Content-Type 확인 | ①은 지금 서버가 PNG라는 사실에만 맞춘 수정이라 서버가 SVG로 바꾸면 같은 결함이 방향만 바꿔 재발한다(계약 L6은 형식을 약속하지 않는다). ②는 판별이 뷰에 숨어 이번처럼 렌더 분기를 빠뜨려도 타입이 잡아 주지 못한다 — 유니온으로 갈라 두면 분기 누락이 타입 오류가 된다. ③은 순수 함수가 아니게 되고(네트워크 왕복 + 로딩 상태) RN 경계 규칙도 깨진다. 우선순위(iconUrl > 로컬 카탈로그 > null)와 L6 계약은 무변경 |
| 2026-08-19 | MSG-430 | 결정(재작업 F2): 잔디 요일 라벨을 셀 높이(9px) View 7개의 flex 열에서 **월·수·금 3개만의 절대 배치**(top = 월라벨행 14 + row×stride 12 + (셀 9 − 캡션 lineHeight 14)/2)로 바꿨다. 대안 기각 ① `lineHeight`를 셀 높이로 압축 ② 라벨 행 높이를 stride(12px)로 확대 ③ `overflow-visible` 명시 | ①·②는 캡션 줄상자가 14px라 9px·12px 어느 쪽에 넣어도 Android가 글리프를 1~2.5px 잘라 같은 결함이 축소된 형태로 남는다. ③은 RN Android의 `overflow:visible`이 조상 경계를 넘지 못해 신뢰할 수 없다(기본값인데도 이번에 잘렸다). 절대 배치는 높이 제약 자체를 없애 클립 가능성을 구조적으로 제거하고, 같은 파일의 월 라벨이 이미 쓰는 관례다. 오프셋은 임의 px이 아니라 셀·stride·`typography.caption.lineHeight` 토큰에서 파생한다 |
| 2026-08-19 | MSG-430 | 발견(재작업): F2 수정으로 nose 게이트에 새 family 1건(`upload-grass-grid.tsx` 월 라벨 절대배치 루프 ↔ 요일 라벨 절대배치 루프, 0 shared lines·1 param) + 변경 1건이 생겼다. 공통 컴포넌트 추출 기각 | 두 루프는 축(left↔top)·데이터(월 경계↔요일 행)·좌표 산식이 모두 달라 nose 자신도 "데이터 shape가 맞을 때만"이라는 단서를 달고 0줄 공유를 보고한다. 베이스라인 등재는 검증자 소관(재작업 지시서가 베이스라인 재수정을 금지) |
| 2026-08-19 | MSG-430 | 결정(재검증 중복 게이트): 재작업으로 생긴 nose family 2건(`af60bfa6e8a2ca55` 신규 · `f0affddf4496d3db` 변경)만 베이스라인에 upsert(317→319, 삭제 0). `--write-baseline` 전면 재작성 금지 지시 준수 | `af60…`은 `upload-grass-grid.tsx:77-87`(월 라벨, `h-3.5` 컨테이너 · `left = weekIndex×stride`)와 `:115-125`(요일 라벨, `w-3.5` 컨테이너 · `top = weekdayLabelTop(row)`)로 축·데이터·산식·컨테이너가 전부 달라 nose 자신이 0 shared lines / ~0 removable / "데이터 shape가 맞을 때만"을 보고한다 — 빌더 주장을 `nose … id=af60bfa6e8 full`로 직접 확인. `f0affddf44`는 `<View className><Text>{x}</Text></View>` RN 제네릭 마크업 15 copies(`ui-native/cell-badge`·`video-card` 포함) family로, badge-medal 텍스트 마크업 변경에 따른 멤버 변동일 뿐 추출 대상이 아니다 |
| 2026-08-19 | MSG-430 | 검증 환경 함정(기록): `CI=1 npx expo start`는 **파일 워처를 끈다**("Metro is running in CI mode, reloads are disabled"). 재작업 전에 띄운 Metro가 그대로 살아 있으면 앱의 번들 요청이 **재작업 이전 그래프**로 응답돼, 고쳐진 코드인데 결함이 그대로 재현되는 **거짓 실패**가 난다(재검증 1시간 소모) | 이번에 F1·F2 둘 다 "수정 후에도 미해소"로 관측됐으나, 워처가 켜진 Metro로 재기동하자 둘 다 즉시 해소됐다. 교차 확증법: 같은 화면의 스크린샷이 **픽셀 단위로 완전 동일**하면 번들 갱신을 의심하고 `curl :8081/json/list`(연결 타깃)와 Metro 로그의 `Android Bundled …`를 확인한다. 모바일 실기 검증은 **재작업마다 Metro를 워치 모드로 재기동**할 것 |
| 2026-08-19 | MSG-430 | 결정(codex 환류): `AppBottomNav`의 활성 탭 판정을 정확 일치에서 **세그먼트 경계 접두 매칭**(`pathname === route || pathname.startsWith(`${route}/`)`)으로 넓히고, `handleSelect`에 **자식 라우트 재탭 → 탭 루트 복귀** 분기를 추가했다. 대안 기각 ① `upload-history-screen`에서만 도감 활성을 주입하는 prop ② 바텀 내비를 이 화면에서 렌더하지 않음 | 커밋 직전 codex 리뷰가 잡았다 — `/dex/history`는 **이 앱 최초의 탭 자식 라우트**라 정확 일치 판정이 처음으로 구멍을 드러냈고, 도감 하위 화면인데 활성 탭이 하나도 없어 현재 섹션을 알 수 없었다. ①은 같은 결함이 다음 자식 라우트에서 재발하고 화면마다 override를 다는 부채가 된다(원인은 위젯의 판정 규칙이다). ②는 Figma 1년 화면이 하단 탭바를 유지하는 정본과 어긋난다. 접두 매칭은 `/` 경계를 요구해 `/dexfoo` 오탐을 막고, `/upload`는 어느 탭 경로의 자식도 아니므로 **"활성 탭 없음"이라는 MSG-302의 의도가 그대로 보존**된다. 재탭 분기가 없으면 활성 판정만 넓힌 탓에 `/dex/history`에서 도감 탭이 `key === activeKey`로 조기 반환돼 무동작이 되는 부작용이 생긴다 — 넓힌 판정과 짝을 이뤄야 하는 수정이다 |
| 2026-08-19 | MSG-431 | 발견(테스트가 잡은 스펙 결함): 승인 스펙의 `cell-{col}-{row}` → 서버 `gridId "{row}_{col}"` 변환이 **틀렸다**. 모바일 라우트 셀 id는 구 위경도 스텝 체계(`grid.ts`, 원점 0,0)이고 서버 gridId는 EPSG:5179 100m 체계라 인덱스 재조합이 성립하지 않는다 — 서면 셀 `cell-112225-39064`가 `"39064_112225"`(부산 밖)가 된다. `grid-5179.ts`에 `encodeGridId`를 복제하고 **셀 중심 좌표를 5179로 인코딩**하는 `serverGridIdFromCellId`로 구현 | 스펙 근거였던 `grid.ts:37` 주석("col = 서버 gridX")은 MSG-325 시절 잔재이고, MSG-423이 5179를 들여오며 `grid-5179.ts` 상단에 "좌표계 이원화 주의 — 격자 탭·상세 라우트는 아직 구 체계"라고 명시해 두었다. proj4 실측으로 서면 중심의 정답이 `"16858_11420"`(기존 테스트 픽스처와 일치)임을 확인했고, 단정을 테스트에 고정해 재발을 막는다. 모바일 격자 전면 5179 이관 시 이 보정은 사라진다 |
| 2026-08-19 | MSG-431 | 결정: 격자 상세 화면 상단의 격자 단위 ⋯(수정/삭제/신고)를 **제거**한다 — ① 현행 유지 ② 항목 축소(수정만 남김) 두 대안 기각 | 영상 단위 ⋯가 행마다 생기면서 세 항목 모두 의미가 겹치고 대상이 불분명해진다: "삭제하기"는 목록 첫 영상을 화면 로컬로 감추는 mock이었고 이제 행마다 실 삭제가 있다, "신고하기"는 대상 영상이 지정되지 않아 실 API(`videoId` 필수)로 옮길 수 없다, "수정하기"는 어떤 영상과도 연결되지 않은 채 `/upload`로 보내는데 영상 교체는 티켓 [제외 범위]다. ①은 같은 화면에 대상이 다르고 정확도도 다른(mock vs 실 API) 두 메뉴를 공존시킨다. ②는 "수정하기" 하나를 위해 시트·트리거를 유지하는데 그 항목 자체가 대상 없는 스텁이라 남길 근거가 없다 |
| 2026-08-19 | MSG-431 | 결정: 실패 안내 토스트를 별도 RN `Modal`(탭하면 즉시 해제)로 띄운다 — ① 호스트 트리 절대 배치 ② 토스트 호스트 라이브러리(react-native-root-siblings) 도입 두 대안 기각 | 이 토스트는 다른 Modal(액션시트·확인 다이얼로그)이 닫힌 직후나 다이얼로그가 열린 채 떠야 하는데, RN Modal은 별도 창이라 호스트 트리 토스트가 그 뒤에 가린다(①은 갤러리 카드가 ScrollView 깊숙이 있어 좌표도 카드 지역에 갇힌다). ②는 단일 티켓 범위를 넘는 인프라 결정이다. Modal 창이 3초간 터치를 삼키는 부작용은 탭 해제로 상쇄했고, 다이얼로그 유지가 필요한 삭제·신고 실패는 카드 안(children)에 그려 같은 문제를 피했다 |
| 2026-08-19 | MSG-431 | 결정: `hasCellVideos`의 판정을 "mock 등록 + videoCount > 0"에서 "인코딩 형식이 맞는 셀"로 좁힌다(이름·시그니처·호출부 diff 0) — ① 현행 유지 ② 비동기 조회 게이트 두 대안 기각 | 격자 상세 영상 목록이 실 API가 된 뒤 mock 게이트는 서버에 영상이 있는 격자의 진입을 막아 타인 영상 신고 경로에 아예 닿지 못하게 한다(①). ②는 탭 시점에 서버 왕복이 필요해 호출부(`map-home-screen.tsx`)를 고쳐야 하는데, 그 파일은 MSG-427·428이 병렬 소유하며 리베이스 순서가 정해져 있어 건드리지 않기로 합의된 파일이다. 빈 격자 안내는 상세 화면의 빈 상태가 실데이터로 대신한다 |
| 2026-08-19 | MSG-431 | 발견(검증이 잡은 회귀) + 결정: 신고 모달의 리셋을 `submit()`이 아니라 **`visible` 닫힘 전이**에 걸고, 전이를 순수 리듀서 `report-form.ts`로 뽑는다 — ① 화면에서 `key={reportTarget}`으로 remount ② `submit()`에 `reset()` 복원 ③ 선택 상태를 화면으로 올리기 세 대안 기각 | 초회 구현이 `submit()`의 `reset()`을 잃었는데, 성공 시 화면은 `setReportTarget(null)`로 `visible`만 내리고 모달은 언마운트되지 않아 선택이 살아남았다 — 영상 A를 "스팸"으로 신고한 뒤 영상 B의 모달이 "스팸" 선택 + [신고 제출] 활성 상태로 열리는 **오신고** 경로다. 테스트 576건 전건 통과로 검증까지 통과했다: 상태가 `useState` 안에 있어 단정할 지점이 없었던 것이 원인이므로, 리듀서로 뽑아 "닫힘=리셋, 실패=유지"를 테스트가 고정하게 했다(회귀 재현으로 RED 3건 확인). ②는 실패 재시도 때 사유가 날아가 "잠시 후 다시 시도해 주세요" 안내와 달리 [신고 제출]이 비활성으로 돌아간다(웹 정본 `ReportDialog`는 닫힘에서만 리셋하고 실패 시 유지한다). ①은 1줄이지만 RN 렌더 테스트 인프라가 없어 영구 미검증으로 남고, ③은 화면 컴포넌트라 역시 단정 불가다 |
| 2026-08-19 | MSG-431 | 결정: 삭제 실패 안내(`deleteMessage`)·신고 실패 안내(`reportFailure`)도 다이얼로그·모달을 닫을 때 함께 비운다 | 위 회귀와 같은 부류(오버레이가 닫혀도 로컬 상태가 남는다)를 전수 점검하다 찾았다. 두 안내는 3초 자동 소멸이라, 소멸 전에 닫고 다시 열면 **지난 실패 문구가 새 대상의 카드에 뜬다**(신고는 다른 영상에까지 번진다). 다른 로컬 상태(`menuOpen`·`deleteOpen`)는 목록이 `key={videoId}`로 영상별 인스턴스를 고정해 대상 간 누출 경로가 없음을 확인했다 — 화면 레벨 싱글턴은 신고 모달 하나뿐이었고 그것이 깨진 유일한 자리였다 |
| 2026-08-19 | MSG-431 | 결정: `invalidate-video-queries`의 웹 동등성을 **소스 대조 parity**(삭제 경로가 건드리는 쿼리 키 팩토리 합집합)로 고정한다 — ① 값 대조 parity ② parity 포기 두 대안 기각 | 웹의 무효화 집합은 순수 함수가 아니라 `useDeleteVideo`의 `onSuccess` 콜백 안에 인라인돼 있어, 꺼내려면 훅을 렌더해야 하는데 모바일에는 RN 렌더 테스트 인프라가 없다(①이 불가능한 이유 — 다른 parity 3종은 순수 모듈이라 동적 import가 통했다). ②는 웹이 키를 추가·제거해도 모바일이 조용히 갈리는 것을 방치한다(검증 리포트 §5-③). 위임 구조가 플랫폼별로 달라(웹은 잔디 이력을 삭제 훅에서 직접, 모바일은 `invalidateGridQueries`에 위임) **경로 전체의 합집합**을 비교했고 양쪽 12종이 일치한다. 호출 형태(`remove` vs `invalidate`·부분 키)는 문자열 단정 2건이, 실제 캐시 상태는 자매 파일의 실 `QueryClient` 테스트가 맡는다 |
| 2026-08-19 | MSG-431 | 발견(커밋 전 codex 리뷰) + 결정: mutation 3종(전환·삭제·신고) 전부에 `mutationKey`를 부여하고 in-flight 가드를 **키를 받는 제네릭 `guardMutate` 하나**로 통일한다 — ① 액션별 전용 가드 함수 ② 화면에서 `isPending`으로 막기 ③ TanStack `scope` 직렬화 세 대안 기각 | 초회 구현은 `setVisibility`에만 `mutationKey`가 있어 삭제·신고는 가드를 안 건 게 아니라 **가드할 수단이 없었다**(`isMutating` 필터가 성립하지 않는다) — 같은 파일 안의 비대칭이라 설계가 아니라 누락이다. 삭제 연타는 중복 DELETE를, 신고 연타는 둘째 요청이 서버 409(`11409`)를 받아 "신고가 접수되었어요" 위에 "이미 신고한 영상이에요"가 덮이는 모순 화면을 만든다. ①은 같은 로직 3벌에 액션이 늘면 또 하나가 된다. ②의 `isPending`은 React 상태라 같은 tick 재탭을 못 막는다(MSG-426 알림 토글에서 확인된 함정 — 기존 전환 가드가 mutation 캐시를 보는 이유가 그것이다). ③은 무시가 아니라 큐잉이라 연타 3번이 DELETE 3번으로 순차 실행돼 2·3번째가 404를 받고, "선착 우선 무시" 결정과도 어긋난다. 발사 가드와 짝으로 `ModalCard`의 기존 `confirmDisabled`를 물려 "눌렸는데 아무 일도 안 난다"는 침묵도 없앴다(ui-native 무수정) |
| 2026-08-19 | MSG-431 | 발견(커밋 전 codex 리뷰) + 결정: 격자 영상 목록 재시도는 실패한 쪽만이 아니라 **두 쿼리를 무조건 모두 refetch**하고, 그 상태 합성을 순수 함수 `combineGridVideoStatus`로 모델에 둔다 — ① 편측 삼항 유지 ② 실패한 쪽만 선별 refetch ③ 훅 인라인 유지 ④ `shared/api/gated-query-status.ts` 승격 네 대안 기각 | 초회 구현의 `retry: global.isError ? globalStatus.retry : mineStatus.retry`는 둘 다 실패한 상태에서 global만 재조회해, 그것이 성공해도 mine이 error로 남아 화면이 계속 실패 화면이다(①이 만든 버그 — 사용자가 재시도를 한 번 더 눌러야 한다). 웹 정본 두 곳(`use-grid-videos-query`·`use-grid-detail-query`)이 모두 실패 여부와 무관하게 양쪽을 부르므로 그것을 기준으로 삼았다 — ②는 지적을 만족하지만 웹과 갈리고, **그 갈림이 애초에 이 버그를 만든 원인**이라 반복할 이유가 없다. ③은 모바일에 RN 렌더 테스트 인프라가 없어(vitest.config 정책) 회귀를 잡을 테스트를 둘 자리가 사라진다. ④는 사용처가 1곳인데다 그 파일이 이미 "모바일에 `gatedQueryStatus` 구현이 3개로 갈렸으니 새 사용처 전에 통합을 먼저 판단하라"고 경고 중이라 하나 더 얹는 것이 최악이다 |
| 2026-08-21 | MSG-451 | 결정(번복): 겹쳐 병합된 집계 마커가 이름을 버리던 것(MSG-410 추정 2 "서로 다른 지역명에 한 이름을 붙일 수 없다")을 **앵커 이름 승계**로 뒤집음 — 개수 최다 지역, 동률은 가나다순(`localeCompare(_,"ko")`), 무귀속(null)은 최후순. 대안 ① 현행 유지 ② 이름 나열("부산진구 외 2") 기각 | 원 판단은 "틀린 이름을 주장하지 않는다"였으나, 실화면에서는 개수만 남아 **마커가 어디를 가리키는지 읽을 수 없어 개수 자체가 쓸모를 잃는다**. 앵커는 이미 count 최대 멤버라 "가장 많았던 지역"이 구성상 나오고, 동률 tiebreak을 이름으로 고정해 재조회에도 같은 이름이 나온다. ②는 마커 원 안에 들어갈 폭이 없다(지름 68~92px). `id.localeCompare`를 최후 tiebreak으로 남긴 것은 동명·무귀속 버킷에서 앵커가 튀지 않게 하기 위함 |
| 2026-08-21 | MSG-451 | 결정: 미션 집계 마커 파생을 별도 `mission-cluster.ts`(스펙 계획)가 아니라 `region-cluster-overlay.ts`의 형제 함수로 배치, 공통 내부 함수에 키 접두·테마만 파라미터화 | 도감 점령 집계와 표시 규칙(키 안정성·시도명 축약·미판정 버킷)이 100% 같아 별 파일로 두면 `check:duplication`이 즉시 잡는다. 칩별 키 접두(`mission-{theme}-`)로 두 층이 한 지도에 서도 렌더 키가 겹치지 않고, 칩 전환 시 이전 마커가 새 자리에 재사용되지 않는다 |
| 2026-08-21 | MSG-451 | 발견(테스트): `mergeOverlappingMarkers`가 병합 결과 객체를 새로 조립하며 `theme`을 누락 — 겹쳐 합쳐진 미션 마커만 도감 색(`bg-primary`)으로 렌더될 상황이었다 | 저줌에서 마커는 자주 겹쳐 실사용에서 바로 드러났을 결함. 재현 테스트 작성 → 실패 확인 → 앵커 테마 승계로 수정. 필드를 늘린 객체를 **재조립**하는 코드는 새 필드가 조용히 빠지는 자리다 |
| 2026-08-21 | MSG-451 | 발견(검증 재작업): 칩 최근접 진입이 콜드 첫 활성화에서 미발동 — 원인이 둘이고 1회차 진단은 절반만 맞았다. ① 확정(commit) 전 목록이 이미 resolved(0건)라 활성화가 소진 ② 확정 후에도 `mapQueryPolicy`의 `keepPreviousData`가 직전 영역 목록을 `isPending:false`로 한 박자 더 반환 | 확정 신호(`committedChip`)만 추가한 수정은 테스트에서 계속 실패했고, 그 실패가 ②를 드러냈다. `keepPreviousData`는 깜빡임 방지용 의도적 정책이라 그대로 두고, "이 영역 기준 목록인가"를 물어야 하는 소비처만 `isPlaceholder`를 보게 했다. **`!isPending`을 "데이터가 이 키의 것"으로 읽으면 안 된다** — `keepPreviousData` 쿼리에서는 거짓이다. 팝업 칩이 콜드에서도 성공한 것은 정상 동작의 근거가 아니라 같은 결함의 반대 사례(확정 전 좁은 영역에도 대상이 있었다)였고, 부작용은 미발동만이 아니라 확정 후 더 가까운 대상을 두고 엉뚱한 대상으로 가는 것이었다 |
| 2026-08-21 | MSG-451 | 결정(스펙 해석): 저줌에서 미션 격자를 걷을 때 **이름표(label)도 함께** 걷는다. AC는 격자만 명시 | 이름표 좌표가 격자 꼭짓점(`decodeGridCorners`)이라 셀 없이 남기면 집계 마커 위에 뜬 미아가 된다. 반대로 **상세를 연 미션의 격자는 저줌에서도 유지**한다 — 최근접 진입이 이동한 직후 도착 화면이 통째로 비기 때문 |
| 2026-08-21 | MSG-451 | 결정: `deriveReloadTarget` 입력을 `isDetailPanel` → `isThemeDetailPanel`로 **이름까지** 변경. 호출부 값만 고치는 대안 기각 | 격자 상세를 제외 대상에서 빼는 변경인데 이름이 "상세면 숨긴다"로 남으면, 다음 소비처가 같은 오해(격자 상세도 목록을 대체한다)를 재생산한다. 격자 상세는 지역 격자 목록 **위에 겹칠 뿐** 대체하지 않는다는 것이 이 변경의 근거 자체다 |
| 2026-08-21 | MSG-451 | 결정: 대표 뱃지 편집 우상단 "완료"를 저장 배선이 아니라 **"취소" 문구로 정정**(MSG-413 추정 A2의 동작은 유지). 원 요구는 "완료도 저장하게" | 완료를 저장으로 만들면 미저장 폐기(취소) 경로가 사라진다. 사용자가 원 요구를 철회하고 문구 정정으로 선회 — 저장 진입점은 하단 "대표 뱃지 저장" 단일로 유지된다. 3개째 클릭 무시(MSG-413 기준 4)는 FIFO 교체로 번복했고, 화면 안내 문구도 함께 고쳐 설명과 동작의 어긋남을 남기지 않았다 |
| 2026-08-21 | MSG-451 | 결정(환류 개정): 칩 최근접 진입에서 **상세 열림(선택)을 제거**하고 지도 이동만 남김 — `useNearestEntry`가 `select` 콜백을 아예 받지 않는다. 대안 ① 선택은 두되 되돌리기 쉽게 ② 첫 진입에만 선택 기각 | 원 요구는 "가까이 뜨는 곳 기준으로 이동하면서 팝업 정보들이 뜨게"였으나, 실제로 써 보니 **칩을 눌렀을 뿐인데 특정 행사가 사용자가 고른 것처럼 선택된 상태**가 됐다. 칩의 목적은 "이 근처에 뭐가 있나"를 보여주는 것이고 어느 것을 볼지는 사용자 몫이다. 콜백을 받지 않는 형태로 막은 것은 `zoomTo`를 받지 않는 선례와 같은 판단 — 수단이 없으면 나중 배선 실수도 없다 |
| 2026-08-21 | MSG-451 | 발견(환류): 경로추천 뒤 축제·팝업으로 갈아타면 줌만 바뀌고 지도가 안 움직임. 원인은 **지도 중심이 바다**라 역지오코딩이 null이고, 확정(commit)이 행정동을 무기한 기다리는데 이동을 "확정 완료"에 묶어 둔 것. 해소: `useChipEntry` 반환을 "확정 완료"→**"진입 정착(목표 줌 도달)"**으로 앞당겨 확정 대기와 이동을 분리 | 확정 규칙(MSG-403)은 정당하다 — 포기하면 이전 확정 영역을 계속 조회한다. 잘못된 것은 **다른 관심사(이동)를 그 대기에 태운 것**이다. 코스는 해안선을 따라가므로 이 조건이 예외가 아니라 상시다. "줌 애니메이션이 panTo를 삼킨다" 가설은 깨진 상태에서 드래그해도 목표로 점프하지 않아 기각했고, 훅 조립 단위 테스트가 통과한 것이 원인을 입력(region)으로 좁혔다 — **조립을 의심하기 전에 입력이 실환경에서 어떤 값을 갖는지 먼저 볼 것** |
| 2026-08-21 | MSG-451 | 결정: 웹 CD 트리거를 전달본의 `develop`에서 **`main`**으로 옮김. 대안 ① 전달본대로 develop 배포(BE 리듬 일치) ② develop 배포 + `workflow_run`으로 CI 성공 후 배포 기각 | README가 이미 `main`=배포로 선언하고 있어 ①은 문서와 코드가 갈라진 채로 굳는다. 더 중요한 건 **CD가 CI를 기다리지 않는다**는 점이다 — 같은 push에 걸린 워크플로는 서로 순서가 없어, develop 배포면 테스트가 깨진 커밋이 먼저 라이브에 나갈 수 있다. ②는 그걸 막지만 배선이 늘고 실패 시 원인 추적이 두 워크플로로 갈린다. main 배포로 옮기면 릴리스가 PR을 거치므로 PR 단계 CI 통과가 자연스러운 관문이 되어 추가 배선 없이 같은 보장을 얻는다. 대가는 BE(develop 라이브)와의 리듬 차이 — 문서에 명시 |
| 2026-08-21 | MSG-451 | 발견: 전달본이 요구한 `VITE_KAKAO_JS_KEY`·`VITE_KAKAO_LOGIN_ENABLED`를 **코드가 전혀 읽지 않음**(전수 grep: 코드가 쓰는 `VITE_*`는 `VITE_API_BASE_URL`·`VITE_NAVER_MAP_NCP_KEY_ID` 둘뿐). 워크플로 env·`.env.example`에서 제거 | 카카오 로그인은 서버 진입점(`/api/auth/oauth/kakao/authorize`) 경유라 앱 키·scope·nonce가 전부 서버 몫이다 — 프론트에 카카오 키가 필요한 경로 자체가 없고, 그래서 키 없이도 로그인이 된다. 플래그도 마찬가지로 `false`여도 로그인이 동작한다. 등록해도 해는 없지만 **"false인데 왜 로그인이 되지"** 를 나중에 누군가 겪는다. `.env.example`의 "서버 교환 엔드포인트가 아직 없다"는 주석도 스테일이었다(`POST /api/auth/oauth/kakao/code`가 명세에 있고 콜백 페이지가 이미 사용) — **오래된 주석을 현재 사실로 읽어 한 번 잘못 판단했고, 명세·호출부 실측으로 정정했다** |
| 2026-08-21 | MSG-451 | 결정: assets S3 sync에만 `--size-only` 추가. 대안 ① 기본 비교 유지 ② 루트 sync에도 함께 적용 기각 | `s3 sync` 기본 비교는 크기+mtime인데 빌드가 매번 파일을 새로 만들어 mtime이 항상 최신이다 — 내용이 같아도 전량 재업로드되고, 산출물 33MB 중 **31MB가 ffmpeg wasm**이라 매 배포 비용·시간이 그만큼 든다(①). `assets/`는 파일명이 내용 해시라 "같은 이름 = 같은 내용"이 구성상 보장되어 크기 비교로 충분하다. ②는 위험하다 — `public/`의 favicon·서비스워커는 해시가 없어 **크기가 같은 채 내용만 바뀌면 갱신을 놓친다** |
| 2026-08-21 | MSG-451 | 발견(codex 리뷰 P1): 3차 재작업이 1차 재작업의 버그를 바다 경로로 되살림 — `region===null`이면 `settledChip`은 발행되는데 commit이 미뤄져, 그 사이 직전 확정 영역 기준 목록으로 활성화가 소진된다. 해소: **실제로 이동했을 때만 소진**(대상 없으면 `handledChipRef` 미갱신) | 정착 신호는 "목록이 이 화면 기준"임을 **보장하지 못한다** — 신호와 목록이 서로 다른 조건(줌 도달 vs 확정)에 걸려 있기 때문이다. 소진 시점을 신호가 아니라 **관측 가능한 결과(이동 발생)** 에 묶으면 두 조건이 어긋나도 안전하다. 브라우저 검증이 놓친 이유는 확정 전 목록에 마침 대상이 있었던 데이터 운 — **실동작 통과가 조합 커버리지를 뜻하지 않는다**. push 전 codex 리뷰를 게이트로 세운 첫 회차에서 바로 잡힌 사례 |
| 2026-08-21 | MSG-451 | 결정(범위 확장): 웹에서 고친 순수 함수 2종(`toggleFeatured` FIFO, `mergeOverlappingMarkers` 이름 규칙)을 **모바일 포팅본에도 동시 반영**. 대안 ① 티켓 범위(웹 전용)대로 웹만 고치고 parity 테스트 완화 ② 모바일 변경을 후속 티켓으로 미룸 기각 | develop 머지로 MSG-428·430의 모바일 포팅본이 들어오자 **parity 테스트가 드리프트를 즉시 잡았다** — 설계대로 작동한 것이다. ①은 parity 테스트의 존재 이유를 지우고 **모바일에 같은 버그 2건(3개째 선택 무반응·병합 마커 이름 없음)을 기록 없이 남긴다**. ②는 그 사이 두 플랫폼이 같은 PUT에 다른 결과를 내는 구간을 만든다. 순수 함수라 parity 테스트가 값으로 동등성을 고정하므로 실기 검증 없이도 안전하다. **caveat**: `localeCompare(_, "ko")`의 로케일 인자는 Hermes에서 무시될 수 있다 — 한글 음절은 코드포인트 순서가 가나다순과 사실상 일치해 실무상 문제없지만, parity 테스트는 Node에서 돌아 이 차이를 못 잡는다(지라 환류). 안내 문구·화면 검증은 MSG-430 몫 |
| 2026-08-21 | MSG-451 | 결정: develop 머지 후 `nose.baseline.json`을 `--write-baseline`으로 2회 재등재(416 families) — 웹↔모바일 포팅 쌍의 지문이 우리 수정으로 바뀐 것 | 레포 관례(MSG-302~305·317·324 선례)대로 의도 중복은 베이스라인 등재로 통과시킨다. **조사 교훈**: `check:duplication`은 "0 new"를 출력하면서도 changed가 있으면 exit 1이다 — 출력 문구만 보고 통과로 판정했다가 pre-push에서 막혔다. 게이트 판정은 **exit code로** 해야 한다 |
| 2026-08-21 | MSG-451 | WAIVED(codex P1, 사용자 수용): 중심이 행정동 밖이라 확정이 미뤄지는 동안 **직전 확정 영역 목록**의 최근접으로 이동하고 활성화가 소진되는 잔여 케이스. 대안 ①`settledChip`+`committedChip` 두 신호로 확정 후 보정 이동 ②확정 전 이동 금지 기각 | 그 "옛 목록"은 **좌측 패널이 지금 보여주는 바로 그 목록**이다(같은 쿼리) — 이동 대상이 화면과 일치하므로 사용자 관점에서 일관된다. 어긋나는 것은 좁은 조합(확정 지역이 멀고 + 불러오기 없이 멀리 이동 + 칩 전환 + 중심이 바다)뿐이고 그때도 목록은 같은 지역을 보여준다. ①은 바다 케이스에서 **지도가 두 번 튀어** AC 11("활성화당 1회")과 정면 충돌 — 확률 낮은 잔여 케이스를 고치려 상시 경로에 새 문제를 들인다. ②는 사용자가 최초 신고한 버그("확대만 되고 이동 안 됨")를 되살린다. 근본 해결은 목록이 어느 bbox 기준인지를 신호로 노출하는 것(별도 티켓) |
| 2026-08-21 | MSG-444 | 결정: 모바일 카카오 로그인을 **네이티브 SDK → ID 토큰 → `POST /api/auth/oauth/{provider}`**로 확정 — ① 웹 서버 주도 OAuth 재사용 ② RN 선행 fetch로 nonce 쿠키 확보 ③ WebView 쿠키통 공유 ④ 서버가 토큰을 딥링크 반환, 네 대안 기각 | ①의 `POST /api/auth/oauth/kakao/code`는 `/authorize`가 심는 `OAUTH_NONCE` **HttpOnly 쿠키**를 요구하는데(실측: 쿠키 없이 POST → 401 `2423`), expo-web-browser로 열면 그 쿠키는 인앱 브라우저 쿠키통에 들어가고 앱 fetch는 다른 통을 쓴다 — 서버 변경 없이는 완결 불가다(`/authorize`가 커스텀 스킴 redirectUri를 통과시키는 것까지는 실측 확인). ②는 RN fetch가 XHR 기반이라 `redirect: "manual"`을 무시해 Location을 읽을 수 없다. ③은 Android만 성립하고(OkHttp `ForwardingCookieHandler` ↔ WebView `CookieManager` 공유) iOS는 `WKHTTPCookieStore`가 분리돼 깨진다 + react-native-webview 신규 의존. ④는 엔드포인트 신설 = 티켓 제외 범위. 결정적으로 생성 SDK 문서가 "네이티브 SDK가 교환까지 해주는 앱은 이 API가 아니라 `POST /api/auth/oauth/{provider}`를 쓴다"고 **백엔드가 앱 경로를 이미 지정**하고 있었고, 그 엔드포인트는 쿠키를 쓰지 않는다 |
| 2026-08-21 | MSG-444 | 발견(로컬 빌드 실측): Expo config plugin 옵션 누락과 Maven 저장소 누락 — 둘 다 **실기 전에는 드러나지 않는** 형태의 블로커였다 | ① `@react-native-kakao/core` 플러그인은 `if (android) {...}` / `if (ios) {...}` 가드가 있어 `nativeAppKey`만 넘기면 두 블록을 통째로 건너뛴다 — **플러그인 목록에는 정상 등록되고 prebuild도 성공하는데 AndroidManifest에는 아무것도 안 들어간다.** prebuild 산출물을 grep해 kakao 항목 0건으로 발견했고, `android: { authCodeHandlerActivity: true }`로 `kakao{앱키}://oauth` 인텐트 필터를 확보했다(없으면 카카오톡 미설치 사용자의 웹 로그인 복귀 경로가 끊긴다). ② `com.kakao.sdk:v2-*`는 Maven Central에 없어(2.20.1 → 404, devrepo → 200) gradle이 의존성 해석 단계에서 실패한다 — `extraMavenRepos`에 `devrepo.kakao.com` 추가로 해소, 이후 `assembleDebug` 성공. 교훈: 네이티브 의존을 추가하는 티켓은 **실기 이전에 prebuild 산출물 대조 + 로컬 gradle 빌드**를 검증 단계에 넣어야 한다(둘 다 기기 없이 잡힌다) |
| 2026-08-21 | PR #84 | 발견(리뷰) + 수정: `adjustStartHandle`의 clamp 상·하한이 5초 미만 원본에서 역전(min=0 > max=끝-5<0) — `clamp`가 max를 그대로 돌려줘 시작 시각이 음수가 된다. 웹·모바일 동시 수정(`max = Math.max(min, 끝-5)`) | 5초 미만 영상은 **예외가 아니라 정상 경로**다 — `select-validation`이 "서버는 durationSec 1초부터 받고 highlight-preview는 5초 미만에 빈 배열을 준다"며 길이 하한을 일부러 두지 않았고, 그 경로의 초기 구간은 `clampSegment`가 `{0, duration}`으로 만든다. 슬라이더의 `ratio()`가 표시만 0~1로 가려 **화면으로는 안 보이고 상태·서버 트리밍 요청에만 음수가 남는** 형태였다. 기존 픽스처의 최단 구간이 `{40,42}`(끝=42라 max=37>0)여서 역전 조합이 한 번도 안 걸렸다 — **경계 테스트는 "구간 길이"가 아니라 "구간 끝값"을 줄여야 이 함정을 밟는다.** 웹→모바일 포팅본이라 parity 테스트가 양쪽을 값으로 묶고 있어 동시 수정이 강제된다 |
| 2026-08-21 | PR #84 | 결정(리뷰 수용): dev 스모크 화면(`app/dev/api-smoke.tsx`)을 `__DEV__` 래퍼로 감싼다. 화면 본체를 `ApiSmokeScreen`으로 분리하고 default export는 `__DEV__ ? <ApiSmokeScreen /> : null` — 리뷰가 제안한 "훅보다 앞에 조건부 return" 형태는 훅 규칙 위반이라 수정해 채택 | expo-router는 **파일 존재만으로 라우트를 등록**하므로 "화면에 진입 버튼이 없다"가 프로덕션 미도달을 뜻하지 않는다. 딥링크(`fillmap://dev/api-smoke`)를 아는 사람은 스토어 빌드에서도 닿고, 이 화면의 dev 로그인은 고정 oid로 **실제 세션 토큰**을 발급받는다. 서버가 운영에서 `/api/auth/dev/social-login`을 막았는지는 FE가 보증할 수 없어 심층 방어로 둔다. 웹 `DevLoginPanel`(`import.meta.env.DEV ? <DevLoginForm/> : null`)과 같은 래퍼 형태로 맞췄다 |
| 2026-08-21 | PR #84 | 결정(리뷰 기각 6건): React Doctor 경고 중 `rn-scrollview-dynamic-padding` 3건·`query-mutation-missing-invalidation` 3건·`no-create-object-url-without-revoke` 1건을 기각. `rn-prefer-expo-image`·`no-giant-component`는 후속으로 이월 | ① scrollview 3건의 `paddingBottom`은 `insets.bottom`(세션 중 불변)이라 규칙이 전제한 "변하는 값"이 아니고, 제안된 `contentInset`은 **RN에서 iOS 전용**이라 채택하면 Android 하단 패딩이 사라진다 — 없는 버그를 고치려 실제 회귀를 만든다. ② mutation 3건은 이미 캐시를 다루고 있다: `use-auth-mutations`의 로그인 3종은 `queryClient.clear()`를 호출하고, `useLogout`은 안 비우는 이유가 JSDoc에 명시돼 있으며, `use-push-toggle`의 상태 정본은 zustand 스토어+브라우저 권한이라 무효화할 쿼리가 없다. ③ `use-video-duration`은 effect cleanup 52행에서 `URL.revokeObjectURL(url)`을 이미 호출한다(정적 분석이 못 증명한 것이지 누락이 아니다). ④ expo-image는 NativeWind v4 allowlist 밖이라 `cssInterop` 등록 없이는 `className`이 무시돼 메달이 0px가 된다 + 대상이 6곳(thumbnail·video-row·avatar 등)인데 1곳만 바꾸면 컨벤션이 갈린다 — 릴리스 머지 직전에 낼 판단이 아니다. ⑤ `GridMap` 496줄 분해는 동작 변화 없는 순수 리스크라 릴리스 이후로 미룬다 |
| 2026-08-21 | MSG-446 | 발견(parity 테스트가 잡은 오타): 웹 `video-playback.ts`를 모바일로 포팅하며 처리 중 안내 어미를 `"재생할 수 있다"`로 잘못 적었다(원본 `"재생할 수 있어요"`) — 구현 직후 parity 테스트가 즉시 실패시켰다 | 웹과 앱이 **같은 상태의 영상에 다른 말투로 안내**하는 어긋남이 커밋 전에 걸렸다. 문구는 typecheck·lint가 볼 수 없는 축이라, "웹 순수 모듈 포팅 시 parity 테스트 동반" 규칙이 유일한 그물이었음이 실증됐다 |
| 2026-08-21 | MSG-446 | 발견(실기 검증이 잡은 결함) + 결정: `dex-screen`의 `BackHandler` 구독을 **포커스 구간으로 한정**한다 — `useFocusEffect`가 관리하는 `focusedRef`를 Effect Event 안에서 읽는 방식. ①구독 자체를 `useFocusEffect`로 이동 ②포커스를 state로 보관 ③도감 갤러리를 라우트로 승격, 세 대안 기각 | RN의 `BackHandler` 구독은 화면 단위가 아니라 **앱 전역**이라 도감이 배경에 있어도 살아 있었다. MSG-446이 갤러리 카드에 `/video/[videoId]`를 push하면서 처음 발현했다 — 뒤로가기 한 번에 라우트 pop과 `setSelected(null)`이 동시에 일어나 보고 있던 갤러리가 닫히고 동 목록으로 떨어졌다(티켓 요구 "직전 목록으로 돌아간다" 위반). **push할 목적지가 없던 때는 발현 경로 자체가 없던 결함**이라 이전 티켓들이 못 잡았고, 모바일에 RN 렌더 테스트 인프라가 없어 테스트로도 잡을 수 없는 부류였다 — 실기 검증이 유일한 그물이었다. ①은 그 안의 Effect Event 호출이 `react-hooks(rules-of-hooks)` 위반이라 oxlint가 실제로 거부했고, ②는 포커스 전환마다 리렌더가 붙으며, ③은 티켓 범위를 훨씬 넘는다. `map-home-screen`은 Effect Event를 쓰지 않아 구독 자체를 `useFocusEffect`에 넣었다 — 목적은 같고 수단만 다르다 |
| 2026-08-21 | MSG-449 | 결정: 검증 스킬의 실동작 경로를 **프로파일(얼마나)과 대상 앱(어디서)** 두 축으로 분리하고 3-B(모바일 dev client)를 신설한다 — ① 프로파일에 "모바일" 값을 추가 ② 모바일 전용 스킬 신설 두 대안 기각 | 프로파일에 값을 더하면 "화면+모바일" 조합이 표현되지 않고, 스킬을 쪼개면 웹/모바일 검증 원칙이 갈라진다. 대상 앱은 변경 파일 경로로 기계적으로 정해지므로 판단 여지가 없다 |
| 2026-08-21 | MSG-449 | 발견(실행 검증) + 결정: `expo run:android`는 설치 직후 Metro 없이 앱을 자동 실행하므로, 진입 전에 `am force-stop`으로 콜드 스타트를 강제한다 | 빌드·설치·Metro가 전부 정상인데 화면만 검게 나오는 증상의 원인. 살아 있는 인스턴스에는 `am start`가 새 실행이 아니라 `onNewIntent`로만 전달돼 앱이 번들을 다시 받지 않는다. 런북 함정 8로 문서화 |
| 2026-08-21 | MSG-449 | 정정(실측): 티켓 함정 5 "`expo run:android --device`는 모델명을 받는다"는 **물리 기기 한정**이다 — 에뮬레이터는 AVD 이름을 받는다(`sdk_gphone16k_arm64` 실패 / `Pixel_8_2` 성공) | 2026-08-20 실기(SM_S931N)로만 확인해 일반화한 진술이었다. 스크립트가 시리얼·모델명·expo 이름을 3열로 분리해 자동 해석한다 |
| 2026-08-21 | MSG-449 | 발견(실행 검증) + 결정: 포트 점유 검사에 `-sTCP:LISTEN`을 필수로 한다 | `lsof -ti tcp:8081`은 그 포트로 **연결한** 프로세스(에뮬레이터 qemu·netsimd)까지 매치해, 포트가 비어 있는데도 점유로 오판하고 엉뚱한 프로세스를 죽이게 한다 |
| 2026-08-21 | MSG-449 | 결정: `apps/mobile`의 `start`를 `expo start --dev-client`로 고정한다 — 런북에 "쓰지 말 것"만 적는 대안 기각 | 이 앱은 네이티브 모듈 때문에 Expo Go에서 즉시 강종한다. 주석·문서로는 못 막는 것이 MSG-427에서 실증됐고, 레포 기본 진입점이 문서와 반대로 동작하면 문서가 무력하다 |
| 2026-08-21 | MSG-449 | 정정(커밋 전 codex 리뷰): `expo prebuild --clean`은 SDK 57에 **없는 플래그**다 — 재생성이 기본이고 opt-out이 `--no-clean`이다. 티켓·스펙이 전제한 `--clean`을 스크립트·런북 전 명령에서 제거 | 실행해 보면 CLI가 알 수 없는 플래그를 조용히 무시한 채 정상 완료하므로(2026-08-20 실측이 통했던 이유) 당장 깨지지는 않는다. 하지만 런북의 "`--clean` 없이는 안 풀린다"는 사실과 반대라, 읽고 `--no-clean`을 붙이면 문제가 오히려 재현된다 |
| 2026-08-21 | MSG-449 | 결정(커밋 전 codex 리뷰 반영): cleanup은 8081 리스너를 무조건 죽이지 않고 **ppid 사슬로 metro_pid의 자손임을 확인한 PID만** 죽인다 | 기동 경합에서 우리 Metro보다 먼저 포트를 잡은 무관한 프로세스를 죽일 수 있다는 지적. 부모를 먼저 죽이면 사슬이 끊기므로 목록 수집을 kill보다 앞에 둔다 |
| 2026-08-21 | MSG-449 | 결정(push 전 codex 리뷰 P2 반영): `android:dev`의 `--device`가 **adb 시리얼도** 받고, 이름이 여러 대에 걸리면 첫 대를 고르지 않고 **거부**한다 | 같은 모델을 여러 대 연결하면(디바이스 랩) 이름만으로 구분이 안 되는데, 종전 awk는 첫 매치를 조용히 골랐다. 그 경우 expo 빌드 대상과 이어지는 시리얼 기반 reverse·launch 대상이 서로 다른 기기가 될 수 있다. 시리얼은 유일하고, expo가 시리얼을 안 받으므로 이름 변환은 스크립트가 맡는다 |
| 2026-08-23 | MSG-447 | 결정: OS 권한 3상태 정의를 `shared/permission-state.ts`로 승격하고 MSG-429의 `PushPermissionStatus`를 그 별칭으로 남긴다 | 위치 축이 알림 축과 **완전히 같은** `{granted, canAskAgain}` 매핑을 필요로 했다. `features/permissions`에 두는 대안은 `shared/geolocation.ts`가 이 타입을 쓰기 때문에 FSD가 막는다(shared → features 금지). 두 벌로 두면 `denied`=영구 거부라는 의미까지 같은 정의가 드리프트한다 |
| 2026-08-23 | MSG-447 | 결정: MSG-429 승인 D2("등록 진입점은 프로필 토글뿐")를 업로드 완료 시점으로 확장하되 **`undetermined`일 때만**으로 묶는다 | D2의 전제는 "거부되면 되살릴 진입점이 사라진다"였는데 이 티켓이 설정 복구 동선을 만들어 그 전제가 바뀌었다. 다만 `granted`인데 보관 토큰이 없는 상태는 **사용자가 토글로 끈 것**이므로 재등록하지 않는다 — 업로드가 사용자 결정을 조용히 뒤집으면 "껐는데 알림이 온다"가 된다 |
| 2026-08-23 | MSG-447 | 결정: 권한 판독이 던질 때(네이티브 접점 장애) `denied`가 아니라 `granted`로 보고한다 | 판정 불가를 거부로 취급하면 권한 축의 문제가 아닌 것을 권한 안내로 띄워, 사용자를 설정 화면으로 보내 놓고 **아무것도 고칠 수 없게** 만든다. 좌표는 어차피 서면으로 폴백되므로 안내만 억제하면 된다 |
| 2026-08-23 | MSG-447 | 발견(실기): 설정에서 권한을 켜고 돌아와도 안내가 남았다 — 포그라운드 재판독이 `permission`만 갱신하고 낡은 `error="denied"`를 안 지웠다. `reconcilePushError`로 재판독 지점에서 정리 | **정적 게이트 6종을 전부 통과한 상태에서 실기가 잡았다.** 이 티켓이 만든 복구 동선이 자기 발등을 찍는 형태(설정으로 내보내 놓고, 시키는 대로 하고 돌아온 화면이 여전히 같은 말을 한다). 안내 파생 쪽에서 `pushError`를 무시하는 대안은 기각 — 토글을 막 거부한 직후에는 재판독이 도착하지 않아 그 값이 유일한 근거다. 낡음을 판정할 수 있는 곳은 새 사실이 도착하는 지점뿐이다 |
| 2026-08-23 | MSG-447 | 발견(실기): 재요청 가능한 상태의 안내가 [권한 요청] 버튼 위에 "기기 설정에서 허용해주세요" 본문을 띄웠다 — 축별 문구를 복구 수단별(`request`/`settings`)로 분리 | 버튼은 앱 안에서 바로 묻겠다는데 글은 설정으로 가라고 한다. 시키는 대로 다녀와도 그 상태에서는 바꿀 항목 자체가 없어 헛걸음한다. 축×상태 전 조합을 테스트로 전수 고정 — 빈 칸을 남기면 알림 축에 재요청 경로가 생길 때 같은 결함이 재발한다 |
| 2026-08-23 | MSG-447 | 발견(검증 방법): `adb pm set-permission-flags … user-fixed`만으로는 expo-notifications가 `denied`로 보고하지 않는다 — Expo가 `canAskAgain`을 자체 "요청 이력"과 함께 판정한다 | 앱이 실제로 한 번 물어본 뒤에야 영구 거부가 성립한다. 이 차이를 모르고 adb 플래그만 세우면 정상 구현을 실패로 오판한다(이번에 오판할 뻔했다). 진짜 초기 상태가 필요하면 `pm clear` 후 dev 로그인(`fillmap://dev/api-smoke`) |
| 2026-08-22 | MSG-448 | 결정: 위치기반(필수) 동의에 **철회 조작을 만들지 않는다** — 읽기 전용 상태 + "철회 불가·되돌리려면 계정 삭제" 상시 고지 + 기존 삭제 흐름 연결. 대안 기각: (B) 철회 버튼 → 항상 1400 실패 안내 (C) 로컬만 철회 (D) BE에 철회 API 요청 후 축 분리 | 서버 계약이 `consented=false`를 developCode 1400으로 거절한다(생성물 주석 2곳). B는 **절대 성공하지 않는 버튼**을 사용자에게 보여준다. C는 서버와 상태가 갈린다. D는 API가 생겨도 남는 구조 문제(=`_layout.tsx` 게이트가 `locationConsent=false`에서 앱 전체를 잠근다 — 설정 화면 자신도 사라진다)를 못 푼다. 웹 MSG-407 v3 결정 1(인앱 동의 접점 제거)과도 정합 |
| 2026-08-22 | MSG-448 | 결정: 약관 문서를 `body: string \| null` + `isTermsPlaceholder`로 모델링하고 전 문서를 `null`로 둔다 — 빈 문자열·"준비 중입니다" 같은 **가짜 본문을 넣지 않는다** | 문구 작성은 기획·법무 몫이고 조회 API도 없다. 가짜 본문을 넣으면 "문구가 있다"와 "없다"가 타입으로 구분되지 않아, 반쯤 채워진 상태가 조용히 흘러간다. 전 문서 `body === null` 단정이 테스트에 있어 문구가 들어오는 순간 RED가 되어 화면 갱신을 강제한다 |
| 2026-08-22 | MSG-448 | 결정: 내 신고 내역은 **빈 목록 + 상시 미연동 고지**로 두고 관리자 전용 `GET /api/admin/reports`를 호출하지 않는다. 대안 기각: mock 신고 4건 표시 | 그 엔드포인트에는 신고자 필터가 없어 **호출이 성공하면 그 자체가 타인 신고 노출 사고**다. 그리고 신고 내역은 "내가 무엇을 신고했는가"라는 사실 진술이라 지어내면 화면이 거짓말을 한다. 고지가 없으면 빈 상태가 "신고한 적 없음"으로 오독돼 실제 신고자에게 거짓이 된다. 교체 이음새는 `use-my-reports-query.ts` 한 파일로 고정 |
| 2026-08-22 | MSG-448 | 결정: 약관 뷰어를 `entities/terms`에 두고 `features/auth`·`features/profile`이 공유한다 — `entities/*`에 `ui/`를 두는 첫 사례 | 소비처가 서로 다른 두 feature라 한쪽에 두면 교차 feature import(FSD 위반)가 된다. `apps/*/shared`의 재사용 UI는 DESIGN_SYSTEM 명시 금지, `ui-native` 승격은 배럴 동결 + 도메인 인지(약관 문서) 두 이유로 불가 |
| 2026-08-22 | MSG-448 | 결정: KST 날짜 변환을 `features/profile/model/profile-format`에서 `shared/format.ts`(`formatKstDate`)로 올리고 가입일·신고 시점이 공유한다 — 신고 내역에 복제하는 대안 기각 | 두 feature가 같은 변환을 쓰게 됐는데 교차 feature import는 FSD 위반이고, 복제하면 "타임존 마커 없는 UTC를 그대로 자르면 하루가 밀린다"는 함정이 두 벌이 된다(구 모바일 구현이 실제로 그 결함을 가졌다). `formatDuration`이 같은 경로로 올라간 선례가 파일 헤더에 있다 |
| 2026-08-22 | MSG-448 | 결정: 조회 실패+재시도 블록을 두 신규 화면에 로컬 복제하고 `nose.baseline.json`에 의도 중복으로 등재 — `DexErrorState`의 `ui-native` 승격 대안은 배럴 동결로 **불가** | 그 컴포넌트 JSDoc이 "사용처 3곳 초과 시 승격 검토"라 적었고 이 티켓이 4·5번째 사용처를 만든다 = 명백한 승격 신호. `packages/ui-native/src/index.ts` export 추가 금지(MSG-420 소유)가 유효해 이번엔 복제로 가되, **배럴 동결이 풀리는 시점의 1순위 승격 대상**으로 남긴다 |
| 2026-08-24 | MSG-462 | 결정: 줌 스텝 정책은 "쿨다운 중 입력 폐기 + 임계 도달 시 방향당 정확히 ±1(초과 누적 미이월)"로 설계, 상수는 zoom-step-policy 단일 정의(쿨다운 200ms·임계 80) | 트랙패드 플링 한 번의 대량 델타를 이월하면 쿨다운이 끝날 때마다 스텝이 연쇄돼 "한 제스처 = 여러 단"이 재발한다(AC 3 위반). SDK에는 줌 속도 옵션이 없어 scrollWheel off + 자체 wheel 리스너가 유일 경로(스펙 실측). 대안(델타 비례 다단 스텝)은 감속이라는 티켓 목적과 반대라 기각 |
| 2026-08-24 | MSG-462 | 결정: 격자 탭의 `getMissionsByGrid` fetchQuery에 `retry: false` | 클릭 응답 경로라 기본 재시도(3회 지수 백오프)를 기다리면 수 초간 아무 패널도 안 열린다 — 실패는 즉시 판정 함수로 흘려 격자 상세 폴백(AC 10)하는 쪽이 UX 요구에 부합. 대안(기본 재시도 유지)은 폴백 지연으로 기각 |
| 2026-08-24 | MSG-462 | 결정: 격자→미션 도형 소속 맵(buildMissionGridMembership)의 클리핑 경계는 오버레이 목록 파생과 같은 **확정 영역**(committedBounds) | 클릭 가능한(게시된) 격자 집합과 판정 집합을 같은 경계로 맞춰야 "보이는데 판정 안 됨"이 없다. 대안(라이브 뷰포트)은 게시 셀과 어긋나고, 상세 열림 중 자기 경계(detailBounds) 셀은 API 판정 또는 동일 미션 재선택으로 수렴해 실익이 없어 기각 |
| 2026-08-24 | MSG-462 | 발견: MapCanvas 휠 리스너 effect의 deps에 넣은 `failed`가 선언 전 참조(TDZ)로 마운트 즉시 크래시 — 신규 스모크(AC 2·3 배선)가 첫 실행에서 잡아 원시 상태(`authFailed`·`sdkStatus`) deps로 수정 | 파생 상수를 훅보다 아래에 선언하는 기존 파일 구조에서 effect가 그 파생을 deps로 집으면 컴파일은 통과하고 런타임에만 터진다 — typecheck·lint 게이트가 못 잡는 유형이라 배선 스모크의 가치 실증 |
| 2026-08-24 | MSG-464 | 결정: 로그인 로고 자산을 새 파일명(fillmap-app-icon-alpha.png) 대신 기존 `fillmap-app-icon.png` 내용 교체(416px 알파 PNG)로 반영 · ui-web `Thumbnail`의 props는 variants.ts 대신 로컬 정의 · 폴백 격자 마크는 `max-h/w-[60%]`로 작은 컨테이너(size-16)에서 자동 축소 | 자산 소비자가 LoginContent 유일이라 이름 유지가 import 불변·고아 파일 0으로 가장 수술적. props 로컬 정의는 ui-native Thumbnail의 "웹 대응과 API가 달라 로컬 정의" 선례를 역방향으로 준수(완전 미러 아님 — 스펙 추정 3). 60% 축소는 추정 5의 builder 재량 — 별도 size prop 추가는 스펙에 없는 확장이라 기각 |
| 2026-08-24 | MSG-463 | 결정: 상세 패널의 로그인 모달 열기를 props 배선 대신 리프(MissionDetailPanel·CourseDetailPanel)의 `useLoginModalStore` 직접 구독으로 — `progressLocked`만 페이지→HomePanelSwitch props로 내린다 | 스펙의 props 강제는 `use-home-panel-state` 수정 금지(병렬 티켓) 때문이고 그 제약은 `isAuthenticated` 판정에만 걸린다. 전역 모달 스토어는 페이지 상태와 이중 구독 우려가 없고 같은 폴더 RegionPanel의 로그인 유도가 이미 같은 방식이다 — 콜백 prop 2개(HomePanelSwitch 경유 3파일)를 늘리는 대안 기각 |
| 2026-08-24 | MSG-463 | 결정: 이어받기 실패 시 자동 재시도 금지 — sentinel을 실패·로딩 중에는 렌더하지 않고 재개는 RetryNotice 수동 재시도(`loadMore`)만. `loadMore`는 진행 중·마지막 페이지엔 no-op 가드 | sentinel이 실패 상태에도 남으면 IntersectionObserver가 실패→재요청→실패 무한 루프를 돈다(서버 장애 시 폭주). fetchNextPage의 cancelRefetch 기본값이 진행 중 요청을 재시작하는 함정도 가드로 차단 — use-occupied-grids-query 선례(상한 가드)와 같은 계열 |
| 2026-08-24 | MSG-465 | 발견: jq `//` 연산자가 `false`를 falsy로 삼켜 `is_error // "null"`이 정상 완주(`is_error=false`)를 `"null"`로 추출 — 워크플로 반영 전 픽스처 1차 실행(RED)이 발견, `tostring` 추출로 교정 | `//`는 null뿐 아니라 false에도 대체값을 적용하는 jq 의미론. 이대로 나갔으면 **모든 정상 리뷰 완주가 실패 판정**되는 역오탐 — 이 티켓이 고치려던 오탐을 반대 방향으로 재생산할 뻔했다. boolean 필드 추출에 `//` 금지, 판정 bash는 워크플로에 넣기 전 픽스처 독립 실행이 값을 증명 |
| 2026-08-25 | MSG-477 | 결정: 에러 화면 "홈으로"를 라우터 `navigate` → `shared/navigation.redirectTo`(내부 `window.location.assign`) 하드 이동으로. 대안 기각: 에러 상태 리셋 · `window.location.assign` 직접 호출 | react-router v7 errorElement의 에러 상태는 **location이 바뀔 때만** 리셋된다 — 홈 URL 자체에서 에러가 난 경우 `navigate("/")`는 같은 location이라 no-op이고, location이 바뀌어도 에러를 만든 모듈 상태가 메모리에 남아 재발한다. 공식 reset API가 없어 리셋 대안은 same-location 케이스를 못 덮는다. 직접 호출 대신 어댑터를 쓰는 이유는 둘: jsdom이 `location.assign` 재정의를 막아 스파이가 어댑터 목으로만 가능하고(login-modal 선례), 문서 이동은 RN 경계 규칙상 `shared/navigation` 경유가 정본이다 |
| 2026-08-25 | MSG-477 | 결정: 100m 격자 전국 확장을 **부산 경계 절단 제거**로 구현 — 대한민국 경계 폴리곤 교체 기각. `MAX_VIEWPORT_SPAN_DEG=0.5`(서버 제약)와 기본 지역 서면은 무수정 | 절단이 사라져도 성능을 지키는 것은 경계가 아니라 줌16 게이트·뷰포트 컬링+1화면 버퍼·선 규모 상한 3종이다(전부 유지). 전국 경계 폴리곤은 정점 수천 개 신규 데이터 + 프레임마다 `pointInPolygon` 비용인데, 얻는 것은 해상·국경 밖 격자선을 감추는 것뿐이라 값이 안 맞는다. 부수 효과로 `occupied-grid-overlay`의 경계 필터도 제거해야 했다 — 그 필터의 근거 주석이 "격자선이 부산 경계로 절단돼 있어서"였고, 남겨두면 부산 밖 점령 셀만 색칠 안 되는 신규 결함이 된다 |
| 2026-08-25 | MSG-477 | 발견: BE가 `PathShape.line`을 문자열 → GeoJSON **객체**로 바꿔 내리는데 `mission.ts:136`이 `typeof shape.line === "string" ? shape.line : null` 가드라 **코스 라인이 예외 없이 전부 null로 떨어져 미표시**된다. 파싱 대응은 MSG-473 소관으로 넘기고 이 티켓은 테스트 픽스처 2곳만 캐스트 | 세션 중 사용자 지시로 OpenAPI 스냅샷을 갱신하다 드러났다 — 커밋 스냅샷이 라이브보다 뒤처져 있었고, 이 계약 변경은 타입 에러 2건(테스트 픽스처)으로만 표면화되고 **런타임에서는 아무 예외 없이 조용히 사라진다**. 타입 가드가 새 계약을 "모르는 값"으로 흡수하는 형태라 typecheck·테스트가 프로덕션 경로에서 침묵한 사례. 병렬 워크트리(MSG-473)가 같은 파일을 작업 중이라 여기서 고치면 머지 충돌 |
| 2026-08-25 | MSG-473 | 결정: `coursePath(line)`을 `parseLineString` 위임 한 줄로 존치(삭제하고 호출부가 parseLineString을 직접 부르는 안 기각) | 수용 기준(AC 5)·parity 테스트가 함수명을 계약으로 지목하고, "지도에 그릴 경로" 의미와 MSG-403 폴백 번복 사유를 담는 문서화 지점이 필요 — 삭제하면 번복 기록이 주석 없이 증발한다 |
| 2026-08-25 | MSG-473 | 결정: hey-api 재생성으로 `MissionResponseDto.shape`가 유니온으로 좁아져 깨진 웹 테스트 픽스처(문자열 line — 사전 존재 1건 포함)는 헬퍼의 shape를 `Omit`+`unknown`으로 넓혀 수용 (픽스처마다 `as unknown as` 캐스팅 안 기각) | FE 런타임 계약은 명세보다 넓다(문자열 원문·깨진 형태까지 방어 흡수 — AC 2·3). 명세 밖 표현이 테스트의 목적 그 자체라 헬퍼 한 곳에서 의도를 주석으로 밝히는 쪽이 산발 캐스팅보다 소급 가능. `& { shape?: unknown }` 교집합은 `T & unknown = T`라 무효 — `Omit` 선행 필수(1차 시도 실측) |
| 2026-08-25 | MSG-476 | 결정: 블러 FE 경로(폴링·대기 스토어·워처·토스트·확인 모달·`pendingVideoStorage`) **전량 삭제** — 하네스 권고는 진입 차단(모듈 휴면 존치)이었으나 사용자가 완전 삭제를 선택 | 서버가 블러를 "당분간"(BE MSG-456) 끈 것이라 권고안은 재점등 비용이 수 줄이라는 근거였다. 사용자 판단이 정본 — 대가로 재개 시 재작성이 필요하고 이 커밋 revert가 출발점(주변 진화로 충돌 가능). 고아 localStorage 키는 읽는 코드가 없어 무해로 수용, 마이그레이션 코드 미추가(방금 지운 개념의 부활 방지) |
| 2026-08-25 | MSG-476 | 발견: 웹 모듈 삭제가 `apps/mobile` 패리티 테스트 5건을 깨뜨렸다 — 모바일 블러 복제본(MSG-424·429)의 패리티 테스트가 **웹 원본을 동적 import**해 동등성을 고정하는 구조 | 빌더의 `--filter web` 스코프 게이트로는 안 잡히고 루트 게이트가 잡았다. **웹 모듈 삭제 티켓은 모바일 패리티 앵커(현재 57개)를 스펙 단계에서 조회해야 한다.** 조치는 모바일 구현 무수정 원칙 유지 — 앵커가 사라진 패리티는 로컬 단위 테스트로 전환(기대값 동결)하거나, 로컬 대체 테스트가 있으면 삭제 |
| 2026-08-25 | MSG-476 | 결정: nose 신규 클론 2건을 추출 대신 베이스라인 등재 | `use-upload-mutations`의 confirm/replace 미러는 원래 있던 구조적 쌍둥이가 `track()` 2줄 제거로 더 닮아진 것이고, nose 자신이 "파라미터 9~11개 — divergence readability" 추출 부적합 판정. 웹↔모바일 문구 상수 복제는 의도된 아키텍처. 등재 시 무관 흡수 2건 발견·되돌림 후 파일집합 기준 재검증(351→345, 추가 id 27건은 전부 삭제 파일만 빠진 재키잉, 새로 용서된 중복 0) |
| 2026-08-26 | MSG-476 | 발견(QA 회귀): 블러 파이프라인 삭제가 **블러와 무관한 책임**(READY 전이 시 격자 쿼리 재무효화)까지 지워 업로드한 영상이 새로고침 전까지 목록에 안 보였다 | 확정 시점 무효화의 재조회는 서버가 아직 non-READY라 새 영상을 빼고 응답한다 — 삭제된 워처의 주석이 이 사실을 명시하고 있었는데도 삭제 범위 산정에서 놓쳤다. **1회차 검증은 12초 188KB 파일로만 해 처리 창이 열리지 않아 통과했다**(1080p 8MB로 재현하니 88초 무변화 → 새로고침에 격자 7→8). 처방: `ready-poll` + `start-ready-refresh`로 재무효화 책임만 복원, 통지 UI는 부활 안 함. 수용 기준 7도 "폴링 미발사" → "블러 통지 폴링만 없음"으로 개정 |
| 2026-08-26 | MSG-476 | 교훈: 모듈 삭제 티켓은 대상이 **겸하고 있던 부수 책임**을 먼저 분리해 목록화한다 | 이번 워처는 이름·문구가 전부 "블러"였지만 실제로는 목록 신선도까지 책임지고 있었다. 파일 단위 의존 조회(누가 import 하는가)로는 안 잡히고, 삭제 대상 안의 콜백이 무엇을 하는지 읽어야 잡힌다 |
| 2026-08-26 | MSG-476 | 결정: 업로드 무효화 집합을 `invalidateUploadSurfaces` 하나로 모아 **확정·교체·READY 세 지점이 같은 집합**을 쓴다 | 1차 보수 후에도 도감 갤러리가 "처리 중"에 머물렀다 — READY 폴링이 `invalidateGridQueries`만 불렀고 도감은 `getRegionVideos`라 그 집합 밖이었다(확정 시점도 마찬가지였고 교체만 하고 있었다). READY 재무효화는 정의상 확정 시점 무효화의 재실행이므로 집합이 갈라지면 한쪽 화면이 영구 스테일이 된다 |
| 2026-08-26 | MSG-476 | 결정: READY 폴링의 세션 판정을 "로그아웃 이벤트 구독"이 아니라 **시작 시점에 잡은 액세스 토큰 비교**로 | codex 2차 리뷰 — 확정 요청 도중 로그아웃이 끝나면 `onSuccess`가 전이 이후에 도착해 구독이 그 전이를 못 본다. 토큰 기준이면 시작 자체를 막고(토큰 null) 계정 전환(토큰 변경)도 같은 규칙 하나로 덮는다. 분리된 타이머는 화면 언마운트를 넘어 사는 것이 목적이므로 컴포넌트 cleanup에 묶을 수 없다 |
| 2026-08-26 | MSG-476 | 결정: READY 폴링 간격을 고정 30초 → `5·5·10·20 → 30초 상한` 백오프로 | 업계 관행 대조 — 영상 처리 통지의 정석은 웹훅+푸시고 폴링은 폴백이다(Mux는 폴링 금지 + `/assets` 레이트 리밋). 우리는 자기 BE라 레이트 리밋 논거는 없고 남는 건 체감 지연뿐이라, 폴백답게 초반만 촘촘히 덮는다 — 첫 100초 6회(고정 30초는 4회), 최악 대기 30초 → 5초대. TanStack `refetchInterval` 관용구는 상주 컴포넌트를 되살려야 해 기각. **후속: FCM 푸시 승격(MSG-408·429 파이프라인 재사용) 시 지연 0·폴링 0** |
| 2026-08-26 | MSG-476 | 정정: READY 폴링의 세션 판정을 액세스 토큰 동등 비교 → `isAuthenticated` 전이로 되돌림 | codex 3차 리뷰 — 401 재발급(`setAccessToken`)이 **같은 세션에서** 토큰을 갈아끼우므로 토큰 동등성으로 판정하면 정상 회전을 계정 변경으로 오인해 인코딩 중인 폴링을 죽인다. 2차 리뷰가 지적한 레이스는 판정 기준이 아니라 **시작 가드**(시작 시점 비로그인이면 미시작)로 막는 것이 옳았다 — 두 겹으로 분리 |
| 2026-08-26 | MSG-476 | 결정: 새로고침 시 READY 폴링 재개 장치를 두지 않는다(한계로 수용) | codex 4차 리뷰 — 타이머가 JS 컨텍스트 수명에 묶여 재진입을 못 잇는다(구 `pendingVideoStorage`가 덮던 시나리오). 되살리면 지우기로 한 영속화가 부분 부활하고 부팅 훅·고아 키 정리 책임이 다시 생긴다. 완화: 새로고침은 캐시를 비워 즉시 신선 조회를 부르고 탭 복귀 시 `refetchOnWindowFocus`(기본 on)가 덮는다 — 남는 틈은 '인코딩 중 새로고침 후 같은 페이지에 계속 머무는 동안'뿐. 근본 해소는 FCM 푸시 승격(후속 티켓) |
| 2026-08-26 | MSG-478 | 발견: vitest(jsdom)에서 `new URL("../x", import.meta.url)` **리터럴** 형태는 Vite 에셋 플러그인이 변환해 `http://localhost:3000/x`가 되고 `node:fs`가 "URL must be of scheme file"로 실패한다(변수 인자면 변환되지 않아 같은 파일에서 한쪽만 통과하는 착시). 정적 파일을 읽는 테스트는 `fileURLToPath(import.meta.url)` + `path.resolve`로 푼다 | seo-static.test A6·document-title.test C1이 RED→GREEN 사이에서 잡음. fs 기반 자산 단정 테스트가 처음이라 선례가 없었다 — 이후 index.html·public/ 대조 테스트는 이 경로를 따른다 |
| 2026-08-26 | MSG-478 | 결정: `/upload` 탭 제목은 `SectionPanel`이 자기 `title` prop으로 `useDocumentTitle(formatDocumentTitle(title))`을 호출해 세운다(router.tsx 무수정) — 대안(router.tsx에 래퍼 엘리먼트 추가·`MapShell`에서 pathname 매핑)은 기각 | 패널 제목=탭 제목이라 정보원이 하나로 닫히고 라우트 등록이 불변(스펙 "라우트 변경 없음"). 셸 pathname 매핑(B안)은 errorElement·콜백 화면이 셸 밖이라 두 화면을 못 덮고, 부모 effect가 자식보다 늦게 돌아 도감 제목을 덮어쓴다 |
| 2026-08-26 | MSG-478 | 검증이 잡음: `check:duplication`(nose) exit 1 — 실 신규 중복은 테스트 헬퍼 `seedRobotsMeta` 1건(→ `src/test/robots-meta.ts` 추출)이고 나머지 8건은 **승인 중복의 라인 시프트**(페이지 파일에 `useDocumentTitle` 1줄, 스모크에 케이스 1개만 넣어도 위치가 밀려 new/changed로 잡힌다. develop 스냅샷 0 families 실측). 등재 파일을 건드리는 티켓은 `pnpm check:duplication --write-baseline` 재등재가 따라와야 하고, 재등재 전에 실 신규가 섞였는지 가족별로 가른다(이번엔 1/9) |
| 2026-08-26 | MSG-475 | 정정: 티켓 [참고]의 "로컬 명세 스냅샷에 hotzones/aggregation 없음 → `api:generate` 1회 필요"가 스테일 — `getHotZoneAggregates`·`HotZoneRegionAggregateResponseDto`가 MSG-445 커밋(`4639232`) 스냅샷에 이미 있었다. 재생성 없이 구현 | 착수 전 생성물 실물 grep이 계획 단계를 통째로 지웠다. 재생성했다면 이 티켓과 무관한 명세 drift가 생성물 4파일에 실려 리뷰가 흐려지고, 같은 파일을 건드리는 병렬 MSG-474와 머지 충돌 창이 열렸을 것 — **티켓의 API 현황 기술은 작성 시점 스냅샷이므로 착수 시 실물로 재확인한다** |
| 2026-08-26 | MSG-475 | 결정: 클러스터 마커 채움을 `bg-primary`(#0066CC) → `bg-secondary`(#0071E3)로 이동, 단위별 크기 3단(size-17/20/23)과 흰 링(`border-3 border-background`) 폐기 | Figma `15221:20097` 실측 `#0071E3`이 `bg-secondary`와 정확히 일치한다 — 브랜드 일관성(파랑 계열 유지)보다 디자인 정본 일치를 택했다. 크기 3단은 노드에 variant 자체가 없고 말풍선은 내용 hug라 폭만 지명 길이로 갈린다(높이 동일) — 원형 마커 시절의 "단위가 클수록 큰 원" 개념이 사라진 것이 이 리디자인의 실질 |
| 2026-08-26 | MSG-475 | 결정: 핫구역 `gridIds` 교집합 좁힘 미구현(티켓 명시 항목의 승인된 축소) | 시/구 마커 클릭은 4km/500m 단(여전히 집계 구간)에 내려 개별 조회가 없고, DONG 클릭만 격자 줌에 닿는데 그 화면은 이미 **확정 행정동**으로 걸러진다. 서버가 집계·개별의 판정 집합 동일을 보장하므로 교집합의 실효는 미판정(null) 버킷뿐인데, 구현하면 포커스 스토어와 "언제 풀리는가"(칩 전환·다음 클릭·확정 지역 변경) 규칙이 따라붙는다. MSG-451이 `missionIds`를 같은 이유로 미룬 선례 |
| 2026-08-26 | MSG-475 | 결정: `MARKER_MERGE_PX`(68/80/92)를 말풍선 전환 후에도 값 불변으로 두고 주석에만 근거를 남긴다 | 이 상수는 원 지름에서 온 값인데 마커가 말풍선(약 61×49, 폭 가변)이 되며 근거가 어긋났다. 그런데 모바일 parity 테스트가 리터럴로 고정하고 RN 렌더(`cluster-marker.tsx`)가 이 값을 **원 지름으로 소비**한다 — 웹만 재보정하면 parity가 깨지고 모바일 마커 크기가 바뀐다. 재보정은 RN 렌더 교체 티켓과 한 묶음 |
| 2026-08-26 | MSG-475 | 결정: 집계 훅 3형제(도감·미션·핫) 공통 헬퍼 추출 기각 → nose baseline 등재. 등재 시 무관 흡수 감사 필수 | 세 훅은 생성 옵션 함수·쿼리 파라미터 형태·게이트 조건·placeholder 비교·마커 팩토리가 9개 지점에서 갈린다(nose 자신의 파라미터 판정 — MSG-476과 같은 근거). `--write-baseline`이 현재 전 패밀리를 무차별로 승인하므로 **등재 후 baseline diff를 파일집합 기준으로 감사해 이번 티켓 밖 패밀리를 되돌리는 절차가 필수**다(MSG-478 머지본 리베이스 후 최종 실측 421 → 429: 신규 9 + 재키잉 8, 무관 흡수 0건. 리베이스 전 1차 등재에서는 develop에도 있던 hidden 패밀리 1건(`profile-edit-modal.smoke` ↔ `envelope-response`)이 흡수돼 손으로 되돌렸다 — 재등재 때는 그 패밀리가 이미 develop baseline에 들어와 재발하지 않았다) |
| 2026-08-26 | MSG-474 | 결정: 비로그인 401 스팸 방지 게이트를 스펙 인벤토리 밖 2훅(`use-grid-names-query`·dex `use-region-videos-query`)에도 추가 | AC 14(비로그인 전 과정 401 0건·reissue 0건)가 최종 방어선인데, 코드 실측에서 두 경로가 인벤토리에 빠져 있었다 — 코스 상세의 스팟 이름(`grids/{gridId}` N회)과 핫구역 요약의 내 수집 영상(`collections/videos`, 확정 지역이 비로그인에도 생기면서 처음 활성화)이 익명 401을 쏜다. 호출부 게이트 대신 훅 내부 `useAuthStore` 게이트(use-collected-grids-query 선례)로 모든 소비처를 한 번에 방어 |
| 2026-08-26 | MSG-474 | 결정: `deriveHomeCellDetail` 입력을 `viewerAuthenticated` 판별식 유니언(로그인=cell 필수 / 비로그인=entryNaming 필수)으로 설계 | 대안 ①cell·entryNaming 둘 다 옵션 + null 반환: 호출부마다 null 방어가 늘고 "둘 다 없음"이 타입으로 안 막힘 ②naming 필드를 별도 필수 입력: 로그인 호출부가 cell을 두 번 넘기는 중복. 판별식 유니언은 비로그인에서 사용자별 값(subtitle·occupied 배지) 접근 자체가 타입으로 차단돼 AC 4를 컴파일 타임에 강제 |
| 2026-08-26 | MSG-474 | 결정: 비로그인 핫구역 요약의 "내 영상 N개"를 progressLocked prop으로 미표시 (스펙 인벤토리 밖 뷰 1줄) | 기획 요약의 "사용자별 값 미발사·미표시" 규칙 — `collections/videos`를 게이트하면 myVideoCount가 항상 0인데 이를 "내 영상 0개"로 그리면 미발사 값을 실측처럼 위장한다. MSG-463 progressLocked(코스 카드 진행 바 숨김)와 같은 결 |
| 2026-08-28 | MSG-488 | 결정: `kindTag`가 tailwind 클래스가 아니라 **tone(의미)** 4종(`place`·`festival`·`popup`·`route`)을 반환하고, 토큰 클래스 매핑은 뷰(`RouteStopCard.TONE_CLASS`)가 소유 | 대안 ①모델이 클래스 문자열 반환(`theme-classes.ts` 선례): 짧지만 model 레이어가 웹 스타일 체계를 알게 돼 RN 재사용 시 그대로 못 옮긴다 ②태그 전부를 뷰로: L2가 모델 유닛으로 매핑을 못박게 요구한다. tone 유니언은 라벨/색을 한 소스에서 갈라두면서 경계를 지킨다 — 색 4종 중 2종(EVENT·MISSION_FESTIVAL)이 같은 tone을 공유하는 승인 Q3도 한 곳에서 표현된다 |
| 2026-08-28 | MSG-488 | 결정: 로딩·결과 공통 각주("축제, 팝업, 코스, 행사 정보와 장소 검색 결과로만 추천해요")를 **결과 상태에도** 렌더 (스펙 S4는 로딩만 명시) | Figma 정본 15666:12855·13416 두 프레임 모두 리스트 아래에 같은 각주를 그렸다. 스펙 S5가 각주를 언급하지 않은 것은 누락으로 판단 — 디자인 정본을 따르고 빌드 리포트에 "스펙과 달라진 점"으로 기록했다. 반대로 스펙만 따르면 결과 화면이 Figma와 어긋나 검증에서 결함으로 잡힌다 |
| 2026-08-28 | MSG-488 | 결정: `nose.baseline.json` 전량 재등재(437 패밀리, +30/−26). 감사 결과 **member 파일이 전부 이 티켓 변경 집합 밖인 추가 패밀리 9건** 중 3건(`1fff7443bd`·`6ead82d6f3` new, `d5c756408e` changed)은 착수 전 HEAD(107aa59, MSG-488 웨이브 0 openapi 스냅샷)에서 이미 off-baseline이었고, 나머지 6건은 내 편집이 이웃 패밀리를 재분할하며 생긴 재키잉이다 | MSG-475가 세운 "등재 후 무관 흡수 감사" 규칙 적용. 9건을 손으로 되돌리는 대안은 기각 — 재키잉 6건은 되돌리면 파티션이 깨져 baseline이 코드와 불일치하고, 나머지 3건의 드리프트 근원이 이 티켓 자신의 웨이브 0 커밋이라 in-scope다. 되돌림 대신 id 단위 실측을 리포트·이 로그에 남겨 추적 가능하게 한다 |
| 2026-08-28 | MSG-489 | 결정: 제출·출발지 판정·자동 이동·2차 재요청을 **한 훅**(`pages/ai-route/ui/use-ai-route-auto-move.ts`)에 모으고, 스펙 §4-1이 정한 `AiRoutePage.tsx` "2줄" 상한을 -w 기준 +14/−12로 넘겼다 | 대안 ①스펙 §4-2 문면대로 `submit()`을 페이지에 남기기: 0.5도 정규화 대기·현위치 1회 측위·출발지 재판정이 전부 페이지로 올라와 오히려 diff가 커지고 자동 이동 훅과 재료(coords·bounds·requestedAt)를 이중으로 들게 된다 ②훅을 둘로 쪼개기: 같은 재료를 두 훅이 각각 구독해 측위가 2회 나간다. 실제 증가분 10줄 중 3줄은 `<RouteToastHost />`+fragment, 나머지는 D9 버튼 문구·S1 표시 행 배선이라 티켓 요구로 전부 소급된다 |
| 2026-08-28 | MSG-489 | 결정: 2차 재요청 발사 조건을 "`viewport-store.bounds` **참조 변경** + `requestedAt + 10.5s` 경과" 두 조건 AND로 두고, 예약 상태(`secondaryPending`)를 스토어에 둔다 | 대안 ①이동 명령 직후 즉시 mutate: 옛 bounds로 쏜다(스펙 R3) ②목표 중심이 bounds 안인지로만 판정: `kind=ZOOM_OUT`은 중심이 이미 옛 뷰포트 안이라 즉시 통과해 R3를 못 막는다 ③예약을 훅 로컬 ref로: 대기 중 섹션 이탈 시 예약이 소실돼 패널이 영구 로딩으로 남는다. 스토어에 두면 재진입이 예약을 이어받고, `onMutate`가 끄므로 중복 발사도 같은 플래그로 막힌다 |
| 2026-08-28 | MSG-489 | 결정: 토스트 제목의 조사를 리터럴 "(으)로"가 아니라 **받침 판정 순수 함수**로 확정한다("부산 서면으로 이동했어요"). 스펙 L10이 인용한 문자열은 기획 표기 관례로 판단 | 대안 ①L10 문자열 그대로 렌더: 사용자에게 "(으)로"가 그대로 보인다 ②서버가 조사를 실어 주기: 계약 변경이 필요하고 이 티켓은 "서버 API는 그대로"가 전제. ㄹ 받침이 "로"를 쓰는 예외까지 3케이스로 고정했다(오케스트레이터 지시) |
| 2026-08-28 | MSG-489 | 결정: 결과 상태(`status === "result"`)에서는 "현재 위치에서 출발" 표시 행을 숨기고 버튼 문구로만 출발지를 알린다 | Figma 15666:13139(origin 결과)에 그 행이 없고 스펙 §8 오탐 방지 5가 "의도된 동작"으로 못박았다. 라이브 판정(`현위치 ∈ 현재 bounds`)만 쓰면 결과 화면에서도 계속 떠 Figma와 어긋난다 |
| 2026-08-29 | MSG-489 | 결정(§11 후속): 제출 시 **항상 1km 축척으로 정규화**하고(현재 줌이 `MAP_SCALE_1KM_ZOOM`이 아니면 줌만 먼저 내고 대기), 0.5도 상한 판정(`MAX_VIEWPORT_SPAN_DEG`·`needsSpanNormalize`)과 그 테스트(L12)를 **폐기**한다 | 사용자 지시("추천할려면 1km로 이동해주고 해당 위치에서만 추천"). 항상 1km면 서버 뷰포트 상한(14401)이 구조적으로 불가능해 0.5도 판정은 죽은 조건이 된다 — 두 규칙을 병존시키면 "언제 정규화되는가"가 두 갈래로 갈려 재현·설명이 어렵다. 이미 1km면 줌 명령을 **내지 않는다**: 목표와 같은 값으로 `zoomTo`하면 지도가 `idle`을 내지 않아 요청이 영영 안 나간다(L21) |
| 2026-08-29 | MSG-489 | 결정(§11 후속): 정규화 대기 중에도 로딩 상태를 켜되(`startNormalize`), rate-limit 기준 시각 `requestedAt`은 **실제 mutate 시점**에만 기록한다 | 대안 ①`startRequest`를 제출 클릭 시점에 호출: 로딩은 켜지지만 정규화 대기(실측 최대 수 초)가 서버 10초 창을 잡아먹어 2차가 조기 발사되고 14429가 난다 ②대기 중 로딩을 켜지 않기: MSG-489 검증이 실측한 14.2초 무피드백이 그대로 남는다. 스토어 액션을 둘로 나눠 "로딩 시작"과 "요청 시각 기록"을 분리했다 |
| 2026-08-29 | MSG-489 | 결정(§11 후속): 2차 발사 조건을 "bounds 참조 변경"에서 "**목표 뷰포트 도달**"(`reachedTargetViewport` — 참조 변경 AND 줌이 목표 단)로 좁히고, 지도는 잠그지 않는다 | 진행 중 사용자가 지도를 만지면 옛 조건은 그 조작 뷰포트로 2차를 쏜다. 대안 ①진행 중 지도 잠금(ZoomControl·MapShell 비활성): 사용자가 명시적으로 기각했고 489 소유권 표의 접촉 금지 파일을 열어야 해 MSG-490 병렬성도 깨진다 |
| 2026-08-29 | MSG-489 | 결정(§11 후속): 두 대기(정규화·2차) 모두 `VIEWPORT_SETTLE_TIMEOUT_MS`(3s) 상한을 두고 목표 도달 실패 경로에서도 현재 뷰포트로 1회 발사한다 — 영구 로딩 금지를 테스트로 고정(L22·L24) | 도달 판정을 좁힌 대가로 "사용자가 대기 중 줌을 되돌려 목표를 영영 벗어나는" 경로가 생겼고, 그 경로에서 패널이 로딩에 갇힌다. 지도를 잠그지 않기로 한 이상 조작은 항상 가능하므로 출구는 시간 상한뿐이다. 대안 ①도달할 때까지 무한 대기: 영구 로딩 ②줌이 벗어나면 즉시 실패 처리: 사용자가 방금 본 화면으로 추천을 받는 편이 낫고, 실패 화면은 서버 오류와 구분이 안 된다 |
| 2026-08-29 | MSG-489 | 발견(§12 재작업): §11의 "항상 1km면 14401은 **구조적으로 불가능**" 전제가 검증 실측으로 반증됐다 — 숨은 탭에서는 rAF가 멈춰 `idle`이 오지 않아 뷰포트가 옛 값에 머무는데, D13이 신설한 3s 상한 출구가 그 값(1.9379° × 4.7461°)으로 그대로 발사해 400 `14401`을 맞았다. D10이 지운 상한 가드가 바로 그 출구의 유일한 방어선이었다 | 같은 §11 안에서 D10(정규화 트리거 일원화)과 D13(종결 보장 출구 신설)이 서로를 무효화했다. 교훈: **정규화로 값을 보장한다**는 논증은 그 정규화를 건너뛰는 탈출 경로가 생기는 순간 깨진다 — 값 보장은 정규화가 아니라 **발사 직전 최종 가드**에 둬야 한다 |
| 2026-08-29 | MSG-489 | 결정(§12): 서버 뷰포트 상한 판정을 `exceedsViewportSpan({ viewport, maxSpanDeg })`으로 되살리되 **정규화 트리거가 아니라 `send()` 한 곳의 최종 가드**로 두고, 걸리면 보내지 않고 `abortPending`으로 에러 안내 종결한다(영구 로딩·확정 400 동시 금지) | `send()`가 이 훅의 유일한 발사 지점이라 네 경로(즉시·정규화 대기·상한 만료·2차)가 전부 통과한다. 대안 ①목표 줌 기준으로 중심에서 span 파생(검증 1안): span이 컨테이너 픽셀·위도에 의존해 지도 밖에서 재현 불가하고 지도가 실제로 그 범위에 있지도 않다 ②bounds를 0.5도로 잘라 발송: 요청은 통과하나 D10("항상 1km")을 깨고 사용자가 보지 않은 55km 범위를 추천한다 ③두 `fire()`에 각각 검사: 경로가 늘 때마다 빠뜨릴 수 있어 이번 결함 구조가 그대로 재발한다 |
| 2026-08-29 | MSG-489 | 결정(§12): 숨은 탭(`document.visibilityState !== "visible"`)에서는 정착 대기 상한 타이머를 **걸지 않고** 복귀 시 다시 3초를 잰다. 가시성 구독은 뷰-레이어 훅 지역 `useDocumentVisible`에만 두고 순수 모델은 모른다 | 숨은 탭은 rAF가 멈춰 지도가 정착할 수 없으므로 대기 자체가 무의미하고, 상한만 소모돼 옛 뷰포트 발사를 유발한다. 이미 목표에 도달한 뒤의 2차 대기(서버 10초 창)는 지도와 무관하므로 숨은 채로도 계속 잰다. 대안 ①`shared/` 가시성 어댑터 신설: 사용처 1곳뿐이라 성급한 공통화(두 번째 사용처에서 승격) ②순수 모델에서 `document` 참조: RN 경계·oxlint 위반 |
| 2026-08-29 | MSG-489 | 결정(§12): `VIEWPORT_SETTLE_TIMEOUT_MS`를 3,000ms로 **유지**한다 — 검증 실측 줌 애니메이션은 16→13 269ms · 10→13 296ms · 10→16 729ms이고, §11이 근거로 삼았던 "정규화 대기 14.2초"는 백그라운드 탭 rAF 정지 아티팩트였다(같은 전이가 숨은 탭 11,162ms / 전면 탭 316ms) | 낮추면(예 1,200ms) 정착 실패 경로가 넓어져 결함 창이 커지고, 높이면 정착 실패 시 사용자 대기가 길어진다. 부수 교훈: **비활성 탭에서 잰 지도 타이밍은 전부 무효** — 계측 전 `visibilityState`와 rAF fps를 먼저 확인한다 |
| 2026-08-29 | MSG-489 | 결정(§12, 사용자 지시): `/ai-route`에서 다른 섹션의 저줌 집계 클러스터 마커 3종을 숨긴다 — `MapShell`이 파생한 `clusters`에 `gateRouteClusters(clusters, pathname)`를 적용(파생 훅은 무수정). MSG-488이 완결 선언한 `MapShell.tsx`를 여는 **소유권 예외**를 이 한 게이트에 한정해 기록한다 | 집계 마커는 줌 16(100m) 미만에서만 존재하는 층이라 경로 게이트 하나로 사용자 지시("100m보다 커지면 안 보이게")가 전부 충족된다 — 줌 분기 추가는 불필요. 대안 ①집계 훅 3종에 라우트 조건 추가: `features/map-home`이 라우트를 알게 되고 다른 섹션 회귀 위험 ②`MapCanvas`에서 숨김: 접촉 금지 파일이고 지도 경계에 라우트 지식이 들어간다. MSG-490도 이 파일 0줄이라 병렬 머지 충돌 없음 |
| 2026-08-29 | MSG-489 | 결정: 14429(요청 과다) 안내를 서버와 같은 문장 `"요청이 너무 잦습니다. 잠시 후 다시 시도해주세요"`로 교체한다(FE 고정 문구 상수 — 서버 응답 문자열을 렌더하는 것이 아니다) | 종전 `"잠시 후 다시 시도해 주세요"`는 사유를 알려주지 않아, 재시도를 연속으로 누른 사용자가 같은 안내만 반복해 본다(사용자 실사용 보고). 대안 ①서버 `message` 패스스루: MSG-488 §1-5의 FE 고정 문구 정책이 깨지고 임의 서버 문자열이 화면에 그대로 나간다 ②클라이언트 쿨다운 타이머로 버튼을 잠금: MSG-488 Q7에서 이미 기각한 범위 |
| 2026-08-29 | MSG-489 | 결정(§13, codex 리뷰 P2): 정착 대기 상한을 "이펙트 재실행마다 다시 거는 타이머"에서 **대기 사이클당 절대 마감 1회**(`advanceSettleDeadline` 순수 함수 + 뷰-레이어 훅의 ref)로 바꾼다 — 재실행은 남은 시간만 스케줄하고, 마감이 지났으면 타이머 없이 즉시 종결한다. 마감은 가시 구간만 소모하고 숨은 구간만큼 뒤로 밀린다 | 종전 구조는 `bounds`·`zoom` 갱신마다 상한을 처음부터 다시 재서, 사용자가 계속 패닝하면 2차·정규화가 무한히 연기되고 패널이 로딩에 갇혔다(§11 D13 "영구 로딩 금지" 위반 — 패닝 반복 테스트가 `Infinity`로 재현). 대안 ①이펙트 deps에서 뷰포트 제거: 도달 판정이 갱신을 못 봐 옛 뷰포트로 발사(L18 파기) ②최초 예약 타이머를 재실행에서 안 건드림: 클로저가 옛 `send`를 잡아 예약 시점 뷰포트로 쏜다 ③복귀 시 상한 전체를 다시 재기: 숨김·복귀 반복으로 같은 무한 연기가 재현된다 |
| 2026-08-29 | MSG-489 | 결정(§13, codex 리뷰 P1): 2차 자동 재요청 인스턴스에도 1차와 같은 `onLoginRequired`를 넘긴다. `docs/spec/MSG-489.md` §10·`docs/STATUS.md`에 "알려진 한계"로 적혀 있던 서술을 결함으로 정정하고 삭제했다 | 지연된 2차(서버 10초 창) 구간에 세션이 만료되면 401에서 패널만 입력 대기로 돌아가고 로그인 모달이 뜨지 않아, 사용자는 결과가 사라진 이유를 알 수 없다. `secondary` 플래그는 요청 **시작** 처리만 가르고 실패 처리는 공통이므로 인증 동작이 요청 순서에 따라 갈릴 이유가 없다 |
| 2026-08-28 | MSG-490 | 결정: walk-paths를 mutation이 아니라 `useQuery`로 부르고 **세그먼트 좌표 자체를 queryKey**로 삼는다 | 서버 응답에 결과 id가 없어(RouteRecommendResponseDto는 points·notice·mentionedArea뿐) 경합 차단의 식별자를 만들 수단이 좌표뿐이다. 새 추천 결과는 좌표가 달라 다른 키를 만들고 늦게 온 이전 응답은 그 키 캐시에만 앉는다 — 스토어에 결과 id 필드를 넣는 대안은 489 소유 파일(ai-route-store)을 열어야 해 병렬 웨이브를 깬다 |
| 2026-08-28 | MSG-490 | 결정: 응답 개수 검증·originOffset 정렬을 `alignWalkSegments` 한 함수로 뽑아 `buildRouteLegs`·`buildAiRouteOverlay`가 공유 | 거리(커넥터)와 폴리라인(지도)이 **같은 인덱스 대응**을 써야 하는데 방어를 각자 구현하면 한쪽만 어긋났을 때 "거리는 실거리인데 선은 직선"처럼 화면이 거짓말한다. 사용처가 둘이라 성급한 추상화도 아니다 |
| 2026-08-28 | MSG-490 | 결정: walk-paths 호출을 생성 mutation 팩토리(`walkPathsMutation().mutationFn`) 대신 생성 SDK `walkPaths`로 직접 | 팩토리의 mutationFn 타입이 `(variables, mutationContext)` 2인자라 useQuery의 queryFn 컨텍스트로는 채울 수 없다(타입 오류). SDK 직접 호출은 같은 생성물을 쓰면서 AbortSignal 전달까지 자연스럽다 — use-route-recommend의 관례(mutation)는 mutation일 때만 성립한다 |
| 2026-08-31 | MSG-516 | 결정: 행사방·테마 칩의 상호 배타(추정 6)를 스토어 교차 호출이 아니라 **뷰 레이어 배선**(ThemeChipsBar 칩 클릭 → room close, EventCapsule 세그먼트 클릭 → 테마 해제)으로 구현하고, panel-branch에는 잔존 상태용 우선순위(event-room이 테마 화면을 이긴다)만 둔다 | theme-filter-store(features/map-home)가 features/event 스토어를 import하면 model 레이어 교차 feature 의존이 처음 생긴다(기존 교차 호출은 전부 같은 feature 안 — theme-filter→cell-detail·mission-selection). 배선 지점은 칩 활성화 진입점이 ThemeChipsBar 하나뿐이라 뷰 배선으로도 누락 경로가 없고, 스모크가 계약을 고정한다 |
| 2026-08-31 | MSG-516 | 결정: 캡슐 펼침 모션을 width 애니메이션 대신 **grid-template-columns 0fr↔1fr 트랜지션**으로 구현 | 세그먼트 폭이 실데이터(제목 길이)에 따라 가변이라 width/max-width 고정값 트랜지션은 성립하지 않는다. 0fr→1fr은 콘텐츠 폭을 몰라도 부드럽게 확장되고 모든 모던 브라우저가 지원 — height-auto 아코디언의 표준 기법을 가로로 적용 |
| 2026-08-31 | MSG-516 | 발견: 시도명 축약 로직이 region-cluster-overlay(비공개 abbreviateSidoName)와 event-chip(cityLabel)에 사실상 중복 — 규칙은 동일(접미 제거+도명 관용 축약)하나 전자는 미export 내부 함수라 재사용하려면 map-home 내부를 열어야 해 신규 작성 | features/event → features/map-home model import는 교차 feature 결합. 세 번째 사용처가 생기면 entities/region 승격이 맞다(두 번째 사용처 규칙의 경계 사례로 기록) |
| 2026-08-31 | MSG-516 | 검증 결정: `check:duplication` 신규 3패밀리를 베이스라인 등재(`--write-baseline`)로 수용 — event-chip KST 헬퍼↔mobile upload-grass(의도 중복, 윗행 발견 기록), web↔mobile panel-branch 테마 switch(기존 이식 중복이 이번 수정의 라인 이동으로 재검출), 페이지 스모크 내부 중복(둘 다 MSG-403 기존 케이스 — 이번 티켓 신규 아님) | 셋 다 추출 대안이 교차 feature/플랫폼 결합을 만들거나(전 2건) 기존 테스트 리팩터링(후 1건)이라 이번 티켓 범위 밖 — 의도 중복은 베이스라인이 정본 기록 위치(검증 스킬 게이트 5 처방) |
| 2026-08-31 | MSG-516 | 결정(codex 리뷰 P2): 행사 조회를 `mapQueryPolicy`(keepPreviousData)에서 `entityQueryPolicy`로 교체하고, D-day "오늘"을 렌더 시점 고정값에서 `useKstToday`(자정 재예약 체인 훅)로 교체 | keepPreviousData는 캡슐 지역명(역지오코딩 독립 갱신)과 행사 목록의 짝을 어긋나게 한다 — 시 경계 이동 중 새 지역명 옆에 이전 시 행사가 남고 뷰포트 밖 행사방을 열 수 있다(격자 상세 entityQueryPolicy와 같은 부류: 독립 도착 데이터의 짝 어긋남). 대안 ①지역명·행사 합동 정착 바인딩: 잔여 창이 네트워크 시차(초 미만)뿐이라 과설계 ②cityName 필터: 서버 묶음 기준 미규정 상태에서 승인 스펙(뷰포트 전건 렌더)을 임의 변경. D-day는 memo가 `[chips]`만 의존해 자정 넘김 화면에서 전날 값에 머물렀다 — state 미변경 시에도 끊기지 않는 setTimeout 재예약 체인으로 |
| 2026-08-31 | MSG-516 | 결정(PR #108 리뷰): 캡슐 펼침 상태의 지역 간 잔존을 "시 라벨 변경 시에만 collapse"로 해소 — 리뷰어 제안(표시 전환마다 접기)은 수정 채택 | 캡슐은 항상 렌더(미표시 = null 렌더)라 expanded가 스토어에 남아 다른 시에서 펼쳐진 채 재등장한다(AC 3 위반, 스모크 RED 재현). 그러나 제안대로 표시 전환마다 접으면, keepPreviousData 제거(codex P2 반영) 이후 같은 시 안 팬 한 번에도 pending 깜빡임으로 캡슐이 사라졌다 재등장해 **지도를 조금만 움직여도 펼침이 접힌다** — 두 리뷰 반영이 상호작용하는 지점. 시 라벨 비교(비-null→비-null 전환만)는 깜빡임·null 경유를 같은 시로 취급해 둘 다 만족. collapse의 행사방 close 체인은 유지(접힘+방 잔존 유령 패널 금지 불변식) |
| 2026-08-31 | MSG-516 | 결정(PR #108 리뷰 2차): 시 경계 전환의 캡슐 접힘을 `collapse`(방 close 체인)에서 신설 `collapseOnly`(방 유지)로 교체 — 행사방 닫힘 체인은 사용자 명시 액션(✕)에만 건다 | 1차 반영이 시 경계 팬에서 읽고 있던 행사방을 조용히 닫았다(사용자 액션 없는 파괴적 전이 — 리뷰 지적). 패널을 지도 이동으로 닫지 않는 것이 코드베이스 관례(격자·미션·코스 상세 동일)고, AC 10의 방 닫힘 규정도 ✕ 명시 클릭에만 있다. 1차의 "접힘+방 잔존 = 유령 패널" 논거는 ✕에 한정해 정정 — 방은 자체 뒤로가기를 갖는 자급 패널이라 고아가 아니다. 대안 ①체인 유지+스펙 명시: 명시 액션 없는 패널 닫힘이라는 관례 위반을 문서로 정당화하는 꼴 ②방의 시 소속 판정 후 선택 닫기: 칩 cityName 정책 미규정 상태라 근거 없음 |
| 2026-08-31 | MSG-519 | 결정: 아카이브 "1개월 유지" 판정 주체 = 서버 status 필드(`UPLOAD_GRACE`/`ARCHIVED`) — FE 날짜 계산 0줄. 명명 함정 확정: 티켓의 "아카이브" 구간은 서버 UPLOAD_GRACE(종료~+30일)이고 서버 ARCHIVED는 "1개월 지난" 상태 | 서버가 종료+30일 경계를 이미 계산해 내려주고(uploadClosesAt·interactionLocked 명세 실측) 뷰포트 칩 목록은 종료 행사를 이미 거른다(2값). FE endsAt 재계산은 클라이언트 시계·KST 자정 함정(MSG-516 use-kst-today가 겪은 문제) 재발. ARCHIVED 상세 도달(연례 작년 회차 previousOccurrences)도 그대로 아카이브 표시 — 서버의 "영구 조회, 입력만 잠금" 설계와 정합(대안 B "보관 끝" 차단 안내 기각) |
| 2026-08-31 | MSG-519 | 결정: EventRoomPanel 접촉을 승인 2건(상세 status 배선+archive 헤더) 외 상세 실패 RetryNotice 1건 확장 | 상세 실패 시 칩 폴백(2값) 렌더는 종료 행사를 활성으로 위장시키고 재시도 수단이 없어 AC 9 위반. archive 판정 전이라 EventArchiveBody(archive 모드에서만 마운트)로는 구조적으로 처리 불가 — 패널이 유일한 위치. 대안(실패 시 폴백 자리표시)은 AC 9 위반으로 기각. 검증자 감사도 "근거 있는 확장(위반 아님)" 판정 |
| 2026-08-31 | MSG-519 | 결정: 아카이브 쿼리 훅이 생성 DTO 타입을 직접 반환(entities/event 파생 미추가) + 519 자체 파일(`use-event-archive-query`) | MSG-517이 같은 상세 엔티티·훅을 병렬로 만들 공산이 커 entities/event·훅 파일명 수렴 충돌을 늘린다. 생성 타입이 전 필드 required라 당장 안전망 손실 없음, queryKey는 endpoint 관례라 517과 수렴 시 dedupe — 웨이브 2 완주 후 통합 정리(1커밋 거리)에 파생 승격 권장 |
| 2026-08-31 | MSG-519 | 발견(검증): 자동화 탭이 숨겨지면 TanStack Query가 재시도를 일시정지(fetchStatus "paused" 직접 실측)해 로더가 고착 — 숨은 탭은 동작 자체가 달라진다(MSG-489 계열 사례 추가) | 해당 구간 관찰을 폐기하고 가시성 복원 후 재판정(3-A 전제 조건 절차 준수). 계측 신뢰성 문제가 아니라 쿼리 라이브러리의 가시성 연동 동작 — 향후 숨은 탭 검증에서 "로더 고착"은 결함이 아닐 수 있음 |
| 2026-08-31 | MSG-518 | 결정: 뒤로가기 2단(위치 해제→방 닫기, AC 12)을 페이지 핸들러 분기가 아니라 event-room-store의 `back()` 액션으로 소유 — 뷰(use-home-close-handlers)는 미니 패널 우선순위만 씌운다 | 스펙은 "페이지 레벨 핸들러에서 위치 유무로 분기"를 제시했으나, 스토어 액션이면 ② 스토어 템플릿으로 값싸게 test-first 되고(getState 분기 훅은 renderHook 필요) RN에서도 그대로 재사용된다. 셸 컴포넌트 개조 아님이라는 스펙 의도는 동일하게 충족. 부수 규칙: 같은 행사 재open(활성 세그먼트 재클릭)은 보던 위치를 유지 — AC 1이 "**다른 행사** open()"만 리셋으로 규정 |
| 2026-08-31 | MSG-518 | 결정: 위치 영상 "더 보기" 이어받기 실패를 별도 표시하지 않음 — hasNext가 남아 버튼이 유지되므로 재클릭이 곧 재시도(explore-regions의 loadMoreFailed 노출 대안 기각) | AC 8(첫 페이지 실패 RetryNotice)·AC 10(더 보기 이어붙임)만이 수용 기준 — 이어받기 실패 전용 UI는 스펙에 없는 동작(Simplicity First). 받은 목록은 유지되고 회복 수단(재클릭)이 존재해 결함 아님. 실사용 실측에서 불편이 확인되면 explore 선례로 후속 추가 |
| 2026-08-31 | MSG-518 | 결정(검증 재작업, 사용자 승인 옵션 A): 홈 이탈 언마운트 클린업을 `collapse`(행사방 close 체인) → `collapseOnly`(캡슐만 접힘)로 교체 — MSG-516 문서화 결정("복귀 화면은 접힌 캡슐+기본 패널") 중 **행사방 리셋 부분 번복** | 스펙 AC 11("섹션 이동 후 홈 복귀 시 행사방·선택 위치 세션 유지")이 사용자 승인 정본 — MSG-516 체인과 정면 충돌(검증 브라우저 실측·스택 트레이스 확정). 섹션 이탈은 ✕·뒤로가기 같은 명시 닫기가 아닌 비명시 문맥 전환이라 시 경계 전환(PR #108 2차 반영)과 같은 부류로 collapseOnly가 정합. 새 동작은 use-home-entry-lifecycle.test.tsx 회귀 테스트(RED 재현 후 GREEN)로 고정. 기각 대안 — 옵션 B(AC 11 WAIVED): 승인 스펙을 코드 기결정에 맞춰 깎는 역전 |
| 2026-08-31 | MSG-517 | 결정: AC 6 "event-tint 채색"을 렌더 결과 기준으로 해석 — 오버레이 셀 color에 event-tint hex(#E8F5FF) 대신 `semantic.primary`를 전달 | MapCanvas가 색에 자체 fillOpacity(0.4)를 곱한다: event-tint hex를 넣으면 백색에 수렴해 보이지 않고, primary를 넣으면 Figma 정본(15518:5963 — #06c 저투명 틴트)과 일치한다. 대안 ①MapCanvas에 불투명 채움 모드 신설: 다른 층까지 만지는 범위 확대 ②occupied 스타일(18%) 재사용: 상시 점령 셀과 시각 구분이 사라져 클릭 결과(강조 vs 격자 상세)가 갈리는 두 층이 같은 모양이 됨 |
| 2026-08-31 | MSG-517 | 결정: 격자 클릭 강조 색 = 기존 `theme-festival`(#AF52DE) 매핑, 신규 토큰 미등재 | 스펙 리스크 처방(시안 실측 후 토큰 등재 또는 기존 매핑) 중 후자 — get_design_context 실측에서 시안 보라 계열("포켓몬 부산" 태그 #af52de)이 theme-festival 값과 정확히 일치해 신규 토큰이 불필요. 강조 렌더는 기존 emphasized(3px 실선) 재사용 — 시안의 영역 단일 아웃라인과 달리 셀 단위 테두리지만, 영역 경계 폴리곤 합성은 MapCanvas 확장 없이 불가한 과설계 |
| 2026-08-31 | MSG-517 | 결정: 게시자 단일화 — 행사방 열림 중 `use-home-overlay-publish`에 `suspended` 플래그로 게시를 정지시키고 `use-event-overlay-publish`가 단독 게시 | map-overlay-store의 cells/labels/onCellClick을 두 effect가 함께 쓰면 나중에 재실행된 쪽이 상대 게시를 지운다(점령 쿼리 refetch 한 번에 행사 오버레이가 걷히는 간헐 결함). 대안 ①홈 게시 훅에 행사 입력 병합: 홈 훅이 행사 도메인을 알게 되고 diff가 커짐 ②스토어를 게시자별 슬롯으로 확장: 공유 파일(map-overlay-store) 개조 — 병렬 충돌 규칙 위반. suspended는 4줄 diff로 최소 |
| 2026-08-31 | MSG-517 | 결정: 강조 위치의 지도 라벨은 위치명 라벨을 행사명 라벨("● {행사명}")로 교체 — 병행 표기 아님 | 두 라벨 다 대표 격자 앵커라 병행하면 같은 좌표에 겹쳐 선다. LabelOverlay는 per-label 오프셋이 없고(MapCanvas 계약) 오프셋 신설은 공유 렌더 경계 개조. AC 7은 행사명 라벨 표시만 요구하므로 교체로 충족 — "●"는 기존 라벨 마커의 색점(dot)이 그린다 |
| 2026-08-31 | MSG-517 | 재작업 결정: `use-event-detail-query` ↔ `use-event-locations-query` 중복(19/25 공유)을 공용 팩토리 추출 대신 **의도 중복으로 baseline 등재** — 테스트 픽스처·setup 계열 6건도 지역성 의도 중복으로 함께 등재, `kstDateOf` export 승격에 따른 기존 패밀리(event-chip↔mobile upload-grass) 변경분은 MSG-516 기존 판정 유지로 재등재 | ①병렬 규칙 우선(코디네이터 지시): 팩토리를 추출하면 MSG-518이 위치 훅 2종(getEventLocationsByGrid·getLocationVideos)을 얹으며 수정할 공유 지점이 새로 생긴다 — 웨이브 2 병렬 진행 중 공유 파일 신설 금지 ②두 훅은 반환 도메인 타입·빈값 폴백(null vs [])·JSDoc 계약이 달라 제네릭 팩토리는 사용처 2곳짜리 4파라미터 추상화(단순성 우선의 성급한 공통화) ③테스트 픽스처 공유 파일화는 테스트 지역성(파일 자체 완결) 훼손 + 또 다른 병렬 접점. 웨이브 2 머지 후 같은 꼴 훅이 3개 이상 확정되면 추출 재검토(두 번째 사용처 규칙) |
| 2026-08-31 | MSG-521 | 결정: 행사 업로드 의도 영속을 별도 sessionStorage 키 대신 기존 `uploadIntentStorage` 단일 키의 `{ event }` 래핑 형식으로 확장 | 업로드 의도는 한 번에 하나다 — 단일 키면 `save`(일반·지목)와 `saveEvent`(행사)가 서로를 자연 덮어써 의도 상호 배타가 저장 계층에서 공짜로 보장되고, 기존 `clear`·`peek`(플래그 판정)·stale 가드가 무수정으로 행사 의도까지 커버한다. 별도 키였다면 취소·소진·stale 청소 경로 전부에 두 키 동기 삭제를 심어야 해 누락 시 유령 재개가 생긴다. `peekTarget`은 event 형식에서 lat/lng 검사 실패로 null을 돌려줘 구형식과 충돌 없음 |
| 2026-08-31 | MSG-520 | 결정: 행사 상세 쿼리 staleTime은 전역 기본(30초) 유지 + `refetchOnWindowFocus/Reconnect` 차단 — 스펙의 "presigned TTL 유도 staleTime(use-video-playback-query 관례)"은 적용 불가 | `EventVideoDetailResponseDto`에 `expiresInSec`가 없다(실측 — getPlayback과 달리 TTL 미제공)라 유도 자체가 불가능. 상세 재조회는 타인 조회수를 올리므로(스펙 리스크) 자동 재조회 트리거를 전부 끄고, 30초 경과 후 재마운트(패널 재오픈) 재조회만 남긴다 — 재오픈은 새 시청 1회로 의미가 맞다. 대안 staleTime Infinity는 재오픈 시 만료 presigned URL이 캐시에서 재생되는 반대 리스크라 기각 |
| 2026-08-31 | MSG-520 | 결정: 댓글 이어받기는 useInfiniteQuery 대신 로컬 축적(useState + `queryClient.fetchQuery`) — `use-event-comments-pages` | 첫 페이지(20건)가 상세 응답에 내장돼 있는데 useInfiniteQuery는 첫 페이지를 자기 fetch로 소유하려 한다 — 첫 페이지용 getComments 중복 호출이 생기고 상세 내장분과 이원화된다. fetchQuery는 use-grid-mission-routing 선례(클릭 응답 경로 retry:false). 커서·병합 판정은 순수 함수(nextCommentsCursor·mergeCommentPages)로 분리해 RN 재사용 보존 |
| 2026-08-31 | MSG-520 | 결정: 댓글 작성 seed는 스펙대로 상세 캐시 내장 첫 페이지에 append(중복 commentId 방어) — 알려진 경계: 댓글 21건+ 상태에서 "더 보기"를 병행하면 새 댓글이 병합 목록 중간(첫 페이지 끝)에 설 수 있다(내용 유실은 없음, dedup이 이중 표시 방지) | 대안(패널 로컬 appended 배열을 병합 맨 끝에 두기)은 순서를 항상 보장하지만 seed 경로가 캐시·로컬 두 갈래로 갈라져 스펙의 "뮤테이션이 append seed" 계약에서 벗어난다. 실사용(행사 영상 댓글 소수)에서 발생 빈도가 낮아 스펙 리터럴 구현 채택, 경계는 빌드 리포트에 스펙 이슈로 보고 |
| 2026-08-31 | MSG-534 | 결정: 개요 위치 카드의 클릭 배선 재료를 `EventLocationCardView.dto`(원본 DTO 동반, 스펙 구현 3(a))로 전달 — 화면이 `locations.locations`를 locationId로 되짚는 3(b)는 기각 | 뷰 파생 한 곳(`toLocationCardViews`)에서 카드↔DTO 짝을 보장하면 id 대조·미스매치 방어 로직이 화면으로 새지 않는다. 3(b)는 `EventRoomOverview`에 find/index 매칭 1줄이 늘고 그 줄이 실패하는 경로(서버 정렬 변화·중복 id)에 대한 판단을 화면이 떠안는다. dto 동반은 순수 파생이라 RN 재사용 경계도 그대로 |
| 2026-08-31 | MSG-534 | 결정: 아카이브 위치 행 배선을 티켓 [동작 요구] 명시에도 불구하고 MSG-535로 분리(A안, 사용자 승인) — 개요 카드 배선만 수행 | MSG-519 예약 문구 "통합 시점 1~2줄"이 코드와 불일치: `event-room-mode.ts:29`가 위치 선택을 보기 전에 archive를 반환(유령 영상 목록 방지 — MSG-519 AC 1·2 의도 주석)하고, videos/empty 본문의 업로드 CTA 2곳이 아카이브 읽기 전용 계약(AC 7)과 충돌해 배선만으로는 무반응 결함의 아카이브판이 된다. B안(mode 분기+readOnly 관통 40~60줄)은 MSG-519 AC 재해석까지 필요해 버그 티켓 범위 초과. 아카이브는 종료 행사 0건+진입 UI 부재로 현재 도달 불가라 체감 손실 0 |
| 2026-09-01 | MSG-541 | 결정: 콘솔 청크 분리를 route-level `lazy` 16개가 **단일 모듈**(`app/console/console-bundle.ts`)을 dynamic import하는 방식으로 구현 — 경로별 개별 dynamic import 기각 | AC 3은 "콘솔 코드가 유저 번들에 없다"만 요구한다. 경로마다 자기 페이지를 직접 import하면 청크가 16개로 쪼개져 콘솔 진입마다 네트워크 왕복이 늘고(스텁 하나가 청크 하나), 후속 티켓이 라우터를 만지지 않고 페이지 파일만 교체한다는 웨이브 규칙과도 무관하게 청크 경계가 흔들린다. 한 모듈을 공유하면 rolldown이 콘솔 코드를 한 청크(5.68kB 실측)로 묶고 유저 번들에는 경로 문자열만 남는다 |
| 2026-09-01 | MSG-541 | 결정: `RequireOrgRole`의 통과 집합을 ORG **+ ADMIN**으로 확정 — 스펙 표의 "보호(ORG)" 문면보다 질문 8 확정(mustChange 게이트 ORG·ADMIN 공통, 착지 `/org/password/setup`)의 귀결을 우선 | ADMIN을 여기서 막으면 초기 비밀번호 상태의 관리자가 `/admin/*` → 착지 `/org/password/setup` → RequireOrgRole 거부 → `/org/login`으로 튕겨 비밀번호를 바꿀 수단이 사라진다(관리자 계정 잠김). 기각 대안 ①`/org/password/setup`만 ADMIN 예외: 예외가 경로 하나에 숨어 후속 티켓이 라우트를 옮기면 조용히 깨진다 ②ADMIN 전용 setup 경로 신설: 스텁 정본 16경로 변경 = 웨이브 정본 훼손 |
| 2026-09-01 | MSG-541 | 결정: `/admin` 404 위장을 자체 화면 복제 대신 `RouteErrorBoundary`를 그대로 렌더 | "기존 404와 **동일 외형**"(AC 8)이 요구인데 복제하면 두 화면이 갈라져 시간이 지나면 위장이 들킨다. 그 화면은 `useRouteError`를 쓰지 않아(오류 내용 비표시 결정 — MSG-325) errorElement 밖 일반 렌더가 가능하고, noindex·탭 제목·랜드마크를 이미 갖췄다 |
| 2026-09-01 | MSG-541 | 결정: mustChange 게이트를 `boolean` 대신 **이동할 경로 or null**을 반환하는 훅으로 설계(착지 경로 예외를 훅 안에 둠) | boolean이면 리다이렉트 루프 방지(착지 경로에서는 발동 금지)를 호출처 2곳(운영자·관리자 가드)에 복제해야 하고, 한쪽이 빠지면 `/org/password/setup`이 자기 자신으로 무한 리다이렉트한다. 경로를 돌려주면 루프 방지가 단일 지점에 남고 호출처는 `!== null` 한 줄이다 |
| 2026-09-01 | MSG-541 | 결정: 콘솔 가드 2종의 공통 판정 순서(role 대기 자리표시 → 통과 판정 → mustChange 게이트 → 본문)를 `ConsoleRoleGuard`로 추출 — 통과 집합·거부 화면만 주입 | 중복 검출(nose 9/13 공유)이 계기지만 근거는 안전성이다: 순서가 갈리면 한쪽에서 권한 없는 세션에 콘솔이 한 프레임 노출되거나 mustChange 게이트가 누락되는데, 그 위험을 두 파일에 복제해 둘 이유가 없다. 사용처 2곳이라 "두 번째 사용처에서 추출" 규칙도 충족 |
| 2026-09-01 | MSG-541 | 스펙 이슈(구현은 스펙 준수): 사이드바 폭 — 스펙 AC 4는 250px, Figma 실측(15525:8660·15525:9133)은 **280px** | 스펙을 임의 변경하지 않는다는 규칙에 따라 250px(`w-62.5`)로 구현하고 이슈로 기록. Figma 정본이 맞다면 `ConsoleSidebar`의 `w-62.5` → `w-70` 한 줄 변경이며, 후속 콘솔 화면 티켓(545·552)이 본문 폭을 그 값 기준으로 잡기 전에 결론이 필요하다 → **같은 날 해소**: 오케스트레이터가 "Figma에 정의된 값만 사용" 규칙(FIGMA_WORKFLOW)으로 280px 확정, `w-70` 정정 후 검증 통과(스펙에 정정 이력 기재). 스펙 초안 250px는 analyst 오기였다 |
| 2026-09-01 | MSG-541 | 결정: 사이드바 콘솔 제목을 heading이 아니라 문단(`<p>`)으로 렌더하고 콘솔 이름을 `<nav>`의 접근 가능한 이름에 실었다 | 사이드바는 본문(`ConsoleStub`의 `<h1>`)보다 DOM에서 앞선다 — 제목을 h2로 만들면 문서 개요가 h2 → h1 순서로 뒤집혀 스크린리더 개요가 깨진다. 시각 결과는 동일하고, 랜드마크 구분은 `aria-label`이 담당한다 |
| 2026-09-02 | MSG-542 | 결정: 첫 비밀번호 설정 성공 시 게이트 통과를 **비밀번호 상태 캐시 갱신**(`getStatusQueryKey` setQueryData(false) + invalidate)으로 처리 — `use-must-change-gate` 본체 수정 기각 | 게이트는 MSG-541 산출물이고 병렬 웨이브가 함께 쓴다. 훅에 "방금 설정했다" 상태를 넣으면 게이트가 두 개의 진실(서버 응답·로컬 플래그)을 갖게 되고 그 플래그를 언제 지울지가 새 결함면이 된다. 캐시 갱신은 게이트가 이미 읽는 유일한 출처를 바꾸므로 협업 지점이 하나다. 라우팅 통합 스모크의 fetch 스텁은 initial 성공에 맞춰 서버의 mustChange를 실제로 뒤집어, 낙관 갱신만으로 통과하는 오탐도 막았다 |
| 2026-09-02 | MSG-542 | 결정: 재설정 링크 진입을 라우트 신설 없이 `/org/password/reset`의 `?token=` 쿼리 분기로 구현하고, 발송 완료는 같은 라우트의 내부 상태로 전환 | 웨이브 규칙이 `router.tsx`·`console-routes.ts`를 불가침으로 묶었다(병렬 워크트리 충돌 방지). 쿼리 분기는 라우트 표를 건드리지 않고 세 모드를 가르며, 메일 링크 형식(`?token={token}`)이 서버 파라미터명과 그대로 맞는다 — 대신 링크 URL은 BE 확인 전까지 FE 전제이므로 환류 대상이다 |
| 2026-09-02 | MSG-542 | 결정: `validateNewPassword`가 요건 3개 개별 판정 + 종합을 한 번에 돌려주고, 스펙 계획의 별도 `passwordChecklist`는 만들지 않았다 | 게이지·힌트가 읽을 목록이 곧 종합 판정의 재료다. 같은 계산을 두 함수로 노출하면 호출처가 어느 쪽을 쓸지 갈리고 규칙이 바뀔 때 한쪽만 고쳐질 수 있다(스펙 계획의 문면보다 단일 출처를 우선) |
| 2026-09-02 | MSG-542 | 결정: 재설정 요청 폼에 `noValidate`를 걸어 이메일 형식 안내를 브라우저 기본 툴팁 대신 폼 안 문구로 낸다 | AC 9가 "제출이 나가지 않고 **안내된다**"를 요구한다. 기본 검증이 켜져 있으면 submit 이벤트 자체가 발생하지 않아 우리 안내가 뜰 자리가 없고(테스트가 실제로 이 경로에서 실패해 발견), 안내 문구가 브라우저·로케일마다 달라진다. 로그인 폼은 자체 이메일 검증 요구가 없어 기본 검증을 그대로 둔다 |
| 2026-09-02 | MSG-542 | 결정: `PasswordInput`(보기 토글)을 ui-web으로 승격하지 않고 `pages/org/ui/`에 둔다 | 도메인 무관이라 성격은 ui-web이지만 현재 사용처가 콘솔 인증 3화면뿐이다. 두 번째 소비 영역(MSG-544 비밀번호 변경)이 생길 때 승격한다 — 액션시트 선례의 "두 번째 사용처" 규칙 |
| 2026-09-02 | MSG-542 | 검증 WAIVED: nose 중복 게이트 red(신규 3가족 — 스모크 스캐폴딩 2 + setup·reset 폼 similar 1)를 의도 중복으로 수용, 베이스라인 등재는 웨이브 1 통합 시 1회로 이월 | 병렬 5티켓이 각자 등재하면 베이스라인이 5회 충돌한다(오케스트레이터 방침). 폼 가족은 0/20 shared·~0 removable로 추출 실익 없음 — 공통부(`NewPasswordFields`)는 이미 추출돼 있다 |
| 2026-09-02 | MSG-542 | 판정: react-doctor `query-mutation-missing-invalidation` 경고 2건(use-password-mutations 57·70행)은 오탐 — 코드 무수정, `--no-verify` 커밋(사유 본문 기재) | reset-request는 클라이언트 캐시 대상이 아예 없고(메일 발송), confirm-reset은 `clearLocalSession()`의 auth 스토어 전이를 `QueryProvider`가 구독해 캐시 전량 `clear()`한다(QueryProvider.tsx:37 — 로그인 훅들의 `queryClient.clear()`와 대칭 설계). 스캐너는 스토어 전이 경유 정리를 볼 수 없다. 가짜 무효화를 넣어 규칙을 달래는 것은 이중 정리 + 오독 유발이라 기각. rule off 설정은 v0.9.3에서 무효(CLAUDE.md 2026-08-28 실측) |
| 2026-09-02 | MSG-545 | 결정: 상태 필터 칩은 ui-web `Chip`을 재사용하고(비활성에 `border border-border` 추가) 상태 표시 칩만 `features/org-submissions/ui/SubmissionStatusChip`으로 신설 | 필터 칩은 상호작용(button·`aria-pressed`)이라 `Chip`의 계약과 정확히 일치한다 — 페이지 로컬 칩을 만들면 ui-web과 역할이 겹치는 중복 UI가 된다. 상태 칩은 클릭 대상이 아닌 정적 라벨이고 톤이 신청 상태(도메인)에 묶여 있어 ui-web 승격 대상이 아니다(디자인 시스템 3조). **잔여 델타**: 활성 필터 칩에 `Chip`의 체크 아이콘이 붙는다(시안에는 없음) — 재사용 이득이 아이콘 하나보다 크다고 판단, Figma 대조 시 알려진 델타로 기록 |
| 2026-09-02 | MSG-545 | 결정: 요약 카드 일자를 `updatedAt`/`history` 두 입력을 받는 순수 함수 하나(`submissionTimelineText`)로 통합 — 뷰가 "이력이 있으면 신청·처리, 없으면 최종 수정"을 분기하지 않는다 | 분기가 뷰에 남으면 AC 4·5의 핵심 파생(신청일=첫 전이·처리일=마지막 전이, 비반려는 updatedAt 1개)이 테스트 밖에 놓인다. 기각 대안: 포맷터 3종(`formatDate`·`formatMonthDay`·`formatPeriod`)을 export하고 카드에서 조립 — 조립 규칙이 컴포넌트로 새어 KST 경계 케이스를 스모크로만 잡게 된다 |
| 2026-09-02 | MSG-545 | 결정: KST 변환 원시(`KST_OFFSET_MS`·`hasTimezoneMarker`)를 `shared/kst-date.ts`로 추출하고 `profile-format`도 그리로 돌렸다 | 중복 게이트(nose)가 세 번째 복사를 exact 계열로 검출했고 web 내 2곳은 실제 중복이다("두 번째 사용처에서 추출" 규칙). `apps/mobile/src/shared/format.ts`의 같은 선언은 앱이 웹을 import할 수 없는 크로스플랫폼 미러라 흡수 대상이 아니다. 기각 대안 ①로컬 유지 + 베이스라인 등재: 진짜 중복을 규칙 예외로 덮는다 ②`formatJoinedDate` 자체를 공용화: 날짜만 오는 입력을 문자열 패딩으로 처리하는 기존 계약(NaN 폴백 포함)과 신규 파생 규칙이 달라 한쪽 동작이 조용히 바뀐다 |
| 2026-09-02 | MSG-545 | 결정: `use-org-profile-query`에만 `enabled: isAuthenticated` 게이트를 두고 `use-my-submissions-query`에는 두지 않았다 | 목록은 가드(`RequireOrgRole`)가 세션을 보장하는 `/org` 본문에서만 호출되지만, 사이드바 슬롯은 **콘솔 셸과 함께** 마운트돼 가드 밖(테스트·스토리) 경로가 존재한다 — 익명 401 + 재발급 재발사(MSG-328 관례)를 만들 이유가 없다. 부수 효과로 MSG-541의 콘솔 라우팅 스모크가 세션 없이도 조용히 통과한다 |
| 2026-09-02 | MSG-545 | 결정: `isOrgSubmissionStatus`를 `status in STATUS_META` 대신 `Object.hasOwn`으로 구현 | `in`은 프로토타입 체인을 타므로 서버가 `"toString"`·`"constructor"` 같은 문자열을 보내면 확정 상태로 오판정해 잘못된 톤·라벨이 붙는다. 미지 값 방어(추정 6)가 이 가드의 존재 이유라 그 구멍을 남길 수 없다 |
| 2026-09-02 | MSG-552 | 결정: 선택 신청 id를 상태로 들지 않고 **파생**한다 — `pinnedId ?? 목록 첫 행 ?? null`, 탭 전환은 `pinnedId`만 비운다 | AC 8(목록 도착 시 첫 행 자동 선택 + 탭 전환 후 재선택)을 effect 없이 충족한다. 기각 대안 ①`useEffect`로 "목록 도착 시 setSelectedId(first)": 도착·전환·placeholder 3전이를 의존성으로 조율해야 하고 한 프레임 미선택 미리보기가 스친다 ②zustand 스토어: 다른 화면과 공유가 없어 스펙대로 페이지 로컬 |
| 2026-09-02 | MSG-552 | 결정: 목록 쿼리에 `keepPreviousData` + `isPlaceholder` 노출 — 탭 뱃지(counts)는 직전 값을 유지하되 **목록 영역은 로딩 표시**(placeholder 목록을 행으로 쓰지 않음) | 추정 9(뱃지 깜빡임 방지)를 만족하면서 직전 탭의 행이 새 탭의 목록으로 보이는 오표시를 막는다(MSG-516이 `keepPreviousData`를 기각한 이유 = 라벨과 데이터의 짝 어긋남 — 여기서는 counts가 필터 무관 값이라 그 문제가 없다). 기각 대안 ①placeholder 미사용: 탭 전환마다 건수 뱃지가 사라져 시안과 어긋남 ②훅이 placeholder일 때 submissions를 빈 배열로 내주기: 뷰 판단을 훅에 숨겨 자동 선택 파생이 무엇을 보고 있는지 불투명 |
| 2026-09-02 | MSG-552 | 결정: 상세(presigned imageUrl) 쿼리 staleTime은 **전역 기본 30초 유지** + `<img>` onError 자리표시 — 명시 오버라이드 없음 | `AdminEventSubmissionDetailResponseDto`에 TTL 필드가 없어(실측) 만료 유도(use-video-playback-query 관례)가 불가능하고, 전역 30초가 이미 스펙이 요구한 "보수적"에 해당한다(더 긴 값은 만료 URL 재사용 위험, 더 짧은 값은 목록 훑기마다 재조회). MSG-520(행사 영상 상세) 선례와 같은 판정 — 단 여기는 조회수 부작용이 없어 자동 재조회 차단은 하지 않았다 |
| 2026-09-02 | MSG-552 | 결정: nose 신규 중복 가족을 **추출하지 않고**(도메인이 다른 두 feature를 6줄짜리 필드 개칭 헬퍼로 묶는 성급한 공통화 — MSG-517 "의도 중복" 선례) 의도 중복 사유 note와 함께 **baseline 등재** → **정정(PR #118 리뷰)**: 이 행의 원문은 "등재하지 않고 환류"였으나 실커밋(f72235a)에 `--write-baseline` 전량 재작성이 포함돼 로그와 diff가 어긋났다 — 로그를 실제 조치(등재)로 정정 | 원문이 우려한 "전량 재작성 ↔ 병렬 5티켓 정면 충돌"은 그대로 현실화됐다(develop의 MSG-542·545도 각자 재작성 후 머지 → `nose.baseline.json` 충돌). 해소: develop 머지에서 양쪽 baseline을 family id 합집합으로 만들고 머지 트리에서 `--write-baseline` 1회 재생성. 실측 함정: `--write-baseline`은 note를 자동 힌트로 다시 쓰므로 수기 suffix가 유실된다 — MSG-552 사유 12건을 id·멤버 집합 일치로 재부착했다. 교훈: 등재 이월 방침은 웨이브 내 전 티켓이 함께 지킬 때만 유효하고, 수기 note는 재생성 시마다 재부착 대상이다 |
| 2026-09-02 | MSG-546 | 결정: 조사 판정 `euroJosa`를 `features/ai-route/model/route-mentioned-area` 로컬 함수에서 `shared/format`으로 올리고 두 소비처가 공유 — 위저드 CTA("{이벤트명}으로 계속")용 자체 구현 기각 | 같은 7줄(한글 종성 산술)을 두 벌 두면 `check:duplication`이 새 클론 가족으로 잡고, 받침 규칙이 갈라질 위험이 실제로 있다(ㄹ 예외). shared/format은 이미 표시 포맷 유틸의 집이고(formatDuration 승격 선례) 플랫폼 무의존이라 RN 경계도 유지된다. ai-route 쪽 변경은 import 1줄 + 로컬 정의 삭제뿐이고 `movedToastTitle` 동작·테스트는 불변 |
| 2026-09-02 | MSG-546 | 결정: 스텝 1에서 이벤트 카드 클릭은 스토어 `type`을 확정하지 않고 모달만 연다(카드 강조는 모달 열림 로컬 state) — 카드 클릭 즉시 `selectType("EVENT")` 기각 | 추정 3의 "취소·X는 유형 확정 없이 모달만 닫힌다"를 스토어에 `clearType` 같은 되돌리기 액션 없이 성립시키는 유일한 방법이다. 즉시 확정 방식은 취소 후 "선택한 유형: 이벤트" 캡션이 남고(계속 버튼은 어차피 비활성) 확정되지 않은 유형이 스토어에 앉아 MSG-548 제출 조립의 전제를 흐린다 |
| 2026-09-02 | MSG-546 | 결정: 승인 이벤트 조회에 `placeholderData: keepPreviousData` 채택 — MSG-516 `use-event-occurrences-query`의 기각 선례와 반대로 간다 | 기각 근거였던 "라벨과 데이터의 짝 어긋남"이 여기선 성립하지 않는다: 서버 계약이 `totalCount`·`cityCounts`를 필터·검색과 무관한 고정값으로 정의하므로(API 주석 실측) 이전 응답의 칩·건수는 항상 옳다. 대신 AC 4의 "칩·건수 라벨은 검색 중에도 고정"은 keepPreviousData 없이는 로딩마다 칩이 사라져 충족 불가 |
| 2026-09-02 | MSG-546 | 결정: 위저드 페이지의 `<h1>`을 스크린리더 전용 "새 행사 등록"으로 두고 스텝 제목은 `<h2>`로 렌더 | 시안 본문에는 라우트 제목이 없고 스텝 제목("어떤 형태를 등록하시나요?")만 있다. h1을 스텝 제목에 주면 스텝 전이마다 문서의 최상위 제목이 바뀌고, MSG-541이 커밋한 라우팅 스모크(`/org/submissions/new`·`/edit`의 h1 = "새 행사 등록")를 이 티켓이 깨뜨린다. sr-only h1은 탭 제목과 같은 문구를 유지하면서 그 스모크를 손대지 않게 해 준다 |
| 2026-09-02 | MSG-546 | 결정: 이미지 업로드 실패 시 s3Key와 함께 `previewUrl`도 비운다 | s3Key 없이 미리보기만 남으면 화면은 "이미지 있음"으로 보이는데 CTA는 비활성이라 사용자가 원인을 못 찾는다(AC 8의 "실패 후 재선택 가능"이 실패 상태 인지에 의존한다). 미리보기 유지 대안은 실패 배지를 미리보기 위에 얹는 UI가 시안에 없어 기각 |
| 2026-09-02 | MSG-546 | 결정: nose 새 클론 가족 판정 — `toImagePresignParams`(웹 profile·모바일 profile과 3벌) 추출 기각 후 베이스라인 등재 | 공유되는 9줄은 "확장자 파싱 + DTO 3필드 조립"이고 갈리는 부분이 검증 규칙 자체다(profile: 비어 있지 않은 확장자 / 이번: jpg·jpeg·png 허용목록 + MIME 쌍 일치 — 서버 계약이 다르다). 세 벌이 웹 2개·모바일 1개로 앱 경계를 넘어 있어 공용 집도 없다(선제적 packages 생성 금지). 스펙이 이미 "재사용이 아니라 선례 답습 신규"로 판정한 지점 — MSG-535 베이스라인 등재 선례를 따랐다 |
| 2026-09-02 | MSG-546 | 판정: react-doctor `query-mutation-missing-invalidation` 경고 1건(use-submission-image-upload:27)은 오탐 — 코드 무수정, `--no-verify` 커밋(사유 본문 기재) | presign 발급+S3 PUT은 어떤 쿼리가 캐시하는 서버 자원도 바꾸지 않는다 — s3Key는 위저드 스토어에만 실리고, 신청 생성(캐시 무효화 지점)은 MSG-548의 POST 제출이 소유한다. MSG-542 동일 계열 판정(2026-09-02 행)과 같은 근거·같은 처리 |
| 2026-09-02 | MSG-546 | 판정: codex 리뷰 P2 "스테일 업로드 콜백이 새 세션 스토어 오염"은 코드 변경 없이 기각 — 회귀 테스트 2건으로 보호를 고정 | react-query 의미론이 두 레이스를 이미 차단한다(실측): ① 같은 observer의 `mutate` per-call 콜백은 최신 호출만 발화 — 빠른 교체 후 이전 업로드의 늦은 정착이 무시된다 ② 관찰자 언마운트 시 콜백이 버려진다 — 이탈 후 정착이 새 세션을 못 건드린다(useLogout MSG-325 주석의 그 동작). 제안된 세션 ID 토큰은 이 암묵 보호의 중복 구현이라 기각하되, 보호가 라이브러리 의미론에 얹혀 있어 깨지기 쉬우므로 submission-image-revoke.test.tsx에 두 경로 회귀 테스트를 박고 코드 주석으로 명문화했다 |
| 2026-09-02 | MSG-554 | 결정: 유저 지도 focus 딥링크의 **형식 정본을 `shared/map-focus-link` 한 파일**에 두고 조립(관리자 콘솔)·해석(지도 홈 진입 훅) 양쪽이 그것만 쓴다 | 파라미터 이름("focus")과 `lat,lng` 표기를 만드는 쪽과 읽는 쪽이 각자 알면 한쪽만 고쳐져도 게이트가 잡지 못하고 링크가 조용히 죽는다. 좌표 타입은 layer 규칙대로 shared 로컬 정의(geolocation `GeoCoords` 선례) |
| 2026-09-02 | MSG-554 | 결정: 승인 행사 테이블을 `role="grid"` + 행 `aria-selected` + Enter/Space 키 선택으로 구현(행 안에 stretched button을 넣는 방식 기각) | 행 전체가 선택 컨트롤이라는 것이 디자인(좌측 선택 바)의 요구다. 버튼을 첫 칸에 넣고 `after:inset-0`으로 늘리면 접근 가능한 이름이 행사명 하나로 좁아지고 선택 상태를 표현할 표준 속성이 없다. `aria-selected`는 grid 안에서 유효하며 스펙이 요구한 표기이기도 하다 |
| 2026-09-02 | MSG-554 | 결정: 이미 중지된 행사의 상세 카드에서 "노출 중지" 버튼을 **없애고** 중지 시각·사유만 보여 준다(비활성 버튼 유지 기각) | 재중지는 서버가 409(13453)로 거절한다 — 누를 수 있게 두면 실패만 만든다. 비활성 버튼은 "언젠가 가능"을 암시하지만 재개 API가 없어 이 상태는 되돌아오지 않는다. 관리자에게 필요한 것은 "무엇을 왜 내렸는지"(서버 doc 취지)뿐이다 |
| 2026-09-02 | MSG-554 | 결정: 목록·상세 훅의 반환 꼬리를 `features/region`의 `gatedQueryStatus`로 재사용(자체 매핑 복제 기각) — 상세는 미선택 시 `isPending`을 게이트로 눌러 준다 | 중복 게이트(nose)가 같은 5~6줄을 짚었고, 이미 "두 번째 사용처에서 추출"이 끝난 헬퍼가 있었다. 부수 효과로 결함이 예방됐다: `enabled: false` 쿼리는 영원히 pending이라 그대로 두면 행 미선택 카드가 빈 상태 대신 로딩으로 보일 수 있다. 헬퍼 위치가 region 도메인인 점은 승격 후보로 기록 |
| 2026-09-02 | MSG-554 | 스펙 요구 반영(이전 티켓 테스트 갱신): MSG-541이 커밋한 `console-routing.smoke.test.tsx`의 `/admin/events` 자리표시 제목 단정을 "승인 행사" → "승인 행사 관리"로 갱신 | AC 1이 화면 헤더를 "승인 행사 관리"(Figma 15579:2385)로 요구한다 — 스텁을 실구현으로 바꾸는 티켓이 그 한 줄을 갱신하는 것이 설계된 흐름이다(사이드바 메뉴 라벨 "승인 행사"는 불변). 웨이브 병렬 티켓 3장이 같은 배열의 다른 줄을 고치므로 머지 시 한 줄 충돌이 예상된다 |
| 2026-09-02 | MSG-554 | 결정: nose 신규·변경 클론 가족(테스트 스캐폴딩·마크업 유사 12건)을 **베이스라인 등재**로 수용하고, 값이 있는 두 곳만 실제로 줄였다 — 스모크의 3회 반복 실행부는 `confirmUnpublish` 헬퍼로 추출, 목록 로딩 스켈레톤은 3단 → 한 덩어리 | nose 자신의 판정이 "~0 removable · per-scenario setup은 통합하지 말 것"이다. 3줄짜리 QueryClient 보일러플레이트와 라벨/값 마크업까지 공통화하면 테스트의 AAA 가독성과 화면 코드의 직관을 잃는다(MSG-535 등재 선례). 베이스라인 파일은 웨이브 병렬 3장이 함께 만지므로 머지 후 재생성이 필요할 수 있다 |
| 2026-09-02 | MSG-554 | 결정: 상세 카드의 "노출 범위" 라벨·값 쌍을 `<dl>/<dt>/<dd>`로 렌더 | 라벨→값 한 쌍의 의미가 정의 목록 그대로라 스크린리더가 "노출 범위: 2곳 · 사각형 3개 · 총 37칸"으로 묶어 읽는다. `div`+`p` 두 줄로도 화면은 같지만 관계가 사라진다(중복 검출이 계기였고, 판단 근거는 접근성) |
| 2026-09-02 | MSG-554 | 재작업 결정: 딥링크 focus 이동의 시퀀싱을 **MapCanvas 명령 큐 + 기존 준비 신호(init·idle) flush**로 풀었다 — ① 셸 컨텍스트에 map-ready 상태 노출 ② 셸이 focus를 `initialCenter`로 직접 주입, 두 대안 기각 | 검증에서 콜드 로드(딥링크 새 탭 = 유일한 실사용 경로)의 focus 이동이 항상 유실됐다: 훅은 마운트 1회에 `moveTo`를 부르는데 그 시점 SDK가 미로드라 `mapRef.current?.panTo`가 조용히 no-op이고 재시도가 없었다. 큐는 접촉면이 지도 경계 안(파일 1개)이고 새 상태·새 전역이 없다. ①은 ready 상태가 Outlet context를 재생성해 전 섹션 리렌더를 유발하고 파일 3개를 건드린다. ②는 pan 없이 처음부터 그 좌표로 생성돼 더 우아하지만 진입 줌이 `DEFAULT_ZOOM === GRID_MIN_ZOOM`이라는 우연한 일치에 기대게 되고(훅의 `zoomTo`는 여전히 유실), 딥링크 계약이 훅과 셸 두 곳으로 쪼개진다 |
| 2026-09-02 | MSG-554 | 재작업 결정: focus 진입에서는 geolocation 조회를 **아예 하지 않는다**(조회는 하되 pan만 억제, 또는 focus를 뒤에 재적용 기각) — 판정은 `widgets/map-shell/use-map-entry-center.ts`로 추출해 테스트했다 | 진입 중심을 두 주체가 늦게 pan으로 다투는 구조였다: 위치가 지도 생성 뒤에 도착하면 `center` prop 효과의 panTo가 focus를 덮는다(도착 시점에 따라 갈리는 비결정 결함). 목적지가 정해진 진입에서 위치를 물을 이유가 없고, 안 물으면 권한 프롬프트도 안 뜬다. "focus를 나중에 다시 적용"은 타이머 경합을 코드로 옮기는 것뿐이다. 훅 추출은 MapShell 렌더 전체(스토어·쿼리 다수)를 세우지 않고 이 우선순위를 단정하기 위한 것 — 뷰-레이어 훅 선례(`use-map-shell`)와 같은 위치 |
| 2026-09-02 | MSG-554 | 재작업 결정: 회귀 테스트를 훅(moveTo 목) 레벨이 아니라 **MapCanvas 스모크(FakeMap + SDK 이벤트 발화)** 로 내렸다 | 유실이 일어난 층이 "명령 → SDK 인스턴스" 사이라, 명령을 목으로 받는 훅 테스트는 원리적으로 이 결함을 통과시킨다(실제로 통과시켰다 — 검증 리포트 지적). 기존 스모크 하네스에 `panTo`/`setZoom` 호출 기록과 `naver.maps.Event` 리스너 레지스트리를 더해 "준비 전 발사 → init에서 1회 적용, idle 반복에도 재적용 없음"을 고정했다. 브라우저 실측으로도 확증(콜드 로드 `getCenter()` = focus 좌표, 숨은 탭에서도 성립 — 이벤트 기반이라 rAF·시간에 무의존) |
| 2026-09-02 | MSG-554 | 판정: react-doctor 경고 2건 기각 — 코드 무수정, `--no-verify` 커밋(사유 본문 기재) | ① `no-giant-component`(MapCanvas:404): 기존 대형 파일에 진입 명령 큐 +33줄을 얹은 것 — 파일 분할은 티켓 범위 밖 별도 PR감(도구 자체 가이드 "unrelated pre-existing code 무시·broad cleanup은 분리" 준수) ② `no-pass-data-to-parent`(use-map-focus-entry:32): 지도 명령을 effect에서 발사하는 뷰-레이어 훅은 map-home의 확립 관례(use-chip-entry·use-nearest-entry·use-home-entry-lifecycle 동일 패턴) — 이 훅만 다른 구조로 바꾸면 오히려 관례 이탈 |
| 2026-09-02 | MSG-558 | 결정: 말풍선 폭을 텍스트 실측이 아니라 **순수 추정 모델**로 정하고 계수를 과대 방향으로 고정(한글 1.0 · ASCII 0.65) | RN `NaverMapMarkerOverlay`는 첫 프레임에 width/height가 있어야 하는데 `onLayout` 실측은 그때 없다. 박스가 투명이라 과대는 비용 0이고 과소만 글자를 자른다 — 비대칭 비용이라 정확도가 아니라 방향을 정책으로 뒀다. 계수 보정 근거는 프로덕션 웹 computed style 실측(Inter 숫자 advance 0.6375em → 0.6은 과소, 0.65로 상향). |
| 2026-09-02 | MSG-558 | 판정: Android 그림자 미표시(S4)를 결함이 아닌 **수용된 플랫폼 편차**로 종결 | `RNCNaverMapMarker.updateCustomView()`가 서브뷰를 `draw(canvas)`로 소프트웨어 비트맵에 그려 올리므로 `elevation` 그림자는 RenderNode 단계에서 잘린다 — 값 조정으로 풀리는 종류가 아니다. 대안(svg 필터·배경 레이어)은 렌더 경로 변경이라 별도 작업. |
